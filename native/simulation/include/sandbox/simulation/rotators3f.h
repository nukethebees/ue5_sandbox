// This file is autogenerated. Do not edit by hand.
// Edit lispb/schema and regenerate it instead.

#pragma once

#include "native_soa/storage.h"
#include "sandbox/core/soa_permutation.h"
#include "sandbox/simulation/rotator_types.h"

namespace ml::simulation {
struct Rotators3fView;
struct Rotators3fConstView;
struct Rotators3fConstView {
    using View = Rotators3fView;
    using ConstView = Rotators3fConstView;
    using size_type = std::int32_t;
    using equivalent_type = ml::simulation::Rotator3f;
    auto operator[](size_type const index) const -> equivalent_type {
        return {pitches[static_cast<std::size_t>(index)],
                yaws[static_cast<std::size_t>(index)],
                rolls[static_cast<std::size_t>(index)]};
    }
    std::span<float const> pitches;
    std::span<float const> yaws;
    std::span<float const> rolls;
    auto num() const noexcept -> size_type { return static_cast<size_type>(pitches.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(pitches);
        fn(yaws);
        fn(rolls);
    }
    void validate_array_sizes() const {
        auto const count{num()};
        each_column([count](auto column) {
            ml::native_soa::require(column.size() == static_cast<std::size_t>(count));
        });
    }
    auto slice(size_type const offset, size_type const count) const -> Rotators3fConstView {
        ml::native_soa::require(offset >= 0 && count >= 0 && offset <= num() &&
                                count <= num() - offset);
        return {
            pitches.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            yaws.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            rolls.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
        };
    }
    auto get_view() const -> Rotators3fConstView { return *this; }
    auto get_view(size_type const offset, size_type const count) const -> Rotators3fConstView {
        return slice(offset, count);
    }
    auto get_const_view() const -> ConstView {
        return {
            pitches,
            yaws,
            rolls,
        };
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto left(size_type const count) const -> Rotators3fConstView { return slice(0, count); }
    auto right(size_type const count) const -> Rotators3fConstView {
        return slice(num() - count, count);
    }
};
struct Rotators3fView {
    using View = Rotators3fView;
    using ConstView = Rotators3fConstView;
    using size_type = std::int32_t;
    using equivalent_type = ml::simulation::Rotator3f;
    auto operator[](size_type const index) const -> equivalent_type {
        return {pitches[static_cast<std::size_t>(index)],
                yaws[static_cast<std::size_t>(index)],
                rolls[static_cast<std::size_t>(index)]};
    }
    std::span<float> pitches;
    std::span<float> yaws;
    std::span<float> rolls;
    auto num() const noexcept -> size_type { return static_cast<size_type>(pitches.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(pitches);
        fn(yaws);
        fn(rolls);
    }
    void validate_array_sizes() const {
        auto const count{num()};
        each_column([count](auto column) {
            ml::native_soa::require(column.size() == static_cast<std::size_t>(count));
        });
    }
    auto slice(size_type const offset, size_type const count) const -> Rotators3fView {
        ml::native_soa::require(offset >= 0 && count >= 0 && offset <= num() &&
                                count <= num() - offset);
        return {
            pitches.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            yaws.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            rolls.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
        };
    }
    auto get_view() const -> Rotators3fView { return *this; }
    auto get_view(size_type const offset, size_type const count) const -> Rotators3fView {
        return slice(offset, count);
    }
    auto get_const_view() const -> ConstView {
        return {
            pitches,
            yaws,
            rolls,
        };
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto left(size_type const count) const -> Rotators3fView { return slice(0, count); }
    auto right(size_type const count) const -> Rotators3fView {
        return slice(num() - count, count);
    }
    void set(size_type const index,
             float const new_pitches,
             float const new_yaws,
             float const new_rolls) const {
        ml::native_soa::require(index >= 0 && index < num());
        pitches[static_cast<std::size_t>(index)] = new_pitches;
        yaws[static_cast<std::size_t>(index)] = new_yaws;
        rolls[static_cast<std::size_t>(index)] = new_rolls;
    }
    void set(size_type const index, equivalent_type const value) const {
        set(index, value.pitch, value.yaw, value.roll);
    }
};
struct Rotators3f {
    using View = Rotators3fView;
    using ConstView = Rotators3fConstView;
    using size_type = std::int32_t;
    using equivalent_type = ml::simulation::Rotator3f;
    auto operator[](size_type const index) const -> equivalent_type {
        return get_const_view()[index];
    }
    ml::native_soa::Vector<float> pitches;
    ml::native_soa::Vector<float> yaws;
    ml::native_soa::Vector<float> rolls;
    auto num() const noexcept -> size_type { return static_cast<size_type>(pitches.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) {
        fn(pitches);
        fn(yaws);
        fn(rolls);
    }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(pitches);
        fn(yaws);
        fn(rolls);
    }
    void validate_array_sizes() const { get_const_view().validate_array_sizes(); }
    void reserve(size_type const count) {
        ml::native_soa::require(count >= 0);
        pitches.reserve(static_cast<std::size_t>(count));
        yaws.reserve(static_cast<std::size_t>(count));
        rolls.reserve(static_cast<std::size_t>(count));
    }
    void reset() noexcept {
        pitches.clear();
        yaws.clear();
        rolls.clear();
    }
    void set_num(size_type const count) {
        ml::native_soa::require(count >= 0);
        auto const size{static_cast<std::size_t>(count)};
        pitches.resize(size);
        yaws.resize(size);
        rolls.resize(size);
    }
    void add_uninitialised(size_type const count) {
        auto const old_num{num()};
        ml::native_soa::require(count >= 0 &&
                                count <= std::numeric_limits<size_type>::max() - old_num);
        set_num(old_num + count);
    }
    void add_defaulted(size_type const count) { add_uninitialised(count); }
    void remove_at_swap(size_type const index, size_type const count) {
        auto const old_num{num()};
        ml::native_soa::require(index >= 0 && index <= old_num && count >= 0 &&
                                count <= old_num - index);
        auto const moved{std::min(count, old_num - index - count)};
        auto const source{old_num - moved};
        for (size_type i{}; i < moved; ++i) {
            pitches[index + i] = pitches[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            yaws[index + i] = yaws[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            rolls[index + i] = rolls[source + i];
        }
        set_num(old_num - count);
    }
    void set(size_type const index,
             float const new_pitches,
             float const new_yaws,
             float const new_rolls) {
        get_view().set(index, new_pitches, new_yaws, new_rolls);
    }
    auto add(float const new_pitches, float const new_yaws, float const new_rolls) -> size_type {
        auto const index{num()};
        add_defaulted(1);
        set(index, new_pitches, new_yaws, new_rolls);
        return index;
    }
    void set(size_type const index, equivalent_type const value) { get_view().set(index, value); }
    auto add(equivalent_type const value) -> size_type {
        auto const index{num()};
        add_defaulted(1);
        set(index, value);
        return index;
    }
    void append_from(ConstView source) {
        auto const count{source.num()};
        ml::native_soa::require(count <= std::numeric_limits<size_type>::max() - num());
        source.validate_array_sizes();
        if (count == 0) {
            return;
        }
        {
            auto const address{reinterpret_cast<std::uintptr_t>(source.pitches.data())};
            auto const begin{reinterpret_cast<std::uintptr_t>(pitches.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + pitches.size() * sizeof(float));
        }
        {
            auto const address{reinterpret_cast<std::uintptr_t>(source.yaws.data())};
            auto const begin{reinterpret_cast<std::uintptr_t>(yaws.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + yaws.size() * sizeof(float));
        }
        {
            auto const address{reinterpret_cast<std::uintptr_t>(source.rolls.data())};
            auto const begin{reinterpret_cast<std::uintptr_t>(rolls.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + rolls.size() * sizeof(float));
        }
        pitches.insert(pitches.end(), source.pitches.begin(), source.pitches.end());
        yaws.insert(yaws.end(), source.yaws.begin(), source.yaws.end());
        rolls.insert(rolls.end(), source.rolls.begin(), source.rolls.end());
    }
    auto get_view() -> View {
        return {
            pitches,
            yaws,
            rolls,
        };
    }
    auto get_view() const -> ConstView {
        return {
            pitches,
            yaws,
            rolls,
        };
    }
    auto get_const_view() const -> ConstView { return get_view(); }
    auto get_view(size_type const offset, size_type const count) -> View {
        return get_view().slice(offset, count);
    }
    auto get_view(size_type const offset, size_type const count) const -> ConstView {
        return get_view().slice(offset, count);
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto slice(size_type const offset, size_type const count) -> View {
        return get_view(offset, count);
    }
    auto slice(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view(offset, count);
    }
    auto left(size_type const count) -> View { return slice(0, count); }
    auto right(size_type const count) -> View { return slice(num() - count, count); }
    auto left(size_type const count) const -> ConstView { return slice(0, count); }
    auto right(size_type const count) const -> ConstView { return slice(num() - count, count); }
    template <typename Other>
    void copy_element(size_type const dst_index, Other const& other, size_type const src_index) {
        pitches[static_cast<std::size_t>(dst_index)] =
            other.pitches[static_cast<std::size_t>(src_index)];
        yaws[static_cast<std::size_t>(dst_index)] = other.yaws[static_cast<std::size_t>(src_index)];
        rolls[static_cast<std::size_t>(dst_index)] =
            other.rolls[static_cast<std::size_t>(src_index)];
    }
    template <typename Other>
    void copy_elements(size_type const dst_index,
                       Other const& other,
                       size_type const src_index,
                       size_type const count) {
        for (size_type i{}; i < count; ++i) {
            copy_element(dst_index + i, other, src_index + i);
        }
    }
    void apply_permutation(std::span<std::int32_t> const indices) {
        validate_array_sizes();
        ml::native_soa::require(indices.size() == static_cast<std::size_t>(num()));
        each_column([indices](auto& column) { ml::apply_permutation(std::span{column}, indices); });
    }
    template <typename Compare>
    void sort(Compare&& compare, std::span<std::int32_t> const scratch_indices) {
        validate_array_sizes();
        ml::native_soa::require(scratch_indices.size() == static_cast<std::size_t>(num()));
        for (size_type i{}; i < num(); ++i) {
            scratch_indices[static_cast<std::size_t>(i)] = i;
        }
        std::sort(scratch_indices.begin(),
                  scratch_indices.end(),
                  [this, &compare](size_type const lhs, size_type const rhs) {
                      return compare(*this, lhs, rhs);
                  });
        apply_permutation(scratch_indices);
    }
};

} // namespace ml::simulation
