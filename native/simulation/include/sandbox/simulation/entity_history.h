// This file is autogenerated. Do not edit by hand.
// Edit lispb/schema and regenerate it instead.

#pragma once

#include "native_soa/storage.h"
#include "sandbox/core/soa_permutation.h"
#include "sandbox/simulation/entity_handle.h"
#include "sandbox/simulation/entity_types.h"

#include <cstring>
#include <memory>
#include <utility>

namespace ml::simulation {
struct EntityHistoryColumnsView;
struct EntityHistoryColumnsConstView;
struct EntityHistoryColumnsConstView {
    using View = EntityHistoryColumnsView;
    using ConstView = EntityHistoryColumnsConstView;
    using size_type = std::int32_t;
    std::span<FRegistryEntityHandle::index_type const> registry_indices;
    std::span<FRegistryEntityHandle::generation_type const> registry_generations;
    std::span<ml::simulation::EntityType const> entity_types;
    std::span<ml::simulation::Team const> teams;
    std::span<std::uint32_t const> kills;
    std::span<std::uint8_t const> alive;
    std::span<ml::simulation::EntityUniqueId const> killed_by;
    std::span<ml::simulation::DeathReason const> death_reason;
    auto num() const noexcept -> size_type {
        return static_cast<size_type>(registry_indices.size());
    }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(registry_indices);
        fn(registry_generations);
        fn(entity_types);
        fn(teams);
        fn(kills);
        fn(alive);
        fn(killed_by);
        fn(death_reason);
    }
    void validate_array_sizes() const {
        auto const count{num()};
        each_column([count](auto column) {
            ml::native_soa::require(column.size() == static_cast<std::size_t>(count));
        });
    }
    auto slice(size_type const offset, size_type const count) const
        -> EntityHistoryColumnsConstView {
        ml::native_soa::require(offset >= 0 && count >= 0 && offset <= num() &&
                                count <= num() - offset);
        return {
            registry_indices.subspan(static_cast<std::size_t>(offset),
                                     static_cast<std::size_t>(count)),
            registry_generations.subspan(static_cast<std::size_t>(offset),
                                         static_cast<std::size_t>(count)),
            entity_types.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            teams.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            kills.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            alive.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            killed_by.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            death_reason.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
        };
    }
    auto get_view() const -> EntityHistoryColumnsConstView { return *this; }
    auto get_view(size_type const offset, size_type const count) const
        -> EntityHistoryColumnsConstView {
        return slice(offset, count);
    }
    auto get_const_view() const -> ConstView {
        return {
            registry_indices,
            registry_generations,
            entity_types,
            teams,
            kills,
            alive,
            killed_by,
            death_reason,
        };
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto left(size_type const count) const -> EntityHistoryColumnsConstView {
        return slice(0, count);
    }
    auto right(size_type const count) const -> EntityHistoryColumnsConstView {
        return slice(num() - count, count);
    }
};
struct EntityHistoryColumnsView {
    using View = EntityHistoryColumnsView;
    using ConstView = EntityHistoryColumnsConstView;
    using size_type = std::int32_t;
    std::span<FRegistryEntityHandle::index_type> registry_indices;
    std::span<FRegistryEntityHandle::generation_type> registry_generations;
    std::span<ml::simulation::EntityType> entity_types;
    std::span<ml::simulation::Team> teams;
    std::span<std::uint32_t> kills;
    std::span<std::uint8_t> alive;
    std::span<ml::simulation::EntityUniqueId> killed_by;
    std::span<ml::simulation::DeathReason> death_reason;
    auto num() const noexcept -> size_type {
        return static_cast<size_type>(registry_indices.size());
    }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(registry_indices);
        fn(registry_generations);
        fn(entity_types);
        fn(teams);
        fn(kills);
        fn(alive);
        fn(killed_by);
        fn(death_reason);
    }
    void validate_array_sizes() const {
        auto const count{num()};
        each_column([count](auto column) {
            ml::native_soa::require(column.size() == static_cast<std::size_t>(count));
        });
    }
    auto slice(size_type const offset, size_type const count) const -> EntityHistoryColumnsView {
        ml::native_soa::require(offset >= 0 && count >= 0 && offset <= num() &&
                                count <= num() - offset);
        return {
            registry_indices.subspan(static_cast<std::size_t>(offset),
                                     static_cast<std::size_t>(count)),
            registry_generations.subspan(static_cast<std::size_t>(offset),
                                         static_cast<std::size_t>(count)),
            entity_types.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            teams.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            kills.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            alive.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            killed_by.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            death_reason.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
        };
    }
    auto get_view() const -> EntityHistoryColumnsView { return *this; }
    auto get_view(size_type const offset, size_type const count) const -> EntityHistoryColumnsView {
        return slice(offset, count);
    }
    auto get_const_view() const -> ConstView {
        return {
            registry_indices,
            registry_generations,
            entity_types,
            teams,
            kills,
            alive,
            killed_by,
            death_reason,
        };
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto left(size_type const count) const -> EntityHistoryColumnsView { return slice(0, count); }
    auto right(size_type const count) const -> EntityHistoryColumnsView {
        return slice(num() - count, count);
    }
    void set(size_type const index,
             FRegistryEntityHandle::index_type const new_registry_indices,
             FRegistryEntityHandle::generation_type const new_registry_generations,
             ml::simulation::EntityType const new_entity_types,
             ml::simulation::Team const new_teams,
             std::uint32_t const new_kills,
             std::uint8_t const new_alive,
             ml::simulation::EntityUniqueId const new_killed_by,
             ml::simulation::DeathReason const new_death_reason) const {
        ml::native_soa::require(index >= 0 && index < num());
        registry_indices[static_cast<std::size_t>(index)] = new_registry_indices;
        registry_generations[static_cast<std::size_t>(index)] = new_registry_generations;
        entity_types[static_cast<std::size_t>(index)] = new_entity_types;
        teams[static_cast<std::size_t>(index)] = new_teams;
        kills[static_cast<std::size_t>(index)] = new_kills;
        alive[static_cast<std::size_t>(index)] = new_alive;
        killed_by[static_cast<std::size_t>(index)] = new_killed_by;
        death_reason[static_cast<std::size_t>(index)] = new_death_reason;
    }
};
struct EntityHistoryColumns {
    using View = EntityHistoryColumnsView;
    using ConstView = EntityHistoryColumnsConstView;
    using size_type = std::int32_t;
    ml::native_soa::Vector<FRegistryEntityHandle::index_type> registry_indices;
    ml::native_soa::Vector<FRegistryEntityHandle::generation_type> registry_generations;
    ml::native_soa::Vector<ml::simulation::EntityType> entity_types;
    ml::native_soa::Vector<ml::simulation::Team> teams;
    ml::native_soa::Vector<std::uint32_t> kills;
    ml::native_soa::Vector<std::uint8_t> alive;
    ml::native_soa::Vector<ml::simulation::EntityUniqueId> killed_by;
    ml::native_soa::Vector<ml::simulation::DeathReason> death_reason;
    auto num() const noexcept -> size_type {
        return static_cast<size_type>(registry_indices.size());
    }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) {
        fn(registry_indices);
        fn(registry_generations);
        fn(entity_types);
        fn(teams);
        fn(kills);
        fn(alive);
        fn(killed_by);
        fn(death_reason);
    }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(registry_indices);
        fn(registry_generations);
        fn(entity_types);
        fn(teams);
        fn(kills);
        fn(alive);
        fn(killed_by);
        fn(death_reason);
    }
    void validate_array_sizes() const { get_const_view().validate_array_sizes(); }
    void reserve(size_type const count) {
        ml::native_soa::require(count >= 0);
        registry_indices.reserve(static_cast<std::size_t>(count));
        registry_generations.reserve(static_cast<std::size_t>(count));
        entity_types.reserve(static_cast<std::size_t>(count));
        teams.reserve(static_cast<std::size_t>(count));
        kills.reserve(static_cast<std::size_t>(count));
        alive.reserve(static_cast<std::size_t>(count));
        killed_by.reserve(static_cast<std::size_t>(count));
        death_reason.reserve(static_cast<std::size_t>(count));
    }
    void reset() noexcept {
        registry_indices.clear();
        registry_generations.clear();
        entity_types.clear();
        teams.clear();
        kills.clear();
        alive.clear();
        killed_by.clear();
        death_reason.clear();
    }
    void set_num(size_type const count) {
        ml::native_soa::require(count >= 0);
        auto const size{static_cast<std::size_t>(count)};
        registry_indices.resize(size);
        registry_generations.resize(size);
        entity_types.resize(size);
        teams.resize(size);
        kills.resize(size);
        alive.resize(size);
        killed_by.resize(size);
        death_reason.resize(size);
    }
    void add_uninitialised(size_type const count) {
        auto const old_num{num()};
        ml::native_soa::require(count >= 0 &&
                                count <= std::numeric_limits<size_type>::max() - old_num);
        set_num(old_num + count);
    }
    void add_defaulted(size_type const count) { add_uninitialised(count); }
    void remove_at_swap(size_type const index, size_type const count) {
        auto const old_num{num()};
        ml::native_soa::require(index >= 0 && index <= old_num && count >= 0 &&
                                count <= old_num - index);
        auto const moved{std::min(count, old_num - index - count)};
        auto const source{old_num - moved};
        for (size_type i{}; i < moved; ++i) {
            registry_indices[index + i] = registry_indices[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            registry_generations[index + i] = registry_generations[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            entity_types[index + i] = entity_types[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            teams[index + i] = teams[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            kills[index + i] = kills[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            alive[index + i] = alive[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            killed_by[index + i] = killed_by[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            death_reason[index + i] = death_reason[source + i];
        }
        set_num(old_num - count);
    }
    void set(size_type const index,
             FRegistryEntityHandle::index_type const new_registry_indices,
             FRegistryEntityHandle::generation_type const new_registry_generations,
             ml::simulation::EntityType const new_entity_types,
             ml::simulation::Team const new_teams,
             std::uint32_t const new_kills,
             std::uint8_t const new_alive,
             ml::simulation::EntityUniqueId const new_killed_by,
             ml::simulation::DeathReason const new_death_reason) {
        get_view().set(index,
                       new_registry_indices,
                       new_registry_generations,
                       new_entity_types,
                       new_teams,
                       new_kills,
                       new_alive,
                       new_killed_by,
                       new_death_reason);
    }
    auto add(FRegistryEntityHandle::index_type const new_registry_indices,
             FRegistryEntityHandle::generation_type const new_registry_generations,
             ml::simulation::EntityType const new_entity_types,
             ml::simulation::Team const new_teams,
             std::uint32_t const new_kills,
             std::uint8_t const new_alive,
             ml::simulation::EntityUniqueId const new_killed_by,
             ml::simulation::DeathReason const new_death_reason) -> size_type {
        auto const index{num()};
        add_defaulted(1);
        set(index,
            new_registry_indices,
            new_registry_generations,
            new_entity_types,
            new_teams,
            new_kills,
            new_alive,
            new_killed_by,
            new_death_reason);
        return index;
    }
    void append_from(ConstView source) {
        auto const count{source.num()};
        ml::native_soa::require(count <= std::numeric_limits<size_type>::max() - num());
        source.validate_array_sizes();
        if (count == 0) {
            return;
        }
        {
            auto const address{reinterpret_cast<std::uintptr_t>(source.registry_indices.data())};
            auto const begin{reinterpret_cast<std::uintptr_t>(registry_indices.data())};
            ml::native_soa::require(address < begin ||
                                    address >=
                                        begin + registry_indices.size() *
                                                    sizeof(FRegistryEntityHandle::index_type));
        }
        {
            auto const address{
                reinterpret_cast<std::uintptr_t>(source.registry_generations.data())};
            auto const begin{reinterpret_cast<std::uintptr_t>(registry_generations.data())};
            ml::native_soa::require(address < begin ||
                                    address >=
                                        begin + registry_generations.size() *
                                                    sizeof(FRegistryEntityHandle::generation_type));
        }
        {
            auto const address{reinterpret_cast<std::uintptr_t>(source.entity_types.data())};
            auto const begin{reinterpret_cast<std::uintptr_t>(entity_types.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + entity_types.size() *
                                                           sizeof(ml::simulation::EntityType));
        }
        {
            auto const address{reinterpret_cast<std::uintptr_t>(source.teams.data())};
            auto const begin{reinterpret_cast<std::uintptr_t>(teams.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + teams.size() * sizeof(ml::simulation::Team));
        }
        {
            auto const address{reinterpret_cast<std::uintptr_t>(source.kills.data())};
            auto const begin{reinterpret_cast<std::uintptr_t>(kills.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + kills.size() * sizeof(std::uint32_t));
        }
        {
            auto const address{reinterpret_cast<std::uintptr_t>(source.alive.data())};
            auto const begin{reinterpret_cast<std::uintptr_t>(alive.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + alive.size() * sizeof(std::uint8_t));
        }
        {
            auto const address{reinterpret_cast<std::uintptr_t>(source.killed_by.data())};
            auto const begin{reinterpret_cast<std::uintptr_t>(killed_by.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + killed_by.size() *
                                                           sizeof(ml::simulation::EntityUniqueId));
        }
        {
            auto const address{reinterpret_cast<std::uintptr_t>(source.death_reason.data())};
            auto const begin{reinterpret_cast<std::uintptr_t>(death_reason.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + death_reason.size() *
                                                           sizeof(ml::simulation::DeathReason));
        }
        registry_indices.insert(
            registry_indices.end(), source.registry_indices.begin(), source.registry_indices.end());
        registry_generations.insert(registry_generations.end(),
                                    source.registry_generations.begin(),
                                    source.registry_generations.end());
        entity_types.insert(
            entity_types.end(), source.entity_types.begin(), source.entity_types.end());
        teams.insert(teams.end(), source.teams.begin(), source.teams.end());
        kills.insert(kills.end(), source.kills.begin(), source.kills.end());
        alive.insert(alive.end(), source.alive.begin(), source.alive.end());
        killed_by.insert(killed_by.end(), source.killed_by.begin(), source.killed_by.end());
        death_reason.insert(
            death_reason.end(), source.death_reason.begin(), source.death_reason.end());
    }
    auto get_view() -> View {
        return {
            registry_indices,
            registry_generations,
            entity_types,
            teams,
            kills,
            alive,
            killed_by,
            death_reason,
        };
    }
    auto get_view() const -> ConstView {
        return {
            registry_indices,
            registry_generations,
            entity_types,
            teams,
            kills,
            alive,
            killed_by,
            death_reason,
        };
    }
    auto get_const_view() const -> ConstView { return get_view(); }
    auto get_view(size_type const offset, size_type const count) -> View {
        return get_view().slice(offset, count);
    }
    auto get_view(size_type const offset, size_type const count) const -> ConstView {
        return get_view().slice(offset, count);
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto slice(size_type const offset, size_type const count) -> View {
        return get_view(offset, count);
    }
    auto slice(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view(offset, count);
    }
    auto left(size_type const count) -> View { return slice(0, count); }
    auto right(size_type const count) -> View { return slice(num() - count, count); }
    auto left(size_type const count) const -> ConstView { return slice(0, count); }
    auto right(size_type const count) const -> ConstView { return slice(num() - count, count); }
    template <typename Other>
    void copy_element(size_type const dst_index, Other const& other, size_type const src_index) {
        registry_indices[static_cast<std::size_t>(dst_index)] =
            other.registry_indices[static_cast<std::size_t>(src_index)];
        registry_generations[static_cast<std::size_t>(dst_index)] =
            other.registry_generations[static_cast<std::size_t>(src_index)];
        entity_types[static_cast<std::size_t>(dst_index)] =
            other.entity_types[static_cast<std::size_t>(src_index)];
        teams[static_cast<std::size_t>(dst_index)] =
            other.teams[static_cast<std::size_t>(src_index)];
        kills[static_cast<std::size_t>(dst_index)] =
            other.kills[static_cast<std::size_t>(src_index)];
        alive[static_cast<std::size_t>(dst_index)] =
            other.alive[static_cast<std::size_t>(src_index)];
        killed_by[static_cast<std::size_t>(dst_index)] =
            other.killed_by[static_cast<std::size_t>(src_index)];
        death_reason[static_cast<std::size_t>(dst_index)] =
            other.death_reason[static_cast<std::size_t>(src_index)];
    }
    template <typename Other>
    void copy_elements(size_type const dst_index,
                       Other const& other,
                       size_type const src_index,
                       size_type const count) {
        for (size_type i{}; i < count; ++i) {
            copy_element(dst_index + i, other, src_index + i);
        }
    }
    void apply_permutation(std::span<std::int32_t> const indices) {
        validate_array_sizes();
        ml::native_soa::require(indices.size() == static_cast<std::size_t>(num()));
        each_column([indices](auto& column) { ml::apply_permutation(std::span{column}, indices); });
    }
    template <typename Compare>
    void sort(Compare&& compare, std::span<std::int32_t> const scratch_indices) {
        validate_array_sizes();
        ml::native_soa::require(scratch_indices.size() == static_cast<std::size_t>(num()));
        for (size_type i{}; i < num(); ++i) {
            scratch_indices[static_cast<std::size_t>(i)] = i;
        }
        std::sort(scratch_indices.begin(),
                  scratch_indices.end(),
                  [this, &compare](size_type const lhs, size_type const rhs) {
                      return compare(*this, lhs, rhs);
                  });
        apply_permutation(scratch_indices);
    }
};

struct EntityHistoryColumnsSingleView;
struct EntityHistoryColumnsSingleConstView;
struct EntityHistoryColumnsSingleLayout {
    using size_type = std::int32_t;
    using byte_size_type = std::size_t;

    inline static constexpr byte_size_type max_allocation_size{
        std::numeric_limits<byte_size_type>::max()};
    inline static constexpr size_type capacity_granularity{64};
    inline static constexpr byte_size_type column_gap{192};

    template <typename T>
    using ColLayout = ml::native_soa::ColumnLayout<T>;
    inline static constexpr ml::native_soa::ColumnLayoutStart LayoutStart{
        capacity_granularity, column_gap, 64};

    inline static constexpr ColLayout<FRegistryEntityHandle::index_type> RegistryIndices{
        LayoutStart};
    inline static constexpr ColLayout<FRegistryEntityHandle::generation_type> RegistryGenerations{
        RegistryIndices};
    inline static constexpr ColLayout<ml::simulation::EntityType> EntityTypes{RegistryGenerations};
    inline static constexpr ColLayout<ml::simulation::Team> Teams{EntityTypes};
    inline static constexpr ColLayout<std::uint32_t> Kills{Teams};
    inline static constexpr ColLayout<std::uint8_t> Alive{Kills};
    inline static constexpr ColLayout<ml::simulation::EntityUniqueId> KilledBy{Alive};
    inline static constexpr ColLayout<ml::simulation::DeathReason> DeathReasonColumn{KilledBy};

    inline static constexpr byte_size_type allocation_alignment{
        ml::native_soa::maximum_alignment(RegistryIndices,
                                          RegistryGenerations,
                                          EntityTypes,
                                          Teams,
                                          Kills,
                                          Alive,
                                          KilledBy,
                                          DeathReasonColumn)};

    // Conservative per-block bound for checked capacity arithmetic; gaps do not scale with
    // capacity.
    inline static constexpr byte_size_type capacity_block_bound{
        ml::native_soa::layout_align(DeathReasonColumn.block_end, allocation_alignment) +
        7 * (column_gap + allocation_alignment - 1)};
    inline static constexpr size_type max_capacity{
        ml::native_soa::maximum_capacity(capacity_block_bound)};
    static constexpr auto layout_bytes(byte_size_type blocks) noexcept -> byte_size_type {
        return blocks == 0 ? 0 : DeathReasonColumn.data_end(blocks);
    }
  private:
    inline static constexpr auto validate_layout = []() consteval -> bool {
        static_assert(
            ml::native_soa::supported_leaf<FRegistryEntityHandle::index_type>,
            "Single-allocation leaf registry_indices requires a non-cv, trivially "
            "copyable/copy-constructible/destructible, nothrow default-constructible object type.");
        static_assert(
            ml::native_soa::supported_leaf<FRegistryEntityHandle::generation_type>,
            "Single-allocation leaf registry_generations requires a non-cv, trivially "
            "copyable/copy-constructible/destructible, nothrow default-constructible object type.");
        static_assert(
            ml::native_soa::supported_leaf<ml::simulation::EntityType>,
            "Single-allocation leaf entity_types requires a non-cv, trivially "
            "copyable/copy-constructible/destructible, nothrow default-constructible object type.");
        static_assert(
            ml::native_soa::supported_leaf<ml::simulation::Team>,
            "Single-allocation leaf teams requires a non-cv, trivially "
            "copyable/copy-constructible/destructible, nothrow default-constructible object type.");
        static_assert(
            ml::native_soa::supported_leaf<std::uint32_t>,
            "Single-allocation leaf kills requires a non-cv, trivially "
            "copyable/copy-constructible/destructible, nothrow default-constructible object type.");
        static_assert(
            ml::native_soa::supported_leaf<std::uint8_t>,
            "Single-allocation leaf alive requires a non-cv, trivially "
            "copyable/copy-constructible/destructible, nothrow default-constructible object type.");
        static_assert(
            ml::native_soa::supported_leaf<ml::simulation::EntityUniqueId>,
            "Single-allocation leaf killed_by requires a non-cv, trivially "
            "copyable/copy-constructible/destructible, nothrow default-constructible object type.");
        static_assert(
            ml::native_soa::supported_leaf<ml::simulation::DeathReason>,
            "Single-allocation leaf death_reason requires a non-cv, trivially "
            "copyable/copy-constructible/destructible, nothrow default-constructible object type.");

        static_assert(
            allocation_alignment <= std::numeric_limits<std::uint32_t>::max(),
            "Single-allocation alignment must fit the allocator's 32-bit alignment argument.");
        static_assert(sizeof(FRegistryEntityHandle::index_type) <=
                      (max_allocation_size - RegistryIndices.block_offset) / capacity_granularity);
        static_assert(sizeof(FRegistryEntityHandle::generation_type) <=
                      (max_allocation_size - RegistryGenerations.block_offset) /
                          capacity_granularity);
        static_assert(sizeof(ml::simulation::EntityType) <=
                      (max_allocation_size - EntityTypes.block_offset) / capacity_granularity);
        static_assert(sizeof(ml::simulation::Team) <=
                      (max_allocation_size - Teams.block_offset) / capacity_granularity);
        static_assert(sizeof(std::uint32_t) <=
                      (max_allocation_size - Kills.block_offset) / capacity_granularity);
        static_assert(sizeof(std::uint8_t) <=
                      (max_allocation_size - Alive.block_offset) / capacity_granularity);
        static_assert(sizeof(ml::simulation::EntityUniqueId) <=
                      (max_allocation_size - KilledBy.block_offset) / capacity_granularity);
        static_assert(sizeof(ml::simulation::DeathReason) <=
                      (max_allocation_size - DeathReasonColumn.block_offset) /
                          capacity_granularity);
        static_assert(
            7 <= (max_allocation_size -
                  ml::native_soa::layout_align(DeathReasonColumn.block_end, allocation_alignment)) /
                     (column_gap + allocation_alignment - 1));
        static_assert(max_capacity >= capacity_granularity);
        return true;
    };
    static_assert(validate_layout());
};

struct EntityHistoryStorage
    : EntityHistoryColumnsSingleLayout
    , protected ml::native_soa::StorageState
    , ml::native_soa::StorageOperations {
    using View = EntityHistoryColumnsSingleView;
    using ConstView = EntityHistoryColumnsSingleConstView;
    /* **************************************** */
    // Lifetime
    /* **************************************** */
    EntityHistoryStorage() noexcept = default;
    ~EntityHistoryStorage() { ml::native_soa::free(data_, allocation_alignment); }
    EntityHistoryStorage(EntityHistoryStorage const&) = delete;
    auto operator=(EntityHistoryStorage const&) -> EntityHistoryStorage& = delete;
    EntityHistoryStorage(EntityHistoryStorage&& other) noexcept
        : StorageState{std::exchange(other.data_, nullptr),
                       std::exchange(other.num_, 0),
                       std::exchange(other.capacity_, 0)} {}
    auto operator=(EntityHistoryStorage&& other) noexcept -> EntityHistoryStorage& {
        if (this != &other) {
            ml::native_soa::free(data_, allocation_alignment);
            data_ = std::exchange(other.data_, nullptr);
            num_ = std::exchange(other.num_, 0);
            capacity_ = std::exchange(other.capacity_, 0);
        }
        return *this;
    }
  protected:
    template <typename Byte>
    struct DataPointers {
        template <typename T>
        using Element = std::conditional_t<std::is_const_v<Byte>, T const, T>;
        Element<FRegistryEntityHandle::index_type>* registry_indices{};
        Element<FRegistryEntityHandle::generation_type>* registry_generations{};
        Element<ml::simulation::EntityType>* entity_types{};
        Element<ml::simulation::Team>* teams{};
        Element<std::uint32_t>* kills{};
        Element<std::uint8_t>* alive{};
        Element<ml::simulation::EntityUniqueId>* killed_by{};
        Element<ml::simulation::DeathReason>* death_reason{};
        auto operator+(size_type const offset) const noexcept -> DataPointers {
            if (registry_indices == nullptr) {
                return {};
            }
            return {registry_indices + offset,
                    registry_generations + offset,
                    entity_types + offset,
                    teams + offset,
                    kills + offset,
                    alive + offset,
                    killed_by + offset,
                    death_reason + offset};
        }
    };
    template <typename Self>
    auto get_data(this Self& self) noexcept {
        using Byte = std::conditional_t<std::is_const_v<Self>, std::byte const, std::byte>;
        if (self.data_ == nullptr) {
            return DataPointers<Byte>{};
        }
        return make_data_unchecked(static_cast<Byte*>(self.data_), self.capacity_blocks());
    }
    template <typename Self>
    auto get_data(this Self& self, size_type const offset) noexcept {
        return self.get_data() + offset;
    }
  private:
    friend struct ml::native_soa::StorageOperations;
    /* **************************************** */
    // Column pointers
    /* **************************************** */
    template <typename Byte>
    static auto make_data_unchecked(Byte* const data, byte_size_type const blocks) noexcept
        -> DataPointers<Byte> {
        auto const pointer_at = [data, blocks](auto const& column) noexcept {
            using Column = std::remove_cvref_t<decltype(column)>;
            using Pointer = std::conditional_t<std::is_const_v<Byte>,
                                               typename Column::const_pointer,
                                               typename Column::pointer>;
            return std::launder(reinterpret_cast<Pointer>(data + column.offset(blocks)));
        };
        return {pointer_at(RegistryIndices),
                pointer_at(RegistryGenerations),
                pointer_at(EntityTypes),
                pointer_at(Teams),
                pointer_at(Kills),
                pointer_at(Alive),
                pointer_at(KilledBy),
                pointer_at(DeathReasonColumn)};
    }
    auto capacity_blocks() const noexcept -> byte_size_type {
        return static_cast<byte_size_type>(capacity_ / capacity_granularity);
    }

    /* **************************************** */
    // Typed mutations and growth
    /* **************************************** */
    void default_construct_columns(size_type const first, size_type const count) {
        auto const columns{make_data_unchecked(data_, capacity_blocks()) + first};
        std::uninitialized_value_construct_n<FRegistryEntityHandle::index_type*>(
            columns.registry_indices, count);
        std::uninitialized_value_construct_n<FRegistryEntityHandle::generation_type*>(
            columns.registry_generations, count);
        std::uninitialized_value_construct_n<ml::simulation::EntityType*>(columns.entity_types,
                                                                          count);
        std::uninitialized_value_construct_n<ml::simulation::Team*>(columns.teams, count);
        std::uninitialized_value_construct_n<std::uint32_t*>(columns.kills, count);
        std::uninitialized_value_construct_n<std::uint8_t*>(columns.alive, count);
        std::uninitialized_value_construct_n<ml::simulation::EntityUniqueId*>(columns.killed_by,
                                                                              count);
        std::uninitialized_value_construct_n<ml::simulation::DeathReason*>(columns.death_reason,
                                                                           count);
    }
    void swap_remove_columns(size_type const index,
                             size_type const source,
                             size_type const move_count) {
        copy_columns(get_data(), index, source, move_count);
    }
    static void copy_columns(DataPointers<std::byte> const& columns,
                             size_type index,
                             size_type source,
                             size_type move_count) {
        auto const elements_to_move{static_cast<byte_size_type>(move_count)};
        auto const registry_indices_bytes{elements_to_move *
                                          sizeof(FRegistryEntityHandle::index_type)};
        auto const registry_generations_bytes{elements_to_move *
                                              sizeof(FRegistryEntityHandle::generation_type)};
        auto const entity_types_bytes{elements_to_move * sizeof(ml::simulation::EntityType)};
        auto const teams_bytes{elements_to_move * sizeof(ml::simulation::Team)};
        auto const kills_bytes{elements_to_move * sizeof(std::uint32_t)};
        auto const alive_bytes{elements_to_move * sizeof(std::uint8_t)};
        auto const killed_by_bytes{elements_to_move * sizeof(ml::simulation::EntityUniqueId)};
        auto const death_reason_bytes{elements_to_move * sizeof(ml::simulation::DeathReason)};
        std::memcpy(columns.registry_indices + index,
                    columns.registry_indices + source,
                    registry_indices_bytes);
        std::memcpy(columns.registry_generations + index,
                    columns.registry_generations + source,
                    registry_generations_bytes);
        std::memcpy(
            columns.entity_types + index, columns.entity_types + source, entity_types_bytes);
        std::memcpy(columns.teams + index, columns.teams + source, teams_bytes);
        std::memcpy(columns.kills + index, columns.kills + source, kills_bytes);
        std::memcpy(columns.alive + index, columns.alive + source, alive_bytes);
        std::memcpy(columns.killed_by + index, columns.killed_by + source, killed_by_bytes);
        std::memcpy(
            columns.death_reason + index, columns.death_reason + source, death_reason_bytes);
    }
    void swap_remove_indices(std::span<size_type const> indices) {
        auto const columns{get_data()};
        ml::soa_storage_detail::for_each_removal_run(
            num_,
            indices,
            ml::native_soa::require,
            [&](size_type index, size_type source, size_type count) {
                copy_columns(columns, index, source, count);
            });
    }
    template <typename Columns>
    void append_columns(Columns const& source, size_type first, size_type count) {
        auto const destination{get_data(first)};
        auto const elements_to_copy{static_cast<byte_size_type>(count)};
        auto const registry_indices_bytes{elements_to_copy *
                                          sizeof(FRegistryEntityHandle::index_type)};
        auto const registry_generations_bytes{elements_to_copy *
                                              sizeof(FRegistryEntityHandle::generation_type)};
        auto const entity_types_bytes{elements_to_copy * sizeof(ml::simulation::EntityType)};
        auto const teams_bytes{elements_to_copy * sizeof(ml::simulation::Team)};
        auto const kills_bytes{elements_to_copy * sizeof(std::uint32_t)};
        auto const alive_bytes{elements_to_copy * sizeof(std::uint8_t)};
        auto const killed_by_bytes{elements_to_copy * sizeof(ml::simulation::EntityUniqueId)};
        auto const death_reason_bytes{elements_to_copy * sizeof(ml::simulation::DeathReason)};
        std::memcpy(
            destination.registry_indices, source.registry_indices.data(), registry_indices_bytes);
        std::memcpy(destination.registry_generations,
                    source.registry_generations.data(),
                    registry_generations_bytes);
        std::memcpy(destination.entity_types, source.entity_types.data(), entity_types_bytes);
        std::memcpy(destination.teams, source.teams.data(), teams_bytes);
        std::memcpy(destination.kills, source.kills.data(), kills_bytes);
        std::memcpy(destination.alive, source.alive.data(), alive_bytes);
        std::memcpy(destination.killed_by, source.killed_by.data(), killed_by_bytes);
        std::memcpy(destination.death_reason, source.death_reason.data(), death_reason_bytes);
    }
    void reallocate(size_type const new_capacity) {
        auto* const new_data{ml::native_soa::allocate(
            layout_bytes(static_cast<byte_size_type>(new_capacity / capacity_granularity)),
            static_cast<std::uint32_t>(allocation_alignment))};
        if (num_ > 0) {
            auto const old_blocks{capacity_blocks()};
            auto const new_blocks{static_cast<byte_size_type>(new_capacity / capacity_granularity)};
            auto const source{
                make_data_unchecked(static_cast<std::byte const*>(data_), old_blocks)};
            auto const destination{make_data_unchecked(new_data, new_blocks)};
            auto const live_count{static_cast<byte_size_type>(num_)};
            auto const registry_indices_bytes{live_count *
                                              sizeof(FRegistryEntityHandle::index_type)};
            auto const registry_generations_bytes{live_count *
                                                  sizeof(FRegistryEntityHandle::generation_type)};
            auto const entity_types_bytes{live_count * sizeof(ml::simulation::EntityType)};
            auto const teams_bytes{live_count * sizeof(ml::simulation::Team)};
            auto const kills_bytes{live_count * sizeof(std::uint32_t)};
            auto const alive_bytes{live_count * sizeof(std::uint8_t)};
            auto const killed_by_bytes{live_count * sizeof(ml::simulation::EntityUniqueId)};
            auto const death_reason_bytes{live_count * sizeof(ml::simulation::DeathReason)};
            std::memcpy(
                destination.registry_indices, source.registry_indices, registry_indices_bytes);
            std::memcpy(destination.registry_generations,
                        source.registry_generations,
                        registry_generations_bytes);
            std::memcpy(destination.entity_types, source.entity_types, entity_types_bytes);
            std::memcpy(destination.teams, source.teams, teams_bytes);
            std::memcpy(destination.kills, source.kills, kills_bytes);
            std::memcpy(destination.alive, source.alive, alive_bytes);
            std::memcpy(destination.killed_by, source.killed_by, killed_by_bytes);
            std::memcpy(destination.death_reason, source.death_reason, death_reason_bytes);
        }
        ml::native_soa::free(data_, allocation_alignment);
        data_ = new_data;
        capacity_ = new_capacity;
    }
};

struct EntityHistoryColumnsSingleConstView : ml::native_soa::CompactViewState<true> {
    using Base = ml::native_soa::CompactViewState<true>;
    using Base::Base;
    using View = EntityHistoryColumnsSingleView;
    using ConstView = EntityHistoryColumnsSingleConstView;
    EntityHistoryColumnsSingleConstView() = default;
    EntityHistoryColumnsSingleConstView(EntityHistoryColumnsSingleView const& other);
    auto get_const_view() const -> ConstView { return *this; }
    auto get_const_view(size_type offset, size_type count) const -> ConstView {
        return slice(offset, count);
    }
    auto registry_indices() const -> std::span<FRegistryEntityHandle::index_type const> {
        return {column_data<FRegistryEntityHandle::index_type>(
                    EntityHistoryColumnsSingleLayout::RegistryIndices.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto registry_generations() const -> std::span<FRegistryEntityHandle::generation_type const> {
        return {
            column_data<FRegistryEntityHandle::generation_type>(
                EntityHistoryColumnsSingleLayout::RegistryGenerations.offset(capacity_blocks())),
            static_cast<std::size_t>(count_)};
    }
    auto entity_types() const -> std::span<ml::simulation::EntityType const> {
        return {column_data<ml::simulation::EntityType>(
                    EntityHistoryColumnsSingleLayout::EntityTypes.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto teams() const -> std::span<ml::simulation::Team const> {
        return {column_data<ml::simulation::Team>(
                    EntityHistoryColumnsSingleLayout::Teams.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto kills() const -> std::span<std::uint32_t const> {
        return {column_data<std::uint32_t>(
                    EntityHistoryColumnsSingleLayout::Kills.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto alive() const -> std::span<std::uint8_t const> {
        return {column_data<std::uint8_t>(
                    EntityHistoryColumnsSingleLayout::Alive.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto killed_by() const -> std::span<ml::simulation::EntityUniqueId const> {
        return {column_data<ml::simulation::EntityUniqueId>(
                    EntityHistoryColumnsSingleLayout::KilledBy.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto death_reason() const -> std::span<ml::simulation::DeathReason const> {
        return {column_data<ml::simulation::DeathReason>(
                    EntityHistoryColumnsSingleLayout::DeathReasonColumn.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto columns() const -> EntityHistoryColumnsConstView {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        return EntityHistoryColumnsConstView{
            {column_data_unchecked<FRegistryEntityHandle::index_type>(
                 EntityHistoryColumnsSingleLayout::RegistryIndices.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<FRegistryEntityHandle::generation_type>(
                 EntityHistoryColumnsSingleLayout::RegistryGenerations.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<ml::simulation::EntityType>(
                 EntityHistoryColumnsSingleLayout::EntityTypes.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<ml::simulation::Team>(
                 EntityHistoryColumnsSingleLayout::Teams.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<std::uint32_t>(
                 EntityHistoryColumnsSingleLayout::Kills.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<std::uint8_t>(
                 EntityHistoryColumnsSingleLayout::Alive.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<ml::simulation::EntityUniqueId>(
                 EntityHistoryColumnsSingleLayout::KilledBy.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<ml::simulation::DeathReason>(
                 EntityHistoryColumnsSingleLayout::DeathReasonColumn.offset(blocks)),
             static_cast<std::size_t>(count_)}};
    }
    template <typename Func>
    void each_column(Func&& func) const {
        columns().each_column(std::forward<Func>(func));
    }
};
static_assert(sizeof(EntityHistoryColumnsSingleConstView) == 16);
static_assert(std::is_trivially_copyable_v<EntityHistoryColumnsSingleConstView>);
struct EntityHistoryColumnsSingleView : ml::native_soa::CompactViewState<false> {
    using Base = ml::native_soa::CompactViewState<false>;
    using Base::Base;
    using View = EntityHistoryColumnsSingleView;
    using ConstView = EntityHistoryColumnsSingleConstView;
    EntityHistoryColumnsSingleView() = default;
    auto get_const_view() const -> ConstView { return *this; }
    auto get_const_view(size_type offset, size_type count) const -> ConstView {
        return slice(offset, count);
    }
    auto registry_indices() const -> std::span<FRegistryEntityHandle::index_type> {
        return {column_data<FRegistryEntityHandle::index_type>(
                    EntityHistoryColumnsSingleLayout::RegistryIndices.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto registry_generations() const -> std::span<FRegistryEntityHandle::generation_type> {
        return {
            column_data<FRegistryEntityHandle::generation_type>(
                EntityHistoryColumnsSingleLayout::RegistryGenerations.offset(capacity_blocks())),
            static_cast<std::size_t>(count_)};
    }
    auto entity_types() const -> std::span<ml::simulation::EntityType> {
        return {column_data<ml::simulation::EntityType>(
                    EntityHistoryColumnsSingleLayout::EntityTypes.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto teams() const -> std::span<ml::simulation::Team> {
        return {column_data<ml::simulation::Team>(
                    EntityHistoryColumnsSingleLayout::Teams.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto kills() const -> std::span<std::uint32_t> {
        return {column_data<std::uint32_t>(
                    EntityHistoryColumnsSingleLayout::Kills.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto alive() const -> std::span<std::uint8_t> {
        return {column_data<std::uint8_t>(
                    EntityHistoryColumnsSingleLayout::Alive.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto killed_by() const -> std::span<ml::simulation::EntityUniqueId> {
        return {column_data<ml::simulation::EntityUniqueId>(
                    EntityHistoryColumnsSingleLayout::KilledBy.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto death_reason() const -> std::span<ml::simulation::DeathReason> {
        return {column_data<ml::simulation::DeathReason>(
                    EntityHistoryColumnsSingleLayout::DeathReasonColumn.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto columns() const -> EntityHistoryColumnsView {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        return EntityHistoryColumnsView{
            {column_data_unchecked<FRegistryEntityHandle::index_type>(
                 EntityHistoryColumnsSingleLayout::RegistryIndices.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<FRegistryEntityHandle::generation_type>(
                 EntityHistoryColumnsSingleLayout::RegistryGenerations.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<ml::simulation::EntityType>(
                 EntityHistoryColumnsSingleLayout::EntityTypes.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<ml::simulation::Team>(
                 EntityHistoryColumnsSingleLayout::Teams.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<std::uint32_t>(
                 EntityHistoryColumnsSingleLayout::Kills.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<std::uint8_t>(
                 EntityHistoryColumnsSingleLayout::Alive.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<ml::simulation::EntityUniqueId>(
                 EntityHistoryColumnsSingleLayout::KilledBy.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<ml::simulation::DeathReason>(
                 EntityHistoryColumnsSingleLayout::DeathReasonColumn.offset(blocks)),
             static_cast<std::size_t>(count_)}};
    }
    template <typename Func>
    void each_column(Func&& func) const {
        columns().each_column(std::forward<Func>(func));
    }
};
static_assert(sizeof(EntityHistoryColumnsSingleView) == 16);
static_assert(std::is_trivially_copyable_v<EntityHistoryColumnsSingleView>);
inline EntityHistoryColumnsSingleConstView::EntityHistoryColumnsSingleConstView(
    EntityHistoryColumnsSingleView const& other)
    : Base{other} {}
struct EntityHistory : EntityHistoryStorage {
    EntityHistory() noexcept = default;
    EntityHistory(EntityHistory const&) = delete;
    auto operator=(EntityHistory const&) -> EntityHistory& = delete;
    EntityHistory(EntityHistory&&) noexcept = default;
    auto operator=(EntityHistory&&) noexcept -> EntityHistory& = default;
    auto get_view() & -> View { return {this, 0, num()}; }
    auto get_view(size_type offset, size_type count) & -> View { return {this, offset, count}; }
    auto slice(size_type offset, size_type count) & -> View { return get_view(offset, count); }
    auto left(size_type count) & -> View { return get_view().left(count); }
    auto right(size_type count) & -> View { return get_view().right(count); }
    auto get_view() && -> View = delete;
    auto get_view(size_type, size_type) && -> View = delete;
    auto slice(size_type, size_type) && -> View = delete;
    auto left(size_type) && -> View = delete;
    auto right(size_type) && -> View = delete;
    auto get_view() const& -> ConstView { return {this, 0, num()}; }
    auto get_view(size_type offset, size_type count) const& -> ConstView {
        return {this, offset, count};
    }
    auto slice(size_type offset, size_type count) const& -> ConstView {
        return get_view(offset, count);
    }
    auto left(size_type count) const& -> ConstView { return get_view().left(count); }
    auto right(size_type count) const& -> ConstView { return get_view().right(count); }
    auto get_view() const&& -> ConstView = delete;
    auto get_view(size_type, size_type) const&& -> ConstView = delete;
    auto slice(size_type, size_type) const&& -> ConstView = delete;
    auto left(size_type) const&& -> ConstView = delete;
    auto right(size_type) const&& -> ConstView = delete;
    auto get_const_view() const& -> ConstView { return get_view(); }
    auto get_const_view(size_type offset, size_type count) const& -> ConstView {
        return get_view(offset, count);
    }
    auto get_const_view() const&& -> ConstView = delete;
    auto get_const_view(size_type, size_type) const&& -> ConstView = delete;
};
} // namespace ml::simulation
