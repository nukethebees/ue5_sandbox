// This file is autogenerated. Do not edit by hand.
// Edit lispb/schema and regenerate it instead.

#pragma once

#include "native_soa/storage.h"
#include "sandbox/core/soa_permutation.h"
#include "sandbox/simulation/entity_handle.h"
#include "sandbox/simulation/entity_types.h"

struct EntityDeathInfoView;
struct EntityDeathInfoConstView;
struct EntityDeathInfoConstView {
    using View = EntityDeathInfoView;
    using ConstView = EntityDeathInfoConstView;
    using size_type = std::int32_t;
    std::span<ml::simulation::DeathReason const> reasons;
    std::span<FRegistryEntityHandle const> victims;
    std::span<FRegistryEntityHandle const> killers;
    auto num() const noexcept -> size_type { return static_cast<size_type>(reasons.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(reasons);
        fn(victims);
        fn(killers);
    }
    void validate_array_sizes() const {
        auto const count{num()};
        each_column([count](auto column) {
            ml::native_soa::require(column.size() == static_cast<std::size_t>(count));
        });
    }
    auto slice(size_type const offset, size_type const count) const -> EntityDeathInfoConstView {
        ml::native_soa::require(offset >= 0 && count >= 0 && offset <= num() &&
                                count <= num() - offset);
        return {
            reasons.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            victims.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            killers.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
        };
    }
    auto get_view() const -> EntityDeathInfoConstView { return *this; }
    auto get_view(size_type const offset, size_type const count) const -> EntityDeathInfoConstView {
        return slice(offset, count);
    }
    auto get_const_view() const -> ConstView {
        return {
            reasons,
            victims,
            killers,
        };
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto left(size_type const count) const -> EntityDeathInfoConstView { return slice(0, count); }
    auto right(size_type const count) const -> EntityDeathInfoConstView {
        return slice(num() - count, count);
    }
};
struct EntityDeathInfoView {
    using View = EntityDeathInfoView;
    using ConstView = EntityDeathInfoConstView;
    using size_type = std::int32_t;
    std::span<ml::simulation::DeathReason> reasons;
    std::span<FRegistryEntityHandle> victims;
    std::span<FRegistryEntityHandle> killers;
    auto num() const noexcept -> size_type { return static_cast<size_type>(reasons.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(reasons);
        fn(victims);
        fn(killers);
    }
    void validate_array_sizes() const {
        auto const count{num()};
        each_column([count](auto column) {
            ml::native_soa::require(column.size() == static_cast<std::size_t>(count));
        });
    }
    auto slice(size_type const offset, size_type const count) const -> EntityDeathInfoView {
        ml::native_soa::require(offset >= 0 && count >= 0 && offset <= num() &&
                                count <= num() - offset);
        return {
            reasons.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            victims.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            killers.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
        };
    }
    auto get_view() const -> EntityDeathInfoView { return *this; }
    auto get_view(size_type const offset, size_type const count) const -> EntityDeathInfoView {
        return slice(offset, count);
    }
    auto get_const_view() const -> ConstView {
        return {
            reasons,
            victims,
            killers,
        };
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto left(size_type const count) const -> EntityDeathInfoView { return slice(0, count); }
    auto right(size_type const count) const -> EntityDeathInfoView {
        return slice(num() - count, count);
    }
    void set(size_type const index,
             ml::simulation::DeathReason const new_reasons,
             FRegistryEntityHandle const new_victims,
             FRegistryEntityHandle const new_killers) const {
        ml::native_soa::require(index >= 0 && index < num());
        reasons[static_cast<std::size_t>(index)] = new_reasons;
        victims[static_cast<std::size_t>(index)] = new_victims;
        killers[static_cast<std::size_t>(index)] = new_killers;
    }
};
struct EntityDeathInfo {
    using View = EntityDeathInfoView;
    using ConstView = EntityDeathInfoConstView;
    using size_type = std::int32_t;
    ml::native_soa::Vector<ml::simulation::DeathReason> reasons;
    ml::native_soa::Vector<FRegistryEntityHandle> victims;
    ml::native_soa::Vector<FRegistryEntityHandle> killers;
    auto num() const noexcept -> size_type { return static_cast<size_type>(reasons.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) {
        fn(reasons);
        fn(victims);
        fn(killers);
    }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(reasons);
        fn(victims);
        fn(killers);
    }
    void validate_array_sizes() const { get_const_view().validate_array_sizes(); }
    void reserve(size_type const count) {
        ml::native_soa::require(count >= 0);
        reasons.reserve(static_cast<std::size_t>(count));
        victims.reserve(static_cast<std::size_t>(count));
        killers.reserve(static_cast<std::size_t>(count));
    }
    void reset() noexcept {
        reasons.clear();
        victims.clear();
        killers.clear();
    }
    void set_num(size_type const count) {
        ml::native_soa::require(count >= 0);
        auto const size{static_cast<std::size_t>(count)};
        reasons.resize(size);
        victims.resize(size);
        killers.resize(size);
    }
    void add_uninitialised(size_type const count) {
        auto const old_num{num()};
        ml::native_soa::require(count >= 0 &&
                                count <= std::numeric_limits<size_type>::max() - old_num);
        set_num(old_num + count);
    }
    void add_defaulted(size_type const count) { add_uninitialised(count); }
    void remove_at_swap(size_type const index, size_type const count) {
        auto const old_num{num()};
        ml::native_soa::require(index >= 0 && index <= old_num && count >= 0 &&
                                count <= old_num - index);
        auto const moved{std::min(count, old_num - index - count)};
        auto const source{old_num - moved};
        for (size_type i{}; i < moved; ++i) {
            reasons[index + i] = reasons[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            victims[index + i] = victims[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            killers[index + i] = killers[source + i];
        }
        set_num(old_num - count);
    }
    void set(size_type const index,
             ml::simulation::DeathReason const new_reasons,
             FRegistryEntityHandle const new_victims,
             FRegistryEntityHandle const new_killers) {
        get_view().set(index, new_reasons, new_victims, new_killers);
    }
    auto add(ml::simulation::DeathReason const new_reasons,
             FRegistryEntityHandle const new_victims,
             FRegistryEntityHandle const new_killers) -> size_type {
        auto const index{num()};
        add_defaulted(1);
        set(index, new_reasons, new_victims, new_killers);
        return index;
    }
    void append_from(ConstView source) {
        auto const count{source.num()};
        ml::native_soa::require(count <= std::numeric_limits<size_type>::max() - num());
        source.validate_array_sizes();
        if (count == 0) {
            return;
        }
        {
            auto const address{reinterpret_cast<std::uintptr_t>(source.reasons.data())};
            auto const begin{reinterpret_cast<std::uintptr_t>(reasons.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + reasons.size() *
                                                           sizeof(ml::simulation::DeathReason));
        }
        {
            auto const address{reinterpret_cast<std::uintptr_t>(source.victims.data())};
            auto const begin{reinterpret_cast<std::uintptr_t>(victims.data())};
            ml::native_soa::require(address < begin ||
                                    address >=
                                        begin + victims.size() * sizeof(FRegistryEntityHandle));
        }
        {
            auto const address{reinterpret_cast<std::uintptr_t>(source.killers.data())};
            auto const begin{reinterpret_cast<std::uintptr_t>(killers.data())};
            ml::native_soa::require(address < begin ||
                                    address >=
                                        begin + killers.size() * sizeof(FRegistryEntityHandle));
        }
        reasons.insert(reasons.end(), source.reasons.begin(), source.reasons.end());
        victims.insert(victims.end(), source.victims.begin(), source.victims.end());
        killers.insert(killers.end(), source.killers.begin(), source.killers.end());
    }
    auto get_view() -> View {
        return {
            reasons,
            victims,
            killers,
        };
    }
    auto get_view() const -> ConstView {
        return {
            reasons,
            victims,
            killers,
        };
    }
    auto get_const_view() const -> ConstView { return get_view(); }
    auto get_view(size_type const offset, size_type const count) -> View {
        return get_view().slice(offset, count);
    }
    auto get_view(size_type const offset, size_type const count) const -> ConstView {
        return get_view().slice(offset, count);
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto slice(size_type const offset, size_type const count) -> View {
        return get_view(offset, count);
    }
    auto slice(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view(offset, count);
    }
    auto left(size_type const count) -> View { return slice(0, count); }
    auto right(size_type const count) -> View { return slice(num() - count, count); }
    auto left(size_type const count) const -> ConstView { return slice(0, count); }
    auto right(size_type const count) const -> ConstView { return slice(num() - count, count); }
    template <typename Other>
    void copy_element(size_type const dst_index, Other const& other, size_type const src_index) {
        reasons[static_cast<std::size_t>(dst_index)] =
            other.reasons[static_cast<std::size_t>(src_index)];
        victims[static_cast<std::size_t>(dst_index)] =
            other.victims[static_cast<std::size_t>(src_index)];
        killers[static_cast<std::size_t>(dst_index)] =
            other.killers[static_cast<std::size_t>(src_index)];
    }
    template <typename Other>
    void copy_elements(size_type const dst_index,
                       Other const& other,
                       size_type const src_index,
                       size_type const count) {
        for (size_type i{}; i < count; ++i) {
            copy_element(dst_index + i, other, src_index + i);
        }
    }
    void apply_permutation(std::span<std::int32_t> const indices) {
        validate_array_sizes();
        ml::native_soa::require(indices.size() == static_cast<std::size_t>(num()));
        each_column([indices](auto& column) { ml::apply_permutation(std::span{column}, indices); });
    }
    template <typename Compare>
    void sort(Compare&& compare, std::span<std::int32_t> const scratch_indices) {
        validate_array_sizes();
        ml::native_soa::require(scratch_indices.size() == static_cast<std::size_t>(num()));
        for (size_type i{}; i < num(); ++i) {
            scratch_indices[static_cast<std::size_t>(i)] = i;
        }
        std::sort(scratch_indices.begin(),
                  scratch_indices.end(),
                  [this, &compare](size_type const lhs, size_type const rhs) {
                      return compare(*this, lhs, rhs);
                  });
        apply_permutation(scratch_indices);
    }
};
