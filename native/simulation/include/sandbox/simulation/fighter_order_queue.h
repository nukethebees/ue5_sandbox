// This file is autogenerated. Do not edit by hand.
// Edit lispb/schema and regenerate it instead.

#pragma once

#include "native_soa/storage.h"
#include "sandbox/core/soa_permutation.h"
#include "sandbox/simulation/entity_handle.h"
#include "sandbox/simulation/fighter_types.h"

struct TestCapitalShipFighterOrderQueueView;
struct TestCapitalShipFighterOrderQueueConstView;
struct TestCapitalShipFighterOrderQueueConstView {
    using View = TestCapitalShipFighterOrderQueueView;
    using ConstView = TestCapitalShipFighterOrderQueueConstView;
    using size_type = std::int32_t;
    std::span<FRegistryEntityHandle const> handles;
    std::span<ml::simulation::CapitalShipFighterOrder const> orders;
    std::span<ml::simulation::CapitalShipFighterTask const> tasks;
    std::span<FRegistryEntityHandle const> targets;
    auto num() const noexcept -> size_type { return static_cast<size_type>(handles.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(handles);
        fn(orders);
        fn(tasks);
        fn(targets);
    }
    void validate_array_sizes() const {
        auto const count{num()};
        each_column([count](auto column) {
            ml::native_soa::require(column.size() == static_cast<std::size_t>(count));
        });
    }
    auto slice(size_type const offset, size_type const count) const
        -> TestCapitalShipFighterOrderQueueConstView {
        ml::native_soa::require(offset >= 0 && count >= 0 && offset <= num() &&
                                count <= num() - offset);
        return {
            handles.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            orders.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            tasks.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            targets.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
        };
    }
    auto get_view() const -> TestCapitalShipFighterOrderQueueConstView { return *this; }
    auto get_view(size_type const offset, size_type const count) const
        -> TestCapitalShipFighterOrderQueueConstView {
        return slice(offset, count);
    }
    auto get_const_view() const -> ConstView {
        return {
            handles,
            orders,
            tasks,
            targets,
        };
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto left(size_type const count) const -> TestCapitalShipFighterOrderQueueConstView {
        return slice(0, count);
    }
    auto right(size_type const count) const -> TestCapitalShipFighterOrderQueueConstView {
        return slice(num() - count, count);
    }
};
struct TestCapitalShipFighterOrderQueueView {
    using View = TestCapitalShipFighterOrderQueueView;
    using ConstView = TestCapitalShipFighterOrderQueueConstView;
    using size_type = std::int32_t;
    std::span<FRegistryEntityHandle> handles;
    std::span<ml::simulation::CapitalShipFighterOrder> orders;
    std::span<ml::simulation::CapitalShipFighterTask> tasks;
    std::span<FRegistryEntityHandle> targets;
    auto num() const noexcept -> size_type { return static_cast<size_type>(handles.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(handles);
        fn(orders);
        fn(tasks);
        fn(targets);
    }
    void validate_array_sizes() const {
        auto const count{num()};
        each_column([count](auto column) {
            ml::native_soa::require(column.size() == static_cast<std::size_t>(count));
        });
    }
    auto slice(size_type const offset, size_type const count) const
        -> TestCapitalShipFighterOrderQueueView {
        ml::native_soa::require(offset >= 0 && count >= 0 && offset <= num() &&
                                count <= num() - offset);
        return {
            handles.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            orders.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            tasks.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            targets.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
        };
    }
    auto get_view() const -> TestCapitalShipFighterOrderQueueView { return *this; }
    auto get_view(size_type const offset, size_type const count) const
        -> TestCapitalShipFighterOrderQueueView {
        return slice(offset, count);
    }
    auto get_const_view() const -> ConstView {
        return {
            handles,
            orders,
            tasks,
            targets,
        };
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto left(size_type const count) const -> TestCapitalShipFighterOrderQueueView {
        return slice(0, count);
    }
    auto right(size_type const count) const -> TestCapitalShipFighterOrderQueueView {
        return slice(num() - count, count);
    }
    void set(size_type const index,
             FRegistryEntityHandle const new_handles,
             ml::simulation::CapitalShipFighterOrder const new_orders,
             ml::simulation::CapitalShipFighterTask const new_tasks,
             FRegistryEntityHandle const new_targets) const {
        ml::native_soa::require(index >= 0 && index < num());
        handles[static_cast<std::size_t>(index)] = new_handles;
        orders[static_cast<std::size_t>(index)] = new_orders;
        tasks[static_cast<std::size_t>(index)] = new_tasks;
        targets[static_cast<std::size_t>(index)] = new_targets;
    }
};
struct TestCapitalShipFighterOrderQueue {
    using View = TestCapitalShipFighterOrderQueueView;
    using ConstView = TestCapitalShipFighterOrderQueueConstView;
    using size_type = std::int32_t;
    ml::native_soa::Vector<FRegistryEntityHandle> handles;
    ml::native_soa::Vector<ml::simulation::CapitalShipFighterOrder> orders;
    ml::native_soa::Vector<ml::simulation::CapitalShipFighterTask> tasks;
    ml::native_soa::Vector<FRegistryEntityHandle> targets;
    auto num() const noexcept -> size_type { return static_cast<size_type>(handles.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) {
        fn(handles);
        fn(orders);
        fn(tasks);
        fn(targets);
    }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(handles);
        fn(orders);
        fn(tasks);
        fn(targets);
    }
    void validate_array_sizes() const { get_const_view().validate_array_sizes(); }
    void reserve(size_type const count) {
        ml::native_soa::require(count >= 0);
        handles.reserve(static_cast<std::size_t>(count));
        orders.reserve(static_cast<std::size_t>(count));
        tasks.reserve(static_cast<std::size_t>(count));
        targets.reserve(static_cast<std::size_t>(count));
    }
    void reset() noexcept {
        handles.clear();
        orders.clear();
        tasks.clear();
        targets.clear();
    }
    void set_num(size_type const count) {
        ml::native_soa::require(count >= 0);
        auto const size{static_cast<std::size_t>(count)};
        handles.resize(size);
        orders.resize(size);
        tasks.resize(size);
        targets.resize(size);
    }
    void add_uninitialised(size_type const count) {
        auto const old_num{num()};
        ml::native_soa::require(count >= 0 &&
                                count <= std::numeric_limits<size_type>::max() - old_num);
        set_num(old_num + count);
    }
    void add_defaulted(size_type const count) { add_uninitialised(count); }
    void remove_at_swap(size_type const index, size_type const count) {
        auto const old_num{num()};
        ml::native_soa::require(index >= 0 && index <= old_num && count >= 0 &&
                                count <= old_num - index);
        auto const moved{std::min(count, old_num - index - count)};
        auto const source{old_num - moved};
        for (size_type i{}; i < moved; ++i) {
            handles[index + i] = handles[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            orders[index + i] = orders[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            tasks[index + i] = tasks[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            targets[index + i] = targets[source + i];
        }
        set_num(old_num - count);
    }
    void set(size_type const index,
             FRegistryEntityHandle const new_handles,
             ml::simulation::CapitalShipFighterOrder const new_orders,
             ml::simulation::CapitalShipFighterTask const new_tasks,
             FRegistryEntityHandle const new_targets) {
        get_view().set(index, new_handles, new_orders, new_tasks, new_targets);
    }
    auto add(FRegistryEntityHandle const new_handles,
             ml::simulation::CapitalShipFighterOrder const new_orders,
             ml::simulation::CapitalShipFighterTask const new_tasks,
             FRegistryEntityHandle const new_targets) -> size_type {
        auto const index{num()};
        add_defaulted(1);
        set(index, new_handles, new_orders, new_tasks, new_targets);
        return index;
    }
    void append_from(ConstView source) {
        auto const count{source.num()};
        ml::native_soa::require(count <= std::numeric_limits<size_type>::max() - num());
        source.validate_array_sizes();
        if (count == 0) {
            return;
        }
        {
            auto const address{reinterpret_cast<std::uintptr_t>(source.handles.data())};
            auto const begin{reinterpret_cast<std::uintptr_t>(handles.data())};
            ml::native_soa::require(address < begin ||
                                    address >=
                                        begin + handles.size() * sizeof(FRegistryEntityHandle));
        }
        {
            auto const address{reinterpret_cast<std::uintptr_t>(source.orders.data())};
            auto const begin{reinterpret_cast<std::uintptr_t>(orders.data())};
            ml::native_soa::require(
                address < begin ||
                address >= begin + orders.size() * sizeof(ml::simulation::CapitalShipFighterOrder));
        }
        {
            auto const address{reinterpret_cast<std::uintptr_t>(source.tasks.data())};
            auto const begin{reinterpret_cast<std::uintptr_t>(tasks.data())};
            ml::native_soa::require(
                address < begin ||
                address >= begin + tasks.size() * sizeof(ml::simulation::CapitalShipFighterTask));
        }
        {
            auto const address{reinterpret_cast<std::uintptr_t>(source.targets.data())};
            auto const begin{reinterpret_cast<std::uintptr_t>(targets.data())};
            ml::native_soa::require(address < begin ||
                                    address >=
                                        begin + targets.size() * sizeof(FRegistryEntityHandle));
        }
        handles.insert(handles.end(), source.handles.begin(), source.handles.end());
        orders.insert(orders.end(), source.orders.begin(), source.orders.end());
        tasks.insert(tasks.end(), source.tasks.begin(), source.tasks.end());
        targets.insert(targets.end(), source.targets.begin(), source.targets.end());
    }
    auto get_view() -> View {
        return {
            handles,
            orders,
            tasks,
            targets,
        };
    }
    auto get_view() const -> ConstView {
        return {
            handles,
            orders,
            tasks,
            targets,
        };
    }
    auto get_const_view() const -> ConstView { return get_view(); }
    auto get_view(size_type const offset, size_type const count) -> View {
        return get_view().slice(offset, count);
    }
    auto get_view(size_type const offset, size_type const count) const -> ConstView {
        return get_view().slice(offset, count);
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto slice(size_type const offset, size_type const count) -> View {
        return get_view(offset, count);
    }
    auto slice(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view(offset, count);
    }
    auto left(size_type const count) -> View { return slice(0, count); }
    auto right(size_type const count) -> View { return slice(num() - count, count); }
    auto left(size_type const count) const -> ConstView { return slice(0, count); }
    auto right(size_type const count) const -> ConstView { return slice(num() - count, count); }
    template <typename Other>
    void copy_element(size_type const dst_index, Other const& other, size_type const src_index) {
        handles[static_cast<std::size_t>(dst_index)] =
            other.handles[static_cast<std::size_t>(src_index)];
        orders[static_cast<std::size_t>(dst_index)] =
            other.orders[static_cast<std::size_t>(src_index)];
        tasks[static_cast<std::size_t>(dst_index)] =
            other.tasks[static_cast<std::size_t>(src_index)];
        targets[static_cast<std::size_t>(dst_index)] =
            other.targets[static_cast<std::size_t>(src_index)];
    }
    template <typename Other>
    void copy_elements(size_type const dst_index,
                       Other const& other,
                       size_type const src_index,
                       size_type const count) {
        for (size_type i{}; i < count; ++i) {
            copy_element(dst_index + i, other, src_index + i);
        }
    }
    void apply_permutation(std::span<std::int32_t> const indices) {
        validate_array_sizes();
        ml::native_soa::require(indices.size() == static_cast<std::size_t>(num()));
        each_column([indices](auto& column) { ml::apply_permutation(std::span{column}, indices); });
    }
    template <typename Compare>
    void sort(Compare&& compare, std::span<std::int32_t> const scratch_indices) {
        validate_array_sizes();
        ml::native_soa::require(scratch_indices.size() == static_cast<std::size_t>(num()));
        for (size_type i{}; i < num(); ++i) {
            scratch_indices[static_cast<std::size_t>(i)] = i;
        }
        std::sort(scratch_indices.begin(),
                  scratch_indices.end(),
                  [this, &compare](size_type const lhs, size_type const rhs) {
                      return compare(*this, lhs, rhs);
                  });
        apply_permutation(scratch_indices);
    }
};
