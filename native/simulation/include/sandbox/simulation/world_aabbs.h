// This file is autogenerated. Do not edit by hand.
// Edit lispb/schema and regenerate it instead.

#pragma once

#include "native_soa/storage.h"
#include "sandbox/core/soa_permutation.h"

#include <cstring>
#include <memory>
#include <utility>

namespace ml::simulation::collision {
struct WorldAABBsColumnsView;
struct WorldAABBsColumnsConstView;
struct WorldAABBsColumnsConstView {
    using View = WorldAABBsColumnsView;
    using ConstView = WorldAABBsColumnsConstView;
    using size_type = std::int32_t;
    std::span<float const> min_xs;
    std::span<float const> min_ys;
    std::span<float const> min_zs;
    std::span<float const> max_xs;
    std::span<float const> max_ys;
    std::span<float const> max_zs;
    auto num() const noexcept -> size_type { return static_cast<size_type>(min_xs.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(min_xs);
        fn(min_ys);
        fn(min_zs);
        fn(max_xs);
        fn(max_ys);
        fn(max_zs);
    }
    void validate_array_sizes() const {
        auto const count{num()};
        each_column([count](auto column) {
            ml::native_soa::require(column.size() == static_cast<std::size_t>(count));
        });
    }
    auto slice(size_type const offset, size_type const count) const -> WorldAABBsColumnsConstView {
        ml::native_soa::require(offset >= 0 && count >= 0 && offset <= num() &&
                                count <= num() - offset);
        return {
            min_xs.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            min_ys.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            min_zs.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            max_xs.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            max_ys.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            max_zs.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
        };
    }
    auto get_view() const -> WorldAABBsColumnsConstView { return *this; }
    auto get_view(size_type const offset, size_type const count) const
        -> WorldAABBsColumnsConstView {
        return slice(offset, count);
    }
    auto get_const_view() const -> ConstView {
        return {
            min_xs,
            min_ys,
            min_zs,
            max_xs,
            max_ys,
            max_zs,
        };
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto left(size_type const count) const -> WorldAABBsColumnsConstView { return slice(0, count); }
    auto right(size_type const count) const -> WorldAABBsColumnsConstView {
        return slice(num() - count, count);
    }
};
struct WorldAABBsColumnsView {
    using View = WorldAABBsColumnsView;
    using ConstView = WorldAABBsColumnsConstView;
    using size_type = std::int32_t;
    std::span<float> min_xs;
    std::span<float> min_ys;
    std::span<float> min_zs;
    std::span<float> max_xs;
    std::span<float> max_ys;
    std::span<float> max_zs;
    auto num() const noexcept -> size_type { return static_cast<size_type>(min_xs.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(min_xs);
        fn(min_ys);
        fn(min_zs);
        fn(max_xs);
        fn(max_ys);
        fn(max_zs);
    }
    void validate_array_sizes() const {
        auto const count{num()};
        each_column([count](auto column) {
            ml::native_soa::require(column.size() == static_cast<std::size_t>(count));
        });
    }
    auto slice(size_type const offset, size_type const count) const -> WorldAABBsColumnsView {
        ml::native_soa::require(offset >= 0 && count >= 0 && offset <= num() &&
                                count <= num() - offset);
        return {
            min_xs.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            min_ys.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            min_zs.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            max_xs.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            max_ys.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            max_zs.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
        };
    }
    auto get_view() const -> WorldAABBsColumnsView { return *this; }
    auto get_view(size_type const offset, size_type const count) const -> WorldAABBsColumnsView {
        return slice(offset, count);
    }
    auto get_const_view() const -> ConstView {
        return {
            min_xs,
            min_ys,
            min_zs,
            max_xs,
            max_ys,
            max_zs,
        };
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto left(size_type const count) const -> WorldAABBsColumnsView { return slice(0, count); }
    auto right(size_type const count) const -> WorldAABBsColumnsView {
        return slice(num() - count, count);
    }
    void set(size_type const index,
             float const new_min_xs,
             float const new_min_ys,
             float const new_min_zs,
             float const new_max_xs,
             float const new_max_ys,
             float const new_max_zs) const {
        ml::native_soa::require(index >= 0 && index < num());
        min_xs[static_cast<std::size_t>(index)] = new_min_xs;
        min_ys[static_cast<std::size_t>(index)] = new_min_ys;
        min_zs[static_cast<std::size_t>(index)] = new_min_zs;
        max_xs[static_cast<std::size_t>(index)] = new_max_xs;
        max_ys[static_cast<std::size_t>(index)] = new_max_ys;
        max_zs[static_cast<std::size_t>(index)] = new_max_zs;
    }
};
struct WorldAABBsColumns {
    using View = WorldAABBsColumnsView;
    using ConstView = WorldAABBsColumnsConstView;
    using size_type = std::int32_t;
    ml::native_soa::Vector<float> min_xs;
    ml::native_soa::Vector<float> min_ys;
    ml::native_soa::Vector<float> min_zs;
    ml::native_soa::Vector<float> max_xs;
    ml::native_soa::Vector<float> max_ys;
    ml::native_soa::Vector<float> max_zs;
    auto num() const noexcept -> size_type { return static_cast<size_type>(min_xs.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) {
        fn(min_xs);
        fn(min_ys);
        fn(min_zs);
        fn(max_xs);
        fn(max_ys);
        fn(max_zs);
    }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(min_xs);
        fn(min_ys);
        fn(min_zs);
        fn(max_xs);
        fn(max_ys);
        fn(max_zs);
    }
    void validate_array_sizes() const { get_const_view().validate_array_sizes(); }
    void reserve(size_type const count) {
        ml::native_soa::require(count >= 0);
        min_xs.reserve(static_cast<std::size_t>(count));
        min_ys.reserve(static_cast<std::size_t>(count));
        min_zs.reserve(static_cast<std::size_t>(count));
        max_xs.reserve(static_cast<std::size_t>(count));
        max_ys.reserve(static_cast<std::size_t>(count));
        max_zs.reserve(static_cast<std::size_t>(count));
    }
    void reset() noexcept {
        min_xs.clear();
        min_ys.clear();
        min_zs.clear();
        max_xs.clear();
        max_ys.clear();
        max_zs.clear();
    }
    void set_num(size_type const count) {
        ml::native_soa::require(count >= 0);
        auto const size{static_cast<std::size_t>(count)};
        min_xs.resize(size);
        min_ys.resize(size);
        min_zs.resize(size);
        max_xs.resize(size);
        max_ys.resize(size);
        max_zs.resize(size);
    }
    void add_uninitialised(size_type const count) {
        auto const old_num{num()};
        ml::native_soa::require(count >= 0 &&
                                count <= std::numeric_limits<size_type>::max() - old_num);
        set_num(old_num + count);
    }
    void add_defaulted(size_type const count) { add_uninitialised(count); }
    void remove_at_swap(size_type const index, size_type const count) {
        auto const old_num{num()};
        ml::native_soa::require(index >= 0 && index <= old_num && count >= 0 &&
                                count <= old_num - index);
        auto const moved{std::min(count, old_num - index - count)};
        auto const source{old_num - moved};
        for (size_type i{}; i < moved; ++i) {
            min_xs[index + i] = min_xs[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            min_ys[index + i] = min_ys[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            min_zs[index + i] = min_zs[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            max_xs[index + i] = max_xs[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            max_ys[index + i] = max_ys[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            max_zs[index + i] = max_zs[source + i];
        }
        set_num(old_num - count);
    }
    void set(size_type const index,
             float const new_min_xs,
             float const new_min_ys,
             float const new_min_zs,
             float const new_max_xs,
             float const new_max_ys,
             float const new_max_zs) {
        get_view().set(
            index, new_min_xs, new_min_ys, new_min_zs, new_max_xs, new_max_ys, new_max_zs);
    }
    auto add(float const new_min_xs,
             float const new_min_ys,
             float const new_min_zs,
             float const new_max_xs,
             float const new_max_ys,
             float const new_max_zs) -> size_type {
        auto const index{num()};
        add_defaulted(1);
        set(index, new_min_xs, new_min_ys, new_min_zs, new_max_xs, new_max_ys, new_max_zs);
        return index;
    }
    void append_from(ConstView source) {
        auto const count{source.num()};
        ml::native_soa::require(count <= std::numeric_limits<size_type>::max() - num());
        source.validate_array_sizes();
        if (count == 0) {
            return;
        }
        {
            auto const address{reinterpret_cast<std::uintptr_t>(source.min_xs.data())};
            auto const begin{reinterpret_cast<std::uintptr_t>(min_xs.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + min_xs.size() * sizeof(float));
        }
        {
            auto const address{reinterpret_cast<std::uintptr_t>(source.min_ys.data())};
            auto const begin{reinterpret_cast<std::uintptr_t>(min_ys.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + min_ys.size() * sizeof(float));
        }
        {
            auto const address{reinterpret_cast<std::uintptr_t>(source.min_zs.data())};
            auto const begin{reinterpret_cast<std::uintptr_t>(min_zs.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + min_zs.size() * sizeof(float));
        }
        {
            auto const address{reinterpret_cast<std::uintptr_t>(source.max_xs.data())};
            auto const begin{reinterpret_cast<std::uintptr_t>(max_xs.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + max_xs.size() * sizeof(float));
        }
        {
            auto const address{reinterpret_cast<std::uintptr_t>(source.max_ys.data())};
            auto const begin{reinterpret_cast<std::uintptr_t>(max_ys.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + max_ys.size() * sizeof(float));
        }
        {
            auto const address{reinterpret_cast<std::uintptr_t>(source.max_zs.data())};
            auto const begin{reinterpret_cast<std::uintptr_t>(max_zs.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + max_zs.size() * sizeof(float));
        }
        min_xs.insert(min_xs.end(), source.min_xs.begin(), source.min_xs.end());
        min_ys.insert(min_ys.end(), source.min_ys.begin(), source.min_ys.end());
        min_zs.insert(min_zs.end(), source.min_zs.begin(), source.min_zs.end());
        max_xs.insert(max_xs.end(), source.max_xs.begin(), source.max_xs.end());
        max_ys.insert(max_ys.end(), source.max_ys.begin(), source.max_ys.end());
        max_zs.insert(max_zs.end(), source.max_zs.begin(), source.max_zs.end());
    }
    auto get_view() -> View {
        return {
            min_xs,
            min_ys,
            min_zs,
            max_xs,
            max_ys,
            max_zs,
        };
    }
    auto get_view() const -> ConstView {
        return {
            min_xs,
            min_ys,
            min_zs,
            max_xs,
            max_ys,
            max_zs,
        };
    }
    auto get_const_view() const -> ConstView { return get_view(); }
    auto get_view(size_type const offset, size_type const count) -> View {
        return get_view().slice(offset, count);
    }
    auto get_view(size_type const offset, size_type const count) const -> ConstView {
        return get_view().slice(offset, count);
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto slice(size_type const offset, size_type const count) -> View {
        return get_view(offset, count);
    }
    auto slice(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view(offset, count);
    }
    auto left(size_type const count) -> View { return slice(0, count); }
    auto right(size_type const count) -> View { return slice(num() - count, count); }
    auto left(size_type const count) const -> ConstView { return slice(0, count); }
    auto right(size_type const count) const -> ConstView { return slice(num() - count, count); }
    template <typename Other>
    void copy_element(size_type const dst_index, Other const& other, size_type const src_index) {
        min_xs[static_cast<std::size_t>(dst_index)] =
            other.min_xs[static_cast<std::size_t>(src_index)];
        min_ys[static_cast<std::size_t>(dst_index)] =
            other.min_ys[static_cast<std::size_t>(src_index)];
        min_zs[static_cast<std::size_t>(dst_index)] =
            other.min_zs[static_cast<std::size_t>(src_index)];
        max_xs[static_cast<std::size_t>(dst_index)] =
            other.max_xs[static_cast<std::size_t>(src_index)];
        max_ys[static_cast<std::size_t>(dst_index)] =
            other.max_ys[static_cast<std::size_t>(src_index)];
        max_zs[static_cast<std::size_t>(dst_index)] =
            other.max_zs[static_cast<std::size_t>(src_index)];
    }
    template <typename Other>
    void copy_elements(size_type const dst_index,
                       Other const& other,
                       size_type const src_index,
                       size_type const count) {
        for (size_type i{}; i < count; ++i) {
            copy_element(dst_index + i, other, src_index + i);
        }
    }
    void apply_permutation(std::span<std::int32_t> const indices) {
        validate_array_sizes();
        ml::native_soa::require(indices.size() == static_cast<std::size_t>(num()));
        each_column([indices](auto& column) { ml::apply_permutation(std::span{column}, indices); });
    }
    template <typename Compare>
    void sort(Compare&& compare, std::span<std::int32_t> const scratch_indices) {
        validate_array_sizes();
        ml::native_soa::require(scratch_indices.size() == static_cast<std::size_t>(num()));
        for (size_type i{}; i < num(); ++i) {
            scratch_indices[static_cast<std::size_t>(i)] = i;
        }
        std::sort(scratch_indices.begin(),
                  scratch_indices.end(),
                  [this, &compare](size_type const lhs, size_type const rhs) {
                      return compare(*this, lhs, rhs);
                  });
        apply_permutation(scratch_indices);
    }
};

struct WorldAABBsColumnsSingleView;
struct WorldAABBsColumnsSingleConstView;
struct WorldAABBsColumnsSingleLayout {
    using size_type = std::int32_t;
    using byte_size_type = std::size_t;

    inline static constexpr byte_size_type max_allocation_size{
        std::numeric_limits<byte_size_type>::max()};
    inline static constexpr size_type capacity_granularity{64};
    inline static constexpr byte_size_type column_gap{192};

    template <typename T>
    using ColLayout = ml::native_soa::ColumnLayout<T>;
    inline static constexpr ml::native_soa::ColumnLayoutStart LayoutStart{
        capacity_granularity, column_gap, 64};

    inline static constexpr ColLayout<float> MinXs{LayoutStart};
    inline static constexpr ColLayout<float> MinYs{MinXs};
    inline static constexpr ColLayout<float> MinZs{MinYs};
    inline static constexpr ColLayout<float> MaxXs{MinZs};
    inline static constexpr ColLayout<float> MaxYs{MaxXs};
    inline static constexpr ColLayout<float> MaxZs{MaxYs};

    inline static constexpr byte_size_type allocation_alignment{
        ml::native_soa::maximum_alignment(MinXs, MinYs, MinZs, MaxXs, MaxYs, MaxZs)};

    // Conservative per-block bound for checked capacity arithmetic; gaps do not scale with
    // capacity.
    inline static constexpr byte_size_type capacity_block_bound{
        ml::native_soa::layout_align(MaxZs.block_end, allocation_alignment) +
        5 * (column_gap + allocation_alignment - 1)};
    inline static constexpr size_type max_capacity{
        ml::native_soa::maximum_capacity(capacity_block_bound)};
    static constexpr auto layout_bytes(byte_size_type blocks) noexcept -> byte_size_type {
        return blocks == 0 ? 0 : MaxZs.data_end(blocks);
    }
  private:
    inline static constexpr auto validate_layout = []() consteval -> bool {
        static_assert(
            ml::native_soa::supported_leaf<float>,
            "Single-allocation leaf min_xs requires a non-cv, trivially "
            "copyable/copy-constructible/destructible, nothrow default-constructible object type.");

        static_assert(
            allocation_alignment <= std::numeric_limits<std::uint32_t>::max(),
            "Single-allocation alignment must fit the allocator's 32-bit alignment argument.");
        static_assert(sizeof(float) <=
                      (max_allocation_size - MinXs.block_offset) / capacity_granularity);
        static_assert(sizeof(float) <=
                      (max_allocation_size - MinYs.block_offset) / capacity_granularity);
        static_assert(sizeof(float) <=
                      (max_allocation_size - MinZs.block_offset) / capacity_granularity);
        static_assert(sizeof(float) <=
                      (max_allocation_size - MaxXs.block_offset) / capacity_granularity);
        static_assert(sizeof(float) <=
                      (max_allocation_size - MaxYs.block_offset) / capacity_granularity);
        static_assert(sizeof(float) <=
                      (max_allocation_size - MaxZs.block_offset) / capacity_granularity);
        static_assert(5 <= (max_allocation_size -
                            ml::native_soa::layout_align(MaxZs.block_end, allocation_alignment)) /
                               (column_gap + allocation_alignment - 1));
        static_assert(max_capacity >= capacity_granularity);
        return true;
    };
    static_assert(validate_layout());
};

struct WorldAABBsStorage
    : WorldAABBsColumnsSingleLayout
    , protected ml::native_soa::StorageState
    , ml::native_soa::StorageOperations {
    using View = WorldAABBsColumnsSingleView;
    using ConstView = WorldAABBsColumnsSingleConstView;
    /* **************************************** */
    // Lifetime
    /* **************************************** */
    WorldAABBsStorage() noexcept = default;
    ~WorldAABBsStorage() { ml::native_soa::free(data_, allocation_alignment); }
    WorldAABBsStorage(WorldAABBsStorage const&) = delete;
    auto operator=(WorldAABBsStorage const&) -> WorldAABBsStorage& = delete;
    WorldAABBsStorage(WorldAABBsStorage&& other) noexcept
        : StorageState{std::exchange(other.data_, nullptr),
                       std::exchange(other.num_, 0),
                       std::exchange(other.capacity_, 0)} {}
    auto operator=(WorldAABBsStorage&& other) noexcept -> WorldAABBsStorage& {
        if (this != &other) {
            ml::native_soa::free(data_, allocation_alignment);
            data_ = std::exchange(other.data_, nullptr);
            num_ = std::exchange(other.num_, 0);
            capacity_ = std::exchange(other.capacity_, 0);
        }
        return *this;
    }
  protected:
    template <typename Byte>
    struct DataPointers {
        template <typename T>
        using Element = std::conditional_t<std::is_const_v<Byte>, T const, T>;
        Element<float>* min_xs{};
        Element<float>* min_ys{};
        Element<float>* min_zs{};
        Element<float>* max_xs{};
        Element<float>* max_ys{};
        Element<float>* max_zs{};
        auto operator+(size_type const offset) const noexcept -> DataPointers {
            if (min_xs == nullptr) {
                return {};
            }
            return {min_xs + offset,
                    min_ys + offset,
                    min_zs + offset,
                    max_xs + offset,
                    max_ys + offset,
                    max_zs + offset};
        }
    };
    template <typename Self>
    auto get_data(this Self& self) noexcept {
        using Byte = std::conditional_t<std::is_const_v<Self>, std::byte const, std::byte>;
        if (self.data_ == nullptr) {
            return DataPointers<Byte>{};
        }
        return make_data_unchecked(static_cast<Byte*>(self.data_), self.capacity_blocks());
    }
    template <typename Self>
    auto get_data(this Self& self, size_type const offset) noexcept {
        return self.get_data() + offset;
    }
  private:
    friend struct ml::native_soa::StorageOperations;
    /* **************************************** */
    // Column pointers
    /* **************************************** */
    template <typename Byte>
    static auto make_data_unchecked(Byte* const data, byte_size_type const blocks) noexcept
        -> DataPointers<Byte> {
        auto const pointer_at = [data, blocks](auto const& column) noexcept {
            using Column = std::remove_cvref_t<decltype(column)>;
            using Pointer = std::conditional_t<std::is_const_v<Byte>,
                                               typename Column::const_pointer,
                                               typename Column::pointer>;
            return std::launder(reinterpret_cast<Pointer>(data + column.offset(blocks)));
        };
        return {pointer_at(MinXs),
                pointer_at(MinYs),
                pointer_at(MinZs),
                pointer_at(MaxXs),
                pointer_at(MaxYs),
                pointer_at(MaxZs)};
    }
    auto capacity_blocks() const noexcept -> byte_size_type {
        return static_cast<byte_size_type>(capacity_ / capacity_granularity);
    }

    /* **************************************** */
    // Typed mutations and growth
    /* **************************************** */
    void default_construct_columns(size_type const first, size_type const count) {
        auto const columns{make_data_unchecked(data_, capacity_blocks()) + first};
        std::uninitialized_value_construct_n<float*>(columns.min_xs, count);
        std::uninitialized_value_construct_n<float*>(columns.min_ys, count);
        std::uninitialized_value_construct_n<float*>(columns.min_zs, count);
        std::uninitialized_value_construct_n<float*>(columns.max_xs, count);
        std::uninitialized_value_construct_n<float*>(columns.max_ys, count);
        std::uninitialized_value_construct_n<float*>(columns.max_zs, count);
    }
    void swap_remove_columns(size_type const index,
                             size_type const source,
                             size_type const move_count) {
        copy_columns(get_data(), index, source, move_count);
    }
    static void copy_columns(DataPointers<std::byte> const& columns,
                             size_type index,
                             size_type source,
                             size_type move_count) {
        auto const elements_to_move{static_cast<byte_size_type>(move_count)};
        auto const min_xs_bytes{elements_to_move * sizeof(float)};
        std::memcpy(columns.min_xs + index, columns.min_xs + source, min_xs_bytes);
        std::memcpy(columns.min_ys + index, columns.min_ys + source, min_xs_bytes);
        std::memcpy(columns.min_zs + index, columns.min_zs + source, min_xs_bytes);
        std::memcpy(columns.max_xs + index, columns.max_xs + source, min_xs_bytes);
        std::memcpy(columns.max_ys + index, columns.max_ys + source, min_xs_bytes);
        std::memcpy(columns.max_zs + index, columns.max_zs + source, min_xs_bytes);
    }
    void swap_remove_indices(std::span<size_type const> indices) {
        auto const columns{get_data()};
        ml::soa_storage_detail::for_each_removal_run(
            num_,
            indices,
            ml::native_soa::require,
            [&](size_type index, size_type source, size_type count) {
                copy_columns(columns, index, source, count);
            });
    }
    template <typename Columns>
    void append_columns(Columns const& source, size_type first, size_type count) {
        auto const destination{get_data(first)};
        auto const elements_to_copy{static_cast<byte_size_type>(count)};
        auto const min_xs_bytes{elements_to_copy * sizeof(float)};
        std::memcpy(destination.min_xs, source.min_xs.data(), min_xs_bytes);
        std::memcpy(destination.min_ys, source.min_ys.data(), min_xs_bytes);
        std::memcpy(destination.min_zs, source.min_zs.data(), min_xs_bytes);
        std::memcpy(destination.max_xs, source.max_xs.data(), min_xs_bytes);
        std::memcpy(destination.max_ys, source.max_ys.data(), min_xs_bytes);
        std::memcpy(destination.max_zs, source.max_zs.data(), min_xs_bytes);
    }
    void reallocate(size_type const new_capacity) {
        auto* const new_data{ml::native_soa::allocate(
            layout_bytes(static_cast<byte_size_type>(new_capacity / capacity_granularity)),
            static_cast<std::uint32_t>(allocation_alignment))};
        if (num_ > 0) {
            auto const old_blocks{capacity_blocks()};
            auto const new_blocks{static_cast<byte_size_type>(new_capacity / capacity_granularity)};
            auto const source{
                make_data_unchecked(static_cast<std::byte const*>(data_), old_blocks)};
            auto const destination{make_data_unchecked(new_data, new_blocks)};
            auto const live_count{static_cast<byte_size_type>(num_)};
            auto const min_xs_bytes{live_count * sizeof(float)};
            std::memcpy(destination.min_xs, source.min_xs, min_xs_bytes);
            std::memcpy(destination.min_ys, source.min_ys, min_xs_bytes);
            std::memcpy(destination.min_zs, source.min_zs, min_xs_bytes);
            std::memcpy(destination.max_xs, source.max_xs, min_xs_bytes);
            std::memcpy(destination.max_ys, source.max_ys, min_xs_bytes);
            std::memcpy(destination.max_zs, source.max_zs, min_xs_bytes);
        }
        ml::native_soa::free(data_, allocation_alignment);
        data_ = new_data;
        capacity_ = new_capacity;
    }
};

struct WorldAABBsColumnsSingleConstView : ml::native_soa::CompactViewState<true> {
    using Base = ml::native_soa::CompactViewState<true>;
    using Base::Base;
    using View = WorldAABBsColumnsSingleView;
    using ConstView = WorldAABBsColumnsSingleConstView;
    WorldAABBsColumnsSingleConstView() = default;
    WorldAABBsColumnsSingleConstView(WorldAABBsColumnsSingleView const& other);
    auto get_const_view() const -> ConstView { return *this; }
    auto get_const_view(size_type offset, size_type count) const -> ConstView {
        return slice(offset, count);
    }
    auto min_xs() const -> std::span<float const> {
        return {column_data<float>(WorldAABBsColumnsSingleLayout::MinXs.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto min_ys() const -> std::span<float const> {
        return {column_data<float>(WorldAABBsColumnsSingleLayout::MinYs.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto min_zs() const -> std::span<float const> {
        return {column_data<float>(WorldAABBsColumnsSingleLayout::MinZs.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto max_xs() const -> std::span<float const> {
        return {column_data<float>(WorldAABBsColumnsSingleLayout::MaxXs.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto max_ys() const -> std::span<float const> {
        return {column_data<float>(WorldAABBsColumnsSingleLayout::MaxYs.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto max_zs() const -> std::span<float const> {
        return {column_data<float>(WorldAABBsColumnsSingleLayout::MaxZs.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto columns() const -> WorldAABBsColumnsConstView {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        return WorldAABBsColumnsConstView{
            {column_data_unchecked<float>(WorldAABBsColumnsSingleLayout::MinXs.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<float>(WorldAABBsColumnsSingleLayout::MinYs.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<float>(WorldAABBsColumnsSingleLayout::MinZs.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<float>(WorldAABBsColumnsSingleLayout::MaxXs.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<float>(WorldAABBsColumnsSingleLayout::MaxYs.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<float>(WorldAABBsColumnsSingleLayout::MaxZs.offset(blocks)),
             static_cast<std::size_t>(count_)}};
    }
    template <typename Func>
    void each_column(Func&& func) const {
        columns().each_column(std::forward<Func>(func));
    }
};
static_assert(sizeof(WorldAABBsColumnsSingleConstView) == 16);
static_assert(std::is_trivially_copyable_v<WorldAABBsColumnsSingleConstView>);
struct WorldAABBsColumnsSingleView : ml::native_soa::CompactViewState<false> {
    using Base = ml::native_soa::CompactViewState<false>;
    using Base::Base;
    using View = WorldAABBsColumnsSingleView;
    using ConstView = WorldAABBsColumnsSingleConstView;
    WorldAABBsColumnsSingleView() = default;
    auto get_const_view() const -> ConstView { return *this; }
    auto get_const_view(size_type offset, size_type count) const -> ConstView {
        return slice(offset, count);
    }
    auto min_xs() const -> std::span<float> {
        return {column_data<float>(WorldAABBsColumnsSingleLayout::MinXs.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto min_ys() const -> std::span<float> {
        return {column_data<float>(WorldAABBsColumnsSingleLayout::MinYs.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto min_zs() const -> std::span<float> {
        return {column_data<float>(WorldAABBsColumnsSingleLayout::MinZs.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto max_xs() const -> std::span<float> {
        return {column_data<float>(WorldAABBsColumnsSingleLayout::MaxXs.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto max_ys() const -> std::span<float> {
        return {column_data<float>(WorldAABBsColumnsSingleLayout::MaxYs.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto max_zs() const -> std::span<float> {
        return {column_data<float>(WorldAABBsColumnsSingleLayout::MaxZs.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto columns() const -> WorldAABBsColumnsView {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        return WorldAABBsColumnsView{
            {column_data_unchecked<float>(WorldAABBsColumnsSingleLayout::MinXs.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<float>(WorldAABBsColumnsSingleLayout::MinYs.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<float>(WorldAABBsColumnsSingleLayout::MinZs.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<float>(WorldAABBsColumnsSingleLayout::MaxXs.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<float>(WorldAABBsColumnsSingleLayout::MaxYs.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<float>(WorldAABBsColumnsSingleLayout::MaxZs.offset(blocks)),
             static_cast<std::size_t>(count_)}};
    }
    template <typename Func>
    void each_column(Func&& func) const {
        columns().each_column(std::forward<Func>(func));
    }
};
static_assert(sizeof(WorldAABBsColumnsSingleView) == 16);
static_assert(std::is_trivially_copyable_v<WorldAABBsColumnsSingleView>);
inline WorldAABBsColumnsSingleConstView::WorldAABBsColumnsSingleConstView(
    WorldAABBsColumnsSingleView const& other)
    : Base{other} {}
struct WorldAABBs : WorldAABBsStorage {
    WorldAABBs() noexcept = default;
    WorldAABBs(WorldAABBs const&) = delete;
    auto operator=(WorldAABBs const&) -> WorldAABBs& = delete;
    WorldAABBs(WorldAABBs&&) noexcept = default;
    auto operator=(WorldAABBs&&) noexcept -> WorldAABBs& = default;
    auto get_view() & -> View { return {this, 0, num()}; }
    auto get_view(size_type offset, size_type count) & -> View { return {this, offset, count}; }
    auto slice(size_type offset, size_type count) & -> View { return get_view(offset, count); }
    auto left(size_type count) & -> View { return get_view().left(count); }
    auto right(size_type count) & -> View { return get_view().right(count); }
    auto get_view() && -> View = delete;
    auto get_view(size_type, size_type) && -> View = delete;
    auto slice(size_type, size_type) && -> View = delete;
    auto left(size_type) && -> View = delete;
    auto right(size_type) && -> View = delete;
    auto get_view() const& -> ConstView { return {this, 0, num()}; }
    auto get_view(size_type offset, size_type count) const& -> ConstView {
        return {this, offset, count};
    }
    auto slice(size_type offset, size_type count) const& -> ConstView {
        return get_view(offset, count);
    }
    auto left(size_type count) const& -> ConstView { return get_view().left(count); }
    auto right(size_type count) const& -> ConstView { return get_view().right(count); }
    auto get_view() const&& -> ConstView = delete;
    auto get_view(size_type, size_type) const&& -> ConstView = delete;
    auto slice(size_type, size_type) const&& -> ConstView = delete;
    auto left(size_type) const&& -> ConstView = delete;
    auto right(size_type) const&& -> ConstView = delete;
    auto get_const_view() const& -> ConstView { return get_view(); }
    auto get_const_view(size_type offset, size_type count) const& -> ConstView {
        return get_view(offset, count);
    }
    auto get_const_view() const&& -> ConstView = delete;
    auto get_const_view(size_type, size_type) const&& -> ConstView = delete;
};
} // namespace ml::simulation::collision
