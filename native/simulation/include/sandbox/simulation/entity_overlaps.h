// This file is autogenerated. Do not edit by hand.
// Edit lispb/schema and regenerate it instead.

#pragma once

#include "native_soa/storage.h"
#include "sandbox/core/soa_permutation.h"
#include "sandbox/simulation/entity_handle.h"

namespace ml::ioj {
struct FEntityEntityOverlapsView;
struct FEntityEntityOverlapsConstView;
struct FEntityEntityOverlapsConstView {
    using View = FEntityEntityOverlapsView;
    using ConstView = FEntityEntityOverlapsConstView;
    using size_type = std::int32_t;
    std::span<FRegistryEntityHandle const> first_entities;
    std::span<FRegistryEntityHandle const> second_entities;
    auto num() const noexcept -> size_type { return static_cast<size_type>(first_entities.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(first_entities);
        fn(second_entities);
    }
    void validate_array_sizes() const {
        auto const count{num()};
        each_column([count](auto column) {
            ml::native_soa::require(column.size() == static_cast<std::size_t>(count));
        });
    }
    auto slice(size_type const offset, size_type const count) const
        -> FEntityEntityOverlapsConstView {
        ml::native_soa::require(offset >= 0 && count >= 0 && offset <= num() &&
                                count <= num() - offset);
        return {
            first_entities.subspan(static_cast<std::size_t>(offset),
                                   static_cast<std::size_t>(count)),
            second_entities.subspan(static_cast<std::size_t>(offset),
                                    static_cast<std::size_t>(count)),
        };
    }
    auto get_view() const -> FEntityEntityOverlapsConstView { return *this; }
    auto get_view(size_type const offset, size_type const count) const
        -> FEntityEntityOverlapsConstView {
        return slice(offset, count);
    }
    auto get_const_view() const -> ConstView {
        return {
            first_entities,
            second_entities,
        };
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto left(size_type const count) const -> FEntityEntityOverlapsConstView {
        return slice(0, count);
    }
    auto right(size_type const count) const -> FEntityEntityOverlapsConstView {
        return slice(num() - count, count);
    }
};
struct FEntityEntityOverlapsView {
    using View = FEntityEntityOverlapsView;
    using ConstView = FEntityEntityOverlapsConstView;
    using size_type = std::int32_t;
    std::span<FRegistryEntityHandle> first_entities;
    std::span<FRegistryEntityHandle> second_entities;
    auto num() const noexcept -> size_type { return static_cast<size_type>(first_entities.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(first_entities);
        fn(second_entities);
    }
    void validate_array_sizes() const {
        auto const count{num()};
        each_column([count](auto column) {
            ml::native_soa::require(column.size() == static_cast<std::size_t>(count));
        });
    }
    auto slice(size_type const offset, size_type const count) const -> FEntityEntityOverlapsView {
        ml::native_soa::require(offset >= 0 && count >= 0 && offset <= num() &&
                                count <= num() - offset);
        return {
            first_entities.subspan(static_cast<std::size_t>(offset),
                                   static_cast<std::size_t>(count)),
            second_entities.subspan(static_cast<std::size_t>(offset),
                                    static_cast<std::size_t>(count)),
        };
    }
    auto get_view() const -> FEntityEntityOverlapsView { return *this; }
    auto get_view(size_type const offset, size_type const count) const
        -> FEntityEntityOverlapsView {
        return slice(offset, count);
    }
    auto get_const_view() const -> ConstView {
        return {
            first_entities,
            second_entities,
        };
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto left(size_type const count) const -> FEntityEntityOverlapsView { return slice(0, count); }
    auto right(size_type const count) const -> FEntityEntityOverlapsView {
        return slice(num() - count, count);
    }
    void set(size_type const index,
             FRegistryEntityHandle const new_first_entities,
             FRegistryEntityHandle const new_second_entities) const {
        ml::native_soa::require(index >= 0 && index < num());
        first_entities[static_cast<std::size_t>(index)] = new_first_entities;
        second_entities[static_cast<std::size_t>(index)] = new_second_entities;
    }
};
struct FEntityEntityOverlaps {
    using View = FEntityEntityOverlapsView;
    using ConstView = FEntityEntityOverlapsConstView;
    using size_type = std::int32_t;
    ml::native_soa::Vector<FRegistryEntityHandle> first_entities;
    ml::native_soa::Vector<FRegistryEntityHandle> second_entities;
    auto num() const noexcept -> size_type { return static_cast<size_type>(first_entities.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) {
        fn(first_entities);
        fn(second_entities);
    }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(first_entities);
        fn(second_entities);
    }
    void validate_array_sizes() const { get_const_view().validate_array_sizes(); }
    void reserve(size_type const count) {
        ml::native_soa::require(count >= 0);
        first_entities.reserve(static_cast<std::size_t>(count));
        second_entities.reserve(static_cast<std::size_t>(count));
    }
    void reset() noexcept {
        first_entities.clear();
        second_entities.clear();
    }
    void set_num(size_type const count) {
        ml::native_soa::require(count >= 0);
        auto const size{static_cast<std::size_t>(count)};
        first_entities.resize(size);
        second_entities.resize(size);
    }
    void add_uninitialised(size_type const count) {
        auto const old_num{num()};
        ml::native_soa::require(count >= 0 &&
                                count <= std::numeric_limits<size_type>::max() - old_num);
        set_num(old_num + count);
    }
    void add_defaulted(size_type const count) { add_uninitialised(count); }
    void remove_at_swap(size_type const index, size_type const count) {
        auto const old_num{num()};
        ml::native_soa::require(index >= 0 && index <= old_num && count >= 0 &&
                                count <= old_num - index);
        auto const moved{std::min(count, old_num - index - count)};
        auto const source{old_num - moved};
        for (size_type i{}; i < moved; ++i) {
            first_entities[index + i] = first_entities[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            second_entities[index + i] = second_entities[source + i];
        }
        set_num(old_num - count);
    }
    void set(size_type const index,
             FRegistryEntityHandle const new_first_entities,
             FRegistryEntityHandle const new_second_entities) {
        get_view().set(index, new_first_entities, new_second_entities);
    }
    auto add(FRegistryEntityHandle const new_first_entities,
             FRegistryEntityHandle const new_second_entities) -> size_type {
        auto const index{num()};
        add_defaulted(1);
        set(index, new_first_entities, new_second_entities);
        return index;
    }
    void append_from(ConstView source) {
        auto const count{source.num()};
        ml::native_soa::require(count <= std::numeric_limits<size_type>::max() - num());
        source.validate_array_sizes();
        if (count == 0) {
            return;
        }
        {
            auto const address{reinterpret_cast<std::uintptr_t>(source.first_entities.data())};
            auto const begin{reinterpret_cast<std::uintptr_t>(first_entities.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + first_entities.size() *
                                                           sizeof(FRegistryEntityHandle));
        }
        {
            auto const address{reinterpret_cast<std::uintptr_t>(source.second_entities.data())};
            auto const begin{reinterpret_cast<std::uintptr_t>(second_entities.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + second_entities.size() *
                                                           sizeof(FRegistryEntityHandle));
        }
        first_entities.insert(
            first_entities.end(), source.first_entities.begin(), source.first_entities.end());
        second_entities.insert(
            second_entities.end(), source.second_entities.begin(), source.second_entities.end());
    }
    auto get_view() -> View {
        return {
            first_entities,
            second_entities,
        };
    }
    auto get_view() const -> ConstView {
        return {
            first_entities,
            second_entities,
        };
    }
    auto get_const_view() const -> ConstView { return get_view(); }
    auto get_view(size_type const offset, size_type const count) -> View {
        return get_view().slice(offset, count);
    }
    auto get_view(size_type const offset, size_type const count) const -> ConstView {
        return get_view().slice(offset, count);
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto slice(size_type const offset, size_type const count) -> View {
        return get_view(offset, count);
    }
    auto slice(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view(offset, count);
    }
    auto left(size_type const count) -> View { return slice(0, count); }
    auto right(size_type const count) -> View { return slice(num() - count, count); }
    auto left(size_type const count) const -> ConstView { return slice(0, count); }
    auto right(size_type const count) const -> ConstView { return slice(num() - count, count); }
    template <typename Other>
    void copy_element(size_type const dst_index, Other const& other, size_type const src_index) {
        first_entities[static_cast<std::size_t>(dst_index)] =
            other.first_entities[static_cast<std::size_t>(src_index)];
        second_entities[static_cast<std::size_t>(dst_index)] =
            other.second_entities[static_cast<std::size_t>(src_index)];
    }
    template <typename Other>
    void copy_elements(size_type const dst_index,
                       Other const& other,
                       size_type const src_index,
                       size_type const count) {
        for (size_type i{}; i < count; ++i) {
            copy_element(dst_index + i, other, src_index + i);
        }
    }
    void apply_permutation(std::span<std::int32_t> const indices) {
        validate_array_sizes();
        ml::native_soa::require(indices.size() == static_cast<std::size_t>(num()));
        each_column([indices](auto& column) { ml::apply_permutation(std::span{column}, indices); });
    }
    template <typename Compare>
    void sort(Compare&& compare, std::span<std::int32_t> const scratch_indices) {
        validate_array_sizes();
        ml::native_soa::require(scratch_indices.size() == static_cast<std::size_t>(num()));
        for (size_type i{}; i < num(); ++i) {
            scratch_indices[static_cast<std::size_t>(i)] = i;
        }
        std::sort(scratch_indices.begin(),
                  scratch_indices.end(),
                  [this, &compare](size_type const lhs, size_type const rhs) {
                      return compare(*this, lhs, rhs);
                  });
        apply_permutation(scratch_indices);
    }
};

struct FEntityStaticOverlapsView;
struct FEntityStaticOverlapsConstView;
struct FEntityStaticOverlapsConstView {
    using View = FEntityStaticOverlapsView;
    using ConstView = FEntityStaticOverlapsConstView;
    using size_type = std::int32_t;
    std::span<FRegistryEntityHandle const> entities;
    std::span<std::int32_t const> static_geometry_indices;
    auto num() const noexcept -> size_type { return static_cast<size_type>(entities.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(entities);
        fn(static_geometry_indices);
    }
    void validate_array_sizes() const {
        auto const count{num()};
        each_column([count](auto column) {
            ml::native_soa::require(column.size() == static_cast<std::size_t>(count));
        });
    }
    auto slice(size_type const offset, size_type const count) const
        -> FEntityStaticOverlapsConstView {
        ml::native_soa::require(offset >= 0 && count >= 0 && offset <= num() &&
                                count <= num() - offset);
        return {
            entities.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            static_geometry_indices.subspan(static_cast<std::size_t>(offset),
                                            static_cast<std::size_t>(count)),
        };
    }
    auto get_view() const -> FEntityStaticOverlapsConstView { return *this; }
    auto get_view(size_type const offset, size_type const count) const
        -> FEntityStaticOverlapsConstView {
        return slice(offset, count);
    }
    auto get_const_view() const -> ConstView {
        return {
            entities,
            static_geometry_indices,
        };
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto left(size_type const count) const -> FEntityStaticOverlapsConstView {
        return slice(0, count);
    }
    auto right(size_type const count) const -> FEntityStaticOverlapsConstView {
        return slice(num() - count, count);
    }
};
struct FEntityStaticOverlapsView {
    using View = FEntityStaticOverlapsView;
    using ConstView = FEntityStaticOverlapsConstView;
    using size_type = std::int32_t;
    std::span<FRegistryEntityHandle> entities;
    std::span<std::int32_t> static_geometry_indices;
    auto num() const noexcept -> size_type { return static_cast<size_type>(entities.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(entities);
        fn(static_geometry_indices);
    }
    void validate_array_sizes() const {
        auto const count{num()};
        each_column([count](auto column) {
            ml::native_soa::require(column.size() == static_cast<std::size_t>(count));
        });
    }
    auto slice(size_type const offset, size_type const count) const -> FEntityStaticOverlapsView {
        ml::native_soa::require(offset >= 0 && count >= 0 && offset <= num() &&
                                count <= num() - offset);
        return {
            entities.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            static_geometry_indices.subspan(static_cast<std::size_t>(offset),
                                            static_cast<std::size_t>(count)),
        };
    }
    auto get_view() const -> FEntityStaticOverlapsView { return *this; }
    auto get_view(size_type const offset, size_type const count) const
        -> FEntityStaticOverlapsView {
        return slice(offset, count);
    }
    auto get_const_view() const -> ConstView {
        return {
            entities,
            static_geometry_indices,
        };
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto left(size_type const count) const -> FEntityStaticOverlapsView { return slice(0, count); }
    auto right(size_type const count) const -> FEntityStaticOverlapsView {
        return slice(num() - count, count);
    }
    void set(size_type const index,
             FRegistryEntityHandle const new_entities,
             std::int32_t const new_static_geometry_indices) const {
        ml::native_soa::require(index >= 0 && index < num());
        entities[static_cast<std::size_t>(index)] = new_entities;
        static_geometry_indices[static_cast<std::size_t>(index)] = new_static_geometry_indices;
    }
};
struct FEntityStaticOverlaps {
    using View = FEntityStaticOverlapsView;
    using ConstView = FEntityStaticOverlapsConstView;
    using size_type = std::int32_t;
    ml::native_soa::Vector<FRegistryEntityHandle> entities;
    ml::native_soa::Vector<std::int32_t> static_geometry_indices;
    auto num() const noexcept -> size_type { return static_cast<size_type>(entities.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) {
        fn(entities);
        fn(static_geometry_indices);
    }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(entities);
        fn(static_geometry_indices);
    }
    void validate_array_sizes() const { get_const_view().validate_array_sizes(); }
    void reserve(size_type const count) {
        ml::native_soa::require(count >= 0);
        entities.reserve(static_cast<std::size_t>(count));
        static_geometry_indices.reserve(static_cast<std::size_t>(count));
    }
    void reset() noexcept {
        entities.clear();
        static_geometry_indices.clear();
    }
    void set_num(size_type const count) {
        ml::native_soa::require(count >= 0);
        auto const size{static_cast<std::size_t>(count)};
        entities.resize(size);
        static_geometry_indices.resize(size);
    }
    void add_uninitialised(size_type const count) {
        auto const old_num{num()};
        ml::native_soa::require(count >= 0 &&
                                count <= std::numeric_limits<size_type>::max() - old_num);
        set_num(old_num + count);
    }
    void add_defaulted(size_type const count) { add_uninitialised(count); }
    void remove_at_swap(size_type const index, size_type const count) {
        auto const old_num{num()};
        ml::native_soa::require(index >= 0 && index <= old_num && count >= 0 &&
                                count <= old_num - index);
        auto const moved{std::min(count, old_num - index - count)};
        auto const source{old_num - moved};
        for (size_type i{}; i < moved; ++i) {
            entities[index + i] = entities[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            static_geometry_indices[index + i] = static_geometry_indices[source + i];
        }
        set_num(old_num - count);
    }
    void set(size_type const index,
             FRegistryEntityHandle const new_entities,
             std::int32_t const new_static_geometry_indices) {
        get_view().set(index, new_entities, new_static_geometry_indices);
    }
    auto add(FRegistryEntityHandle const new_entities,
             std::int32_t const new_static_geometry_indices) -> size_type {
        auto const index{num()};
        add_defaulted(1);
        set(index, new_entities, new_static_geometry_indices);
        return index;
    }
    void append_from(ConstView source) {
        auto const count{source.num()};
        ml::native_soa::require(count <= std::numeric_limits<size_type>::max() - num());
        source.validate_array_sizes();
        if (count == 0) {
            return;
        }
        {
            auto const address{reinterpret_cast<std::uintptr_t>(source.entities.data())};
            auto const begin{reinterpret_cast<std::uintptr_t>(entities.data())};
            ml::native_soa::require(address < begin ||
                                    address >=
                                        begin + entities.size() * sizeof(FRegistryEntityHandle));
        }
        {
            auto const address{
                reinterpret_cast<std::uintptr_t>(source.static_geometry_indices.data())};
            auto const begin{reinterpret_cast<std::uintptr_t>(static_geometry_indices.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + static_geometry_indices.size() *
                                                           sizeof(std::int32_t));
        }
        entities.insert(entities.end(), source.entities.begin(), source.entities.end());
        static_geometry_indices.insert(static_geometry_indices.end(),
                                       source.static_geometry_indices.begin(),
                                       source.static_geometry_indices.end());
    }
    auto get_view() -> View {
        return {
            entities,
            static_geometry_indices,
        };
    }
    auto get_view() const -> ConstView {
        return {
            entities,
            static_geometry_indices,
        };
    }
    auto get_const_view() const -> ConstView { return get_view(); }
    auto get_view(size_type const offset, size_type const count) -> View {
        return get_view().slice(offset, count);
    }
    auto get_view(size_type const offset, size_type const count) const -> ConstView {
        return get_view().slice(offset, count);
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto slice(size_type const offset, size_type const count) -> View {
        return get_view(offset, count);
    }
    auto slice(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view(offset, count);
    }
    auto left(size_type const count) -> View { return slice(0, count); }
    auto right(size_type const count) -> View { return slice(num() - count, count); }
    auto left(size_type const count) const -> ConstView { return slice(0, count); }
    auto right(size_type const count) const -> ConstView { return slice(num() - count, count); }
    template <typename Other>
    void copy_element(size_type const dst_index, Other const& other, size_type const src_index) {
        entities[static_cast<std::size_t>(dst_index)] =
            other.entities[static_cast<std::size_t>(src_index)];
        static_geometry_indices[static_cast<std::size_t>(dst_index)] =
            other.static_geometry_indices[static_cast<std::size_t>(src_index)];
    }
    template <typename Other>
    void copy_elements(size_type const dst_index,
                       Other const& other,
                       size_type const src_index,
                       size_type const count) {
        for (size_type i{}; i < count; ++i) {
            copy_element(dst_index + i, other, src_index + i);
        }
    }
    void apply_permutation(std::span<std::int32_t> const indices) {
        validate_array_sizes();
        ml::native_soa::require(indices.size() == static_cast<std::size_t>(num()));
        each_column([indices](auto& column) { ml::apply_permutation(std::span{column}, indices); });
    }
    template <typename Compare>
    void sort(Compare&& compare, std::span<std::int32_t> const scratch_indices) {
        validate_array_sizes();
        ml::native_soa::require(scratch_indices.size() == static_cast<std::size_t>(num()));
        for (size_type i{}; i < num(); ++i) {
            scratch_indices[static_cast<std::size_t>(i)] = i;
        }
        std::sort(scratch_indices.begin(),
                  scratch_indices.end(),
                  [this, &compare](size_type const lhs, size_type const rhs) {
                      return compare(*this, lhs, rhs);
                  });
        apply_permutation(scratch_indices);
    }
};

} // namespace ml::ioj
