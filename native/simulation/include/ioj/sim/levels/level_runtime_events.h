// This file is autogenerated. Do not edit by hand.
// Edit lispb/schema and regenerate it instead.

#pragma once

#include "ioj/sim/entity_type.h"
#include "ioj/sim/entity_types.h"
#include "ioj/sim/health.h"
#include "ioj/sim/level_event_types.h"
#include "ioj/sim/rotators3f.h"
#include "ioj/sim/vectors3f.h"
#include "sandbox/core/address_cast.h"
#include "sandbox/core/native_soa/storage.h"
#include "sandbox/core/soa_permutation.h"

#include <cstring>
#include <memory>
#include <utility>

namespace ioj::sim {
struct LevelSpawnGroupsView;
struct LevelSpawnGroupsConstView;
struct LevelSpawnGroupsConstView {
    using View = LevelSpawnGroupsView;
    using ConstView = LevelSpawnGroupsConstView;
    using size_type = std::int32_t;
    std::span<ioj::sim::EntityType const> types;
    std::span<std::int32_t const> offsets;
    std::span<LevelEventCount const> counts;
    auto num() const noexcept -> size_type { return static_cast<size_type>(types.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(types);
        fn(offsets);
        fn(counts);
    }
    void validate_array_sizes() const {
        auto const count{num()};
        each_column([count](auto column) {
            ml::native_soa::require(column.size() == static_cast<std::size_t>(count));
        });
    }
    auto slice(size_type const offset, size_type const count) const -> LevelSpawnGroupsConstView {
        ml::native_soa::require(offset >= 0 && count >= 0 && offset <= num() &&
                                count <= num() - offset);
        return {
            types.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            offsets.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            counts.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
        };
    }
    auto get_view() const -> LevelSpawnGroupsConstView { return *this; }
    auto get_view(size_type const offset, size_type const count) const
        -> LevelSpawnGroupsConstView {
        return slice(offset, count);
    }
    auto get_const_view() const -> ConstView {
        return {
            types,
            offsets,
            counts,
        };
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto left(size_type const count) const -> LevelSpawnGroupsConstView { return slice(0, count); }
    auto right(size_type const count) const -> LevelSpawnGroupsConstView {
        return slice(num() - count, count);
    }
};
struct LevelSpawnGroupsView {
    using View = LevelSpawnGroupsView;
    using ConstView = LevelSpawnGroupsConstView;
    using size_type = std::int32_t;
    std::span<ioj::sim::EntityType> types;
    std::span<std::int32_t> offsets;
    std::span<LevelEventCount> counts;
    auto num() const noexcept -> size_type { return static_cast<size_type>(types.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(types);
        fn(offsets);
        fn(counts);
    }
    void validate_array_sizes() const {
        auto const count{num()};
        each_column([count](auto column) {
            ml::native_soa::require(column.size() == static_cast<std::size_t>(count));
        });
    }
    auto slice(size_type const offset, size_type const count) const -> LevelSpawnGroupsView {
        ml::native_soa::require(offset >= 0 && count >= 0 && offset <= num() &&
                                count <= num() - offset);
        return {
            types.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            offsets.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            counts.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
        };
    }
    auto get_view() const -> LevelSpawnGroupsView { return *this; }
    auto get_view(size_type const offset, size_type const count) const -> LevelSpawnGroupsView {
        return slice(offset, count);
    }
    auto get_const_view() const -> ConstView {
        return {
            types,
            offsets,
            counts,
        };
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto left(size_type const count) const -> LevelSpawnGroupsView { return slice(0, count); }
    auto right(size_type const count) const -> LevelSpawnGroupsView {
        return slice(num() - count, count);
    }
    void set(size_type const index,
             ioj::sim::EntityType const new_types,
             std::int32_t const new_offsets,
             LevelEventCount const new_counts) const {
        ml::native_soa::require(index >= 0 && index < num());
        types[static_cast<std::size_t>(index)] = new_types;
        offsets[static_cast<std::size_t>(index)] = new_offsets;
        counts[static_cast<std::size_t>(index)] = new_counts;
    }
};
struct LevelSpawnGroups {
    using View = LevelSpawnGroupsView;
    using ConstView = LevelSpawnGroupsConstView;
    using size_type = std::int32_t;
    ml::native_soa::Vector<ioj::sim::EntityType> types;
    ml::native_soa::Vector<std::int32_t> offsets;
    ml::native_soa::Vector<LevelEventCount> counts;
    auto num() const noexcept -> size_type { return static_cast<size_type>(types.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) {
        fn(types);
        fn(offsets);
        fn(counts);
    }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(types);
        fn(offsets);
        fn(counts);
    }
    void validate_array_sizes() const { get_const_view().validate_array_sizes(); }
    void reserve(size_type const count) {
        ml::native_soa::require(count >= 0);
        types.reserve(static_cast<std::size_t>(count));
        offsets.reserve(static_cast<std::size_t>(count));
        counts.reserve(static_cast<std::size_t>(count));
    }
    void reset() noexcept {
        types.clear();
        offsets.clear();
        counts.clear();
    }
    void set_num(size_type const count) {
        ml::native_soa::require(count >= 0);
        auto const size{static_cast<std::size_t>(count)};
        types.resize(size);
        offsets.resize(size);
        counts.resize(size);
    }
    void add_uninitialised(size_type const count) {
        auto const old_num{num()};
        ml::native_soa::require(count >= 0 &&
                                count <= std::numeric_limits<size_type>::max() - old_num);
        set_num(old_num + count);
    }
    void add_defaulted(size_type const count) { add_uninitialised(count); }
    void remove_at_swap(size_type const index, size_type const count) {
        auto const old_num{num()};
        ml::native_soa::require(index >= 0 && index <= old_num && count >= 0 &&
                                count <= old_num - index);
        auto const moved{std::min(count, old_num - index - count)};
        auto const source{old_num - moved};
        for (size_type i{}; i < moved; ++i) {
            types[index + i] = types[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            offsets[index + i] = offsets[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            counts[index + i] = counts[source + i];
        }
        set_num(old_num - count);
    }
    void set(size_type const index,
             ioj::sim::EntityType const new_types,
             std::int32_t const new_offsets,
             LevelEventCount const new_counts) {
        get_view().set(index, new_types, new_offsets, new_counts);
    }
    auto add(ioj::sim::EntityType const new_types,
             std::int32_t const new_offsets,
             LevelEventCount const new_counts) -> size_type {
        auto const index{num()};
        add_defaulted(1);
        set(index, new_types, new_offsets, new_counts);
        return index;
    }
    void append_from(ConstView source) {
        auto const count{source.num()};
        ml::native_soa::require(count <= std::numeric_limits<size_type>::max() - num());
        source.validate_array_sizes();
        if (count == 0) {
            return;
        }
        {
            auto const address{ml::address_cast(source.types.data())};
            auto const begin{ml::address_cast(types.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + types.size() * sizeof(ioj::sim::EntityType));
        }
        {
            auto const address{ml::address_cast(source.offsets.data())};
            auto const begin{ml::address_cast(offsets.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + offsets.size() * sizeof(std::int32_t));
        }
        {
            auto const address{ml::address_cast(source.counts.data())};
            auto const begin{ml::address_cast(counts.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + counts.size() * sizeof(LevelEventCount));
        }
        types.insert(types.end(), source.types.data(), source.types.data() + count);
        offsets.insert(offsets.end(), source.offsets.data(), source.offsets.data() + count);
        counts.insert(counts.end(), source.counts.data(), source.counts.data() + count);
    }
    auto get_view() -> View {
        return {
            types,
            offsets,
            counts,
        };
    }
    auto get_view() const -> ConstView {
        return {
            types,
            offsets,
            counts,
        };
    }
    auto get_const_view() const -> ConstView { return get_view(); }
    auto get_view(size_type const offset, size_type const count) -> View {
        return get_view().slice(offset, count);
    }
    auto get_view(size_type const offset, size_type const count) const -> ConstView {
        return get_view().slice(offset, count);
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto slice(size_type const offset, size_type const count) -> View {
        return get_view(offset, count);
    }
    auto slice(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view(offset, count);
    }
    auto left(size_type const count) -> View { return slice(0, count); }
    auto right(size_type const count) -> View { return slice(num() - count, count); }
    auto left(size_type const count) const -> ConstView { return slice(0, count); }
    auto right(size_type const count) const -> ConstView { return slice(num() - count, count); }
    template <typename Other>
    void copy_element(size_type const dst_index, Other const& other, size_type const src_index) {
        types[static_cast<std::size_t>(dst_index)] =
            other.types[static_cast<std::size_t>(src_index)];
        offsets[static_cast<std::size_t>(dst_index)] =
            other.offsets[static_cast<std::size_t>(src_index)];
        counts[static_cast<std::size_t>(dst_index)] =
            other.counts[static_cast<std::size_t>(src_index)];
    }
    template <typename Other>
    void copy_elements(size_type const dst_index,
                       Other const& other,
                       size_type const src_index,
                       size_type const count) {
        for (size_type i{}; i < count; ++i) {
            copy_element(dst_index + i, other, src_index + i);
        }
    }
    void apply_permutation(std::span<std::int32_t> const indices) {
        validate_array_sizes();
        ml::native_soa::require(indices.size() == static_cast<std::size_t>(num()));
        each_column([indices](auto& column) { ml::apply_permutation(std::span{column}, indices); });
    }
    template <typename Compare>
    void sort(Compare&& compare, std::span<std::int32_t> const scratch_indices) {
        validate_array_sizes();
        ml::native_soa::require(scratch_indices.size() == static_cast<std::size_t>(num()));
        for (size_type i{}; i < num(); ++i) {
            scratch_indices[static_cast<std::size_t>(i)] = i;
        }
        std::sort(scratch_indices.begin(),
                  scratch_indices.end(),
                  [this, &compare](size_type const lhs, size_type const rhs) {
                      return compare(*this, lhs, rhs);
                  });
        apply_permutation(scratch_indices);
    }
};

struct LevelCapitalSpawnEventsView;
struct LevelCapitalSpawnEventsConstView;
struct LevelCapitalSpawnEventsConstView {
    using View = LevelCapitalSpawnEventsView;
    using ConstView = LevelCapitalSpawnEventsConstView;
    using size_type = std::int32_t;
    std::span<std::int32_t const> entity_indices;
    std::span<std::int32_t const> target_entity_indices;
    Vectors3fConstView locations;
    Rotators3fConstView rotations;
    std::span<Team const> teams;
    std::span<Health const> healths;
    std::span<float const> initial_fighter_spawn_delays;
    std::span<float const> fighter_spawn_cooldowns;
    auto num() const noexcept -> size_type { return static_cast<size_type>(entity_indices.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(entity_indices);
        fn(target_entity_indices);
        fn(locations.xs_span());
        fn(locations.ys_span());
        fn(locations.zs_span());
        fn(rotations.pitches);
        fn(rotations.yaws);
        fn(rotations.rolls);
        fn(teams);
        fn(healths);
        fn(initial_fighter_spawn_delays);
        fn(fighter_spawn_cooldowns);
    }
    void validate_array_sizes() const {
        auto const count{num()};
        each_column([count](auto column) {
            ml::native_soa::require(column.size() == static_cast<std::size_t>(count));
        });
    }
    auto slice(size_type const offset, size_type const count) const
        -> LevelCapitalSpawnEventsConstView {
        ml::native_soa::require(offset >= 0 && count >= 0 && offset <= num() &&
                                count <= num() - offset);
        return {
            entity_indices.subspan(static_cast<std::size_t>(offset),
                                   static_cast<std::size_t>(count)),
            target_entity_indices.subspan(static_cast<std::size_t>(offset),
                                          static_cast<std::size_t>(count)),
            locations.slice(offset, count),
            rotations.slice(offset, count),
            teams.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            healths.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            initial_fighter_spawn_delays.subspan(static_cast<std::size_t>(offset),
                                                 static_cast<std::size_t>(count)),
            fighter_spawn_cooldowns.subspan(static_cast<std::size_t>(offset),
                                            static_cast<std::size_t>(count)),
        };
    }
    auto get_view() const -> LevelCapitalSpawnEventsConstView { return *this; }
    auto get_view(size_type const offset, size_type const count) const
        -> LevelCapitalSpawnEventsConstView {
        return slice(offset, count);
    }
    auto get_const_view() const -> ConstView {
        return {
            entity_indices,
            target_entity_indices,
            locations.get_const_view(),
            rotations.get_const_view(),
            teams,
            healths,
            initial_fighter_spawn_delays,
            fighter_spawn_cooldowns,
        };
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto left(size_type const count) const -> LevelCapitalSpawnEventsConstView {
        return slice(0, count);
    }
    auto right(size_type const count) const -> LevelCapitalSpawnEventsConstView {
        return slice(num() - count, count);
    }
};
struct LevelCapitalSpawnEventsView {
    using View = LevelCapitalSpawnEventsView;
    using ConstView = LevelCapitalSpawnEventsConstView;
    using size_type = std::int32_t;
    std::span<std::int32_t> entity_indices;
    std::span<std::int32_t> target_entity_indices;
    Vectors3fView locations;
    Rotators3fView rotations;
    std::span<Team> teams;
    std::span<Health> healths;
    std::span<float> initial_fighter_spawn_delays;
    std::span<float> fighter_spawn_cooldowns;
    auto num() const noexcept -> size_type { return static_cast<size_type>(entity_indices.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(entity_indices);
        fn(target_entity_indices);
        fn(locations.xs_span());
        fn(locations.ys_span());
        fn(locations.zs_span());
        fn(rotations.pitches);
        fn(rotations.yaws);
        fn(rotations.rolls);
        fn(teams);
        fn(healths);
        fn(initial_fighter_spawn_delays);
        fn(fighter_spawn_cooldowns);
    }
    void validate_array_sizes() const {
        auto const count{num()};
        each_column([count](auto column) {
            ml::native_soa::require(column.size() == static_cast<std::size_t>(count));
        });
    }
    auto slice(size_type const offset, size_type const count) const -> LevelCapitalSpawnEventsView {
        ml::native_soa::require(offset >= 0 && count >= 0 && offset <= num() &&
                                count <= num() - offset);
        return {
            entity_indices.subspan(static_cast<std::size_t>(offset),
                                   static_cast<std::size_t>(count)),
            target_entity_indices.subspan(static_cast<std::size_t>(offset),
                                          static_cast<std::size_t>(count)),
            locations.slice(offset, count),
            rotations.slice(offset, count),
            teams.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            healths.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            initial_fighter_spawn_delays.subspan(static_cast<std::size_t>(offset),
                                                 static_cast<std::size_t>(count)),
            fighter_spawn_cooldowns.subspan(static_cast<std::size_t>(offset),
                                            static_cast<std::size_t>(count)),
        };
    }
    auto get_view() const -> LevelCapitalSpawnEventsView { return *this; }
    auto get_view(size_type const offset, size_type const count) const
        -> LevelCapitalSpawnEventsView {
        return slice(offset, count);
    }
    auto get_const_view() const -> ConstView {
        return {
            entity_indices,
            target_entity_indices,
            locations.get_const_view(),
            rotations.get_const_view(),
            teams,
            healths,
            initial_fighter_spawn_delays,
            fighter_spawn_cooldowns,
        };
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto left(size_type const count) const -> LevelCapitalSpawnEventsView {
        return slice(0, count);
    }
    auto right(size_type const count) const -> LevelCapitalSpawnEventsView {
        return slice(num() - count, count);
    }
    void set(size_type const index,
             std::int32_t const new_entity_indices,
             std::int32_t const new_target_entity_indices,
             float const new_locations_xs,
             float const new_locations_ys,
             float const new_locations_zs,
             float const new_rotations_pitches,
             float const new_rotations_yaws,
             float const new_rotations_rolls,
             Team const new_teams,
             Health const new_healths,
             float const new_initial_fighter_spawn_delays,
             float const new_fighter_spawn_cooldowns) const {
        ml::native_soa::require(index >= 0 && index < num());
        entity_indices[static_cast<std::size_t>(index)] = new_entity_indices;
        target_entity_indices[static_cast<std::size_t>(index)] = new_target_entity_indices;
        locations.xs[static_cast<std::size_t>(index)] = new_locations_xs;
        locations.ys[static_cast<std::size_t>(index)] = new_locations_ys;
        locations.zs[static_cast<std::size_t>(index)] = new_locations_zs;
        rotations.pitches[static_cast<std::size_t>(index)] = new_rotations_pitches;
        rotations.yaws[static_cast<std::size_t>(index)] = new_rotations_yaws;
        rotations.rolls[static_cast<std::size_t>(index)] = new_rotations_rolls;
        teams[static_cast<std::size_t>(index)] = new_teams;
        healths[static_cast<std::size_t>(index)] = new_healths;
        initial_fighter_spawn_delays[static_cast<std::size_t>(index)] =
            new_initial_fighter_spawn_delays;
        fighter_spawn_cooldowns[static_cast<std::size_t>(index)] = new_fighter_spawn_cooldowns;
    }
};
struct LevelCapitalSpawnEvents {
    using View = LevelCapitalSpawnEventsView;
    using ConstView = LevelCapitalSpawnEventsConstView;
    using size_type = std::int32_t;
    ml::native_soa::Vector<std::int32_t> entity_indices;
    ml::native_soa::Vector<std::int32_t> target_entity_indices;
    Vectors3f locations;
    Rotators3f rotations;
    ml::native_soa::Vector<Team> teams;
    ml::native_soa::Vector<Health> healths;
    ml::native_soa::Vector<float> initial_fighter_spawn_delays;
    ml::native_soa::Vector<float> fighter_spawn_cooldowns;
    auto num() const noexcept -> size_type { return static_cast<size_type>(entity_indices.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) {
        fn(entity_indices);
        fn(target_entity_indices);
        fn(locations.xs);
        fn(locations.ys);
        fn(locations.zs);
        fn(rotations.pitches);
        fn(rotations.yaws);
        fn(rotations.rolls);
        fn(teams);
        fn(healths);
        fn(initial_fighter_spawn_delays);
        fn(fighter_spawn_cooldowns);
    }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(entity_indices);
        fn(target_entity_indices);
        fn(locations.xs);
        fn(locations.ys);
        fn(locations.zs);
        fn(rotations.pitches);
        fn(rotations.yaws);
        fn(rotations.rolls);
        fn(teams);
        fn(healths);
        fn(initial_fighter_spawn_delays);
        fn(fighter_spawn_cooldowns);
    }
    void validate_array_sizes() const { get_const_view().validate_array_sizes(); }
    void reserve(size_type const count) {
        ml::native_soa::require(count >= 0);
        entity_indices.reserve(static_cast<std::size_t>(count));
        target_entity_indices.reserve(static_cast<std::size_t>(count));
        locations.xs.reserve(static_cast<std::size_t>(count));
        locations.ys.reserve(static_cast<std::size_t>(count));
        locations.zs.reserve(static_cast<std::size_t>(count));
        rotations.pitches.reserve(static_cast<std::size_t>(count));
        rotations.yaws.reserve(static_cast<std::size_t>(count));
        rotations.rolls.reserve(static_cast<std::size_t>(count));
        teams.reserve(static_cast<std::size_t>(count));
        healths.reserve(static_cast<std::size_t>(count));
        initial_fighter_spawn_delays.reserve(static_cast<std::size_t>(count));
        fighter_spawn_cooldowns.reserve(static_cast<std::size_t>(count));
    }
    void reset() noexcept {
        entity_indices.clear();
        target_entity_indices.clear();
        locations.xs.clear();
        locations.ys.clear();
        locations.zs.clear();
        rotations.pitches.clear();
        rotations.yaws.clear();
        rotations.rolls.clear();
        teams.clear();
        healths.clear();
        initial_fighter_spawn_delays.clear();
        fighter_spawn_cooldowns.clear();
    }
    void set_num(size_type const count) {
        ml::native_soa::require(count >= 0);
        auto const size{static_cast<std::size_t>(count)};
        entity_indices.resize(size);
        target_entity_indices.resize(size);
        locations.xs.resize(size);
        locations.ys.resize(size);
        locations.zs.resize(size);
        rotations.pitches.resize(size);
        rotations.yaws.resize(size);
        rotations.rolls.resize(size);
        teams.resize(size);
        healths.resize(size);
        initial_fighter_spawn_delays.resize(size);
        fighter_spawn_cooldowns.resize(size);
    }
    void add_uninitialised(size_type const count) {
        auto const old_num{num()};
        ml::native_soa::require(count >= 0 &&
                                count <= std::numeric_limits<size_type>::max() - old_num);
        set_num(old_num + count);
    }
    void add_defaulted(size_type const count) { add_uninitialised(count); }
    void remove_at_swap(size_type const index, size_type const count) {
        auto const old_num{num()};
        ml::native_soa::require(index >= 0 && index <= old_num && count >= 0 &&
                                count <= old_num - index);
        auto const moved{std::min(count, old_num - index - count)};
        auto const source{old_num - moved};
        for (size_type i{}; i < moved; ++i) {
            entity_indices[index + i] = entity_indices[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            target_entity_indices[index + i] = target_entity_indices[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            locations.xs[index + i] = locations.xs[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            locations.ys[index + i] = locations.ys[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            locations.zs[index + i] = locations.zs[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            rotations.pitches[index + i] = rotations.pitches[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            rotations.yaws[index + i] = rotations.yaws[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            rotations.rolls[index + i] = rotations.rolls[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            teams[index + i] = teams[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            healths[index + i] = healths[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            initial_fighter_spawn_delays[index + i] = initial_fighter_spawn_delays[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            fighter_spawn_cooldowns[index + i] = fighter_spawn_cooldowns[source + i];
        }
        set_num(old_num - count);
    }
    void set(size_type const index,
             std::int32_t const new_entity_indices,
             std::int32_t const new_target_entity_indices,
             float const new_locations_xs,
             float const new_locations_ys,
             float const new_locations_zs,
             float const new_rotations_pitches,
             float const new_rotations_yaws,
             float const new_rotations_rolls,
             Team const new_teams,
             Health const new_healths,
             float const new_initial_fighter_spawn_delays,
             float const new_fighter_spawn_cooldowns) {
        get_view().set(index,
                       new_entity_indices,
                       new_target_entity_indices,
                       new_locations_xs,
                       new_locations_ys,
                       new_locations_zs,
                       new_rotations_pitches,
                       new_rotations_yaws,
                       new_rotations_rolls,
                       new_teams,
                       new_healths,
                       new_initial_fighter_spawn_delays,
                       new_fighter_spawn_cooldowns);
    }
    auto add(std::int32_t const new_entity_indices,
             std::int32_t const new_target_entity_indices,
             float const new_locations_xs,
             float const new_locations_ys,
             float const new_locations_zs,
             float const new_rotations_pitches,
             float const new_rotations_yaws,
             float const new_rotations_rolls,
             Team const new_teams,
             Health const new_healths,
             float const new_initial_fighter_spawn_delays,
             float const new_fighter_spawn_cooldowns) -> size_type {
        auto const index{num()};
        add_defaulted(1);
        set(index,
            new_entity_indices,
            new_target_entity_indices,
            new_locations_xs,
            new_locations_ys,
            new_locations_zs,
            new_rotations_pitches,
            new_rotations_yaws,
            new_rotations_rolls,
            new_teams,
            new_healths,
            new_initial_fighter_spawn_delays,
            new_fighter_spawn_cooldowns);
        return index;
    }
    void append_from(ConstView source) {
        auto const count{source.num()};
        ml::native_soa::require(count <= std::numeric_limits<size_type>::max() - num());
        source.validate_array_sizes();
        if (count == 0) {
            return;
        }
        {
            auto const address{ml::address_cast(source.entity_indices.data())};
            auto const begin{ml::address_cast(entity_indices.data())};
            ml::native_soa::require(address < begin || address >= begin + entity_indices.size() *
                                                                              sizeof(std::int32_t));
        }
        {
            auto const address{ml::address_cast(source.target_entity_indices.data())};
            auto const begin{ml::address_cast(target_entity_indices.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + target_entity_indices.size() *
                                                           sizeof(std::int32_t));
        }
        {
            auto const address{ml::address_cast(source.locations.xs)};
            auto const begin{ml::address_cast(locations.xs.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + locations.xs.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.locations.ys)};
            auto const begin{ml::address_cast(locations.ys.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + locations.ys.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.locations.zs)};
            auto const begin{ml::address_cast(locations.zs.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + locations.zs.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.rotations.pitches.data())};
            auto const begin{ml::address_cast(rotations.pitches.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + rotations.pitches.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.rotations.yaws.data())};
            auto const begin{ml::address_cast(rotations.yaws.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + rotations.yaws.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.rotations.rolls.data())};
            auto const begin{ml::address_cast(rotations.rolls.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + rotations.rolls.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.teams.data())};
            auto const begin{ml::address_cast(teams.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + teams.size() * sizeof(Team));
        }
        {
            auto const address{ml::address_cast(source.healths.data())};
            auto const begin{ml::address_cast(healths.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + healths.size() * sizeof(Health));
        }
        {
            auto const address{ml::address_cast(source.initial_fighter_spawn_delays.data())};
            auto const begin{ml::address_cast(initial_fighter_spawn_delays.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + initial_fighter_spawn_delays.size() *
                                                           sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.fighter_spawn_cooldowns.data())};
            auto const begin{ml::address_cast(fighter_spawn_cooldowns.data())};
            ml::native_soa::require(address < begin ||
                                    address >=
                                        begin + fighter_spawn_cooldowns.size() * sizeof(float));
        }
        entity_indices.insert(entity_indices.end(),
                              source.entity_indices.data(),
                              source.entity_indices.data() + count);
        target_entity_indices.insert(target_entity_indices.end(),
                                     source.target_entity_indices.data(),
                                     source.target_entity_indices.data() + count);
        locations.xs.insert(locations.xs.end(), source.locations.xs, source.locations.xs + count);
        locations.ys.insert(locations.ys.end(), source.locations.ys, source.locations.ys + count);
        locations.zs.insert(locations.zs.end(), source.locations.zs, source.locations.zs + count);
        rotations.pitches.insert(rotations.pitches.end(),
                                 source.rotations.pitches.data(),
                                 source.rotations.pitches.data() + count);
        rotations.yaws.insert(rotations.yaws.end(),
                              source.rotations.yaws.data(),
                              source.rotations.yaws.data() + count);
        rotations.rolls.insert(rotations.rolls.end(),
                               source.rotations.rolls.data(),
                               source.rotations.rolls.data() + count);
        teams.insert(teams.end(), source.teams.data(), source.teams.data() + count);
        healths.insert(healths.end(), source.healths.data(), source.healths.data() + count);
        initial_fighter_spawn_delays.insert(initial_fighter_spawn_delays.end(),
                                            source.initial_fighter_spawn_delays.data(),
                                            source.initial_fighter_spawn_delays.data() + count);
        fighter_spawn_cooldowns.insert(fighter_spawn_cooldowns.end(),
                                       source.fighter_spawn_cooldowns.data(),
                                       source.fighter_spawn_cooldowns.data() + count);
    }
    auto get_view() -> View {
        return {
            entity_indices,
            target_entity_indices,
            locations.get_view(),
            rotations.get_view(),
            teams,
            healths,
            initial_fighter_spawn_delays,
            fighter_spawn_cooldowns,
        };
    }
    auto get_view() const -> ConstView {
        return {
            entity_indices,
            target_entity_indices,
            locations.get_view(),
            rotations.get_view(),
            teams,
            healths,
            initial_fighter_spawn_delays,
            fighter_spawn_cooldowns,
        };
    }
    auto get_const_view() const -> ConstView { return get_view(); }
    auto get_view(size_type const offset, size_type const count) -> View {
        return get_view().slice(offset, count);
    }
    auto get_view(size_type const offset, size_type const count) const -> ConstView {
        return get_view().slice(offset, count);
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto slice(size_type const offset, size_type const count) -> View {
        return get_view(offset, count);
    }
    auto slice(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view(offset, count);
    }
    auto left(size_type const count) -> View { return slice(0, count); }
    auto right(size_type const count) -> View { return slice(num() - count, count); }
    auto left(size_type const count) const -> ConstView { return slice(0, count); }
    auto right(size_type const count) const -> ConstView { return slice(num() - count, count); }
    template <typename Other>
    void copy_element(size_type const dst_index, Other const& other, size_type const src_index) {
        entity_indices[static_cast<std::size_t>(dst_index)] =
            other.entity_indices[static_cast<std::size_t>(src_index)];
        target_entity_indices[static_cast<std::size_t>(dst_index)] =
            other.target_entity_indices[static_cast<std::size_t>(src_index)];
        locations.copy_element(dst_index, other.locations, src_index);
        rotations.copy_element(dst_index, other.rotations, src_index);
        teams[static_cast<std::size_t>(dst_index)] =
            other.teams[static_cast<std::size_t>(src_index)];
        healths[static_cast<std::size_t>(dst_index)] =
            other.healths[static_cast<std::size_t>(src_index)];
        initial_fighter_spawn_delays[static_cast<std::size_t>(dst_index)] =
            other.initial_fighter_spawn_delays[static_cast<std::size_t>(src_index)];
        fighter_spawn_cooldowns[static_cast<std::size_t>(dst_index)] =
            other.fighter_spawn_cooldowns[static_cast<std::size_t>(src_index)];
    }
    template <typename Other>
    void copy_elements(size_type const dst_index,
                       Other const& other,
                       size_type const src_index,
                       size_type const count) {
        for (size_type i{}; i < count; ++i) {
            copy_element(dst_index + i, other, src_index + i);
        }
    }
    void apply_permutation(std::span<std::int32_t> const indices) {
        validate_array_sizes();
        ml::native_soa::require(indices.size() == static_cast<std::size_t>(num()));
        each_column([indices](auto& column) { ml::apply_permutation(std::span{column}, indices); });
    }
    template <typename Compare>
    void sort(Compare&& compare, std::span<std::int32_t> const scratch_indices) {
        validate_array_sizes();
        ml::native_soa::require(scratch_indices.size() == static_cast<std::size_t>(num()));
        for (size_type i{}; i < num(); ++i) {
            scratch_indices[static_cast<std::size_t>(i)] = i;
        }
        std::sort(scratch_indices.begin(),
                  scratch_indices.end(),
                  [this, &compare](size_type const lhs, size_type const rhs) {
                      return compare(*this, lhs, rhs);
                  });
        apply_permutation(scratch_indices);
    }
};

struct LevelCapitalSpawnEventsSingleView;
struct LevelCapitalSpawnEventsSingleConstView;
struct LevelCapitalSpawnEventsSingleLayout {
    using size_type = std::int32_t;
    using byte_size_type = std::size_t;

    inline static constexpr byte_size_type max_allocation_size{
        std::numeric_limits<byte_size_type>::max()};
    inline static constexpr size_type capacity_granularity{64};
    inline static constexpr byte_size_type column_gap{192};

    template <typename T>
    using ColLayout = ml::native_soa::ColumnLayout<T>;
    inline static constexpr ml::native_soa::ColumnLayoutStart LayoutStart{
        capacity_granularity, column_gap, 64};

    inline static constexpr ColLayout<std::int32_t> EntityIndices{LayoutStart};
    inline static constexpr ColLayout<std::int32_t> TargetEntityIndices{EntityIndices};
    inline static constexpr ColLayout<float> LocationsXs{TargetEntityIndices};
    inline static constexpr ColLayout<float> LocationsYs{LocationsXs};
    inline static constexpr ColLayout<float> LocationsZs{LocationsYs};
    inline static constexpr ColLayout<float> RotationsPitches{LocationsZs};
    inline static constexpr ColLayout<float> RotationsYaws{RotationsPitches};
    inline static constexpr ColLayout<float> RotationsRolls{RotationsYaws};
    inline static constexpr ColLayout<Team> Teams{RotationsRolls};
    inline static constexpr ColLayout<Health> Healths{Teams};
    inline static constexpr ColLayout<float> InitialFighterSpawnDelays{Healths};
    inline static constexpr ColLayout<float> FighterSpawnCooldowns{InitialFighterSpawnDelays};

    inline static constexpr byte_size_type allocation_alignment{
        ml::native_soa::maximum_alignment(EntityIndices,
                                          TargetEntityIndices,
                                          LocationsXs,
                                          LocationsYs,
                                          LocationsZs,
                                          RotationsPitches,
                                          RotationsYaws,
                                          RotationsRolls,
                                          Teams,
                                          Healths,
                                          InitialFighterSpawnDelays,
                                          FighterSpawnCooldowns)};

    // Conservative per-block bound for checked capacity arithmetic; gaps do not scale with
    // capacity.
    inline static constexpr byte_size_type capacity_block_bound{
        ml::native_soa::layout_align(FighterSpawnCooldowns.block_end, allocation_alignment) +
        11 * (column_gap + allocation_alignment - 1)};
    inline static constexpr size_type max_capacity{
        ml::native_soa::maximum_capacity(capacity_block_bound)};
    static constexpr auto layout_bytes(byte_size_type blocks) noexcept -> byte_size_type {
        return blocks == 0 ? 0 : FighterSpawnCooldowns.data_end(blocks);
    }
  private:
    inline static constexpr auto validate_layout = []() consteval -> bool {
        static_assert(
            ml::native_soa::supported_leaf<std::int32_t>,
            "Single-allocation leaf entity_indices requires a non-cv, trivially "
            "copyable/copy-constructible/destructible, nothrow default-constructible object type.");
        static_assert(
            ml::native_soa::supported_leaf<float>,
            "Single-allocation leaf locations.xs requires a non-cv, trivially "
            "copyable/copy-constructible/destructible, nothrow default-constructible object type.");
        static_assert(
            ml::native_soa::supported_leaf<Team>,
            "Single-allocation leaf teams requires a non-cv, trivially "
            "copyable/copy-constructible/destructible, nothrow default-constructible object type.");
        static_assert(
            ml::native_soa::supported_leaf<Health>,
            "Single-allocation leaf healths requires a non-cv, trivially "
            "copyable/copy-constructible/destructible, nothrow default-constructible object type.");

        static_assert(
            allocation_alignment <= std::numeric_limits<std::uint32_t>::max(),
            "Single-allocation alignment must fit the allocator's 32-bit alignment argument.");
        static_assert(sizeof(std::int32_t) <=
                      (max_allocation_size - EntityIndices.block_offset) / capacity_granularity);
        static_assert(sizeof(std::int32_t) <=
                      (max_allocation_size - TargetEntityIndices.block_offset) /
                          capacity_granularity);
        static_assert(sizeof(float) <=
                      (max_allocation_size - LocationsXs.block_offset) / capacity_granularity);
        static_assert(sizeof(float) <=
                      (max_allocation_size - LocationsYs.block_offset) / capacity_granularity);
        static_assert(sizeof(float) <=
                      (max_allocation_size - LocationsZs.block_offset) / capacity_granularity);
        static_assert(sizeof(float) <=
                      (max_allocation_size - RotationsPitches.block_offset) / capacity_granularity);
        static_assert(sizeof(float) <=
                      (max_allocation_size - RotationsYaws.block_offset) / capacity_granularity);
        static_assert(sizeof(float) <=
                      (max_allocation_size - RotationsRolls.block_offset) / capacity_granularity);
        static_assert(sizeof(Team) <=
                      (max_allocation_size - Teams.block_offset) / capacity_granularity);
        static_assert(sizeof(Health) <=
                      (max_allocation_size - Healths.block_offset) / capacity_granularity);
        static_assert(sizeof(float) <=
                      (max_allocation_size - InitialFighterSpawnDelays.block_offset) /
                          capacity_granularity);
        static_assert(sizeof(float) <= (max_allocation_size - FighterSpawnCooldowns.block_offset) /
                                           capacity_granularity);
        static_assert(11 <= (max_allocation_size -
                             ml::native_soa::layout_align(FighterSpawnCooldowns.block_end,
                                                          allocation_alignment)) /
                                (column_gap + allocation_alignment - 1));
        static_assert(max_capacity >= capacity_granularity);
        return true;
    };
    static_assert(validate_layout());
};

struct SingleAllocationLevelCapitalSpawnEventsStorage
    : LevelCapitalSpawnEventsSingleLayout
    , protected ml::native_soa::StorageState
    , ml::native_soa::StorageOperations {
    using View = LevelCapitalSpawnEventsSingleView;
    using ConstView = LevelCapitalSpawnEventsSingleConstView;
    using SchemaConstView = LevelCapitalSpawnEventsConstView;
    using ml::native_soa::StorageOperations::append_from;
    auto append_from(LevelCapitalSpawnEventsConstView const& source) -> size_type {
        source.validate_array_sizes();
        auto const count{source.num()};
        auto const first{num_};
        ml::native_soa::require((count <= max_capacity - first));
        if (count == 0) {
            return first;
        }
        auto const new_num{first + count};
        if (new_num > capacity_) {
            ml::native_soa::require(!ordinary_source_aliases_storage(source));
            reallocate(ml::native_soa::growth_capacity(new_num, capacity_, capacity_block_bound));
        }
        append_columns(source, first, count);
        num_ = new_num;
        return first;
    }
    /* **************************************** */
    // Lifetime
    /* **************************************** */
    SingleAllocationLevelCapitalSpawnEventsStorage() noexcept = default;
    ~SingleAllocationLevelCapitalSpawnEventsStorage() {
        ml::native_soa::free(data_, allocation_alignment);
    }
    SingleAllocationLevelCapitalSpawnEventsStorage(
        SingleAllocationLevelCapitalSpawnEventsStorage const&) = delete;
    auto operator=(SingleAllocationLevelCapitalSpawnEventsStorage const&)
        -> SingleAllocationLevelCapitalSpawnEventsStorage& = delete;
    SingleAllocationLevelCapitalSpawnEventsStorage(
        SingleAllocationLevelCapitalSpawnEventsStorage&& other) noexcept
        : StorageState{std::exchange(other.data_, nullptr),
                       std::exchange(other.num_, 0),
                       std::exchange(other.capacity_, 0)} {}
    auto operator=(SingleAllocationLevelCapitalSpawnEventsStorage&& other) noexcept
        -> SingleAllocationLevelCapitalSpawnEventsStorage& {
        if (this != &other) {
            ml::native_soa::free(data_, allocation_alignment);
            data_ = std::exchange(other.data_, nullptr);
            num_ = std::exchange(other.num_, 0);
            capacity_ = std::exchange(other.capacity_, 0);
        }
        return *this;
    }
  protected:
    template <typename Byte>
    struct DataPointers {
        template <typename T>
        using Element = std::conditional_t<std::is_const_v<Byte>, T const, T>;
        Element<std::int32_t>* entity_indices{};
        Element<std::int32_t>* target_entity_indices{};
        Element<float>* locations_xs{};
        Element<float>* locations_ys{};
        Element<float>* locations_zs{};
        Element<float>* rotations_pitches{};
        Element<float>* rotations_yaws{};
        Element<float>* rotations_rolls{};
        Element<Team>* teams{};
        Element<Health>* healths{};
        Element<float>* initial_fighter_spawn_delays{};
        Element<float>* fighter_spawn_cooldowns{};
        auto operator+(size_type const offset) const noexcept -> DataPointers {
            if (entity_indices == nullptr) {
                return {};
            }
            return {entity_indices + offset,
                    target_entity_indices + offset,
                    locations_xs + offset,
                    locations_ys + offset,
                    locations_zs + offset,
                    rotations_pitches + offset,
                    rotations_yaws + offset,
                    rotations_rolls + offset,
                    teams + offset,
                    healths + offset,
                    initial_fighter_spawn_delays + offset,
                    fighter_spawn_cooldowns + offset};
        }
    };
    template <typename Self>
    auto get_data(this Self& self) noexcept {
        using Byte = std::conditional_t<std::is_const_v<Self>, std::byte const, std::byte>;
        if (self.data_ == nullptr) {
            return DataPointers<Byte>{};
        }
        return make_data_unchecked(static_cast<Byte*>(self.data_), self.capacity_blocks());
    }
    template <typename Self>
    auto get_data(this Self& self, size_type const offset) noexcept {
        return self.get_data() + offset;
    }
  private:
    friend struct ml::native_soa::StorageOperations;
    /* **************************************** */
    // Column pointers
    /* **************************************** */
    template <typename Byte>
    static auto make_data_unchecked(Byte* const data, byte_size_type const blocks) noexcept
        -> DataPointers<Byte> {
        auto const pointer_at = [data](auto const& column, byte_size_type offset) noexcept {
            using Column = std::remove_cvref_t<decltype(column)>;
            using Pointer = std::conditional_t<std::is_const_v<Byte>,
                                               typename Column::const_pointer,
                                               typename Column::pointer>;
            return std::launder(reinterpret_cast<Pointer>(data + offset));
        };
        auto const entity_indices_offset{byte_size_type{}};
        auto const target_entity_indices_offset{ml::native_soa::layout_align(
            entity_indices_offset + blocks * capacity_granularity * sizeof(std::int32_t) +
                column_gap,
            TargetEntityIndices.alignment)};
        auto const locations_xs_offset{ml::native_soa::layout_align(
            target_entity_indices_offset + blocks * capacity_granularity * sizeof(std::int32_t) +
                column_gap,
            LocationsXs.alignment)};
        auto const locations_ys_offset{ml::native_soa::layout_align(
            locations_xs_offset + blocks * capacity_granularity * sizeof(float) + column_gap,
            LocationsYs.alignment)};
        auto const locations_zs_offset{ml::native_soa::layout_align(
            locations_ys_offset + blocks * capacity_granularity * sizeof(float) + column_gap,
            LocationsZs.alignment)};
        auto const rotations_pitches_offset{ml::native_soa::layout_align(
            locations_zs_offset + blocks * capacity_granularity * sizeof(float) + column_gap,
            RotationsPitches.alignment)};
        auto const rotations_yaws_offset{ml::native_soa::layout_align(
            rotations_pitches_offset + blocks * capacity_granularity * sizeof(float) + column_gap,
            RotationsYaws.alignment)};
        auto const rotations_rolls_offset{ml::native_soa::layout_align(
            rotations_yaws_offset + blocks * capacity_granularity * sizeof(float) + column_gap,
            RotationsRolls.alignment)};
        auto const teams_offset{ml::native_soa::layout_align(
            rotations_rolls_offset + blocks * capacity_granularity * sizeof(float) + column_gap,
            Teams.alignment)};
        auto const healths_offset{ml::native_soa::layout_align(
            teams_offset + blocks * capacity_granularity * sizeof(Team) + column_gap,
            Healths.alignment)};
        auto const initial_fighter_spawn_delays_offset{ml::native_soa::layout_align(
            healths_offset + blocks * capacity_granularity * sizeof(Health) + column_gap,
            InitialFighterSpawnDelays.alignment)};
        auto const fighter_spawn_cooldowns_offset{ml::native_soa::layout_align(
            initial_fighter_spawn_delays_offset + blocks * capacity_granularity * sizeof(float) +
                column_gap,
            FighterSpawnCooldowns.alignment)};
        return {pointer_at(EntityIndices, entity_indices_offset),
                pointer_at(TargetEntityIndices, target_entity_indices_offset),
                pointer_at(LocationsXs, locations_xs_offset),
                pointer_at(LocationsYs, locations_ys_offset),
                pointer_at(LocationsZs, locations_zs_offset),
                pointer_at(RotationsPitches, rotations_pitches_offset),
                pointer_at(RotationsYaws, rotations_yaws_offset),
                pointer_at(RotationsRolls, rotations_rolls_offset),
                pointer_at(Teams, teams_offset),
                pointer_at(Healths, healths_offset),
                pointer_at(InitialFighterSpawnDelays, initial_fighter_spawn_delays_offset),
                pointer_at(FighterSpawnCooldowns, fighter_spawn_cooldowns_offset)};
    }
    auto capacity_blocks() const noexcept -> byte_size_type {
        return static_cast<byte_size_type>(capacity_ / capacity_granularity);
    }

    /* **************************************** */
    // Typed mutations and growth
    /* **************************************** */
    void default_construct_columns(size_type const first, size_type const count) {
        auto const columns{make_data_unchecked(data_, capacity_blocks()) + first};
        std::uninitialized_value_construct_n<std::int32_t*>(columns.entity_indices, count);
        std::uninitialized_value_construct_n<std::int32_t*>(columns.target_entity_indices, count);
        std::uninitialized_value_construct_n<float*>(columns.locations_xs, count);
        std::uninitialized_value_construct_n<float*>(columns.locations_ys, count);
        std::uninitialized_value_construct_n<float*>(columns.locations_zs, count);
        std::uninitialized_value_construct_n<float*>(columns.rotations_pitches, count);
        std::uninitialized_value_construct_n<float*>(columns.rotations_yaws, count);
        std::uninitialized_value_construct_n<float*>(columns.rotations_rolls, count);
        std::uninitialized_value_construct_n<Team*>(columns.teams, count);
        std::uninitialized_value_construct_n<Health*>(columns.healths, count);
        std::uninitialized_value_construct_n<float*>(columns.initial_fighter_spawn_delays, count);
        std::uninitialized_value_construct_n<float*>(columns.fighter_spawn_cooldowns, count);
    }
    void swap_remove_columns(size_type const index,
                             size_type const source,
                             size_type const move_count) {
        copy_columns(get_data(), index, source, move_count);
    }
    static void copy_columns(DataPointers<std::byte> const& columns,
                             size_type index,
                             size_type source,
                             size_type move_count) {
        auto const elements_to_move{static_cast<byte_size_type>(move_count)};
        auto const entity_indices_bytes{elements_to_move * sizeof(std::int32_t)};
        auto const locations_xs_bytes{elements_to_move * sizeof(float)};
        auto const teams_bytes{elements_to_move * sizeof(Team)};
        auto const healths_bytes{elements_to_move * sizeof(Health)};
        std::memcpy(
            columns.entity_indices + index, columns.entity_indices + source, entity_indices_bytes);
        std::memcpy(columns.target_entity_indices + index,
                    columns.target_entity_indices + source,
                    entity_indices_bytes);
        std::memcpy(
            columns.locations_xs + index, columns.locations_xs + source, locations_xs_bytes);
        std::memcpy(
            columns.locations_ys + index, columns.locations_ys + source, locations_xs_bytes);
        std::memcpy(
            columns.locations_zs + index, columns.locations_zs + source, locations_xs_bytes);
        std::memcpy(columns.rotations_pitches + index,
                    columns.rotations_pitches + source,
                    locations_xs_bytes);
        std::memcpy(
            columns.rotations_yaws + index, columns.rotations_yaws + source, locations_xs_bytes);
        std::memcpy(
            columns.rotations_rolls + index, columns.rotations_rolls + source, locations_xs_bytes);
        std::memcpy(columns.teams + index, columns.teams + source, teams_bytes);
        std::memcpy(columns.healths + index, columns.healths + source, healths_bytes);
        std::memcpy(columns.initial_fighter_spawn_delays + index,
                    columns.initial_fighter_spawn_delays + source,
                    locations_xs_bytes);
        std::memcpy(columns.fighter_spawn_cooldowns + index,
                    columns.fighter_spawn_cooldowns + source,
                    locations_xs_bytes);
    }
    void swap_remove_indices(std::span<size_type const> indices) {
        auto const columns{get_data()};
        ml::soa_storage_detail::for_each_removal_run(
            num_,
            indices,
            ml::native_soa::require,
            [&](size_type index, size_type source, size_type count) {
                copy_columns(columns, index, source, count);
            });
    }
    auto ordinary_source_aliases_storage(
        LevelCapitalSpawnEventsConstView const& source) const noexcept -> bool {
        if (data_ == nullptr) {
            return false;
        }
        auto const allocation_begin{reinterpret_cast<std::uintptr_t>(data_)};
        auto const allocation_end{allocation_begin + layout_bytes(capacity_blocks())};
        auto const aliases = [allocation_begin, allocation_end](auto const* pointer) noexcept {
            auto const address{reinterpret_cast<std::uintptr_t>(pointer)};
            return address >= allocation_begin && address < allocation_end;
        };
        return aliases(source.entity_indices.data()) ||
               aliases(source.target_entity_indices.data()) || aliases(source.locations.xs) ||
               aliases(source.locations.ys) || aliases(source.locations.zs) ||
               aliases(source.rotations.pitches.data()) || aliases(source.rotations.yaws.data()) ||
               aliases(source.rotations.rolls.data()) || aliases(source.teams.data()) ||
               aliases(source.healths.data()) ||
               aliases(source.initial_fighter_spawn_delays.data()) ||
               aliases(source.fighter_spawn_cooldowns.data());
    }
    template <typename Columns>
    void append_columns(Columns const& source, size_type first, size_type count) {
        auto const destination{get_data(first)};
        auto const elements_to_copy{static_cast<byte_size_type>(count)};
        auto const entity_indices_bytes{elements_to_copy * sizeof(std::int32_t)};
        auto const locations_xs_bytes{elements_to_copy * sizeof(float)};
        auto const teams_bytes{elements_to_copy * sizeof(Team)};
        auto const healths_bytes{elements_to_copy * sizeof(Health)};
        std::memcpy(destination.entity_indices, source.entity_indices.data(), entity_indices_bytes);
        std::memcpy(destination.target_entity_indices,
                    source.target_entity_indices.data(),
                    entity_indices_bytes);
        std::memcpy(destination.locations_xs, source.locations.xs, locations_xs_bytes);
        std::memcpy(destination.locations_ys, source.locations.ys, locations_xs_bytes);
        std::memcpy(destination.locations_zs, source.locations.zs, locations_xs_bytes);
        std::memcpy(
            destination.rotations_pitches, source.rotations.pitches.data(), locations_xs_bytes);
        std::memcpy(destination.rotations_yaws, source.rotations.yaws.data(), locations_xs_bytes);
        std::memcpy(destination.rotations_rolls, source.rotations.rolls.data(), locations_xs_bytes);
        std::memcpy(destination.teams, source.teams.data(), teams_bytes);
        std::memcpy(destination.healths, source.healths.data(), healths_bytes);
        std::memcpy(destination.initial_fighter_spawn_delays,
                    source.initial_fighter_spawn_delays.data(),
                    locations_xs_bytes);
        std::memcpy(destination.fighter_spawn_cooldowns,
                    source.fighter_spawn_cooldowns.data(),
                    locations_xs_bytes);
    }
    void reallocate(size_type const new_capacity) {
        auto* const new_data{ml::native_soa::allocate(
            layout_bytes(static_cast<byte_size_type>(new_capacity / capacity_granularity)),
            static_cast<std::uint32_t>(allocation_alignment))};
        if (num_ > 0) {
            auto const old_blocks{capacity_blocks()};
            auto const new_blocks{static_cast<byte_size_type>(new_capacity / capacity_granularity)};
            auto const source{
                make_data_unchecked(static_cast<std::byte const*>(data_), old_blocks)};
            auto const destination{make_data_unchecked(new_data, new_blocks)};
            auto const live_count{static_cast<byte_size_type>(num_)};
            auto const entity_indices_bytes{live_count * sizeof(std::int32_t)};
            auto const locations_xs_bytes{live_count * sizeof(float)};
            auto const teams_bytes{live_count * sizeof(Team)};
            auto const healths_bytes{live_count * sizeof(Health)};
            std::memcpy(destination.entity_indices, source.entity_indices, entity_indices_bytes);
            std::memcpy(destination.target_entity_indices,
                        source.target_entity_indices,
                        entity_indices_bytes);
            std::memcpy(destination.locations_xs, source.locations_xs, locations_xs_bytes);
            std::memcpy(destination.locations_ys, source.locations_ys, locations_xs_bytes);
            std::memcpy(destination.locations_zs, source.locations_zs, locations_xs_bytes);
            std::memcpy(
                destination.rotations_pitches, source.rotations_pitches, locations_xs_bytes);
            std::memcpy(destination.rotations_yaws, source.rotations_yaws, locations_xs_bytes);
            std::memcpy(destination.rotations_rolls, source.rotations_rolls, locations_xs_bytes);
            std::memcpy(destination.teams, source.teams, teams_bytes);
            std::memcpy(destination.healths, source.healths, healths_bytes);
            std::memcpy(destination.initial_fighter_spawn_delays,
                        source.initial_fighter_spawn_delays,
                        locations_xs_bytes);
            std::memcpy(destination.fighter_spawn_cooldowns,
                        source.fighter_spawn_cooldowns,
                        locations_xs_bytes);
        }
        ml::native_soa::free(data_, allocation_alignment);
        data_ = new_data;
        capacity_ = new_capacity;
    }
};

struct LevelCapitalSpawnEventsSingleConstView : ml::native_soa::CompactViewState<true> {
    using Base = ml::native_soa::CompactViewState<true>;
    using Base::Base;
    using View = LevelCapitalSpawnEventsSingleView;
    using ConstView = LevelCapitalSpawnEventsSingleConstView;
    LevelCapitalSpawnEventsSingleConstView() = default;
    LevelCapitalSpawnEventsSingleConstView(LevelCapitalSpawnEventsSingleView const& other);
    auto get_const_view() const -> ConstView { return *this; }
    auto get_const_view(size_type offset, size_type count) const -> ConstView {
        return slice(offset, count);
    }
    auto entity_indices() const -> std::span<std::int32_t const> {
        return {column_data<std::int32_t>(
                    LevelCapitalSpawnEventsSingleLayout::EntityIndices.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto target_entity_indices() const -> std::span<std::int32_t const> {
        return {
            column_data<std::int32_t>(
                LevelCapitalSpawnEventsSingleLayout::TargetEntityIndices.offset(capacity_blocks())),
            static_cast<std::size_t>(count_)};
    }
    auto view_locations() const -> ml::native_soa::Vector3ConstView<float> {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        auto const first{LevelCapitalSpawnEventsSingleLayout::LocationsXs.offset(blocks)};
        auto const stride{LevelCapitalSpawnEventsSingleLayout::LocationsYs.offset(blocks) - first};
        return {column_data_unchecked<float>(first), stride, count_};
    }
    auto view_rotations() const -> Rotators3fConstView {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        return Rotators3fConstView{
            {column_data_unchecked<float>(
                 LevelCapitalSpawnEventsSingleLayout::RotationsPitches.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<float>(
                 LevelCapitalSpawnEventsSingleLayout::RotationsYaws.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<float>(
                 LevelCapitalSpawnEventsSingleLayout::RotationsRolls.offset(blocks)),
             static_cast<std::size_t>(count_)}};
    }
    auto teams() const -> std::span<Team const> {
        return {
            column_data<Team>(LevelCapitalSpawnEventsSingleLayout::Teams.offset(capacity_blocks())),
            static_cast<std::size_t>(count_)};
    }
    auto healths() const -> std::span<Health const> {
        return {column_data<Health>(
                    LevelCapitalSpawnEventsSingleLayout::Healths.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto initial_fighter_spawn_delays() const -> std::span<float const> {
        return {column_data<float>(
                    LevelCapitalSpawnEventsSingleLayout::InitialFighterSpawnDelays.offset(
                        capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto fighter_spawn_cooldowns() const -> std::span<float const> {
        return {
            column_data<float>(LevelCapitalSpawnEventsSingleLayout::FighterSpawnCooldowns.offset(
                capacity_blocks())),
            static_cast<std::size_t>(count_)};
    }
    auto columns() const -> LevelCapitalSpawnEventsConstView {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        return LevelCapitalSpawnEventsConstView{
            {column_data_unchecked<std::int32_t>(
                 LevelCapitalSpawnEventsSingleLayout::EntityIndices.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<std::int32_t>(
                 LevelCapitalSpawnEventsSingleLayout::TargetEntityIndices.offset(blocks)),
             static_cast<std::size_t>(count_)},
            Vectors3fConstView{
                {column_data_unchecked<float>(
                     LevelCapitalSpawnEventsSingleLayout::LocationsXs.offset(blocks)),
                 static_cast<std::size_t>(count_)},
                {column_data_unchecked<float>(
                     LevelCapitalSpawnEventsSingleLayout::LocationsYs.offset(blocks)),
                 static_cast<std::size_t>(count_)},
                {column_data_unchecked<float>(
                     LevelCapitalSpawnEventsSingleLayout::LocationsZs.offset(blocks)),
                 static_cast<std::size_t>(count_)}},
            Rotators3fConstView{
                {column_data_unchecked<float>(
                     LevelCapitalSpawnEventsSingleLayout::RotationsPitches.offset(blocks)),
                 static_cast<std::size_t>(count_)},
                {column_data_unchecked<float>(
                     LevelCapitalSpawnEventsSingleLayout::RotationsYaws.offset(blocks)),
                 static_cast<std::size_t>(count_)},
                {column_data_unchecked<float>(
                     LevelCapitalSpawnEventsSingleLayout::RotationsRolls.offset(blocks)),
                 static_cast<std::size_t>(count_)}},
            {column_data_unchecked<Team>(LevelCapitalSpawnEventsSingleLayout::Teams.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<Health>(
                 LevelCapitalSpawnEventsSingleLayout::Healths.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<float>(
                 LevelCapitalSpawnEventsSingleLayout::InitialFighterSpawnDelays.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<float>(
                 LevelCapitalSpawnEventsSingleLayout::FighterSpawnCooldowns.offset(blocks)),
             static_cast<std::size_t>(count_)}};
    }
    template <typename Func>
    void each_column(Func&& func) const {
        columns().each_column(std::forward<Func>(func));
    }
};
static_assert(sizeof(LevelCapitalSpawnEventsSingleConstView) == 16);
static_assert(std::is_trivially_copyable_v<LevelCapitalSpawnEventsSingleConstView>);
struct LevelCapitalSpawnEventsSingleView : ml::native_soa::CompactViewState<false> {
    using Base = ml::native_soa::CompactViewState<false>;
    using Base::Base;
    using View = LevelCapitalSpawnEventsSingleView;
    using ConstView = LevelCapitalSpawnEventsSingleConstView;
    LevelCapitalSpawnEventsSingleView() = default;
    auto get_const_view() const -> ConstView { return *this; }
    auto get_const_view(size_type offset, size_type count) const -> ConstView {
        return slice(offset, count);
    }
    auto entity_indices() const -> std::span<std::int32_t> {
        return {column_data<std::int32_t>(
                    LevelCapitalSpawnEventsSingleLayout::EntityIndices.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto target_entity_indices() const -> std::span<std::int32_t> {
        return {
            column_data<std::int32_t>(
                LevelCapitalSpawnEventsSingleLayout::TargetEntityIndices.offset(capacity_blocks())),
            static_cast<std::size_t>(count_)};
    }
    auto view_locations() const -> ml::native_soa::Vector3View<float> {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        auto const first{LevelCapitalSpawnEventsSingleLayout::LocationsXs.offset(blocks)};
        auto const stride{LevelCapitalSpawnEventsSingleLayout::LocationsYs.offset(blocks) - first};
        return {column_data_unchecked<float>(first), stride, count_};
    }
    auto view_rotations() const -> Rotators3fView {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        return Rotators3fView{
            {column_data_unchecked<float>(
                 LevelCapitalSpawnEventsSingleLayout::RotationsPitches.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<float>(
                 LevelCapitalSpawnEventsSingleLayout::RotationsYaws.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<float>(
                 LevelCapitalSpawnEventsSingleLayout::RotationsRolls.offset(blocks)),
             static_cast<std::size_t>(count_)}};
    }
    auto teams() const -> std::span<Team> {
        return {
            column_data<Team>(LevelCapitalSpawnEventsSingleLayout::Teams.offset(capacity_blocks())),
            static_cast<std::size_t>(count_)};
    }
    auto healths() const -> std::span<Health> {
        return {column_data<Health>(
                    LevelCapitalSpawnEventsSingleLayout::Healths.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto initial_fighter_spawn_delays() const -> std::span<float> {
        return {column_data<float>(
                    LevelCapitalSpawnEventsSingleLayout::InitialFighterSpawnDelays.offset(
                        capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto fighter_spawn_cooldowns() const -> std::span<float> {
        return {
            column_data<float>(LevelCapitalSpawnEventsSingleLayout::FighterSpawnCooldowns.offset(
                capacity_blocks())),
            static_cast<std::size_t>(count_)};
    }
    auto columns() const -> LevelCapitalSpawnEventsView {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        return LevelCapitalSpawnEventsView{
            {column_data_unchecked<std::int32_t>(
                 LevelCapitalSpawnEventsSingleLayout::EntityIndices.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<std::int32_t>(
                 LevelCapitalSpawnEventsSingleLayout::TargetEntityIndices.offset(blocks)),
             static_cast<std::size_t>(count_)},
            Vectors3fView{{column_data_unchecked<float>(
                               LevelCapitalSpawnEventsSingleLayout::LocationsXs.offset(blocks)),
                           static_cast<std::size_t>(count_)},
                          {column_data_unchecked<float>(
                               LevelCapitalSpawnEventsSingleLayout::LocationsYs.offset(blocks)),
                           static_cast<std::size_t>(count_)},
                          {column_data_unchecked<float>(
                               LevelCapitalSpawnEventsSingleLayout::LocationsZs.offset(blocks)),
                           static_cast<std::size_t>(count_)}},
            Rotators3fView{
                {column_data_unchecked<float>(
                     LevelCapitalSpawnEventsSingleLayout::RotationsPitches.offset(blocks)),
                 static_cast<std::size_t>(count_)},
                {column_data_unchecked<float>(
                     LevelCapitalSpawnEventsSingleLayout::RotationsYaws.offset(blocks)),
                 static_cast<std::size_t>(count_)},
                {column_data_unchecked<float>(
                     LevelCapitalSpawnEventsSingleLayout::RotationsRolls.offset(blocks)),
                 static_cast<std::size_t>(count_)}},
            {column_data_unchecked<Team>(LevelCapitalSpawnEventsSingleLayout::Teams.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<Health>(
                 LevelCapitalSpawnEventsSingleLayout::Healths.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<float>(
                 LevelCapitalSpawnEventsSingleLayout::InitialFighterSpawnDelays.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<float>(
                 LevelCapitalSpawnEventsSingleLayout::FighterSpawnCooldowns.offset(blocks)),
             static_cast<std::size_t>(count_)}};
    }
    template <typename Func>
    void each_column(Func&& func) const {
        columns().each_column(std::forward<Func>(func));
    }
};
static_assert(sizeof(LevelCapitalSpawnEventsSingleView) == 16);
static_assert(std::is_trivially_copyable_v<LevelCapitalSpawnEventsSingleView>);
inline LevelCapitalSpawnEventsSingleConstView::LevelCapitalSpawnEventsSingleConstView(
    LevelCapitalSpawnEventsSingleView const& other)
    : Base{other} {}
struct SingleAllocationLevelCapitalSpawnEvents : SingleAllocationLevelCapitalSpawnEventsStorage {
    SingleAllocationLevelCapitalSpawnEvents() noexcept = default;
    SingleAllocationLevelCapitalSpawnEvents(SingleAllocationLevelCapitalSpawnEvents const&) =
        delete;
    auto operator=(SingleAllocationLevelCapitalSpawnEvents const&)
        -> SingleAllocationLevelCapitalSpawnEvents& = delete;
    SingleAllocationLevelCapitalSpawnEvents(SingleAllocationLevelCapitalSpawnEvents&&) noexcept =
        default;
    auto operator=(SingleAllocationLevelCapitalSpawnEvents&&) noexcept
        -> SingleAllocationLevelCapitalSpawnEvents& = default;
    auto get_view() & -> View { return {this, 0, num()}; }
    auto get_view(size_type offset, size_type count) & -> View { return {this, offset, count}; }
    auto slice(size_type offset, size_type count) & -> View { return get_view(offset, count); }
    auto left(size_type count) & -> View { return get_view().left(count); }
    auto right(size_type count) & -> View { return get_view().right(count); }
    auto get_view() && -> View = delete;
    auto get_view(size_type, size_type) && -> View = delete;
    auto slice(size_type, size_type) && -> View = delete;
    auto left(size_type) && -> View = delete;
    auto right(size_type) && -> View = delete;
    auto get_view() const& -> ConstView { return {this, 0, num()}; }
    auto get_view(size_type offset, size_type count) const& -> ConstView {
        return {this, offset, count};
    }
    auto slice(size_type offset, size_type count) const& -> ConstView {
        return get_view(offset, count);
    }
    auto left(size_type count) const& -> ConstView { return get_view().left(count); }
    auto right(size_type count) const& -> ConstView { return get_view().right(count); }
    auto get_view() const&& -> ConstView = delete;
    auto get_view(size_type, size_type) const&& -> ConstView = delete;
    auto slice(size_type, size_type) const&& -> ConstView = delete;
    auto left(size_type) const&& -> ConstView = delete;
    auto right(size_type) const&& -> ConstView = delete;
    auto get_const_view() const& -> ConstView { return get_view(); }
    auto get_const_view(size_type offset, size_type count) const& -> ConstView {
        return get_view(offset, count);
    }
    auto get_const_view() const&& -> ConstView = delete;
    auto get_const_view(size_type, size_type) const&& -> ConstView = delete;
};

struct LevelTurretSpawnEventsView;
struct LevelTurretSpawnEventsConstView;
struct LevelTurretSpawnEventsConstView {
    using View = LevelTurretSpawnEventsView;
    using ConstView = LevelTurretSpawnEventsConstView;
    using size_type = std::int32_t;
    std::span<std::int32_t const> entity_indices;
    Vectors3fConstView locations;
    Rotators3fConstView rotations;
    std::span<Team const> teams;
    std::span<Health const> healths;
    std::span<std::int32_t const> laser_damages;
    auto num() const noexcept -> size_type { return static_cast<size_type>(entity_indices.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(entity_indices);
        fn(locations.xs_span());
        fn(locations.ys_span());
        fn(locations.zs_span());
        fn(rotations.pitches);
        fn(rotations.yaws);
        fn(rotations.rolls);
        fn(teams);
        fn(healths);
        fn(laser_damages);
    }
    void validate_array_sizes() const {
        auto const count{num()};
        each_column([count](auto column) {
            ml::native_soa::require(column.size() == static_cast<std::size_t>(count));
        });
    }
    auto slice(size_type const offset, size_type const count) const
        -> LevelTurretSpawnEventsConstView {
        ml::native_soa::require(offset >= 0 && count >= 0 && offset <= num() &&
                                count <= num() - offset);
        return {
            entity_indices.subspan(static_cast<std::size_t>(offset),
                                   static_cast<std::size_t>(count)),
            locations.slice(offset, count),
            rotations.slice(offset, count),
            teams.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            healths.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            laser_damages.subspan(static_cast<std::size_t>(offset),
                                  static_cast<std::size_t>(count)),
        };
    }
    auto get_view() const -> LevelTurretSpawnEventsConstView { return *this; }
    auto get_view(size_type const offset, size_type const count) const
        -> LevelTurretSpawnEventsConstView {
        return slice(offset, count);
    }
    auto get_const_view() const -> ConstView {
        return {
            entity_indices,
            locations.get_const_view(),
            rotations.get_const_view(),
            teams,
            healths,
            laser_damages,
        };
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto left(size_type const count) const -> LevelTurretSpawnEventsConstView {
        return slice(0, count);
    }
    auto right(size_type const count) const -> LevelTurretSpawnEventsConstView {
        return slice(num() - count, count);
    }
};
struct LevelTurretSpawnEventsView {
    using View = LevelTurretSpawnEventsView;
    using ConstView = LevelTurretSpawnEventsConstView;
    using size_type = std::int32_t;
    std::span<std::int32_t> entity_indices;
    Vectors3fView locations;
    Rotators3fView rotations;
    std::span<Team> teams;
    std::span<Health> healths;
    std::span<std::int32_t> laser_damages;
    auto num() const noexcept -> size_type { return static_cast<size_type>(entity_indices.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(entity_indices);
        fn(locations.xs_span());
        fn(locations.ys_span());
        fn(locations.zs_span());
        fn(rotations.pitches);
        fn(rotations.yaws);
        fn(rotations.rolls);
        fn(teams);
        fn(healths);
        fn(laser_damages);
    }
    void validate_array_sizes() const {
        auto const count{num()};
        each_column([count](auto column) {
            ml::native_soa::require(column.size() == static_cast<std::size_t>(count));
        });
    }
    auto slice(size_type const offset, size_type const count) const -> LevelTurretSpawnEventsView {
        ml::native_soa::require(offset >= 0 && count >= 0 && offset <= num() &&
                                count <= num() - offset);
        return {
            entity_indices.subspan(static_cast<std::size_t>(offset),
                                   static_cast<std::size_t>(count)),
            locations.slice(offset, count),
            rotations.slice(offset, count),
            teams.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            healths.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            laser_damages.subspan(static_cast<std::size_t>(offset),
                                  static_cast<std::size_t>(count)),
        };
    }
    auto get_view() const -> LevelTurretSpawnEventsView { return *this; }
    auto get_view(size_type const offset, size_type const count) const
        -> LevelTurretSpawnEventsView {
        return slice(offset, count);
    }
    auto get_const_view() const -> ConstView {
        return {
            entity_indices,
            locations.get_const_view(),
            rotations.get_const_view(),
            teams,
            healths,
            laser_damages,
        };
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto left(size_type const count) const -> LevelTurretSpawnEventsView { return slice(0, count); }
    auto right(size_type const count) const -> LevelTurretSpawnEventsView {
        return slice(num() - count, count);
    }
    void set(size_type const index,
             std::int32_t const new_entity_indices,
             float const new_locations_xs,
             float const new_locations_ys,
             float const new_locations_zs,
             float const new_rotations_pitches,
             float const new_rotations_yaws,
             float const new_rotations_rolls,
             Team const new_teams,
             Health const new_healths,
             std::int32_t const new_laser_damages) const {
        ml::native_soa::require(index >= 0 && index < num());
        entity_indices[static_cast<std::size_t>(index)] = new_entity_indices;
        locations.xs[static_cast<std::size_t>(index)] = new_locations_xs;
        locations.ys[static_cast<std::size_t>(index)] = new_locations_ys;
        locations.zs[static_cast<std::size_t>(index)] = new_locations_zs;
        rotations.pitches[static_cast<std::size_t>(index)] = new_rotations_pitches;
        rotations.yaws[static_cast<std::size_t>(index)] = new_rotations_yaws;
        rotations.rolls[static_cast<std::size_t>(index)] = new_rotations_rolls;
        teams[static_cast<std::size_t>(index)] = new_teams;
        healths[static_cast<std::size_t>(index)] = new_healths;
        laser_damages[static_cast<std::size_t>(index)] = new_laser_damages;
    }
};
struct LevelTurretSpawnEvents {
    using View = LevelTurretSpawnEventsView;
    using ConstView = LevelTurretSpawnEventsConstView;
    using size_type = std::int32_t;
    ml::native_soa::Vector<std::int32_t> entity_indices;
    Vectors3f locations;
    Rotators3f rotations;
    ml::native_soa::Vector<Team> teams;
    ml::native_soa::Vector<Health> healths;
    ml::native_soa::Vector<std::int32_t> laser_damages;
    auto num() const noexcept -> size_type { return static_cast<size_type>(entity_indices.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) {
        fn(entity_indices);
        fn(locations.xs);
        fn(locations.ys);
        fn(locations.zs);
        fn(rotations.pitches);
        fn(rotations.yaws);
        fn(rotations.rolls);
        fn(teams);
        fn(healths);
        fn(laser_damages);
    }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(entity_indices);
        fn(locations.xs);
        fn(locations.ys);
        fn(locations.zs);
        fn(rotations.pitches);
        fn(rotations.yaws);
        fn(rotations.rolls);
        fn(teams);
        fn(healths);
        fn(laser_damages);
    }
    void validate_array_sizes() const { get_const_view().validate_array_sizes(); }
    void reserve(size_type const count) {
        ml::native_soa::require(count >= 0);
        entity_indices.reserve(static_cast<std::size_t>(count));
        locations.xs.reserve(static_cast<std::size_t>(count));
        locations.ys.reserve(static_cast<std::size_t>(count));
        locations.zs.reserve(static_cast<std::size_t>(count));
        rotations.pitches.reserve(static_cast<std::size_t>(count));
        rotations.yaws.reserve(static_cast<std::size_t>(count));
        rotations.rolls.reserve(static_cast<std::size_t>(count));
        teams.reserve(static_cast<std::size_t>(count));
        healths.reserve(static_cast<std::size_t>(count));
        laser_damages.reserve(static_cast<std::size_t>(count));
    }
    void reset() noexcept {
        entity_indices.clear();
        locations.xs.clear();
        locations.ys.clear();
        locations.zs.clear();
        rotations.pitches.clear();
        rotations.yaws.clear();
        rotations.rolls.clear();
        teams.clear();
        healths.clear();
        laser_damages.clear();
    }
    void set_num(size_type const count) {
        ml::native_soa::require(count >= 0);
        auto const size{static_cast<std::size_t>(count)};
        entity_indices.resize(size);
        locations.xs.resize(size);
        locations.ys.resize(size);
        locations.zs.resize(size);
        rotations.pitches.resize(size);
        rotations.yaws.resize(size);
        rotations.rolls.resize(size);
        teams.resize(size);
        healths.resize(size);
        laser_damages.resize(size);
    }
    void add_uninitialised(size_type const count) {
        auto const old_num{num()};
        ml::native_soa::require(count >= 0 &&
                                count <= std::numeric_limits<size_type>::max() - old_num);
        set_num(old_num + count);
    }
    void add_defaulted(size_type const count) { add_uninitialised(count); }
    void remove_at_swap(size_type const index, size_type const count) {
        auto const old_num{num()};
        ml::native_soa::require(index >= 0 && index <= old_num && count >= 0 &&
                                count <= old_num - index);
        auto const moved{std::min(count, old_num - index - count)};
        auto const source{old_num - moved};
        for (size_type i{}; i < moved; ++i) {
            entity_indices[index + i] = entity_indices[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            locations.xs[index + i] = locations.xs[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            locations.ys[index + i] = locations.ys[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            locations.zs[index + i] = locations.zs[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            rotations.pitches[index + i] = rotations.pitches[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            rotations.yaws[index + i] = rotations.yaws[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            rotations.rolls[index + i] = rotations.rolls[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            teams[index + i] = teams[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            healths[index + i] = healths[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            laser_damages[index + i] = laser_damages[source + i];
        }
        set_num(old_num - count);
    }
    void set(size_type const index,
             std::int32_t const new_entity_indices,
             float const new_locations_xs,
             float const new_locations_ys,
             float const new_locations_zs,
             float const new_rotations_pitches,
             float const new_rotations_yaws,
             float const new_rotations_rolls,
             Team const new_teams,
             Health const new_healths,
             std::int32_t const new_laser_damages) {
        get_view().set(index,
                       new_entity_indices,
                       new_locations_xs,
                       new_locations_ys,
                       new_locations_zs,
                       new_rotations_pitches,
                       new_rotations_yaws,
                       new_rotations_rolls,
                       new_teams,
                       new_healths,
                       new_laser_damages);
    }
    auto add(std::int32_t const new_entity_indices,
             float const new_locations_xs,
             float const new_locations_ys,
             float const new_locations_zs,
             float const new_rotations_pitches,
             float const new_rotations_yaws,
             float const new_rotations_rolls,
             Team const new_teams,
             Health const new_healths,
             std::int32_t const new_laser_damages) -> size_type {
        auto const index{num()};
        add_defaulted(1);
        set(index,
            new_entity_indices,
            new_locations_xs,
            new_locations_ys,
            new_locations_zs,
            new_rotations_pitches,
            new_rotations_yaws,
            new_rotations_rolls,
            new_teams,
            new_healths,
            new_laser_damages);
        return index;
    }
    void append_from(ConstView source) {
        auto const count{source.num()};
        ml::native_soa::require(count <= std::numeric_limits<size_type>::max() - num());
        source.validate_array_sizes();
        if (count == 0) {
            return;
        }
        {
            auto const address{ml::address_cast(source.entity_indices.data())};
            auto const begin{ml::address_cast(entity_indices.data())};
            ml::native_soa::require(address < begin || address >= begin + entity_indices.size() *
                                                                              sizeof(std::int32_t));
        }
        {
            auto const address{ml::address_cast(source.locations.xs)};
            auto const begin{ml::address_cast(locations.xs.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + locations.xs.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.locations.ys)};
            auto const begin{ml::address_cast(locations.ys.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + locations.ys.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.locations.zs)};
            auto const begin{ml::address_cast(locations.zs.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + locations.zs.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.rotations.pitches.data())};
            auto const begin{ml::address_cast(rotations.pitches.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + rotations.pitches.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.rotations.yaws.data())};
            auto const begin{ml::address_cast(rotations.yaws.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + rotations.yaws.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.rotations.rolls.data())};
            auto const begin{ml::address_cast(rotations.rolls.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + rotations.rolls.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.teams.data())};
            auto const begin{ml::address_cast(teams.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + teams.size() * sizeof(Team));
        }
        {
            auto const address{ml::address_cast(source.healths.data())};
            auto const begin{ml::address_cast(healths.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + healths.size() * sizeof(Health));
        }
        {
            auto const address{ml::address_cast(source.laser_damages.data())};
            auto const begin{ml::address_cast(laser_damages.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + laser_damages.size() * sizeof(std::int32_t));
        }
        entity_indices.insert(entity_indices.end(),
                              source.entity_indices.data(),
                              source.entity_indices.data() + count);
        locations.xs.insert(locations.xs.end(), source.locations.xs, source.locations.xs + count);
        locations.ys.insert(locations.ys.end(), source.locations.ys, source.locations.ys + count);
        locations.zs.insert(locations.zs.end(), source.locations.zs, source.locations.zs + count);
        rotations.pitches.insert(rotations.pitches.end(),
                                 source.rotations.pitches.data(),
                                 source.rotations.pitches.data() + count);
        rotations.yaws.insert(rotations.yaws.end(),
                              source.rotations.yaws.data(),
                              source.rotations.yaws.data() + count);
        rotations.rolls.insert(rotations.rolls.end(),
                               source.rotations.rolls.data(),
                               source.rotations.rolls.data() + count);
        teams.insert(teams.end(), source.teams.data(), source.teams.data() + count);
        healths.insert(healths.end(), source.healths.data(), source.healths.data() + count);
        laser_damages.insert(
            laser_damages.end(), source.laser_damages.data(), source.laser_damages.data() + count);
    }
    auto get_view() -> View {
        return {
            entity_indices,
            locations.get_view(),
            rotations.get_view(),
            teams,
            healths,
            laser_damages,
        };
    }
    auto get_view() const -> ConstView {
        return {
            entity_indices,
            locations.get_view(),
            rotations.get_view(),
            teams,
            healths,
            laser_damages,
        };
    }
    auto get_const_view() const -> ConstView { return get_view(); }
    auto get_view(size_type const offset, size_type const count) -> View {
        return get_view().slice(offset, count);
    }
    auto get_view(size_type const offset, size_type const count) const -> ConstView {
        return get_view().slice(offset, count);
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto slice(size_type const offset, size_type const count) -> View {
        return get_view(offset, count);
    }
    auto slice(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view(offset, count);
    }
    auto left(size_type const count) -> View { return slice(0, count); }
    auto right(size_type const count) -> View { return slice(num() - count, count); }
    auto left(size_type const count) const -> ConstView { return slice(0, count); }
    auto right(size_type const count) const -> ConstView { return slice(num() - count, count); }
    template <typename Other>
    void copy_element(size_type const dst_index, Other const& other, size_type const src_index) {
        entity_indices[static_cast<std::size_t>(dst_index)] =
            other.entity_indices[static_cast<std::size_t>(src_index)];
        locations.copy_element(dst_index, other.locations, src_index);
        rotations.copy_element(dst_index, other.rotations, src_index);
        teams[static_cast<std::size_t>(dst_index)] =
            other.teams[static_cast<std::size_t>(src_index)];
        healths[static_cast<std::size_t>(dst_index)] =
            other.healths[static_cast<std::size_t>(src_index)];
        laser_damages[static_cast<std::size_t>(dst_index)] =
            other.laser_damages[static_cast<std::size_t>(src_index)];
    }
    template <typename Other>
    void copy_elements(size_type const dst_index,
                       Other const& other,
                       size_type const src_index,
                       size_type const count) {
        for (size_type i{}; i < count; ++i) {
            copy_element(dst_index + i, other, src_index + i);
        }
    }
    void apply_permutation(std::span<std::int32_t> const indices) {
        validate_array_sizes();
        ml::native_soa::require(indices.size() == static_cast<std::size_t>(num()));
        each_column([indices](auto& column) { ml::apply_permutation(std::span{column}, indices); });
    }
    template <typename Compare>
    void sort(Compare&& compare, std::span<std::int32_t> const scratch_indices) {
        validate_array_sizes();
        ml::native_soa::require(scratch_indices.size() == static_cast<std::size_t>(num()));
        for (size_type i{}; i < num(); ++i) {
            scratch_indices[static_cast<std::size_t>(i)] = i;
        }
        std::sort(scratch_indices.begin(),
                  scratch_indices.end(),
                  [this, &compare](size_type const lhs, size_type const rhs) {
                      return compare(*this, lhs, rhs);
                  });
        apply_permutation(scratch_indices);
    }
};

struct LevelTurretSpawnEventsSingleView;
struct LevelTurretSpawnEventsSingleConstView;
struct LevelTurretSpawnEventsSingleLayout {
    using size_type = std::int32_t;
    using byte_size_type = std::size_t;

    inline static constexpr byte_size_type max_allocation_size{
        std::numeric_limits<byte_size_type>::max()};
    inline static constexpr size_type capacity_granularity{64};
    inline static constexpr byte_size_type column_gap{192};

    template <typename T>
    using ColLayout = ml::native_soa::ColumnLayout<T>;
    inline static constexpr ml::native_soa::ColumnLayoutStart LayoutStart{
        capacity_granularity, column_gap, 64};

    inline static constexpr ColLayout<std::int32_t> EntityIndices{LayoutStart};
    inline static constexpr ColLayout<float> LocationsXs{EntityIndices};
    inline static constexpr ColLayout<float> LocationsYs{LocationsXs};
    inline static constexpr ColLayout<float> LocationsZs{LocationsYs};
    inline static constexpr ColLayout<float> RotationsPitches{LocationsZs};
    inline static constexpr ColLayout<float> RotationsYaws{RotationsPitches};
    inline static constexpr ColLayout<float> RotationsRolls{RotationsYaws};
    inline static constexpr ColLayout<Team> Teams{RotationsRolls};
    inline static constexpr ColLayout<Health> Healths{Teams};
    inline static constexpr ColLayout<std::int32_t> LaserDamages{Healths};

    inline static constexpr byte_size_type allocation_alignment{
        ml::native_soa::maximum_alignment(EntityIndices,
                                          LocationsXs,
                                          LocationsYs,
                                          LocationsZs,
                                          RotationsPitches,
                                          RotationsYaws,
                                          RotationsRolls,
                                          Teams,
                                          Healths,
                                          LaserDamages)};

    // Conservative per-block bound for checked capacity arithmetic; gaps do not scale with
    // capacity.
    inline static constexpr byte_size_type capacity_block_bound{
        ml::native_soa::layout_align(LaserDamages.block_end, allocation_alignment) +
        9 * (column_gap + allocation_alignment - 1)};
    inline static constexpr size_type max_capacity{
        ml::native_soa::maximum_capacity(capacity_block_bound)};
    static constexpr auto layout_bytes(byte_size_type blocks) noexcept -> byte_size_type {
        return blocks == 0 ? 0 : LaserDamages.data_end(blocks);
    }
  private:
    inline static constexpr auto validate_layout = []() consteval -> bool {
        static_assert(
            ml::native_soa::supported_leaf<std::int32_t>,
            "Single-allocation leaf entity_indices requires a non-cv, trivially "
            "copyable/copy-constructible/destructible, nothrow default-constructible object type.");
        static_assert(
            ml::native_soa::supported_leaf<float>,
            "Single-allocation leaf locations.xs requires a non-cv, trivially "
            "copyable/copy-constructible/destructible, nothrow default-constructible object type.");
        static_assert(
            ml::native_soa::supported_leaf<Team>,
            "Single-allocation leaf teams requires a non-cv, trivially "
            "copyable/copy-constructible/destructible, nothrow default-constructible object type.");
        static_assert(
            ml::native_soa::supported_leaf<Health>,
            "Single-allocation leaf healths requires a non-cv, trivially "
            "copyable/copy-constructible/destructible, nothrow default-constructible object type.");

        static_assert(
            allocation_alignment <= std::numeric_limits<std::uint32_t>::max(),
            "Single-allocation alignment must fit the allocator's 32-bit alignment argument.");
        static_assert(sizeof(std::int32_t) <=
                      (max_allocation_size - EntityIndices.block_offset) / capacity_granularity);
        static_assert(sizeof(float) <=
                      (max_allocation_size - LocationsXs.block_offset) / capacity_granularity);
        static_assert(sizeof(float) <=
                      (max_allocation_size - LocationsYs.block_offset) / capacity_granularity);
        static_assert(sizeof(float) <=
                      (max_allocation_size - LocationsZs.block_offset) / capacity_granularity);
        static_assert(sizeof(float) <=
                      (max_allocation_size - RotationsPitches.block_offset) / capacity_granularity);
        static_assert(sizeof(float) <=
                      (max_allocation_size - RotationsYaws.block_offset) / capacity_granularity);
        static_assert(sizeof(float) <=
                      (max_allocation_size - RotationsRolls.block_offset) / capacity_granularity);
        static_assert(sizeof(Team) <=
                      (max_allocation_size - Teams.block_offset) / capacity_granularity);
        static_assert(sizeof(Health) <=
                      (max_allocation_size - Healths.block_offset) / capacity_granularity);
        static_assert(sizeof(std::int32_t) <=
                      (max_allocation_size - LaserDamages.block_offset) / capacity_granularity);
        static_assert(9 <=
                      (max_allocation_size -
                       ml::native_soa::layout_align(LaserDamages.block_end, allocation_alignment)) /
                          (column_gap + allocation_alignment - 1));
        static_assert(max_capacity >= capacity_granularity);
        return true;
    };
    static_assert(validate_layout());
};

struct SingleAllocationLevelTurretSpawnEventsStorage
    : LevelTurretSpawnEventsSingleLayout
    , protected ml::native_soa::StorageState
    , ml::native_soa::StorageOperations {
    using View = LevelTurretSpawnEventsSingleView;
    using ConstView = LevelTurretSpawnEventsSingleConstView;
    using SchemaConstView = LevelTurretSpawnEventsConstView;
    using ml::native_soa::StorageOperations::append_from;
    auto append_from(LevelTurretSpawnEventsConstView const& source) -> size_type {
        source.validate_array_sizes();
        auto const count{source.num()};
        auto const first{num_};
        ml::native_soa::require((count <= max_capacity - first));
        if (count == 0) {
            return first;
        }
        auto const new_num{first + count};
        if (new_num > capacity_) {
            ml::native_soa::require(!ordinary_source_aliases_storage(source));
            reallocate(ml::native_soa::growth_capacity(new_num, capacity_, capacity_block_bound));
        }
        append_columns(source, first, count);
        num_ = new_num;
        return first;
    }
    /* **************************************** */
    // Lifetime
    /* **************************************** */
    SingleAllocationLevelTurretSpawnEventsStorage() noexcept = default;
    ~SingleAllocationLevelTurretSpawnEventsStorage() {
        ml::native_soa::free(data_, allocation_alignment);
    }
    SingleAllocationLevelTurretSpawnEventsStorage(
        SingleAllocationLevelTurretSpawnEventsStorage const&) = delete;
    auto operator=(SingleAllocationLevelTurretSpawnEventsStorage const&)
        -> SingleAllocationLevelTurretSpawnEventsStorage& = delete;
    SingleAllocationLevelTurretSpawnEventsStorage(
        SingleAllocationLevelTurretSpawnEventsStorage&& other) noexcept
        : StorageState{std::exchange(other.data_, nullptr),
                       std::exchange(other.num_, 0),
                       std::exchange(other.capacity_, 0)} {}
    auto operator=(SingleAllocationLevelTurretSpawnEventsStorage&& other) noexcept
        -> SingleAllocationLevelTurretSpawnEventsStorage& {
        if (this != &other) {
            ml::native_soa::free(data_, allocation_alignment);
            data_ = std::exchange(other.data_, nullptr);
            num_ = std::exchange(other.num_, 0);
            capacity_ = std::exchange(other.capacity_, 0);
        }
        return *this;
    }
  protected:
    template <typename Byte>
    struct DataPointers {
        template <typename T>
        using Element = std::conditional_t<std::is_const_v<Byte>, T const, T>;
        Element<std::int32_t>* entity_indices{};
        Element<float>* locations_xs{};
        Element<float>* locations_ys{};
        Element<float>* locations_zs{};
        Element<float>* rotations_pitches{};
        Element<float>* rotations_yaws{};
        Element<float>* rotations_rolls{};
        Element<Team>* teams{};
        Element<Health>* healths{};
        Element<std::int32_t>* laser_damages{};
        auto operator+(size_type const offset) const noexcept -> DataPointers {
            if (entity_indices == nullptr) {
                return {};
            }
            return {entity_indices + offset,
                    locations_xs + offset,
                    locations_ys + offset,
                    locations_zs + offset,
                    rotations_pitches + offset,
                    rotations_yaws + offset,
                    rotations_rolls + offset,
                    teams + offset,
                    healths + offset,
                    laser_damages + offset};
        }
    };
    template <typename Self>
    auto get_data(this Self& self) noexcept {
        using Byte = std::conditional_t<std::is_const_v<Self>, std::byte const, std::byte>;
        if (self.data_ == nullptr) {
            return DataPointers<Byte>{};
        }
        return make_data_unchecked(static_cast<Byte*>(self.data_), self.capacity_blocks());
    }
    template <typename Self>
    auto get_data(this Self& self, size_type const offset) noexcept {
        return self.get_data() + offset;
    }
  private:
    friend struct ml::native_soa::StorageOperations;
    /* **************************************** */
    // Column pointers
    /* **************************************** */
    template <typename Byte>
    static auto make_data_unchecked(Byte* const data, byte_size_type const blocks) noexcept
        -> DataPointers<Byte> {
        auto const pointer_at = [data](auto const& column, byte_size_type offset) noexcept {
            using Column = std::remove_cvref_t<decltype(column)>;
            using Pointer = std::conditional_t<std::is_const_v<Byte>,
                                               typename Column::const_pointer,
                                               typename Column::pointer>;
            return std::launder(reinterpret_cast<Pointer>(data + offset));
        };
        auto const entity_indices_offset{byte_size_type{}};
        auto const locations_xs_offset{ml::native_soa::layout_align(
            entity_indices_offset + blocks * capacity_granularity * sizeof(std::int32_t) +
                column_gap,
            LocationsXs.alignment)};
        auto const locations_ys_offset{ml::native_soa::layout_align(
            locations_xs_offset + blocks * capacity_granularity * sizeof(float) + column_gap,
            LocationsYs.alignment)};
        auto const locations_zs_offset{ml::native_soa::layout_align(
            locations_ys_offset + blocks * capacity_granularity * sizeof(float) + column_gap,
            LocationsZs.alignment)};
        auto const rotations_pitches_offset{ml::native_soa::layout_align(
            locations_zs_offset + blocks * capacity_granularity * sizeof(float) + column_gap,
            RotationsPitches.alignment)};
        auto const rotations_yaws_offset{ml::native_soa::layout_align(
            rotations_pitches_offset + blocks * capacity_granularity * sizeof(float) + column_gap,
            RotationsYaws.alignment)};
        auto const rotations_rolls_offset{ml::native_soa::layout_align(
            rotations_yaws_offset + blocks * capacity_granularity * sizeof(float) + column_gap,
            RotationsRolls.alignment)};
        auto const teams_offset{ml::native_soa::layout_align(
            rotations_rolls_offset + blocks * capacity_granularity * sizeof(float) + column_gap,
            Teams.alignment)};
        auto const healths_offset{ml::native_soa::layout_align(
            teams_offset + blocks * capacity_granularity * sizeof(Team) + column_gap,
            Healths.alignment)};
        auto const laser_damages_offset{ml::native_soa::layout_align(
            healths_offset + blocks * capacity_granularity * sizeof(Health) + column_gap,
            LaserDamages.alignment)};
        return {pointer_at(EntityIndices, entity_indices_offset),
                pointer_at(LocationsXs, locations_xs_offset),
                pointer_at(LocationsYs, locations_ys_offset),
                pointer_at(LocationsZs, locations_zs_offset),
                pointer_at(RotationsPitches, rotations_pitches_offset),
                pointer_at(RotationsYaws, rotations_yaws_offset),
                pointer_at(RotationsRolls, rotations_rolls_offset),
                pointer_at(Teams, teams_offset),
                pointer_at(Healths, healths_offset),
                pointer_at(LaserDamages, laser_damages_offset)};
    }
    auto capacity_blocks() const noexcept -> byte_size_type {
        return static_cast<byte_size_type>(capacity_ / capacity_granularity);
    }

    /* **************************************** */
    // Typed mutations and growth
    /* **************************************** */
    void default_construct_columns(size_type const first, size_type const count) {
        auto const columns{make_data_unchecked(data_, capacity_blocks()) + first};
        std::uninitialized_value_construct_n<std::int32_t*>(columns.entity_indices, count);
        std::uninitialized_value_construct_n<float*>(columns.locations_xs, count);
        std::uninitialized_value_construct_n<float*>(columns.locations_ys, count);
        std::uninitialized_value_construct_n<float*>(columns.locations_zs, count);
        std::uninitialized_value_construct_n<float*>(columns.rotations_pitches, count);
        std::uninitialized_value_construct_n<float*>(columns.rotations_yaws, count);
        std::uninitialized_value_construct_n<float*>(columns.rotations_rolls, count);
        std::uninitialized_value_construct_n<Team*>(columns.teams, count);
        std::uninitialized_value_construct_n<Health*>(columns.healths, count);
        std::uninitialized_value_construct_n<std::int32_t*>(columns.laser_damages, count);
    }
    void swap_remove_columns(size_type const index,
                             size_type const source,
                             size_type const move_count) {
        copy_columns(get_data(), index, source, move_count);
    }
    static void copy_columns(DataPointers<std::byte> const& columns,
                             size_type index,
                             size_type source,
                             size_type move_count) {
        auto const elements_to_move{static_cast<byte_size_type>(move_count)};
        auto const entity_indices_bytes{elements_to_move * sizeof(std::int32_t)};
        auto const locations_xs_bytes{elements_to_move * sizeof(float)};
        auto const teams_bytes{elements_to_move * sizeof(Team)};
        auto const healths_bytes{elements_to_move * sizeof(Health)};
        std::memcpy(
            columns.entity_indices + index, columns.entity_indices + source, entity_indices_bytes);
        std::memcpy(
            columns.locations_xs + index, columns.locations_xs + source, locations_xs_bytes);
        std::memcpy(
            columns.locations_ys + index, columns.locations_ys + source, locations_xs_bytes);
        std::memcpy(
            columns.locations_zs + index, columns.locations_zs + source, locations_xs_bytes);
        std::memcpy(columns.rotations_pitches + index,
                    columns.rotations_pitches + source,
                    locations_xs_bytes);
        std::memcpy(
            columns.rotations_yaws + index, columns.rotations_yaws + source, locations_xs_bytes);
        std::memcpy(
            columns.rotations_rolls + index, columns.rotations_rolls + source, locations_xs_bytes);
        std::memcpy(columns.teams + index, columns.teams + source, teams_bytes);
        std::memcpy(columns.healths + index, columns.healths + source, healths_bytes);
        std::memcpy(
            columns.laser_damages + index, columns.laser_damages + source, entity_indices_bytes);
    }
    void swap_remove_indices(std::span<size_type const> indices) {
        auto const columns{get_data()};
        ml::soa_storage_detail::for_each_removal_run(
            num_,
            indices,
            ml::native_soa::require,
            [&](size_type index, size_type source, size_type count) {
                copy_columns(columns, index, source, count);
            });
    }
    auto ordinary_source_aliases_storage(
        LevelTurretSpawnEventsConstView const& source) const noexcept -> bool {
        if (data_ == nullptr) {
            return false;
        }
        auto const allocation_begin{reinterpret_cast<std::uintptr_t>(data_)};
        auto const allocation_end{allocation_begin + layout_bytes(capacity_blocks())};
        auto const aliases = [allocation_begin, allocation_end](auto const* pointer) noexcept {
            auto const address{reinterpret_cast<std::uintptr_t>(pointer)};
            return address >= allocation_begin && address < allocation_end;
        };
        return aliases(source.entity_indices.data()) || aliases(source.locations.xs) ||
               aliases(source.locations.ys) || aliases(source.locations.zs) ||
               aliases(source.rotations.pitches.data()) || aliases(source.rotations.yaws.data()) ||
               aliases(source.rotations.rolls.data()) || aliases(source.teams.data()) ||
               aliases(source.healths.data()) || aliases(source.laser_damages.data());
    }
    template <typename Columns>
    void append_columns(Columns const& source, size_type first, size_type count) {
        auto const destination{get_data(first)};
        auto const elements_to_copy{static_cast<byte_size_type>(count)};
        auto const entity_indices_bytes{elements_to_copy * sizeof(std::int32_t)};
        auto const locations_xs_bytes{elements_to_copy * sizeof(float)};
        auto const teams_bytes{elements_to_copy * sizeof(Team)};
        auto const healths_bytes{elements_to_copy * sizeof(Health)};
        std::memcpy(destination.entity_indices, source.entity_indices.data(), entity_indices_bytes);
        std::memcpy(destination.locations_xs, source.locations.xs, locations_xs_bytes);
        std::memcpy(destination.locations_ys, source.locations.ys, locations_xs_bytes);
        std::memcpy(destination.locations_zs, source.locations.zs, locations_xs_bytes);
        std::memcpy(
            destination.rotations_pitches, source.rotations.pitches.data(), locations_xs_bytes);
        std::memcpy(destination.rotations_yaws, source.rotations.yaws.data(), locations_xs_bytes);
        std::memcpy(destination.rotations_rolls, source.rotations.rolls.data(), locations_xs_bytes);
        std::memcpy(destination.teams, source.teams.data(), teams_bytes);
        std::memcpy(destination.healths, source.healths.data(), healths_bytes);
        std::memcpy(destination.laser_damages, source.laser_damages.data(), entity_indices_bytes);
    }
    void reallocate(size_type const new_capacity) {
        auto* const new_data{ml::native_soa::allocate(
            layout_bytes(static_cast<byte_size_type>(new_capacity / capacity_granularity)),
            static_cast<std::uint32_t>(allocation_alignment))};
        if (num_ > 0) {
            auto const old_blocks{capacity_blocks()};
            auto const new_blocks{static_cast<byte_size_type>(new_capacity / capacity_granularity)};
            auto const source{
                make_data_unchecked(static_cast<std::byte const*>(data_), old_blocks)};
            auto const destination{make_data_unchecked(new_data, new_blocks)};
            auto const live_count{static_cast<byte_size_type>(num_)};
            auto const entity_indices_bytes{live_count * sizeof(std::int32_t)};
            auto const locations_xs_bytes{live_count * sizeof(float)};
            auto const teams_bytes{live_count * sizeof(Team)};
            auto const healths_bytes{live_count * sizeof(Health)};
            std::memcpy(destination.entity_indices, source.entity_indices, entity_indices_bytes);
            std::memcpy(destination.locations_xs, source.locations_xs, locations_xs_bytes);
            std::memcpy(destination.locations_ys, source.locations_ys, locations_xs_bytes);
            std::memcpy(destination.locations_zs, source.locations_zs, locations_xs_bytes);
            std::memcpy(
                destination.rotations_pitches, source.rotations_pitches, locations_xs_bytes);
            std::memcpy(destination.rotations_yaws, source.rotations_yaws, locations_xs_bytes);
            std::memcpy(destination.rotations_rolls, source.rotations_rolls, locations_xs_bytes);
            std::memcpy(destination.teams, source.teams, teams_bytes);
            std::memcpy(destination.healths, source.healths, healths_bytes);
            std::memcpy(destination.laser_damages, source.laser_damages, entity_indices_bytes);
        }
        ml::native_soa::free(data_, allocation_alignment);
        data_ = new_data;
        capacity_ = new_capacity;
    }
};

struct LevelTurretSpawnEventsSingleConstView : ml::native_soa::CompactViewState<true> {
    using Base = ml::native_soa::CompactViewState<true>;
    using Base::Base;
    using View = LevelTurretSpawnEventsSingleView;
    using ConstView = LevelTurretSpawnEventsSingleConstView;
    LevelTurretSpawnEventsSingleConstView() = default;
    LevelTurretSpawnEventsSingleConstView(LevelTurretSpawnEventsSingleView const& other);
    auto get_const_view() const -> ConstView { return *this; }
    auto get_const_view(size_type offset, size_type count) const -> ConstView {
        return slice(offset, count);
    }
    auto entity_indices() const -> std::span<std::int32_t const> {
        return {column_data<std::int32_t>(
                    LevelTurretSpawnEventsSingleLayout::EntityIndices.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto view_locations() const -> ml::native_soa::Vector3ConstView<float> {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        auto const first{LevelTurretSpawnEventsSingleLayout::LocationsXs.offset(blocks)};
        auto const stride{LevelTurretSpawnEventsSingleLayout::LocationsYs.offset(blocks) - first};
        return {column_data_unchecked<float>(first), stride, count_};
    }
    auto view_rotations() const -> Rotators3fConstView {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        return Rotators3fConstView{
            {column_data_unchecked<float>(
                 LevelTurretSpawnEventsSingleLayout::RotationsPitches.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<float>(
                 LevelTurretSpawnEventsSingleLayout::RotationsYaws.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<float>(
                 LevelTurretSpawnEventsSingleLayout::RotationsRolls.offset(blocks)),
             static_cast<std::size_t>(count_)}};
    }
    auto teams() const -> std::span<Team const> {
        return {
            column_data<Team>(LevelTurretSpawnEventsSingleLayout::Teams.offset(capacity_blocks())),
            static_cast<std::size_t>(count_)};
    }
    auto healths() const -> std::span<Health const> {
        return {column_data<Health>(
                    LevelTurretSpawnEventsSingleLayout::Healths.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto laser_damages() const -> std::span<std::int32_t const> {
        return {column_data<std::int32_t>(
                    LevelTurretSpawnEventsSingleLayout::LaserDamages.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto columns() const -> LevelTurretSpawnEventsConstView {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        return LevelTurretSpawnEventsConstView{
            {column_data_unchecked<std::int32_t>(
                 LevelTurretSpawnEventsSingleLayout::EntityIndices.offset(blocks)),
             static_cast<std::size_t>(count_)},
            Vectors3fConstView{{column_data_unchecked<float>(
                                    LevelTurretSpawnEventsSingleLayout::LocationsXs.offset(blocks)),
                                static_cast<std::size_t>(count_)},
                               {column_data_unchecked<float>(
                                    LevelTurretSpawnEventsSingleLayout::LocationsYs.offset(blocks)),
                                static_cast<std::size_t>(count_)},
                               {column_data_unchecked<float>(
                                    LevelTurretSpawnEventsSingleLayout::LocationsZs.offset(blocks)),
                                static_cast<std::size_t>(count_)}},
            Rotators3fConstView{
                {column_data_unchecked<float>(
                     LevelTurretSpawnEventsSingleLayout::RotationsPitches.offset(blocks)),
                 static_cast<std::size_t>(count_)},
                {column_data_unchecked<float>(
                     LevelTurretSpawnEventsSingleLayout::RotationsYaws.offset(blocks)),
                 static_cast<std::size_t>(count_)},
                {column_data_unchecked<float>(
                     LevelTurretSpawnEventsSingleLayout::RotationsRolls.offset(blocks)),
                 static_cast<std::size_t>(count_)}},
            {column_data_unchecked<Team>(LevelTurretSpawnEventsSingleLayout::Teams.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<Health>(
                 LevelTurretSpawnEventsSingleLayout::Healths.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<std::int32_t>(
                 LevelTurretSpawnEventsSingleLayout::LaserDamages.offset(blocks)),
             static_cast<std::size_t>(count_)}};
    }
    template <typename Func>
    void each_column(Func&& func) const {
        columns().each_column(std::forward<Func>(func));
    }
};
static_assert(sizeof(LevelTurretSpawnEventsSingleConstView) == 16);
static_assert(std::is_trivially_copyable_v<LevelTurretSpawnEventsSingleConstView>);
struct LevelTurretSpawnEventsSingleView : ml::native_soa::CompactViewState<false> {
    using Base = ml::native_soa::CompactViewState<false>;
    using Base::Base;
    using View = LevelTurretSpawnEventsSingleView;
    using ConstView = LevelTurretSpawnEventsSingleConstView;
    LevelTurretSpawnEventsSingleView() = default;
    auto get_const_view() const -> ConstView { return *this; }
    auto get_const_view(size_type offset, size_type count) const -> ConstView {
        return slice(offset, count);
    }
    auto entity_indices() const -> std::span<std::int32_t> {
        return {column_data<std::int32_t>(
                    LevelTurretSpawnEventsSingleLayout::EntityIndices.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto view_locations() const -> ml::native_soa::Vector3View<float> {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        auto const first{LevelTurretSpawnEventsSingleLayout::LocationsXs.offset(blocks)};
        auto const stride{LevelTurretSpawnEventsSingleLayout::LocationsYs.offset(blocks) - first};
        return {column_data_unchecked<float>(first), stride, count_};
    }
    auto view_rotations() const -> Rotators3fView {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        return Rotators3fView{
            {column_data_unchecked<float>(
                 LevelTurretSpawnEventsSingleLayout::RotationsPitches.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<float>(
                 LevelTurretSpawnEventsSingleLayout::RotationsYaws.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<float>(
                 LevelTurretSpawnEventsSingleLayout::RotationsRolls.offset(blocks)),
             static_cast<std::size_t>(count_)}};
    }
    auto teams() const -> std::span<Team> {
        return {
            column_data<Team>(LevelTurretSpawnEventsSingleLayout::Teams.offset(capacity_blocks())),
            static_cast<std::size_t>(count_)};
    }
    auto healths() const -> std::span<Health> {
        return {column_data<Health>(
                    LevelTurretSpawnEventsSingleLayout::Healths.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto laser_damages() const -> std::span<std::int32_t> {
        return {column_data<std::int32_t>(
                    LevelTurretSpawnEventsSingleLayout::LaserDamages.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto columns() const -> LevelTurretSpawnEventsView {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        return LevelTurretSpawnEventsView{
            {column_data_unchecked<std::int32_t>(
                 LevelTurretSpawnEventsSingleLayout::EntityIndices.offset(blocks)),
             static_cast<std::size_t>(count_)},
            Vectors3fView{{column_data_unchecked<float>(
                               LevelTurretSpawnEventsSingleLayout::LocationsXs.offset(blocks)),
                           static_cast<std::size_t>(count_)},
                          {column_data_unchecked<float>(
                               LevelTurretSpawnEventsSingleLayout::LocationsYs.offset(blocks)),
                           static_cast<std::size_t>(count_)},
                          {column_data_unchecked<float>(
                               LevelTurretSpawnEventsSingleLayout::LocationsZs.offset(blocks)),
                           static_cast<std::size_t>(count_)}},
            Rotators3fView{
                {column_data_unchecked<float>(
                     LevelTurretSpawnEventsSingleLayout::RotationsPitches.offset(blocks)),
                 static_cast<std::size_t>(count_)},
                {column_data_unchecked<float>(
                     LevelTurretSpawnEventsSingleLayout::RotationsYaws.offset(blocks)),
                 static_cast<std::size_t>(count_)},
                {column_data_unchecked<float>(
                     LevelTurretSpawnEventsSingleLayout::RotationsRolls.offset(blocks)),
                 static_cast<std::size_t>(count_)}},
            {column_data_unchecked<Team>(LevelTurretSpawnEventsSingleLayout::Teams.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<Health>(
                 LevelTurretSpawnEventsSingleLayout::Healths.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<std::int32_t>(
                 LevelTurretSpawnEventsSingleLayout::LaserDamages.offset(blocks)),
             static_cast<std::size_t>(count_)}};
    }
    template <typename Func>
    void each_column(Func&& func) const {
        columns().each_column(std::forward<Func>(func));
    }
};
static_assert(sizeof(LevelTurretSpawnEventsSingleView) == 16);
static_assert(std::is_trivially_copyable_v<LevelTurretSpawnEventsSingleView>);
inline LevelTurretSpawnEventsSingleConstView::LevelTurretSpawnEventsSingleConstView(
    LevelTurretSpawnEventsSingleView const& other)
    : Base{other} {}
struct SingleAllocationLevelTurretSpawnEvents : SingleAllocationLevelTurretSpawnEventsStorage {
    SingleAllocationLevelTurretSpawnEvents() noexcept = default;
    SingleAllocationLevelTurretSpawnEvents(SingleAllocationLevelTurretSpawnEvents const&) = delete;
    auto operator=(SingleAllocationLevelTurretSpawnEvents const&)
        -> SingleAllocationLevelTurretSpawnEvents& = delete;
    SingleAllocationLevelTurretSpawnEvents(SingleAllocationLevelTurretSpawnEvents&&) noexcept =
        default;
    auto operator=(SingleAllocationLevelTurretSpawnEvents&&) noexcept
        -> SingleAllocationLevelTurretSpawnEvents& = default;
    auto get_view() & -> View { return {this, 0, num()}; }
    auto get_view(size_type offset, size_type count) & -> View { return {this, offset, count}; }
    auto slice(size_type offset, size_type count) & -> View { return get_view(offset, count); }
    auto left(size_type count) & -> View { return get_view().left(count); }
    auto right(size_type count) & -> View { return get_view().right(count); }
    auto get_view() && -> View = delete;
    auto get_view(size_type, size_type) && -> View = delete;
    auto slice(size_type, size_type) && -> View = delete;
    auto left(size_type) && -> View = delete;
    auto right(size_type) && -> View = delete;
    auto get_view() const& -> ConstView { return {this, 0, num()}; }
    auto get_view(size_type offset, size_type count) const& -> ConstView {
        return {this, offset, count};
    }
    auto slice(size_type offset, size_type count) const& -> ConstView {
        return get_view(offset, count);
    }
    auto left(size_type count) const& -> ConstView { return get_view().left(count); }
    auto right(size_type count) const& -> ConstView { return get_view().right(count); }
    auto get_view() const&& -> ConstView = delete;
    auto get_view(size_type, size_type) const&& -> ConstView = delete;
    auto slice(size_type, size_type) const&& -> ConstView = delete;
    auto left(size_type) const&& -> ConstView = delete;
    auto right(size_type) const&& -> ConstView = delete;
    auto get_const_view() const& -> ConstView { return get_view(); }
    auto get_const_view(size_type offset, size_type count) const& -> ConstView {
        return get_view(offset, count);
    }
    auto get_const_view() const&& -> ConstView = delete;
    auto get_const_view(size_type, size_type) const&& -> ConstView = delete;
};

struct LevelSpinnerSpawnEventsView;
struct LevelSpinnerSpawnEventsConstView;
struct LevelSpinnerSpawnEventsConstView {
    using View = LevelSpinnerSpawnEventsView;
    using ConstView = LevelSpinnerSpawnEventsConstView;
    using size_type = std::int32_t;
    std::span<std::int32_t const> entity_indices;
    Vectors3fConstView locations;
    std::span<float const> yaws;
    std::span<std::int32_t const> initial_fire_point_indices;
    auto num() const noexcept -> size_type { return static_cast<size_type>(entity_indices.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(entity_indices);
        fn(locations.xs_span());
        fn(locations.ys_span());
        fn(locations.zs_span());
        fn(yaws);
        fn(initial_fire_point_indices);
    }
    void validate_array_sizes() const {
        auto const count{num()};
        each_column([count](auto column) {
            ml::native_soa::require(column.size() == static_cast<std::size_t>(count));
        });
    }
    auto slice(size_type const offset, size_type const count) const
        -> LevelSpinnerSpawnEventsConstView {
        ml::native_soa::require(offset >= 0 && count >= 0 && offset <= num() &&
                                count <= num() - offset);
        return {
            entity_indices.subspan(static_cast<std::size_t>(offset),
                                   static_cast<std::size_t>(count)),
            locations.slice(offset, count),
            yaws.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            initial_fire_point_indices.subspan(static_cast<std::size_t>(offset),
                                               static_cast<std::size_t>(count)),
        };
    }
    auto get_view() const -> LevelSpinnerSpawnEventsConstView { return *this; }
    auto get_view(size_type const offset, size_type const count) const
        -> LevelSpinnerSpawnEventsConstView {
        return slice(offset, count);
    }
    auto get_const_view() const -> ConstView {
        return {
            entity_indices,
            locations.get_const_view(),
            yaws,
            initial_fire_point_indices,
        };
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto left(size_type const count) const -> LevelSpinnerSpawnEventsConstView {
        return slice(0, count);
    }
    auto right(size_type const count) const -> LevelSpinnerSpawnEventsConstView {
        return slice(num() - count, count);
    }
};
struct LevelSpinnerSpawnEventsView {
    using View = LevelSpinnerSpawnEventsView;
    using ConstView = LevelSpinnerSpawnEventsConstView;
    using size_type = std::int32_t;
    std::span<std::int32_t> entity_indices;
    Vectors3fView locations;
    std::span<float> yaws;
    std::span<std::int32_t> initial_fire_point_indices;
    auto num() const noexcept -> size_type { return static_cast<size_type>(entity_indices.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(entity_indices);
        fn(locations.xs_span());
        fn(locations.ys_span());
        fn(locations.zs_span());
        fn(yaws);
        fn(initial_fire_point_indices);
    }
    void validate_array_sizes() const {
        auto const count{num()};
        each_column([count](auto column) {
            ml::native_soa::require(column.size() == static_cast<std::size_t>(count));
        });
    }
    auto slice(size_type const offset, size_type const count) const -> LevelSpinnerSpawnEventsView {
        ml::native_soa::require(offset >= 0 && count >= 0 && offset <= num() &&
                                count <= num() - offset);
        return {
            entity_indices.subspan(static_cast<std::size_t>(offset),
                                   static_cast<std::size_t>(count)),
            locations.slice(offset, count),
            yaws.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            initial_fire_point_indices.subspan(static_cast<std::size_t>(offset),
                                               static_cast<std::size_t>(count)),
        };
    }
    auto get_view() const -> LevelSpinnerSpawnEventsView { return *this; }
    auto get_view(size_type const offset, size_type const count) const
        -> LevelSpinnerSpawnEventsView {
        return slice(offset, count);
    }
    auto get_const_view() const -> ConstView {
        return {
            entity_indices,
            locations.get_const_view(),
            yaws,
            initial_fire_point_indices,
        };
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto left(size_type const count) const -> LevelSpinnerSpawnEventsView {
        return slice(0, count);
    }
    auto right(size_type const count) const -> LevelSpinnerSpawnEventsView {
        return slice(num() - count, count);
    }
    void set(size_type const index,
             std::int32_t const new_entity_indices,
             float const new_locations_xs,
             float const new_locations_ys,
             float const new_locations_zs,
             float const new_yaws,
             std::int32_t const new_initial_fire_point_indices) const {
        ml::native_soa::require(index >= 0 && index < num());
        entity_indices[static_cast<std::size_t>(index)] = new_entity_indices;
        locations.xs[static_cast<std::size_t>(index)] = new_locations_xs;
        locations.ys[static_cast<std::size_t>(index)] = new_locations_ys;
        locations.zs[static_cast<std::size_t>(index)] = new_locations_zs;
        yaws[static_cast<std::size_t>(index)] = new_yaws;
        initial_fire_point_indices[static_cast<std::size_t>(index)] =
            new_initial_fire_point_indices;
    }
};
struct LevelSpinnerSpawnEvents {
    using View = LevelSpinnerSpawnEventsView;
    using ConstView = LevelSpinnerSpawnEventsConstView;
    using size_type = std::int32_t;
    ml::native_soa::Vector<std::int32_t> entity_indices;
    Vectors3f locations;
    ml::native_soa::Vector<float> yaws;
    ml::native_soa::Vector<std::int32_t> initial_fire_point_indices;
    auto num() const noexcept -> size_type { return static_cast<size_type>(entity_indices.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) {
        fn(entity_indices);
        fn(locations.xs);
        fn(locations.ys);
        fn(locations.zs);
        fn(yaws);
        fn(initial_fire_point_indices);
    }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(entity_indices);
        fn(locations.xs);
        fn(locations.ys);
        fn(locations.zs);
        fn(yaws);
        fn(initial_fire_point_indices);
    }
    void validate_array_sizes() const { get_const_view().validate_array_sizes(); }
    void reserve(size_type const count) {
        ml::native_soa::require(count >= 0);
        entity_indices.reserve(static_cast<std::size_t>(count));
        locations.xs.reserve(static_cast<std::size_t>(count));
        locations.ys.reserve(static_cast<std::size_t>(count));
        locations.zs.reserve(static_cast<std::size_t>(count));
        yaws.reserve(static_cast<std::size_t>(count));
        initial_fire_point_indices.reserve(static_cast<std::size_t>(count));
    }
    void reset() noexcept {
        entity_indices.clear();
        locations.xs.clear();
        locations.ys.clear();
        locations.zs.clear();
        yaws.clear();
        initial_fire_point_indices.clear();
    }
    void set_num(size_type const count) {
        ml::native_soa::require(count >= 0);
        auto const size{static_cast<std::size_t>(count)};
        entity_indices.resize(size);
        locations.xs.resize(size);
        locations.ys.resize(size);
        locations.zs.resize(size);
        yaws.resize(size);
        initial_fire_point_indices.resize(size);
    }
    void add_uninitialised(size_type const count) {
        auto const old_num{num()};
        ml::native_soa::require(count >= 0 &&
                                count <= std::numeric_limits<size_type>::max() - old_num);
        set_num(old_num + count);
    }
    void add_defaulted(size_type const count) { add_uninitialised(count); }
    void remove_at_swap(size_type const index, size_type const count) {
        auto const old_num{num()};
        ml::native_soa::require(index >= 0 && index <= old_num && count >= 0 &&
                                count <= old_num - index);
        auto const moved{std::min(count, old_num - index - count)};
        auto const source{old_num - moved};
        for (size_type i{}; i < moved; ++i) {
            entity_indices[index + i] = entity_indices[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            locations.xs[index + i] = locations.xs[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            locations.ys[index + i] = locations.ys[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            locations.zs[index + i] = locations.zs[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            yaws[index + i] = yaws[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            initial_fire_point_indices[index + i] = initial_fire_point_indices[source + i];
        }
        set_num(old_num - count);
    }
    void set(size_type const index,
             std::int32_t const new_entity_indices,
             float const new_locations_xs,
             float const new_locations_ys,
             float const new_locations_zs,
             float const new_yaws,
             std::int32_t const new_initial_fire_point_indices) {
        get_view().set(index,
                       new_entity_indices,
                       new_locations_xs,
                       new_locations_ys,
                       new_locations_zs,
                       new_yaws,
                       new_initial_fire_point_indices);
    }
    auto add(std::int32_t const new_entity_indices,
             float const new_locations_xs,
             float const new_locations_ys,
             float const new_locations_zs,
             float const new_yaws,
             std::int32_t const new_initial_fire_point_indices) -> size_type {
        auto const index{num()};
        add_defaulted(1);
        set(index,
            new_entity_indices,
            new_locations_xs,
            new_locations_ys,
            new_locations_zs,
            new_yaws,
            new_initial_fire_point_indices);
        return index;
    }
    void append_from(ConstView source) {
        auto const count{source.num()};
        ml::native_soa::require(count <= std::numeric_limits<size_type>::max() - num());
        source.validate_array_sizes();
        if (count == 0) {
            return;
        }
        {
            auto const address{ml::address_cast(source.entity_indices.data())};
            auto const begin{ml::address_cast(entity_indices.data())};
            ml::native_soa::require(address < begin || address >= begin + entity_indices.size() *
                                                                              sizeof(std::int32_t));
        }
        {
            auto const address{ml::address_cast(source.locations.xs)};
            auto const begin{ml::address_cast(locations.xs.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + locations.xs.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.locations.ys)};
            auto const begin{ml::address_cast(locations.ys.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + locations.ys.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.locations.zs)};
            auto const begin{ml::address_cast(locations.zs.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + locations.zs.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.yaws.data())};
            auto const begin{ml::address_cast(yaws.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + yaws.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.initial_fire_point_indices.data())};
            auto const begin{ml::address_cast(initial_fire_point_indices.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + initial_fire_point_indices.size() *
                                                           sizeof(std::int32_t));
        }
        entity_indices.insert(entity_indices.end(),
                              source.entity_indices.data(),
                              source.entity_indices.data() + count);
        locations.xs.insert(locations.xs.end(), source.locations.xs, source.locations.xs + count);
        locations.ys.insert(locations.ys.end(), source.locations.ys, source.locations.ys + count);
        locations.zs.insert(locations.zs.end(), source.locations.zs, source.locations.zs + count);
        yaws.insert(yaws.end(), source.yaws.data(), source.yaws.data() + count);
        initial_fire_point_indices.insert(initial_fire_point_indices.end(),
                                          source.initial_fire_point_indices.data(),
                                          source.initial_fire_point_indices.data() + count);
    }
    auto get_view() -> View {
        return {
            entity_indices,
            locations.get_view(),
            yaws,
            initial_fire_point_indices,
        };
    }
    auto get_view() const -> ConstView {
        return {
            entity_indices,
            locations.get_view(),
            yaws,
            initial_fire_point_indices,
        };
    }
    auto get_const_view() const -> ConstView { return get_view(); }
    auto get_view(size_type const offset, size_type const count) -> View {
        return get_view().slice(offset, count);
    }
    auto get_view(size_type const offset, size_type const count) const -> ConstView {
        return get_view().slice(offset, count);
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto slice(size_type const offset, size_type const count) -> View {
        return get_view(offset, count);
    }
    auto slice(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view(offset, count);
    }
    auto left(size_type const count) -> View { return slice(0, count); }
    auto right(size_type const count) -> View { return slice(num() - count, count); }
    auto left(size_type const count) const -> ConstView { return slice(0, count); }
    auto right(size_type const count) const -> ConstView { return slice(num() - count, count); }
    template <typename Other>
    void copy_element(size_type const dst_index, Other const& other, size_type const src_index) {
        entity_indices[static_cast<std::size_t>(dst_index)] =
            other.entity_indices[static_cast<std::size_t>(src_index)];
        locations.copy_element(dst_index, other.locations, src_index);
        yaws[static_cast<std::size_t>(dst_index)] = other.yaws[static_cast<std::size_t>(src_index)];
        initial_fire_point_indices[static_cast<std::size_t>(dst_index)] =
            other.initial_fire_point_indices[static_cast<std::size_t>(src_index)];
    }
    template <typename Other>
    void copy_elements(size_type const dst_index,
                       Other const& other,
                       size_type const src_index,
                       size_type const count) {
        for (size_type i{}; i < count; ++i) {
            copy_element(dst_index + i, other, src_index + i);
        }
    }
    void apply_permutation(std::span<std::int32_t> const indices) {
        validate_array_sizes();
        ml::native_soa::require(indices.size() == static_cast<std::size_t>(num()));
        each_column([indices](auto& column) { ml::apply_permutation(std::span{column}, indices); });
    }
    template <typename Compare>
    void sort(Compare&& compare, std::span<std::int32_t> const scratch_indices) {
        validate_array_sizes();
        ml::native_soa::require(scratch_indices.size() == static_cast<std::size_t>(num()));
        for (size_type i{}; i < num(); ++i) {
            scratch_indices[static_cast<std::size_t>(i)] = i;
        }
        std::sort(scratch_indices.begin(),
                  scratch_indices.end(),
                  [this, &compare](size_type const lhs, size_type const rhs) {
                      return compare(*this, lhs, rhs);
                  });
        apply_permutation(scratch_indices);
    }
};

struct LevelSpinnerSpawnEventsSingleView;
struct LevelSpinnerSpawnEventsSingleConstView;
struct LevelSpinnerSpawnEventsSingleLayout {
    using size_type = std::int32_t;
    using byte_size_type = std::size_t;

    inline static constexpr byte_size_type max_allocation_size{
        std::numeric_limits<byte_size_type>::max()};
    inline static constexpr size_type capacity_granularity{64};
    inline static constexpr byte_size_type column_gap{192};

    template <typename T>
    using ColLayout = ml::native_soa::ColumnLayout<T>;
    inline static constexpr ml::native_soa::ColumnLayoutStart LayoutStart{
        capacity_granularity, column_gap, 64};

    inline static constexpr ColLayout<std::int32_t> EntityIndices{LayoutStart};
    inline static constexpr ColLayout<float> LocationsXs{EntityIndices};
    inline static constexpr ColLayout<float> LocationsYs{LocationsXs};
    inline static constexpr ColLayout<float> LocationsZs{LocationsYs};
    inline static constexpr ColLayout<float> Yaws{LocationsZs};
    inline static constexpr ColLayout<std::int32_t> InitialFirePointIndices{Yaws};

    inline static constexpr byte_size_type allocation_alignment{ml::native_soa::maximum_alignment(
        EntityIndices, LocationsXs, LocationsYs, LocationsZs, Yaws, InitialFirePointIndices)};

    // Conservative per-block bound for checked capacity arithmetic; gaps do not scale with
    // capacity.
    inline static constexpr byte_size_type capacity_block_bound{
        ml::native_soa::layout_align(InitialFirePointIndices.block_end, allocation_alignment) +
        5 * (column_gap + allocation_alignment - 1)};
    inline static constexpr size_type max_capacity{
        ml::native_soa::maximum_capacity(capacity_block_bound)};
    static constexpr auto layout_bytes(byte_size_type blocks) noexcept -> byte_size_type {
        return blocks == 0 ? 0 : InitialFirePointIndices.data_end(blocks);
    }
  private:
    inline static constexpr auto validate_layout = []() consteval -> bool {
        static_assert(
            ml::native_soa::supported_leaf<std::int32_t>,
            "Single-allocation leaf entity_indices requires a non-cv, trivially "
            "copyable/copy-constructible/destructible, nothrow default-constructible object type.");
        static_assert(
            ml::native_soa::supported_leaf<float>,
            "Single-allocation leaf locations.xs requires a non-cv, trivially "
            "copyable/copy-constructible/destructible, nothrow default-constructible object type.");

        static_assert(
            allocation_alignment <= std::numeric_limits<std::uint32_t>::max(),
            "Single-allocation alignment must fit the allocator's 32-bit alignment argument.");
        static_assert(sizeof(std::int32_t) <=
                      (max_allocation_size - EntityIndices.block_offset) / capacity_granularity);
        static_assert(sizeof(float) <=
                      (max_allocation_size - LocationsXs.block_offset) / capacity_granularity);
        static_assert(sizeof(float) <=
                      (max_allocation_size - LocationsYs.block_offset) / capacity_granularity);
        static_assert(sizeof(float) <=
                      (max_allocation_size - LocationsZs.block_offset) / capacity_granularity);
        static_assert(sizeof(float) <=
                      (max_allocation_size - Yaws.block_offset) / capacity_granularity);
        static_assert(sizeof(std::int32_t) <=
                      (max_allocation_size - InitialFirePointIndices.block_offset) /
                          capacity_granularity);
        static_assert(5 <= (max_allocation_size -
                            ml::native_soa::layout_align(InitialFirePointIndices.block_end,
                                                         allocation_alignment)) /
                               (column_gap + allocation_alignment - 1));
        static_assert(max_capacity >= capacity_granularity);
        return true;
    };
    static_assert(validate_layout());
};

struct SingleAllocationLevelSpinnerSpawnEventsStorage
    : LevelSpinnerSpawnEventsSingleLayout
    , protected ml::native_soa::StorageState
    , ml::native_soa::StorageOperations {
    using View = LevelSpinnerSpawnEventsSingleView;
    using ConstView = LevelSpinnerSpawnEventsSingleConstView;
    using SchemaConstView = LevelSpinnerSpawnEventsConstView;
    using ml::native_soa::StorageOperations::append_from;
    auto append_from(LevelSpinnerSpawnEventsConstView const& source) -> size_type {
        source.validate_array_sizes();
        auto const count{source.num()};
        auto const first{num_};
        ml::native_soa::require((count <= max_capacity - first));
        if (count == 0) {
            return first;
        }
        auto const new_num{first + count};
        if (new_num > capacity_) {
            ml::native_soa::require(!ordinary_source_aliases_storage(source));
            reallocate(ml::native_soa::growth_capacity(new_num, capacity_, capacity_block_bound));
        }
        append_columns(source, first, count);
        num_ = new_num;
        return first;
    }
    /* **************************************** */
    // Lifetime
    /* **************************************** */
    SingleAllocationLevelSpinnerSpawnEventsStorage() noexcept = default;
    ~SingleAllocationLevelSpinnerSpawnEventsStorage() {
        ml::native_soa::free(data_, allocation_alignment);
    }
    SingleAllocationLevelSpinnerSpawnEventsStorage(
        SingleAllocationLevelSpinnerSpawnEventsStorage const&) = delete;
    auto operator=(SingleAllocationLevelSpinnerSpawnEventsStorage const&)
        -> SingleAllocationLevelSpinnerSpawnEventsStorage& = delete;
    SingleAllocationLevelSpinnerSpawnEventsStorage(
        SingleAllocationLevelSpinnerSpawnEventsStorage&& other) noexcept
        : StorageState{std::exchange(other.data_, nullptr),
                       std::exchange(other.num_, 0),
                       std::exchange(other.capacity_, 0)} {}
    auto operator=(SingleAllocationLevelSpinnerSpawnEventsStorage&& other) noexcept
        -> SingleAllocationLevelSpinnerSpawnEventsStorage& {
        if (this != &other) {
            ml::native_soa::free(data_, allocation_alignment);
            data_ = std::exchange(other.data_, nullptr);
            num_ = std::exchange(other.num_, 0);
            capacity_ = std::exchange(other.capacity_, 0);
        }
        return *this;
    }
  protected:
    template <typename Byte>
    struct DataPointers {
        template <typename T>
        using Element = std::conditional_t<std::is_const_v<Byte>, T const, T>;
        Element<std::int32_t>* entity_indices{};
        Element<float>* locations_xs{};
        Element<float>* locations_ys{};
        Element<float>* locations_zs{};
        Element<float>* yaws{};
        Element<std::int32_t>* initial_fire_point_indices{};
        auto operator+(size_type const offset) const noexcept -> DataPointers {
            if (entity_indices == nullptr) {
                return {};
            }
            return {entity_indices + offset,
                    locations_xs + offset,
                    locations_ys + offset,
                    locations_zs + offset,
                    yaws + offset,
                    initial_fire_point_indices + offset};
        }
    };
    template <typename Self>
    auto get_data(this Self& self) noexcept {
        using Byte = std::conditional_t<std::is_const_v<Self>, std::byte const, std::byte>;
        if (self.data_ == nullptr) {
            return DataPointers<Byte>{};
        }
        return make_data_unchecked(static_cast<Byte*>(self.data_), self.capacity_blocks());
    }
    template <typename Self>
    auto get_data(this Self& self, size_type const offset) noexcept {
        return self.get_data() + offset;
    }
  private:
    friend struct ml::native_soa::StorageOperations;
    /* **************************************** */
    // Column pointers
    /* **************************************** */
    template <typename Byte>
    static auto make_data_unchecked(Byte* const data, byte_size_type const blocks) noexcept
        -> DataPointers<Byte> {
        auto const pointer_at = [data](auto const& column, byte_size_type offset) noexcept {
            using Column = std::remove_cvref_t<decltype(column)>;
            using Pointer = std::conditional_t<std::is_const_v<Byte>,
                                               typename Column::const_pointer,
                                               typename Column::pointer>;
            return std::launder(reinterpret_cast<Pointer>(data + offset));
        };
        auto const entity_indices_offset{byte_size_type{}};
        auto const locations_xs_offset{ml::native_soa::layout_align(
            entity_indices_offset + blocks * capacity_granularity * sizeof(std::int32_t) +
                column_gap,
            LocationsXs.alignment)};
        auto const locations_ys_offset{ml::native_soa::layout_align(
            locations_xs_offset + blocks * capacity_granularity * sizeof(float) + column_gap,
            LocationsYs.alignment)};
        auto const locations_zs_offset{ml::native_soa::layout_align(
            locations_ys_offset + blocks * capacity_granularity * sizeof(float) + column_gap,
            LocationsZs.alignment)};
        auto const yaws_offset{ml::native_soa::layout_align(
            locations_zs_offset + blocks * capacity_granularity * sizeof(float) + column_gap,
            Yaws.alignment)};
        auto const initial_fire_point_indices_offset{ml::native_soa::layout_align(
            yaws_offset + blocks * capacity_granularity * sizeof(float) + column_gap,
            InitialFirePointIndices.alignment)};
        return {pointer_at(EntityIndices, entity_indices_offset),
                pointer_at(LocationsXs, locations_xs_offset),
                pointer_at(LocationsYs, locations_ys_offset),
                pointer_at(LocationsZs, locations_zs_offset),
                pointer_at(Yaws, yaws_offset),
                pointer_at(InitialFirePointIndices, initial_fire_point_indices_offset)};
    }
    auto capacity_blocks() const noexcept -> byte_size_type {
        return static_cast<byte_size_type>(capacity_ / capacity_granularity);
    }

    /* **************************************** */
    // Typed mutations and growth
    /* **************************************** */
    void default_construct_columns(size_type const first, size_type const count) {
        auto const columns{make_data_unchecked(data_, capacity_blocks()) + first};
        std::uninitialized_value_construct_n<std::int32_t*>(columns.entity_indices, count);
        std::uninitialized_value_construct_n<float*>(columns.locations_xs, count);
        std::uninitialized_value_construct_n<float*>(columns.locations_ys, count);
        std::uninitialized_value_construct_n<float*>(columns.locations_zs, count);
        std::uninitialized_value_construct_n<float*>(columns.yaws, count);
        std::uninitialized_value_construct_n<std::int32_t*>(columns.initial_fire_point_indices,
                                                            count);
    }
    void swap_remove_columns(size_type const index,
                             size_type const source,
                             size_type const move_count) {
        copy_columns(get_data(), index, source, move_count);
    }
    static void copy_columns(DataPointers<std::byte> const& columns,
                             size_type index,
                             size_type source,
                             size_type move_count) {
        auto const elements_to_move{static_cast<byte_size_type>(move_count)};
        auto const entity_indices_bytes{elements_to_move * sizeof(std::int32_t)};
        auto const locations_xs_bytes{elements_to_move * sizeof(float)};
        std::memcpy(
            columns.entity_indices + index, columns.entity_indices + source, entity_indices_bytes);
        std::memcpy(
            columns.locations_xs + index, columns.locations_xs + source, locations_xs_bytes);
        std::memcpy(
            columns.locations_ys + index, columns.locations_ys + source, locations_xs_bytes);
        std::memcpy(
            columns.locations_zs + index, columns.locations_zs + source, locations_xs_bytes);
        std::memcpy(columns.yaws + index, columns.yaws + source, locations_xs_bytes);
        std::memcpy(columns.initial_fire_point_indices + index,
                    columns.initial_fire_point_indices + source,
                    entity_indices_bytes);
    }
    void swap_remove_indices(std::span<size_type const> indices) {
        auto const columns{get_data()};
        ml::soa_storage_detail::for_each_removal_run(
            num_,
            indices,
            ml::native_soa::require,
            [&](size_type index, size_type source, size_type count) {
                copy_columns(columns, index, source, count);
            });
    }
    auto ordinary_source_aliases_storage(
        LevelSpinnerSpawnEventsConstView const& source) const noexcept -> bool {
        if (data_ == nullptr) {
            return false;
        }
        auto const allocation_begin{reinterpret_cast<std::uintptr_t>(data_)};
        auto const allocation_end{allocation_begin + layout_bytes(capacity_blocks())};
        auto const aliases = [allocation_begin, allocation_end](auto const* pointer) noexcept {
            auto const address{reinterpret_cast<std::uintptr_t>(pointer)};
            return address >= allocation_begin && address < allocation_end;
        };
        return aliases(source.entity_indices.data()) || aliases(source.locations.xs) ||
               aliases(source.locations.ys) || aliases(source.locations.zs) ||
               aliases(source.yaws.data()) || aliases(source.initial_fire_point_indices.data());
    }
    template <typename Columns>
    void append_columns(Columns const& source, size_type first, size_type count) {
        auto const destination{get_data(first)};
        auto const elements_to_copy{static_cast<byte_size_type>(count)};
        auto const entity_indices_bytes{elements_to_copy * sizeof(std::int32_t)};
        auto const locations_xs_bytes{elements_to_copy * sizeof(float)};
        std::memcpy(destination.entity_indices, source.entity_indices.data(), entity_indices_bytes);
        std::memcpy(destination.locations_xs, source.locations.xs, locations_xs_bytes);
        std::memcpy(destination.locations_ys, source.locations.ys, locations_xs_bytes);
        std::memcpy(destination.locations_zs, source.locations.zs, locations_xs_bytes);
        std::memcpy(destination.yaws, source.yaws.data(), locations_xs_bytes);
        std::memcpy(destination.initial_fire_point_indices,
                    source.initial_fire_point_indices.data(),
                    entity_indices_bytes);
    }
    void reallocate(size_type const new_capacity) {
        auto* const new_data{ml::native_soa::allocate(
            layout_bytes(static_cast<byte_size_type>(new_capacity / capacity_granularity)),
            static_cast<std::uint32_t>(allocation_alignment))};
        if (num_ > 0) {
            auto const old_blocks{capacity_blocks()};
            auto const new_blocks{static_cast<byte_size_type>(new_capacity / capacity_granularity)};
            auto const source{
                make_data_unchecked(static_cast<std::byte const*>(data_), old_blocks)};
            auto const destination{make_data_unchecked(new_data, new_blocks)};
            auto const live_count{static_cast<byte_size_type>(num_)};
            auto const entity_indices_bytes{live_count * sizeof(std::int32_t)};
            auto const locations_xs_bytes{live_count * sizeof(float)};
            std::memcpy(destination.entity_indices, source.entity_indices, entity_indices_bytes);
            std::memcpy(destination.locations_xs, source.locations_xs, locations_xs_bytes);
            std::memcpy(destination.locations_ys, source.locations_ys, locations_xs_bytes);
            std::memcpy(destination.locations_zs, source.locations_zs, locations_xs_bytes);
            std::memcpy(destination.yaws, source.yaws, locations_xs_bytes);
            std::memcpy(destination.initial_fire_point_indices,
                        source.initial_fire_point_indices,
                        entity_indices_bytes);
        }
        ml::native_soa::free(data_, allocation_alignment);
        data_ = new_data;
        capacity_ = new_capacity;
    }
};

struct LevelSpinnerSpawnEventsSingleConstView : ml::native_soa::CompactViewState<true> {
    using Base = ml::native_soa::CompactViewState<true>;
    using Base::Base;
    using View = LevelSpinnerSpawnEventsSingleView;
    using ConstView = LevelSpinnerSpawnEventsSingleConstView;
    LevelSpinnerSpawnEventsSingleConstView() = default;
    LevelSpinnerSpawnEventsSingleConstView(LevelSpinnerSpawnEventsSingleView const& other);
    auto get_const_view() const -> ConstView { return *this; }
    auto get_const_view(size_type offset, size_type count) const -> ConstView {
        return slice(offset, count);
    }
    auto entity_indices() const -> std::span<std::int32_t const> {
        return {column_data<std::int32_t>(
                    LevelSpinnerSpawnEventsSingleLayout::EntityIndices.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto view_locations() const -> ml::native_soa::Vector3ConstView<float> {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        auto const first{LevelSpinnerSpawnEventsSingleLayout::LocationsXs.offset(blocks)};
        auto const stride{LevelSpinnerSpawnEventsSingleLayout::LocationsYs.offset(blocks) - first};
        return {column_data_unchecked<float>(first), stride, count_};
    }
    auto yaws() const -> std::span<float const> {
        return {
            column_data<float>(LevelSpinnerSpawnEventsSingleLayout::Yaws.offset(capacity_blocks())),
            static_cast<std::size_t>(count_)};
    }
    auto initial_fire_point_indices() const -> std::span<std::int32_t const> {
        return {column_data<std::int32_t>(
                    LevelSpinnerSpawnEventsSingleLayout::InitialFirePointIndices.offset(
                        capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto columns() const -> LevelSpinnerSpawnEventsConstView {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        return LevelSpinnerSpawnEventsConstView{
            {column_data_unchecked<std::int32_t>(
                 LevelSpinnerSpawnEventsSingleLayout::EntityIndices.offset(blocks)),
             static_cast<std::size_t>(count_)},
            Vectors3fConstView{
                {column_data_unchecked<float>(
                     LevelSpinnerSpawnEventsSingleLayout::LocationsXs.offset(blocks)),
                 static_cast<std::size_t>(count_)},
                {column_data_unchecked<float>(
                     LevelSpinnerSpawnEventsSingleLayout::LocationsYs.offset(blocks)),
                 static_cast<std::size_t>(count_)},
                {column_data_unchecked<float>(
                     LevelSpinnerSpawnEventsSingleLayout::LocationsZs.offset(blocks)),
                 static_cast<std::size_t>(count_)}},
            {column_data_unchecked<float>(LevelSpinnerSpawnEventsSingleLayout::Yaws.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<std::int32_t>(
                 LevelSpinnerSpawnEventsSingleLayout::InitialFirePointIndices.offset(blocks)),
             static_cast<std::size_t>(count_)}};
    }
    template <typename Func>
    void each_column(Func&& func) const {
        columns().each_column(std::forward<Func>(func));
    }
};
static_assert(sizeof(LevelSpinnerSpawnEventsSingleConstView) == 16);
static_assert(std::is_trivially_copyable_v<LevelSpinnerSpawnEventsSingleConstView>);
struct LevelSpinnerSpawnEventsSingleView : ml::native_soa::CompactViewState<false> {
    using Base = ml::native_soa::CompactViewState<false>;
    using Base::Base;
    using View = LevelSpinnerSpawnEventsSingleView;
    using ConstView = LevelSpinnerSpawnEventsSingleConstView;
    LevelSpinnerSpawnEventsSingleView() = default;
    auto get_const_view() const -> ConstView { return *this; }
    auto get_const_view(size_type offset, size_type count) const -> ConstView {
        return slice(offset, count);
    }
    auto entity_indices() const -> std::span<std::int32_t> {
        return {column_data<std::int32_t>(
                    LevelSpinnerSpawnEventsSingleLayout::EntityIndices.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto view_locations() const -> ml::native_soa::Vector3View<float> {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        auto const first{LevelSpinnerSpawnEventsSingleLayout::LocationsXs.offset(blocks)};
        auto const stride{LevelSpinnerSpawnEventsSingleLayout::LocationsYs.offset(blocks) - first};
        return {column_data_unchecked<float>(first), stride, count_};
    }
    auto yaws() const -> std::span<float> {
        return {
            column_data<float>(LevelSpinnerSpawnEventsSingleLayout::Yaws.offset(capacity_blocks())),
            static_cast<std::size_t>(count_)};
    }
    auto initial_fire_point_indices() const -> std::span<std::int32_t> {
        return {column_data<std::int32_t>(
                    LevelSpinnerSpawnEventsSingleLayout::InitialFirePointIndices.offset(
                        capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto columns() const -> LevelSpinnerSpawnEventsView {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        return LevelSpinnerSpawnEventsView{
            {column_data_unchecked<std::int32_t>(
                 LevelSpinnerSpawnEventsSingleLayout::EntityIndices.offset(blocks)),
             static_cast<std::size_t>(count_)},
            Vectors3fView{{column_data_unchecked<float>(
                               LevelSpinnerSpawnEventsSingleLayout::LocationsXs.offset(blocks)),
                           static_cast<std::size_t>(count_)},
                          {column_data_unchecked<float>(
                               LevelSpinnerSpawnEventsSingleLayout::LocationsYs.offset(blocks)),
                           static_cast<std::size_t>(count_)},
                          {column_data_unchecked<float>(
                               LevelSpinnerSpawnEventsSingleLayout::LocationsZs.offset(blocks)),
                           static_cast<std::size_t>(count_)}},
            {column_data_unchecked<float>(LevelSpinnerSpawnEventsSingleLayout::Yaws.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<std::int32_t>(
                 LevelSpinnerSpawnEventsSingleLayout::InitialFirePointIndices.offset(blocks)),
             static_cast<std::size_t>(count_)}};
    }
    template <typename Func>
    void each_column(Func&& func) const {
        columns().each_column(std::forward<Func>(func));
    }
};
static_assert(sizeof(LevelSpinnerSpawnEventsSingleView) == 16);
static_assert(std::is_trivially_copyable_v<LevelSpinnerSpawnEventsSingleView>);
inline LevelSpinnerSpawnEventsSingleConstView::LevelSpinnerSpawnEventsSingleConstView(
    LevelSpinnerSpawnEventsSingleView const& other)
    : Base{other} {}
struct SingleAllocationLevelSpinnerSpawnEvents : SingleAllocationLevelSpinnerSpawnEventsStorage {
    SingleAllocationLevelSpinnerSpawnEvents() noexcept = default;
    SingleAllocationLevelSpinnerSpawnEvents(SingleAllocationLevelSpinnerSpawnEvents const&) =
        delete;
    auto operator=(SingleAllocationLevelSpinnerSpawnEvents const&)
        -> SingleAllocationLevelSpinnerSpawnEvents& = delete;
    SingleAllocationLevelSpinnerSpawnEvents(SingleAllocationLevelSpinnerSpawnEvents&&) noexcept =
        default;
    auto operator=(SingleAllocationLevelSpinnerSpawnEvents&&) noexcept
        -> SingleAllocationLevelSpinnerSpawnEvents& = default;
    auto get_view() & -> View { return {this, 0, num()}; }
    auto get_view(size_type offset, size_type count) & -> View { return {this, offset, count}; }
    auto slice(size_type offset, size_type count) & -> View { return get_view(offset, count); }
    auto left(size_type count) & -> View { return get_view().left(count); }
    auto right(size_type count) & -> View { return get_view().right(count); }
    auto get_view() && -> View = delete;
    auto get_view(size_type, size_type) && -> View = delete;
    auto slice(size_type, size_type) && -> View = delete;
    auto left(size_type) && -> View = delete;
    auto right(size_type) && -> View = delete;
    auto get_view() const& -> ConstView { return {this, 0, num()}; }
    auto get_view(size_type offset, size_type count) const& -> ConstView {
        return {this, offset, count};
    }
    auto slice(size_type offset, size_type count) const& -> ConstView {
        return get_view(offset, count);
    }
    auto left(size_type count) const& -> ConstView { return get_view().left(count); }
    auto right(size_type count) const& -> ConstView { return get_view().right(count); }
    auto get_view() const&& -> ConstView = delete;
    auto get_view(size_type, size_type) const&& -> ConstView = delete;
    auto slice(size_type, size_type) const&& -> ConstView = delete;
    auto left(size_type) const&& -> ConstView = delete;
    auto right(size_type) const&& -> ConstView = delete;
    auto get_const_view() const& -> ConstView { return get_view(); }
    auto get_const_view(size_type offset, size_type count) const& -> ConstView {
        return get_view(offset, count);
    }
    auto get_const_view() const&& -> ConstView = delete;
    auto get_const_view(size_type, size_type) const&& -> ConstView = delete;
};

struct LevelMissionEventGroupsView;
struct LevelMissionEventGroupsConstView;
struct LevelMissionEventGroupsConstView {
    using View = LevelMissionEventGroupsView;
    using ConstView = LevelMissionEventGroupsConstView;
    using size_type = std::int32_t;
    std::span<LevelMissionEventType const> types;
    std::span<std::int32_t const> offsets;
    std::span<LevelEventCount const> counts;
    auto num() const noexcept -> size_type { return static_cast<size_type>(types.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(types);
        fn(offsets);
        fn(counts);
    }
    void validate_array_sizes() const {
        auto const count{num()};
        each_column([count](auto column) {
            ml::native_soa::require(column.size() == static_cast<std::size_t>(count));
        });
    }
    auto slice(size_type const offset, size_type const count) const
        -> LevelMissionEventGroupsConstView {
        ml::native_soa::require(offset >= 0 && count >= 0 && offset <= num() &&
                                count <= num() - offset);
        return {
            types.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            offsets.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            counts.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
        };
    }
    auto get_view() const -> LevelMissionEventGroupsConstView { return *this; }
    auto get_view(size_type const offset, size_type const count) const
        -> LevelMissionEventGroupsConstView {
        return slice(offset, count);
    }
    auto get_const_view() const -> ConstView {
        return {
            types,
            offsets,
            counts,
        };
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto left(size_type const count) const -> LevelMissionEventGroupsConstView {
        return slice(0, count);
    }
    auto right(size_type const count) const -> LevelMissionEventGroupsConstView {
        return slice(num() - count, count);
    }
};
struct LevelMissionEventGroupsView {
    using View = LevelMissionEventGroupsView;
    using ConstView = LevelMissionEventGroupsConstView;
    using size_type = std::int32_t;
    std::span<LevelMissionEventType> types;
    std::span<std::int32_t> offsets;
    std::span<LevelEventCount> counts;
    auto num() const noexcept -> size_type { return static_cast<size_type>(types.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(types);
        fn(offsets);
        fn(counts);
    }
    void validate_array_sizes() const {
        auto const count{num()};
        each_column([count](auto column) {
            ml::native_soa::require(column.size() == static_cast<std::size_t>(count));
        });
    }
    auto slice(size_type const offset, size_type const count) const -> LevelMissionEventGroupsView {
        ml::native_soa::require(offset >= 0 && count >= 0 && offset <= num() &&
                                count <= num() - offset);
        return {
            types.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            offsets.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            counts.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
        };
    }
    auto get_view() const -> LevelMissionEventGroupsView { return *this; }
    auto get_view(size_type const offset, size_type const count) const
        -> LevelMissionEventGroupsView {
        return slice(offset, count);
    }
    auto get_const_view() const -> ConstView {
        return {
            types,
            offsets,
            counts,
        };
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto left(size_type const count) const -> LevelMissionEventGroupsView {
        return slice(0, count);
    }
    auto right(size_type const count) const -> LevelMissionEventGroupsView {
        return slice(num() - count, count);
    }
    void set(size_type const index,
             LevelMissionEventType const new_types,
             std::int32_t const new_offsets,
             LevelEventCount const new_counts) const {
        ml::native_soa::require(index >= 0 && index < num());
        types[static_cast<std::size_t>(index)] = new_types;
        offsets[static_cast<std::size_t>(index)] = new_offsets;
        counts[static_cast<std::size_t>(index)] = new_counts;
    }
};
struct LevelMissionEventGroups {
    using View = LevelMissionEventGroupsView;
    using ConstView = LevelMissionEventGroupsConstView;
    using size_type = std::int32_t;
    ml::native_soa::Vector<LevelMissionEventType> types;
    ml::native_soa::Vector<std::int32_t> offsets;
    ml::native_soa::Vector<LevelEventCount> counts;
    auto num() const noexcept -> size_type { return static_cast<size_type>(types.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) {
        fn(types);
        fn(offsets);
        fn(counts);
    }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(types);
        fn(offsets);
        fn(counts);
    }
    void validate_array_sizes() const { get_const_view().validate_array_sizes(); }
    void reserve(size_type const count) {
        ml::native_soa::require(count >= 0);
        types.reserve(static_cast<std::size_t>(count));
        offsets.reserve(static_cast<std::size_t>(count));
        counts.reserve(static_cast<std::size_t>(count));
    }
    void reset() noexcept {
        types.clear();
        offsets.clear();
        counts.clear();
    }
    void set_num(size_type const count) {
        ml::native_soa::require(count >= 0);
        auto const size{static_cast<std::size_t>(count)};
        types.resize(size);
        offsets.resize(size);
        counts.resize(size);
    }
    void add_uninitialised(size_type const count) {
        auto const old_num{num()};
        ml::native_soa::require(count >= 0 &&
                                count <= std::numeric_limits<size_type>::max() - old_num);
        set_num(old_num + count);
    }
    void add_defaulted(size_type const count) { add_uninitialised(count); }
    void remove_at_swap(size_type const index, size_type const count) {
        auto const old_num{num()};
        ml::native_soa::require(index >= 0 && index <= old_num && count >= 0 &&
                                count <= old_num - index);
        auto const moved{std::min(count, old_num - index - count)};
        auto const source{old_num - moved};
        for (size_type i{}; i < moved; ++i) {
            types[index + i] = types[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            offsets[index + i] = offsets[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            counts[index + i] = counts[source + i];
        }
        set_num(old_num - count);
    }
    void set(size_type const index,
             LevelMissionEventType const new_types,
             std::int32_t const new_offsets,
             LevelEventCount const new_counts) {
        get_view().set(index, new_types, new_offsets, new_counts);
    }
    auto add(LevelMissionEventType const new_types,
             std::int32_t const new_offsets,
             LevelEventCount const new_counts) -> size_type {
        auto const index{num()};
        add_defaulted(1);
        set(index, new_types, new_offsets, new_counts);
        return index;
    }
    void append_from(ConstView source) {
        auto const count{source.num()};
        ml::native_soa::require(count <= std::numeric_limits<size_type>::max() - num());
        source.validate_array_sizes();
        if (count == 0) {
            return;
        }
        {
            auto const address{ml::address_cast(source.types.data())};
            auto const begin{ml::address_cast(types.data())};
            ml::native_soa::require(
                address < begin || address >= begin + types.size() * sizeof(LevelMissionEventType));
        }
        {
            auto const address{ml::address_cast(source.offsets.data())};
            auto const begin{ml::address_cast(offsets.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + offsets.size() * sizeof(std::int32_t));
        }
        {
            auto const address{ml::address_cast(source.counts.data())};
            auto const begin{ml::address_cast(counts.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + counts.size() * sizeof(LevelEventCount));
        }
        types.insert(types.end(), source.types.data(), source.types.data() + count);
        offsets.insert(offsets.end(), source.offsets.data(), source.offsets.data() + count);
        counts.insert(counts.end(), source.counts.data(), source.counts.data() + count);
    }
    auto get_view() -> View {
        return {
            types,
            offsets,
            counts,
        };
    }
    auto get_view() const -> ConstView {
        return {
            types,
            offsets,
            counts,
        };
    }
    auto get_const_view() const -> ConstView { return get_view(); }
    auto get_view(size_type const offset, size_type const count) -> View {
        return get_view().slice(offset, count);
    }
    auto get_view(size_type const offset, size_type const count) const -> ConstView {
        return get_view().slice(offset, count);
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto slice(size_type const offset, size_type const count) -> View {
        return get_view(offset, count);
    }
    auto slice(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view(offset, count);
    }
    auto left(size_type const count) -> View { return slice(0, count); }
    auto right(size_type const count) -> View { return slice(num() - count, count); }
    auto left(size_type const count) const -> ConstView { return slice(0, count); }
    auto right(size_type const count) const -> ConstView { return slice(num() - count, count); }
    template <typename Other>
    void copy_element(size_type const dst_index, Other const& other, size_type const src_index) {
        types[static_cast<std::size_t>(dst_index)] =
            other.types[static_cast<std::size_t>(src_index)];
        offsets[static_cast<std::size_t>(dst_index)] =
            other.offsets[static_cast<std::size_t>(src_index)];
        counts[static_cast<std::size_t>(dst_index)] =
            other.counts[static_cast<std::size_t>(src_index)];
    }
    template <typename Other>
    void copy_elements(size_type const dst_index,
                       Other const& other,
                       size_type const src_index,
                       size_type const count) {
        for (size_type i{}; i < count; ++i) {
            copy_element(dst_index + i, other, src_index + i);
        }
    }
    void apply_permutation(std::span<std::int32_t> const indices) {
        validate_array_sizes();
        ml::native_soa::require(indices.size() == static_cast<std::size_t>(num()));
        each_column([indices](auto& column) { ml::apply_permutation(std::span{column}, indices); });
    }
    template <typename Compare>
    void sort(Compare&& compare, std::span<std::int32_t> const scratch_indices) {
        validate_array_sizes();
        ml::native_soa::require(scratch_indices.size() == static_cast<std::size_t>(num()));
        for (size_type i{}; i < num(); ++i) {
            scratch_indices[static_cast<std::size_t>(i)] = i;
        }
        std::sort(scratch_indices.begin(),
                  scratch_indices.end(),
                  [this, &compare](size_type const lhs, size_type const rhs) {
                      return compare(*this, lhs, rhs);
                  });
        apply_permutation(scratch_indices);
    }
};

} // namespace ioj::sim
