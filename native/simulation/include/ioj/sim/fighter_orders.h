// This file is autogenerated. Do not edit by hand.
// Edit lispb/schema and regenerate it instead.

#pragma once

#include "sandbox/core/packed_value.h"

#include <cassert>
#include <compare>
#include <cstdint>
#include <limits>
#include <type_traits>

#include <array>

#include <cstdint>

#include <cstddef>

#include <optional>

#include <string_view>

#include "sandbox/core/enum_traits.h"

namespace ioj::sim {
enum class FighterTask : std::uint8_t {
    Standby = 0,
    MoveToDestination = 1,
    Attack = 2,
    COUNT = 3,
};

[[nodiscard]] constexpr auto to_string(FighterTask const value) noexcept -> std::string_view {
    switch (value) {
        case FighterTask::Standby: {
            return "Standby";
        }
        case FighterTask::MoveToDestination: {
            return "MoveToDestination";
        }
        case FighterTask::Attack: {
            return "Attack";
        }
        case FighterTask::COUNT: {
            return "COUNT";
        }
    }

    return "<invalid FighterTask>";
}

[[nodiscard]] constexpr auto try_parse_fighter_task(std::string_view const value) noexcept
    -> std::optional<FighterTask> {
    if (value == "Standby") {
        return FighterTask::Standby;
    }
    if (value == "MoveToDestination") {
        return FighterTask::MoveToDestination;
    }
    if (value == "Attack") {
        return FighterTask::Attack;
    }
    if (value == "COUNT") {
        return FighterTask::COUNT;
    }

    return std::nullopt;
}

struct FighterOrder {
    using storage_type = std::uint8_t;
    static_assert(ml::valid_packed_storage<storage_type, 8>());
    using task_type = std::uint8_t;
    using task_field = ml::PackedField<storage_type, task_type, 0, 1>;
    using target_type = std::uint8_t;
    using target_field = ml::PackedField<storage_type, target_type, 1, 1>;

    constexpr FighterOrder() noexcept = default;
    [[nodiscard]] static constexpr auto from_raw(storage_type const raw) noexcept -> FighterOrder {
        FighterOrder result;
        result.value_ = raw;
        return result;
    }

    [[nodiscard]] constexpr auto raw_value() const noexcept -> storage_type { return value_; }

    explicit constexpr FighterOrder(task_type const task_value,
                                    target_type const target_value) noexcept {
        assert(task_value <= static_cast<task_type>(task_field::value_mask));
        assert(target_value <= static_cast<target_type>(target_field::value_mask));
        value_ = static_cast<storage_type>(ml::packed_pack<task_field>(task_value) |
                                           ml::packed_pack<target_field>(target_value));
    }

    [[nodiscard]] constexpr auto is_valid() const noexcept -> bool { return true; }

    [[nodiscard]] constexpr auto operator<=>(FighterOrder const&) const noexcept = default;

    [[nodiscard]] constexpr auto task() const noexcept -> task_type {
        return ml::packed_extract<task_field>(value_);
    }

    [[nodiscard]] constexpr auto target() const noexcept -> target_type {
        return ml::packed_extract<target_field>(value_);
    }
  private:
    storage_type value_{0x0};
};
static_assert(sizeof(FighterOrder) == sizeof(FighterOrder::storage_type));
static_assert(std::is_trivially_copyable_v<FighterOrder>);
static_assert(std::is_standard_layout_v<FighterOrder>);
} // namespace ioj::sim
namespace ml {
template <>
struct EnumTraits<::ioj::sim::FighterTask> {
    inline static constexpr std::array values{
        ::ioj::sim::FighterTask::Standby,
        ::ioj::sim::FighterTask::MoveToDestination,
        ::ioj::sim::FighterTask::Attack,
    };
    inline static constexpr std::array names{
        std::string_view{"Standby"},
        std::string_view{"MoveToDestination"},
        std::string_view{"Attack"},
    };
    inline static constexpr std::size_t count{values.size()};
};

} // namespace ml
