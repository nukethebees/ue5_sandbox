#pragma once

#include <ioj/sim/turret_spawn_data.h>

namespace ioj::sim::turrets {
using SpawnData = ioj::sim::TurretSpawnData;
using SpawnDataView = ioj::sim::TurretSpawnDataView;
using SpawnDataConstView = ioj::sim::TurretSpawnDataConstView;
}
