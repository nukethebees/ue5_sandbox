// This file is autogenerated. Do not edit by hand.
// Edit lispb/schema and regenerate it instead.

#pragma once

#include "ioj/sim/entity_types.h"
#include "ioj/sim/entity_unique_id.h"
#include "ioj/sim/health_table.h"
#include "ioj/sim/rotators3f.h"
#include "ioj/sim/vectors3f.h"
#include "sandbox/core/address_cast.h"
#include "sandbox/core/native_soa/storage.h"
#include "sandbox/core/soa_permutation.h"

#include <cstring>
#include <memory>
#include <utility>

namespace ioj::sim {
struct TurretEntityDataView;
struct TurretEntityDataConstView;
struct TurretEntityDataConstView {
    using View = TurretEntityDataView;
    using ConstView = TurretEntityDataConstView;
    using size_type = std::int32_t;
    std::span<EntityUniqueId const> entity_ids;
    std::span<std::uint32_t const> integral_biases;
    Vectors3fConstView locations;
    Vectors3fConstView fire_point_locations;
    Rotators3fConstView rotations;
    std::span<Team const> teams;
    std::span<std::int16_t const> laser_cooldowns;
    std::span<std::int32_t const> laser_damages;
    std::span<std::int16_t const> target_refresh_countdowns_periods;
    std::span<std::int16_t const> target_refresh_countdowns_remaining_ticks;
    std::span<EntityUniqueId const> target_ids;
    Vectors3fConstView target_locations;
    Vectors3fConstView target_velocities;
    std::span<HealthIndex const> health_indices;
    auto num() const noexcept -> size_type { return static_cast<size_type>(entity_ids.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(entity_ids);
        fn(integral_biases);
        fn(locations.xs_span());
        fn(locations.ys_span());
        fn(locations.zs_span());
        fn(fire_point_locations.xs_span());
        fn(fire_point_locations.ys_span());
        fn(fire_point_locations.zs_span());
        fn(rotations.pitches);
        fn(rotations.yaws);
        fn(rotations.rolls);
        fn(teams);
        fn(laser_cooldowns);
        fn(laser_damages);
        fn(target_refresh_countdowns_periods);
        fn(target_refresh_countdowns_remaining_ticks);
        fn(target_ids);
        fn(target_locations.xs_span());
        fn(target_locations.ys_span());
        fn(target_locations.zs_span());
        fn(target_velocities.xs_span());
        fn(target_velocities.ys_span());
        fn(target_velocities.zs_span());
        fn(health_indices);
    }
    void validate_array_sizes() const {
        auto const count{num()};
        each_column([count](auto column) {
            ml::native_soa::require(column.size() == static_cast<std::size_t>(count));
        });
    }
    auto slice(size_type const offset, size_type const count) const -> TurretEntityDataConstView {
        ml::native_soa::require(offset >= 0 && count >= 0 && offset <= num() &&
                                count <= num() - offset);
        return {
            entity_ids.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            integral_biases.subspan(static_cast<std::size_t>(offset),
                                    static_cast<std::size_t>(count)),
            locations.slice(offset, count),
            fire_point_locations.slice(offset, count),
            rotations.slice(offset, count),
            teams.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            laser_cooldowns.subspan(static_cast<std::size_t>(offset),
                                    static_cast<std::size_t>(count)),
            laser_damages.subspan(static_cast<std::size_t>(offset),
                                  static_cast<std::size_t>(count)),
            target_refresh_countdowns_periods.subspan(static_cast<std::size_t>(offset),
                                                      static_cast<std::size_t>(count)),
            target_refresh_countdowns_remaining_ticks.subspan(static_cast<std::size_t>(offset),
                                                              static_cast<std::size_t>(count)),
            target_ids.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            target_locations.slice(offset, count),
            target_velocities.slice(offset, count),
            health_indices.subspan(static_cast<std::size_t>(offset),
                                   static_cast<std::size_t>(count)),
        };
    }
    auto get_view() const -> TurretEntityDataConstView { return *this; }
    auto get_view(size_type const offset, size_type const count) const
        -> TurretEntityDataConstView {
        return slice(offset, count);
    }
    auto get_const_view() const -> ConstView {
        return {
            entity_ids,
            integral_biases,
            locations.get_const_view(),
            fire_point_locations.get_const_view(),
            rotations.get_const_view(),
            teams,
            laser_cooldowns,
            laser_damages,
            target_refresh_countdowns_periods,
            target_refresh_countdowns_remaining_ticks,
            target_ids,
            target_locations.get_const_view(),
            target_velocities.get_const_view(),
            health_indices,
        };
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto left(size_type const count) const -> TurretEntityDataConstView { return slice(0, count); }
    auto right(size_type const count) const -> TurretEntityDataConstView {
        return slice(num() - count, count);
    }
};
struct TurretEntityDataView {
    using View = TurretEntityDataView;
    using ConstView = TurretEntityDataConstView;
    using size_type = std::int32_t;
    std::span<EntityUniqueId> entity_ids;
    std::span<std::uint32_t> integral_biases;
    Vectors3fView locations;
    Vectors3fView fire_point_locations;
    Rotators3fView rotations;
    std::span<Team> teams;
    std::span<std::int16_t> laser_cooldowns;
    std::span<std::int32_t> laser_damages;
    std::span<std::int16_t> target_refresh_countdowns_periods;
    std::span<std::int16_t> target_refresh_countdowns_remaining_ticks;
    std::span<EntityUniqueId> target_ids;
    Vectors3fView target_locations;
    Vectors3fView target_velocities;
    std::span<HealthIndex> health_indices;
    auto num() const noexcept -> size_type { return static_cast<size_type>(entity_ids.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(entity_ids);
        fn(integral_biases);
        fn(locations.xs_span());
        fn(locations.ys_span());
        fn(locations.zs_span());
        fn(fire_point_locations.xs_span());
        fn(fire_point_locations.ys_span());
        fn(fire_point_locations.zs_span());
        fn(rotations.pitches);
        fn(rotations.yaws);
        fn(rotations.rolls);
        fn(teams);
        fn(laser_cooldowns);
        fn(laser_damages);
        fn(target_refresh_countdowns_periods);
        fn(target_refresh_countdowns_remaining_ticks);
        fn(target_ids);
        fn(target_locations.xs_span());
        fn(target_locations.ys_span());
        fn(target_locations.zs_span());
        fn(target_velocities.xs_span());
        fn(target_velocities.ys_span());
        fn(target_velocities.zs_span());
        fn(health_indices);
    }
    void validate_array_sizes() const {
        auto const count{num()};
        each_column([count](auto column) {
            ml::native_soa::require(column.size() == static_cast<std::size_t>(count));
        });
    }
    auto slice(size_type const offset, size_type const count) const -> TurretEntityDataView {
        ml::native_soa::require(offset >= 0 && count >= 0 && offset <= num() &&
                                count <= num() - offset);
        return {
            entity_ids.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            integral_biases.subspan(static_cast<std::size_t>(offset),
                                    static_cast<std::size_t>(count)),
            locations.slice(offset, count),
            fire_point_locations.slice(offset, count),
            rotations.slice(offset, count),
            teams.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            laser_cooldowns.subspan(static_cast<std::size_t>(offset),
                                    static_cast<std::size_t>(count)),
            laser_damages.subspan(static_cast<std::size_t>(offset),
                                  static_cast<std::size_t>(count)),
            target_refresh_countdowns_periods.subspan(static_cast<std::size_t>(offset),
                                                      static_cast<std::size_t>(count)),
            target_refresh_countdowns_remaining_ticks.subspan(static_cast<std::size_t>(offset),
                                                              static_cast<std::size_t>(count)),
            target_ids.subspan(static_cast<std::size_t>(offset), static_cast<std::size_t>(count)),
            target_locations.slice(offset, count),
            target_velocities.slice(offset, count),
            health_indices.subspan(static_cast<std::size_t>(offset),
                                   static_cast<std::size_t>(count)),
        };
    }
    auto get_view() const -> TurretEntityDataView { return *this; }
    auto get_view(size_type const offset, size_type const count) const -> TurretEntityDataView {
        return slice(offset, count);
    }
    auto get_const_view() const -> ConstView {
        return {
            entity_ids,
            integral_biases,
            locations.get_const_view(),
            fire_point_locations.get_const_view(),
            rotations.get_const_view(),
            teams,
            laser_cooldowns,
            laser_damages,
            target_refresh_countdowns_periods,
            target_refresh_countdowns_remaining_ticks,
            target_ids,
            target_locations.get_const_view(),
            target_velocities.get_const_view(),
            health_indices,
        };
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto left(size_type const count) const -> TurretEntityDataView { return slice(0, count); }
    auto right(size_type const count) const -> TurretEntityDataView {
        return slice(num() - count, count);
    }
    void set(size_type const index,
             EntityUniqueId const new_entity_ids,
             std::uint32_t const new_integral_biases,
             float const new_locations_xs,
             float const new_locations_ys,
             float const new_locations_zs,
             float const new_fire_point_locations_xs,
             float const new_fire_point_locations_ys,
             float const new_fire_point_locations_zs,
             float const new_rotations_pitches,
             float const new_rotations_yaws,
             float const new_rotations_rolls,
             Team const new_teams,
             std::int16_t const new_laser_cooldowns,
             std::int32_t const new_laser_damages,
             std::int16_t const new_target_refresh_countdowns_periods,
             std::int16_t const new_target_refresh_countdowns_remaining_ticks,
             EntityUniqueId const new_target_ids,
             float const new_target_locations_xs,
             float const new_target_locations_ys,
             float const new_target_locations_zs,
             float const new_target_velocities_xs,
             float const new_target_velocities_ys,
             float const new_target_velocities_zs,
             HealthIndex const new_health_indices) const {
        ml::native_soa::require(index >= 0 && index < num());
        entity_ids[static_cast<std::size_t>(index)] = new_entity_ids;
        integral_biases[static_cast<std::size_t>(index)] = new_integral_biases;
        locations.xs[static_cast<std::size_t>(index)] = new_locations_xs;
        locations.ys[static_cast<std::size_t>(index)] = new_locations_ys;
        locations.zs[static_cast<std::size_t>(index)] = new_locations_zs;
        fire_point_locations.xs[static_cast<std::size_t>(index)] = new_fire_point_locations_xs;
        fire_point_locations.ys[static_cast<std::size_t>(index)] = new_fire_point_locations_ys;
        fire_point_locations.zs[static_cast<std::size_t>(index)] = new_fire_point_locations_zs;
        rotations.pitches[static_cast<std::size_t>(index)] = new_rotations_pitches;
        rotations.yaws[static_cast<std::size_t>(index)] = new_rotations_yaws;
        rotations.rolls[static_cast<std::size_t>(index)] = new_rotations_rolls;
        teams[static_cast<std::size_t>(index)] = new_teams;
        laser_cooldowns[static_cast<std::size_t>(index)] = new_laser_cooldowns;
        laser_damages[static_cast<std::size_t>(index)] = new_laser_damages;
        target_refresh_countdowns_periods[static_cast<std::size_t>(index)] =
            new_target_refresh_countdowns_periods;
        target_refresh_countdowns_remaining_ticks[static_cast<std::size_t>(index)] =
            new_target_refresh_countdowns_remaining_ticks;
        target_ids[static_cast<std::size_t>(index)] = new_target_ids;
        target_locations.xs[static_cast<std::size_t>(index)] = new_target_locations_xs;
        target_locations.ys[static_cast<std::size_t>(index)] = new_target_locations_ys;
        target_locations.zs[static_cast<std::size_t>(index)] = new_target_locations_zs;
        target_velocities.xs[static_cast<std::size_t>(index)] = new_target_velocities_xs;
        target_velocities.ys[static_cast<std::size_t>(index)] = new_target_velocities_ys;
        target_velocities.zs[static_cast<std::size_t>(index)] = new_target_velocities_zs;
        health_indices[static_cast<std::size_t>(index)] = new_health_indices;
    }
};
struct TurretEntityData {
    using View = TurretEntityDataView;
    using ConstView = TurretEntityDataConstView;
    using size_type = std::int32_t;
    ml::native_soa::Vector<EntityUniqueId> entity_ids;
    ml::native_soa::Vector<std::uint32_t> integral_biases;
    Vectors3f locations;
    Vectors3f fire_point_locations;
    Rotators3f rotations;
    ml::native_soa::Vector<Team> teams;
    ml::native_soa::Vector<std::int16_t> laser_cooldowns;
    ml::native_soa::Vector<std::int32_t> laser_damages;
    ml::native_soa::Vector<std::int16_t> target_refresh_countdowns_periods;
    ml::native_soa::Vector<std::int16_t> target_refresh_countdowns_remaining_ticks;
    ml::native_soa::Vector<EntityUniqueId> target_ids;
    Vectors3f target_locations;
    Vectors3f target_velocities;
    ml::native_soa::Vector<HealthIndex> health_indices;
    auto num() const noexcept -> size_type { return static_cast<size_type>(entity_ids.size()); }
    auto is_empty() const noexcept -> bool { return num() == 0; }
    template <typename Fn>
    void each_column(Fn&& fn) {
        fn(entity_ids);
        fn(integral_biases);
        fn(locations.xs);
        fn(locations.ys);
        fn(locations.zs);
        fn(fire_point_locations.xs);
        fn(fire_point_locations.ys);
        fn(fire_point_locations.zs);
        fn(rotations.pitches);
        fn(rotations.yaws);
        fn(rotations.rolls);
        fn(teams);
        fn(laser_cooldowns);
        fn(laser_damages);
        fn(target_refresh_countdowns_periods);
        fn(target_refresh_countdowns_remaining_ticks);
        fn(target_ids);
        fn(target_locations.xs);
        fn(target_locations.ys);
        fn(target_locations.zs);
        fn(target_velocities.xs);
        fn(target_velocities.ys);
        fn(target_velocities.zs);
        fn(health_indices);
    }
    template <typename Fn>
    void each_column(Fn&& fn) const {
        fn(entity_ids);
        fn(integral_biases);
        fn(locations.xs);
        fn(locations.ys);
        fn(locations.zs);
        fn(fire_point_locations.xs);
        fn(fire_point_locations.ys);
        fn(fire_point_locations.zs);
        fn(rotations.pitches);
        fn(rotations.yaws);
        fn(rotations.rolls);
        fn(teams);
        fn(laser_cooldowns);
        fn(laser_damages);
        fn(target_refresh_countdowns_periods);
        fn(target_refresh_countdowns_remaining_ticks);
        fn(target_ids);
        fn(target_locations.xs);
        fn(target_locations.ys);
        fn(target_locations.zs);
        fn(target_velocities.xs);
        fn(target_velocities.ys);
        fn(target_velocities.zs);
        fn(health_indices);
    }
    void validate_array_sizes() const { get_const_view().validate_array_sizes(); }
    void reserve(size_type const count) {
        ml::native_soa::require(count >= 0);
        entity_ids.reserve(static_cast<std::size_t>(count));
        integral_biases.reserve(static_cast<std::size_t>(count));
        locations.xs.reserve(static_cast<std::size_t>(count));
        locations.ys.reserve(static_cast<std::size_t>(count));
        locations.zs.reserve(static_cast<std::size_t>(count));
        fire_point_locations.xs.reserve(static_cast<std::size_t>(count));
        fire_point_locations.ys.reserve(static_cast<std::size_t>(count));
        fire_point_locations.zs.reserve(static_cast<std::size_t>(count));
        rotations.pitches.reserve(static_cast<std::size_t>(count));
        rotations.yaws.reserve(static_cast<std::size_t>(count));
        rotations.rolls.reserve(static_cast<std::size_t>(count));
        teams.reserve(static_cast<std::size_t>(count));
        laser_cooldowns.reserve(static_cast<std::size_t>(count));
        laser_damages.reserve(static_cast<std::size_t>(count));
        target_refresh_countdowns_periods.reserve(static_cast<std::size_t>(count));
        target_refresh_countdowns_remaining_ticks.reserve(static_cast<std::size_t>(count));
        target_ids.reserve(static_cast<std::size_t>(count));
        target_locations.xs.reserve(static_cast<std::size_t>(count));
        target_locations.ys.reserve(static_cast<std::size_t>(count));
        target_locations.zs.reserve(static_cast<std::size_t>(count));
        target_velocities.xs.reserve(static_cast<std::size_t>(count));
        target_velocities.ys.reserve(static_cast<std::size_t>(count));
        target_velocities.zs.reserve(static_cast<std::size_t>(count));
        health_indices.reserve(static_cast<std::size_t>(count));
    }
    void reset() noexcept {
        entity_ids.clear();
        integral_biases.clear();
        locations.xs.clear();
        locations.ys.clear();
        locations.zs.clear();
        fire_point_locations.xs.clear();
        fire_point_locations.ys.clear();
        fire_point_locations.zs.clear();
        rotations.pitches.clear();
        rotations.yaws.clear();
        rotations.rolls.clear();
        teams.clear();
        laser_cooldowns.clear();
        laser_damages.clear();
        target_refresh_countdowns_periods.clear();
        target_refresh_countdowns_remaining_ticks.clear();
        target_ids.clear();
        target_locations.xs.clear();
        target_locations.ys.clear();
        target_locations.zs.clear();
        target_velocities.xs.clear();
        target_velocities.ys.clear();
        target_velocities.zs.clear();
        health_indices.clear();
    }
    void set_num(size_type const count) {
        ml::native_soa::require(count >= 0);
        auto const size{static_cast<std::size_t>(count)};
        entity_ids.resize(size);
        integral_biases.resize(size);
        locations.xs.resize(size);
        locations.ys.resize(size);
        locations.zs.resize(size);
        fire_point_locations.xs.resize(size);
        fire_point_locations.ys.resize(size);
        fire_point_locations.zs.resize(size);
        rotations.pitches.resize(size);
        rotations.yaws.resize(size);
        rotations.rolls.resize(size);
        teams.resize(size);
        laser_cooldowns.resize(size);
        laser_damages.resize(size);
        target_refresh_countdowns_periods.resize(size);
        target_refresh_countdowns_remaining_ticks.resize(size);
        target_ids.resize(size);
        target_locations.xs.resize(size);
        target_locations.ys.resize(size);
        target_locations.zs.resize(size);
        target_velocities.xs.resize(size);
        target_velocities.ys.resize(size);
        target_velocities.zs.resize(size);
        health_indices.resize(size);
    }
    void add_uninitialised(size_type const count) {
        auto const old_num{num()};
        ml::native_soa::require(count >= 0 &&
                                count <= std::numeric_limits<size_type>::max() - old_num);
        set_num(old_num + count);
    }
    void add_defaulted(size_type const count) { add_uninitialised(count); }
    void remove_at_swap(size_type const index, size_type const count) {
        auto const old_num{num()};
        ml::native_soa::require(index >= 0 && index <= old_num && count >= 0 &&
                                count <= old_num - index);
        auto const moved{std::min(count, old_num - index - count)};
        auto const source{old_num - moved};
        for (size_type i{}; i < moved; ++i) {
            entity_ids[index + i] = entity_ids[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            integral_biases[index + i] = integral_biases[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            locations.xs[index + i] = locations.xs[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            locations.ys[index + i] = locations.ys[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            locations.zs[index + i] = locations.zs[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            fire_point_locations.xs[index + i] = fire_point_locations.xs[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            fire_point_locations.ys[index + i] = fire_point_locations.ys[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            fire_point_locations.zs[index + i] = fire_point_locations.zs[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            rotations.pitches[index + i] = rotations.pitches[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            rotations.yaws[index + i] = rotations.yaws[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            rotations.rolls[index + i] = rotations.rolls[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            teams[index + i] = teams[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            laser_cooldowns[index + i] = laser_cooldowns[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            laser_damages[index + i] = laser_damages[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            target_refresh_countdowns_periods[index + i] =
                target_refresh_countdowns_periods[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            target_refresh_countdowns_remaining_ticks[index + i] =
                target_refresh_countdowns_remaining_ticks[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            target_ids[index + i] = target_ids[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            target_locations.xs[index + i] = target_locations.xs[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            target_locations.ys[index + i] = target_locations.ys[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            target_locations.zs[index + i] = target_locations.zs[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            target_velocities.xs[index + i] = target_velocities.xs[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            target_velocities.ys[index + i] = target_velocities.ys[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            target_velocities.zs[index + i] = target_velocities.zs[source + i];
        }
        for (size_type i{}; i < moved; ++i) {
            health_indices[index + i] = health_indices[source + i];
        }
        set_num(old_num - count);
    }
    void set(size_type const index,
             EntityUniqueId const new_entity_ids,
             std::uint32_t const new_integral_biases,
             float const new_locations_xs,
             float const new_locations_ys,
             float const new_locations_zs,
             float const new_fire_point_locations_xs,
             float const new_fire_point_locations_ys,
             float const new_fire_point_locations_zs,
             float const new_rotations_pitches,
             float const new_rotations_yaws,
             float const new_rotations_rolls,
             Team const new_teams,
             std::int16_t const new_laser_cooldowns,
             std::int32_t const new_laser_damages,
             std::int16_t const new_target_refresh_countdowns_periods,
             std::int16_t const new_target_refresh_countdowns_remaining_ticks,
             EntityUniqueId const new_target_ids,
             float const new_target_locations_xs,
             float const new_target_locations_ys,
             float const new_target_locations_zs,
             float const new_target_velocities_xs,
             float const new_target_velocities_ys,
             float const new_target_velocities_zs,
             HealthIndex const new_health_indices) {
        get_view().set(index,
                       new_entity_ids,
                       new_integral_biases,
                       new_locations_xs,
                       new_locations_ys,
                       new_locations_zs,
                       new_fire_point_locations_xs,
                       new_fire_point_locations_ys,
                       new_fire_point_locations_zs,
                       new_rotations_pitches,
                       new_rotations_yaws,
                       new_rotations_rolls,
                       new_teams,
                       new_laser_cooldowns,
                       new_laser_damages,
                       new_target_refresh_countdowns_periods,
                       new_target_refresh_countdowns_remaining_ticks,
                       new_target_ids,
                       new_target_locations_xs,
                       new_target_locations_ys,
                       new_target_locations_zs,
                       new_target_velocities_xs,
                       new_target_velocities_ys,
                       new_target_velocities_zs,
                       new_health_indices);
    }
    auto add(EntityUniqueId const new_entity_ids,
             std::uint32_t const new_integral_biases,
             float const new_locations_xs,
             float const new_locations_ys,
             float const new_locations_zs,
             float const new_fire_point_locations_xs,
             float const new_fire_point_locations_ys,
             float const new_fire_point_locations_zs,
             float const new_rotations_pitches,
             float const new_rotations_yaws,
             float const new_rotations_rolls,
             Team const new_teams,
             std::int16_t const new_laser_cooldowns,
             std::int32_t const new_laser_damages,
             std::int16_t const new_target_refresh_countdowns_periods,
             std::int16_t const new_target_refresh_countdowns_remaining_ticks,
             EntityUniqueId const new_target_ids,
             float const new_target_locations_xs,
             float const new_target_locations_ys,
             float const new_target_locations_zs,
             float const new_target_velocities_xs,
             float const new_target_velocities_ys,
             float const new_target_velocities_zs,
             HealthIndex const new_health_indices) -> size_type {
        auto const index{num()};
        add_defaulted(1);
        set(index,
            new_entity_ids,
            new_integral_biases,
            new_locations_xs,
            new_locations_ys,
            new_locations_zs,
            new_fire_point_locations_xs,
            new_fire_point_locations_ys,
            new_fire_point_locations_zs,
            new_rotations_pitches,
            new_rotations_yaws,
            new_rotations_rolls,
            new_teams,
            new_laser_cooldowns,
            new_laser_damages,
            new_target_refresh_countdowns_periods,
            new_target_refresh_countdowns_remaining_ticks,
            new_target_ids,
            new_target_locations_xs,
            new_target_locations_ys,
            new_target_locations_zs,
            new_target_velocities_xs,
            new_target_velocities_ys,
            new_target_velocities_zs,
            new_health_indices);
        return index;
    }
    void append_from(ConstView source) {
        auto const count{source.num()};
        ml::native_soa::require(count <= std::numeric_limits<size_type>::max() - num());
        source.validate_array_sizes();
        if (count == 0) {
            return;
        }
        {
            auto const address{ml::address_cast(source.entity_ids.data())};
            auto const begin{ml::address_cast(entity_ids.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + entity_ids.size() * sizeof(EntityUniqueId));
        }
        {
            auto const address{ml::address_cast(source.integral_biases.data())};
            auto const begin{ml::address_cast(integral_biases.data())};
            ml::native_soa::require(address < begin ||
                                    address >=
                                        begin + integral_biases.size() * sizeof(std::uint32_t));
        }
        {
            auto const address{ml::address_cast(source.locations.xs)};
            auto const begin{ml::address_cast(locations.xs.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + locations.xs.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.locations.ys)};
            auto const begin{ml::address_cast(locations.ys.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + locations.ys.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.locations.zs)};
            auto const begin{ml::address_cast(locations.zs.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + locations.zs.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.fire_point_locations.xs)};
            auto const begin{ml::address_cast(fire_point_locations.xs.data())};
            ml::native_soa::require(address < begin ||
                                    address >=
                                        begin + fire_point_locations.xs.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.fire_point_locations.ys)};
            auto const begin{ml::address_cast(fire_point_locations.ys.data())};
            ml::native_soa::require(address < begin ||
                                    address >=
                                        begin + fire_point_locations.ys.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.fire_point_locations.zs)};
            auto const begin{ml::address_cast(fire_point_locations.zs.data())};
            ml::native_soa::require(address < begin ||
                                    address >=
                                        begin + fire_point_locations.zs.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.rotations.pitches.data())};
            auto const begin{ml::address_cast(rotations.pitches.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + rotations.pitches.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.rotations.yaws.data())};
            auto const begin{ml::address_cast(rotations.yaws.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + rotations.yaws.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.rotations.rolls.data())};
            auto const begin{ml::address_cast(rotations.rolls.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + rotations.rolls.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.teams.data())};
            auto const begin{ml::address_cast(teams.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + teams.size() * sizeof(Team));
        }
        {
            auto const address{ml::address_cast(source.laser_cooldowns.data())};
            auto const begin{ml::address_cast(laser_cooldowns.data())};
            ml::native_soa::require(address < begin || address >= begin + laser_cooldowns.size() *
                                                                              sizeof(std::int16_t));
        }
        {
            auto const address{ml::address_cast(source.laser_damages.data())};
            auto const begin{ml::address_cast(laser_damages.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + laser_damages.size() * sizeof(std::int32_t));
        }
        {
            auto const address{ml::address_cast(source.target_refresh_countdowns_periods.data())};
            auto const begin{ml::address_cast(target_refresh_countdowns_periods.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + target_refresh_countdowns_periods.size() *
                                                           sizeof(std::int16_t));
        }
        {
            auto const address{
                ml::address_cast(source.target_refresh_countdowns_remaining_ticks.data())};
            auto const begin{ml::address_cast(target_refresh_countdowns_remaining_ticks.data())};
            ml::native_soa::require(address < begin ||
                                    address >=
                                        begin + target_refresh_countdowns_remaining_ticks.size() *
                                                    sizeof(std::int16_t));
        }
        {
            auto const address{ml::address_cast(source.target_ids.data())};
            auto const begin{ml::address_cast(target_ids.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + target_ids.size() * sizeof(EntityUniqueId));
        }
        {
            auto const address{ml::address_cast(source.target_locations.xs)};
            auto const begin{ml::address_cast(target_locations.xs.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + target_locations.xs.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.target_locations.ys)};
            auto const begin{ml::address_cast(target_locations.ys.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + target_locations.ys.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.target_locations.zs)};
            auto const begin{ml::address_cast(target_locations.zs.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + target_locations.zs.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.target_velocities.xs)};
            auto const begin{ml::address_cast(target_velocities.xs.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + target_velocities.xs.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.target_velocities.ys)};
            auto const begin{ml::address_cast(target_velocities.ys.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + target_velocities.ys.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.target_velocities.zs)};
            auto const begin{ml::address_cast(target_velocities.zs.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + target_velocities.zs.size() * sizeof(float));
        }
        {
            auto const address{ml::address_cast(source.health_indices.data())};
            auto const begin{ml::address_cast(health_indices.data())};
            ml::native_soa::require(address < begin ||
                                    address >= begin + health_indices.size() * sizeof(HealthIndex));
        }
        entity_ids.insert(
            entity_ids.end(), source.entity_ids.data(), source.entity_ids.data() + count);
        integral_biases.insert(integral_biases.end(),
                               source.integral_biases.data(),
                               source.integral_biases.data() + count);
        locations.xs.insert(locations.xs.end(), source.locations.xs, source.locations.xs + count);
        locations.ys.insert(locations.ys.end(), source.locations.ys, source.locations.ys + count);
        locations.zs.insert(locations.zs.end(), source.locations.zs, source.locations.zs + count);
        fire_point_locations.xs.insert(fire_point_locations.xs.end(),
                                       source.fire_point_locations.xs,
                                       source.fire_point_locations.xs + count);
        fire_point_locations.ys.insert(fire_point_locations.ys.end(),
                                       source.fire_point_locations.ys,
                                       source.fire_point_locations.ys + count);
        fire_point_locations.zs.insert(fire_point_locations.zs.end(),
                                       source.fire_point_locations.zs,
                                       source.fire_point_locations.zs + count);
        rotations.pitches.insert(rotations.pitches.end(),
                                 source.rotations.pitches.data(),
                                 source.rotations.pitches.data() + count);
        rotations.yaws.insert(rotations.yaws.end(),
                              source.rotations.yaws.data(),
                              source.rotations.yaws.data() + count);
        rotations.rolls.insert(rotations.rolls.end(),
                               source.rotations.rolls.data(),
                               source.rotations.rolls.data() + count);
        teams.insert(teams.end(), source.teams.data(), source.teams.data() + count);
        laser_cooldowns.insert(laser_cooldowns.end(),
                               source.laser_cooldowns.data(),
                               source.laser_cooldowns.data() + count);
        laser_damages.insert(
            laser_damages.end(), source.laser_damages.data(), source.laser_damages.data() + count);
        target_refresh_countdowns_periods.insert(target_refresh_countdowns_periods.end(),
                                                 source.target_refresh_countdowns_periods.data(),
                                                 source.target_refresh_countdowns_periods.data() +
                                                     count);
        target_refresh_countdowns_remaining_ticks.insert(
            target_refresh_countdowns_remaining_ticks.end(),
            source.target_refresh_countdowns_remaining_ticks.data(),
            source.target_refresh_countdowns_remaining_ticks.data() + count);
        target_ids.insert(
            target_ids.end(), source.target_ids.data(), source.target_ids.data() + count);
        target_locations.xs.insert(target_locations.xs.end(),
                                   source.target_locations.xs,
                                   source.target_locations.xs + count);
        target_locations.ys.insert(target_locations.ys.end(),
                                   source.target_locations.ys,
                                   source.target_locations.ys + count);
        target_locations.zs.insert(target_locations.zs.end(),
                                   source.target_locations.zs,
                                   source.target_locations.zs + count);
        target_velocities.xs.insert(target_velocities.xs.end(),
                                    source.target_velocities.xs,
                                    source.target_velocities.xs + count);
        target_velocities.ys.insert(target_velocities.ys.end(),
                                    source.target_velocities.ys,
                                    source.target_velocities.ys + count);
        target_velocities.zs.insert(target_velocities.zs.end(),
                                    source.target_velocities.zs,
                                    source.target_velocities.zs + count);
        health_indices.insert(health_indices.end(),
                              source.health_indices.data(),
                              source.health_indices.data() + count);
    }
    auto get_view() -> View {
        return {
            entity_ids,
            integral_biases,
            locations.get_view(),
            fire_point_locations.get_view(),
            rotations.get_view(),
            teams,
            laser_cooldowns,
            laser_damages,
            target_refresh_countdowns_periods,
            target_refresh_countdowns_remaining_ticks,
            target_ids,
            target_locations.get_view(),
            target_velocities.get_view(),
            health_indices,
        };
    }
    auto get_view() const -> ConstView {
        return {
            entity_ids,
            integral_biases,
            locations.get_view(),
            fire_point_locations.get_view(),
            rotations.get_view(),
            teams,
            laser_cooldowns,
            laser_damages,
            target_refresh_countdowns_periods,
            target_refresh_countdowns_remaining_ticks,
            target_ids,
            target_locations.get_view(),
            target_velocities.get_view(),
            health_indices,
        };
    }
    auto get_const_view() const -> ConstView { return get_view(); }
    auto get_view(size_type const offset, size_type const count) -> View {
        return get_view().slice(offset, count);
    }
    auto get_view(size_type const offset, size_type const count) const -> ConstView {
        return get_view().slice(offset, count);
    }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    auto slice(size_type const offset, size_type const count) -> View {
        return get_view(offset, count);
    }
    auto slice(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view(offset, count);
    }
    auto left(size_type const count) -> View { return slice(0, count); }
    auto right(size_type const count) -> View { return slice(num() - count, count); }
    auto left(size_type const count) const -> ConstView { return slice(0, count); }
    auto right(size_type const count) const -> ConstView { return slice(num() - count, count); }
    template <typename Other>
    void copy_element(size_type const dst_index, Other const& other, size_type const src_index) {
        entity_ids[static_cast<std::size_t>(dst_index)] =
            other.entity_ids[static_cast<std::size_t>(src_index)];
        integral_biases[static_cast<std::size_t>(dst_index)] =
            other.integral_biases[static_cast<std::size_t>(src_index)];
        locations.copy_element(dst_index, other.locations, src_index);
        fire_point_locations.copy_element(dst_index, other.fire_point_locations, src_index);
        rotations.copy_element(dst_index, other.rotations, src_index);
        teams[static_cast<std::size_t>(dst_index)] =
            other.teams[static_cast<std::size_t>(src_index)];
        laser_cooldowns[static_cast<std::size_t>(dst_index)] =
            other.laser_cooldowns[static_cast<std::size_t>(src_index)];
        laser_damages[static_cast<std::size_t>(dst_index)] =
            other.laser_damages[static_cast<std::size_t>(src_index)];
        target_refresh_countdowns_periods[static_cast<std::size_t>(dst_index)] =
            other.target_refresh_countdowns_periods[static_cast<std::size_t>(src_index)];
        target_refresh_countdowns_remaining_ticks[static_cast<std::size_t>(dst_index)] =
            other.target_refresh_countdowns_remaining_ticks[static_cast<std::size_t>(src_index)];
        target_ids[static_cast<std::size_t>(dst_index)] =
            other.target_ids[static_cast<std::size_t>(src_index)];
        target_locations.copy_element(dst_index, other.target_locations, src_index);
        target_velocities.copy_element(dst_index, other.target_velocities, src_index);
        health_indices[static_cast<std::size_t>(dst_index)] =
            other.health_indices[static_cast<std::size_t>(src_index)];
    }
    template <typename Other>
    void copy_elements(size_type const dst_index,
                       Other const& other,
                       size_type const src_index,
                       size_type const count) {
        for (size_type i{}; i < count; ++i) {
            copy_element(dst_index + i, other, src_index + i);
        }
    }
    void apply_permutation(std::span<std::int32_t> const indices) {
        validate_array_sizes();
        ml::native_soa::require(indices.size() == static_cast<std::size_t>(num()));
        each_column([indices](auto& column) { ml::apply_permutation(std::span{column}, indices); });
    }
    template <typename Compare>
    void sort(Compare&& compare, std::span<std::int32_t> const scratch_indices) {
        validate_array_sizes();
        ml::native_soa::require(scratch_indices.size() == static_cast<std::size_t>(num()));
        for (size_type i{}; i < num(); ++i) {
            scratch_indices[static_cast<std::size_t>(i)] = i;
        }
        std::sort(scratch_indices.begin(),
                  scratch_indices.end(),
                  [this, &compare](size_type const lhs, size_type const rhs) {
                      return compare(*this, lhs, rhs);
                  });
        apply_permutation(scratch_indices);
    }
};

struct TurretEntityDataSingleView;
struct TurretEntityDataSingleConstView;
struct TurretEntityDataSingleLayout {
    using size_type = std::int32_t;
    using byte_size_type = std::size_t;

    inline static constexpr byte_size_type max_allocation_size{
        std::numeric_limits<byte_size_type>::max()};
    inline static constexpr size_type capacity_granularity{64};
    inline static constexpr byte_size_type column_gap{192};

    template <typename T>
    using ColLayout = ml::native_soa::ColumnLayout<T>;
    inline static constexpr ml::native_soa::ColumnLayoutStart LayoutStart{
        capacity_granularity, column_gap, 64};

    inline static constexpr ColLayout<EntityUniqueId> EntityIds{LayoutStart};
    inline static constexpr ColLayout<std::uint32_t> IntegralBiases{EntityIds};
    inline static constexpr ColLayout<float> LocationsXs{IntegralBiases};
    inline static constexpr ColLayout<float> LocationsYs{LocationsXs};
    inline static constexpr ColLayout<float> LocationsZs{LocationsYs};
    inline static constexpr ColLayout<float> FirePointLocationsXs{LocationsZs};
    inline static constexpr ColLayout<float> FirePointLocationsYs{FirePointLocationsXs};
    inline static constexpr ColLayout<float> FirePointLocationsZs{FirePointLocationsYs};
    inline static constexpr ColLayout<float> RotationsPitches{FirePointLocationsZs};
    inline static constexpr ColLayout<float> RotationsYaws{RotationsPitches};
    inline static constexpr ColLayout<float> RotationsRolls{RotationsYaws};
    inline static constexpr ColLayout<Team> Teams{RotationsRolls};
    inline static constexpr ColLayout<std::int16_t> LaserCooldowns{Teams};
    inline static constexpr ColLayout<std::int32_t> LaserDamages{LaserCooldowns};
    inline static constexpr ColLayout<std::int16_t> TargetRefreshCountdownsPeriods{LaserDamages};
    inline static constexpr ColLayout<std::int16_t> TargetRefreshCountdownsRemainingTicks{
        TargetRefreshCountdownsPeriods};
    inline static constexpr ColLayout<EntityUniqueId> TargetIds{
        TargetRefreshCountdownsRemainingTicks};
    inline static constexpr ColLayout<float> TargetLocationsXs{TargetIds};
    inline static constexpr ColLayout<float> TargetLocationsYs{TargetLocationsXs};
    inline static constexpr ColLayout<float> TargetLocationsZs{TargetLocationsYs};
    inline static constexpr ColLayout<float> TargetVelocitiesXs{TargetLocationsZs};
    inline static constexpr ColLayout<float> TargetVelocitiesYs{TargetVelocitiesXs};
    inline static constexpr ColLayout<float> TargetVelocitiesZs{TargetVelocitiesYs};
    inline static constexpr ColLayout<HealthIndex> HealthIndices{TargetVelocitiesZs};

    inline static constexpr byte_size_type allocation_alignment{
        ml::native_soa::maximum_alignment(EntityIds,
                                          IntegralBiases,
                                          LocationsXs,
                                          LocationsYs,
                                          LocationsZs,
                                          FirePointLocationsXs,
                                          FirePointLocationsYs,
                                          FirePointLocationsZs,
                                          RotationsPitches,
                                          RotationsYaws,
                                          RotationsRolls,
                                          Teams,
                                          LaserCooldowns,
                                          LaserDamages,
                                          TargetRefreshCountdownsPeriods,
                                          TargetRefreshCountdownsRemainingTicks,
                                          TargetIds,
                                          TargetLocationsXs,
                                          TargetLocationsYs,
                                          TargetLocationsZs,
                                          TargetVelocitiesXs,
                                          TargetVelocitiesYs,
                                          TargetVelocitiesZs,
                                          HealthIndices)};

    // Conservative per-block bound for checked capacity arithmetic; gaps do not scale with
    // capacity.
    inline static constexpr byte_size_type capacity_block_bound{
        ml::native_soa::layout_align(HealthIndices.block_end, allocation_alignment) +
        23 * (column_gap + allocation_alignment - 1)};
    inline static constexpr size_type max_capacity{
        ml::native_soa::maximum_capacity(capacity_block_bound)};
    static constexpr auto layout_bytes(byte_size_type blocks) noexcept -> byte_size_type {
        return blocks == 0 ? 0 : HealthIndices.data_end(blocks);
    }
  private:
    inline static constexpr auto validate_layout = []() consteval -> bool {
        static_assert(
            ml::native_soa::supported_leaf<EntityUniqueId>,
            "Single-allocation leaf entity_ids requires a non-cv, trivially "
            "copyable/copy-constructible/destructible, nothrow default-constructible object type.");
        static_assert(
            ml::native_soa::supported_leaf<std::uint32_t>,
            "Single-allocation leaf integral_biases requires a non-cv, trivially "
            "copyable/copy-constructible/destructible, nothrow default-constructible object type.");
        static_assert(
            ml::native_soa::supported_leaf<float>,
            "Single-allocation leaf locations.xs requires a non-cv, trivially "
            "copyable/copy-constructible/destructible, nothrow default-constructible object type.");
        static_assert(
            ml::native_soa::supported_leaf<Team>,
            "Single-allocation leaf teams requires a non-cv, trivially "
            "copyable/copy-constructible/destructible, nothrow default-constructible object type.");
        static_assert(
            ml::native_soa::supported_leaf<std::int16_t>,
            "Single-allocation leaf laser_cooldowns requires a non-cv, trivially "
            "copyable/copy-constructible/destructible, nothrow default-constructible object type.");
        static_assert(
            ml::native_soa::supported_leaf<std::int32_t>,
            "Single-allocation leaf laser_damages requires a non-cv, trivially "
            "copyable/copy-constructible/destructible, nothrow default-constructible object type.");
        static_assert(
            ml::native_soa::supported_leaf<HealthIndex>,
            "Single-allocation leaf health_indices requires a non-cv, trivially "
            "copyable/copy-constructible/destructible, nothrow default-constructible object type.");

        static_assert(
            allocation_alignment <= std::numeric_limits<std::uint32_t>::max(),
            "Single-allocation alignment must fit the allocator's 32-bit alignment argument.");
        static_assert(sizeof(EntityUniqueId) <=
                      (max_allocation_size - EntityIds.block_offset) / capacity_granularity);
        static_assert(sizeof(std::uint32_t) <=
                      (max_allocation_size - IntegralBiases.block_offset) / capacity_granularity);
        static_assert(sizeof(float) <=
                      (max_allocation_size - LocationsXs.block_offset) / capacity_granularity);
        static_assert(sizeof(float) <=
                      (max_allocation_size - LocationsYs.block_offset) / capacity_granularity);
        static_assert(sizeof(float) <=
                      (max_allocation_size - LocationsZs.block_offset) / capacity_granularity);
        static_assert(sizeof(float) <= (max_allocation_size - FirePointLocationsXs.block_offset) /
                                           capacity_granularity);
        static_assert(sizeof(float) <= (max_allocation_size - FirePointLocationsYs.block_offset) /
                                           capacity_granularity);
        static_assert(sizeof(float) <= (max_allocation_size - FirePointLocationsZs.block_offset) /
                                           capacity_granularity);
        static_assert(sizeof(float) <=
                      (max_allocation_size - RotationsPitches.block_offset) / capacity_granularity);
        static_assert(sizeof(float) <=
                      (max_allocation_size - RotationsYaws.block_offset) / capacity_granularity);
        static_assert(sizeof(float) <=
                      (max_allocation_size - RotationsRolls.block_offset) / capacity_granularity);
        static_assert(sizeof(Team) <=
                      (max_allocation_size - Teams.block_offset) / capacity_granularity);
        static_assert(sizeof(std::int16_t) <=
                      (max_allocation_size - LaserCooldowns.block_offset) / capacity_granularity);
        static_assert(sizeof(std::int32_t) <=
                      (max_allocation_size - LaserDamages.block_offset) / capacity_granularity);
        static_assert(sizeof(std::int16_t) <=
                      (max_allocation_size - TargetRefreshCountdownsPeriods.block_offset) /
                          capacity_granularity);
        static_assert(sizeof(std::int16_t) <=
                      (max_allocation_size - TargetRefreshCountdownsRemainingTicks.block_offset) /
                          capacity_granularity);
        static_assert(sizeof(EntityUniqueId) <=
                      (max_allocation_size - TargetIds.block_offset) / capacity_granularity);
        static_assert(sizeof(float) <= (max_allocation_size - TargetLocationsXs.block_offset) /
                                           capacity_granularity);
        static_assert(sizeof(float) <= (max_allocation_size - TargetLocationsYs.block_offset) /
                                           capacity_granularity);
        static_assert(sizeof(float) <= (max_allocation_size - TargetLocationsZs.block_offset) /
                                           capacity_granularity);
        static_assert(sizeof(float) <= (max_allocation_size - TargetVelocitiesXs.block_offset) /
                                           capacity_granularity);
        static_assert(sizeof(float) <= (max_allocation_size - TargetVelocitiesYs.block_offset) /
                                           capacity_granularity);
        static_assert(sizeof(float) <= (max_allocation_size - TargetVelocitiesZs.block_offset) /
                                           capacity_granularity);
        static_assert(sizeof(HealthIndex) <=
                      (max_allocation_size - HealthIndices.block_offset) / capacity_granularity);
        static_assert(23 <=
                      (max_allocation_size - ml::native_soa::layout_align(HealthIndices.block_end,
                                                                          allocation_alignment)) /
                          (column_gap + allocation_alignment - 1));
        static_assert(max_capacity >= capacity_granularity);
        return true;
    };
    static_assert(validate_layout());
};

struct SingleAllocationTurretEntityDataStorage
    : TurretEntityDataSingleLayout
    , protected ml::native_soa::StorageState
    , ml::native_soa::StorageOperations {
    using View = TurretEntityDataSingleView;
    using ConstView = TurretEntityDataSingleConstView;
    using SchemaConstView = TurretEntityDataConstView;
    using ml::native_soa::StorageOperations::append_from;
    auto append_from(TurretEntityDataConstView const& source) -> size_type {
        source.validate_array_sizes();
        auto const count{source.num()};
        auto const first{num_};
        ml::native_soa::require((count <= max_capacity - first));
        if (count == 0) {
            return first;
        }
        auto const new_num{first + count};
        if (new_num > capacity_) {
            ml::native_soa::require(!ordinary_source_aliases_storage(source));
            reallocate(ml::native_soa::growth_capacity(new_num, capacity_, capacity_block_bound));
        }
        append_columns(source, first, count);
        num_ = new_num;
        return first;
    }
    /* **************************************** */
    // Lifetime
    /* **************************************** */
    SingleAllocationTurretEntityDataStorage() noexcept = default;
    ~SingleAllocationTurretEntityDataStorage() {
        ml::native_soa::free(data_, allocation_alignment);
    }
    SingleAllocationTurretEntityDataStorage(SingleAllocationTurretEntityDataStorage const&) =
        delete;
    auto operator=(SingleAllocationTurretEntityDataStorage const&)
        -> SingleAllocationTurretEntityDataStorage& = delete;
    SingleAllocationTurretEntityDataStorage(
        SingleAllocationTurretEntityDataStorage&& other) noexcept
        : StorageState{std::exchange(other.data_, nullptr),
                       std::exchange(other.num_, 0),
                       std::exchange(other.capacity_, 0)} {}
    auto operator=(SingleAllocationTurretEntityDataStorage&& other) noexcept
        -> SingleAllocationTurretEntityDataStorage& {
        if (this != &other) {
            ml::native_soa::free(data_, allocation_alignment);
            data_ = std::exchange(other.data_, nullptr);
            num_ = std::exchange(other.num_, 0);
            capacity_ = std::exchange(other.capacity_, 0);
        }
        return *this;
    }
  protected:
    template <typename Byte>
    struct DataPointers {
        template <typename T>
        using Element = std::conditional_t<std::is_const_v<Byte>, T const, T>;
        Element<EntityUniqueId>* entity_ids{};
        Element<std::uint32_t>* integral_biases{};
        Element<float>* locations_xs{};
        Element<float>* locations_ys{};
        Element<float>* locations_zs{};
        Element<float>* fire_point_locations_xs{};
        Element<float>* fire_point_locations_ys{};
        Element<float>* fire_point_locations_zs{};
        Element<float>* rotations_pitches{};
        Element<float>* rotations_yaws{};
        Element<float>* rotations_rolls{};
        Element<Team>* teams{};
        Element<std::int16_t>* laser_cooldowns{};
        Element<std::int32_t>* laser_damages{};
        Element<std::int16_t>* target_refresh_countdowns_periods{};
        Element<std::int16_t>* target_refresh_countdowns_remaining_ticks{};
        Element<EntityUniqueId>* target_ids{};
        Element<float>* target_locations_xs{};
        Element<float>* target_locations_ys{};
        Element<float>* target_locations_zs{};
        Element<float>* target_velocities_xs{};
        Element<float>* target_velocities_ys{};
        Element<float>* target_velocities_zs{};
        Element<HealthIndex>* health_indices{};
        auto operator+(size_type const offset) const noexcept -> DataPointers {
            if (entity_ids == nullptr) {
                return {};
            }
            return {entity_ids + offset,
                    integral_biases + offset,
                    locations_xs + offset,
                    locations_ys + offset,
                    locations_zs + offset,
                    fire_point_locations_xs + offset,
                    fire_point_locations_ys + offset,
                    fire_point_locations_zs + offset,
                    rotations_pitches + offset,
                    rotations_yaws + offset,
                    rotations_rolls + offset,
                    teams + offset,
                    laser_cooldowns + offset,
                    laser_damages + offset,
                    target_refresh_countdowns_periods + offset,
                    target_refresh_countdowns_remaining_ticks + offset,
                    target_ids + offset,
                    target_locations_xs + offset,
                    target_locations_ys + offset,
                    target_locations_zs + offset,
                    target_velocities_xs + offset,
                    target_velocities_ys + offset,
                    target_velocities_zs + offset,
                    health_indices + offset};
        }
    };
    template <typename Self>
    auto get_data(this Self& self) noexcept {
        using Byte = std::conditional_t<std::is_const_v<Self>, std::byte const, std::byte>;
        if (self.data_ == nullptr) {
            return DataPointers<Byte>{};
        }
        return make_data_unchecked(static_cast<Byte*>(self.data_), self.capacity_blocks());
    }
    template <typename Self>
    auto get_data(this Self& self, size_type const offset) noexcept {
        return self.get_data() + offset;
    }
  private:
    friend struct ml::native_soa::StorageOperations;
    /* **************************************** */
    // Column pointers
    /* **************************************** */
    template <typename Byte>
    static auto make_data_unchecked(Byte* const data, byte_size_type const blocks) noexcept
        -> DataPointers<Byte> {
        auto const pointer_at = [data](auto const& column, byte_size_type offset) noexcept {
            using Column = std::remove_cvref_t<decltype(column)>;
            using Pointer = std::conditional_t<std::is_const_v<Byte>,
                                               typename Column::const_pointer,
                                               typename Column::pointer>;
            return std::launder(reinterpret_cast<Pointer>(data + offset));
        };
        auto const entity_ids_offset{byte_size_type{}};
        auto const integral_biases_offset{ml::native_soa::layout_align(
            entity_ids_offset + blocks * capacity_granularity * sizeof(EntityUniqueId) + column_gap,
            IntegralBiases.alignment)};
        auto const locations_xs_offset{ml::native_soa::layout_align(
            integral_biases_offset + blocks * capacity_granularity * sizeof(std::uint32_t) +
                column_gap,
            LocationsXs.alignment)};
        auto const locations_ys_offset{ml::native_soa::layout_align(
            locations_xs_offset + blocks * capacity_granularity * sizeof(float) + column_gap,
            LocationsYs.alignment)};
        auto const locations_zs_offset{ml::native_soa::layout_align(
            locations_ys_offset + blocks * capacity_granularity * sizeof(float) + column_gap,
            LocationsZs.alignment)};
        auto const fire_point_locations_xs_offset{ml::native_soa::layout_align(
            locations_zs_offset + blocks * capacity_granularity * sizeof(float) + column_gap,
            FirePointLocationsXs.alignment)};
        auto const fire_point_locations_ys_offset{ml::native_soa::layout_align(
            fire_point_locations_xs_offset + blocks * capacity_granularity * sizeof(float) +
                column_gap,
            FirePointLocationsYs.alignment)};
        auto const fire_point_locations_zs_offset{ml::native_soa::layout_align(
            fire_point_locations_ys_offset + blocks * capacity_granularity * sizeof(float) +
                column_gap,
            FirePointLocationsZs.alignment)};
        auto const rotations_pitches_offset{ml::native_soa::layout_align(
            fire_point_locations_zs_offset + blocks * capacity_granularity * sizeof(float) +
                column_gap,
            RotationsPitches.alignment)};
        auto const rotations_yaws_offset{ml::native_soa::layout_align(
            rotations_pitches_offset + blocks * capacity_granularity * sizeof(float) + column_gap,
            RotationsYaws.alignment)};
        auto const rotations_rolls_offset{ml::native_soa::layout_align(
            rotations_yaws_offset + blocks * capacity_granularity * sizeof(float) + column_gap,
            RotationsRolls.alignment)};
        auto const teams_offset{ml::native_soa::layout_align(
            rotations_rolls_offset + blocks * capacity_granularity * sizeof(float) + column_gap,
            Teams.alignment)};
        auto const laser_cooldowns_offset{ml::native_soa::layout_align(
            teams_offset + blocks * capacity_granularity * sizeof(Team) + column_gap,
            LaserCooldowns.alignment)};
        auto const laser_damages_offset{ml::native_soa::layout_align(
            laser_cooldowns_offset + blocks * capacity_granularity * sizeof(std::int16_t) +
                column_gap,
            LaserDamages.alignment)};
        auto const target_refresh_countdowns_periods_offset{ml::native_soa::layout_align(
            laser_damages_offset + blocks * capacity_granularity * sizeof(std::int32_t) +
                column_gap,
            TargetRefreshCountdownsPeriods.alignment)};
        auto const target_refresh_countdowns_remaining_ticks_offset{ml::native_soa::layout_align(
            target_refresh_countdowns_periods_offset +
                blocks * capacity_granularity * sizeof(std::int16_t) + column_gap,
            TargetRefreshCountdownsRemainingTicks.alignment)};
        auto const target_ids_offset{ml::native_soa::layout_align(
            target_refresh_countdowns_remaining_ticks_offset +
                blocks * capacity_granularity * sizeof(std::int16_t) + column_gap,
            TargetIds.alignment)};
        auto const target_locations_xs_offset{ml::native_soa::layout_align(
            target_ids_offset + blocks * capacity_granularity * sizeof(EntityUniqueId) + column_gap,
            TargetLocationsXs.alignment)};
        auto const target_locations_ys_offset{ml::native_soa::layout_align(
            target_locations_xs_offset + blocks * capacity_granularity * sizeof(float) + column_gap,
            TargetLocationsYs.alignment)};
        auto const target_locations_zs_offset{ml::native_soa::layout_align(
            target_locations_ys_offset + blocks * capacity_granularity * sizeof(float) + column_gap,
            TargetLocationsZs.alignment)};
        auto const target_velocities_xs_offset{ml::native_soa::layout_align(
            target_locations_zs_offset + blocks * capacity_granularity * sizeof(float) + column_gap,
            TargetVelocitiesXs.alignment)};
        auto const target_velocities_ys_offset{ml::native_soa::layout_align(
            target_velocities_xs_offset + blocks * capacity_granularity * sizeof(float) +
                column_gap,
            TargetVelocitiesYs.alignment)};
        auto const target_velocities_zs_offset{ml::native_soa::layout_align(
            target_velocities_ys_offset + blocks * capacity_granularity * sizeof(float) +
                column_gap,
            TargetVelocitiesZs.alignment)};
        auto const health_indices_offset{ml::native_soa::layout_align(
            target_velocities_zs_offset + blocks * capacity_granularity * sizeof(float) +
                column_gap,
            HealthIndices.alignment)};
        return {
            pointer_at(EntityIds, entity_ids_offset),
            pointer_at(IntegralBiases, integral_biases_offset),
            pointer_at(LocationsXs, locations_xs_offset),
            pointer_at(LocationsYs, locations_ys_offset),
            pointer_at(LocationsZs, locations_zs_offset),
            pointer_at(FirePointLocationsXs, fire_point_locations_xs_offset),
            pointer_at(FirePointLocationsYs, fire_point_locations_ys_offset),
            pointer_at(FirePointLocationsZs, fire_point_locations_zs_offset),
            pointer_at(RotationsPitches, rotations_pitches_offset),
            pointer_at(RotationsYaws, rotations_yaws_offset),
            pointer_at(RotationsRolls, rotations_rolls_offset),
            pointer_at(Teams, teams_offset),
            pointer_at(LaserCooldowns, laser_cooldowns_offset),
            pointer_at(LaserDamages, laser_damages_offset),
            pointer_at(TargetRefreshCountdownsPeriods, target_refresh_countdowns_periods_offset),
            pointer_at(TargetRefreshCountdownsRemainingTicks,
                       target_refresh_countdowns_remaining_ticks_offset),
            pointer_at(TargetIds, target_ids_offset),
            pointer_at(TargetLocationsXs, target_locations_xs_offset),
            pointer_at(TargetLocationsYs, target_locations_ys_offset),
            pointer_at(TargetLocationsZs, target_locations_zs_offset),
            pointer_at(TargetVelocitiesXs, target_velocities_xs_offset),
            pointer_at(TargetVelocitiesYs, target_velocities_ys_offset),
            pointer_at(TargetVelocitiesZs, target_velocities_zs_offset),
            pointer_at(HealthIndices, health_indices_offset)};
    }
    auto capacity_blocks() const noexcept -> byte_size_type {
        return static_cast<byte_size_type>(capacity_ / capacity_granularity);
    }

    /* **************************************** */
    // Typed mutations and growth
    /* **************************************** */
    void default_construct_columns(size_type const first, size_type const count) {
        auto const columns{make_data_unchecked(data_, capacity_blocks()) + first};
        std::uninitialized_value_construct_n<EntityUniqueId*>(columns.entity_ids, count);
        std::uninitialized_value_construct_n<std::uint32_t*>(columns.integral_biases, count);
        std::uninitialized_value_construct_n<float*>(columns.locations_xs, count);
        std::uninitialized_value_construct_n<float*>(columns.locations_ys, count);
        std::uninitialized_value_construct_n<float*>(columns.locations_zs, count);
        std::uninitialized_value_construct_n<float*>(columns.fire_point_locations_xs, count);
        std::uninitialized_value_construct_n<float*>(columns.fire_point_locations_ys, count);
        std::uninitialized_value_construct_n<float*>(columns.fire_point_locations_zs, count);
        std::uninitialized_value_construct_n<float*>(columns.rotations_pitches, count);
        std::uninitialized_value_construct_n<float*>(columns.rotations_yaws, count);
        std::uninitialized_value_construct_n<float*>(columns.rotations_rolls, count);
        std::uninitialized_value_construct_n<Team*>(columns.teams, count);
        std::uninitialized_value_construct_n<std::int16_t*>(columns.laser_cooldowns, count);
        std::uninitialized_value_construct_n<std::int32_t*>(columns.laser_damages, count);
        std::uninitialized_value_construct_n<std::int16_t*>(
            columns.target_refresh_countdowns_periods, count);
        std::uninitialized_value_construct_n<std::int16_t*>(
            columns.target_refresh_countdowns_remaining_ticks, count);
        std::uninitialized_value_construct_n<EntityUniqueId*>(columns.target_ids, count);
        std::uninitialized_value_construct_n<float*>(columns.target_locations_xs, count);
        std::uninitialized_value_construct_n<float*>(columns.target_locations_ys, count);
        std::uninitialized_value_construct_n<float*>(columns.target_locations_zs, count);
        std::uninitialized_value_construct_n<float*>(columns.target_velocities_xs, count);
        std::uninitialized_value_construct_n<float*>(columns.target_velocities_ys, count);
        std::uninitialized_value_construct_n<float*>(columns.target_velocities_zs, count);
        std::uninitialized_value_construct_n<HealthIndex*>(columns.health_indices, count);
    }
    void swap_remove_columns(size_type const index,
                             size_type const source,
                             size_type const move_count) {
        copy_columns(get_data(), index, source, move_count);
    }
    static void copy_columns(DataPointers<std::byte> const& columns,
                             size_type index,
                             size_type source,
                             size_type move_count) {
        auto const elements_to_move{static_cast<byte_size_type>(move_count)};
        auto const entity_ids_bytes{elements_to_move * sizeof(EntityUniqueId)};
        auto const integral_biases_bytes{elements_to_move * sizeof(std::uint32_t)};
        auto const locations_xs_bytes{elements_to_move * sizeof(float)};
        auto const teams_bytes{elements_to_move * sizeof(Team)};
        auto const laser_cooldowns_bytes{elements_to_move * sizeof(std::int16_t)};
        auto const laser_damages_bytes{elements_to_move * sizeof(std::int32_t)};
        auto const health_indices_bytes{elements_to_move * sizeof(HealthIndex)};
        std::memcpy(columns.entity_ids + index, columns.entity_ids + source, entity_ids_bytes);
        std::memcpy(columns.integral_biases + index,
                    columns.integral_biases + source,
                    integral_biases_bytes);
        std::memcpy(
            columns.locations_xs + index, columns.locations_xs + source, locations_xs_bytes);
        std::memcpy(
            columns.locations_ys + index, columns.locations_ys + source, locations_xs_bytes);
        std::memcpy(
            columns.locations_zs + index, columns.locations_zs + source, locations_xs_bytes);
        std::memcpy(columns.fire_point_locations_xs + index,
                    columns.fire_point_locations_xs + source,
                    locations_xs_bytes);
        std::memcpy(columns.fire_point_locations_ys + index,
                    columns.fire_point_locations_ys + source,
                    locations_xs_bytes);
        std::memcpy(columns.fire_point_locations_zs + index,
                    columns.fire_point_locations_zs + source,
                    locations_xs_bytes);
        std::memcpy(columns.rotations_pitches + index,
                    columns.rotations_pitches + source,
                    locations_xs_bytes);
        std::memcpy(
            columns.rotations_yaws + index, columns.rotations_yaws + source, locations_xs_bytes);
        std::memcpy(
            columns.rotations_rolls + index, columns.rotations_rolls + source, locations_xs_bytes);
        std::memcpy(columns.teams + index, columns.teams + source, teams_bytes);
        std::memcpy(columns.laser_cooldowns + index,
                    columns.laser_cooldowns + source,
                    laser_cooldowns_bytes);
        std::memcpy(
            columns.laser_damages + index, columns.laser_damages + source, laser_damages_bytes);
        std::memcpy(columns.target_refresh_countdowns_periods + index,
                    columns.target_refresh_countdowns_periods + source,
                    laser_cooldowns_bytes);
        std::memcpy(columns.target_refresh_countdowns_remaining_ticks + index,
                    columns.target_refresh_countdowns_remaining_ticks + source,
                    laser_cooldowns_bytes);
        std::memcpy(columns.target_ids + index, columns.target_ids + source, entity_ids_bytes);
        std::memcpy(columns.target_locations_xs + index,
                    columns.target_locations_xs + source,
                    locations_xs_bytes);
        std::memcpy(columns.target_locations_ys + index,
                    columns.target_locations_ys + source,
                    locations_xs_bytes);
        std::memcpy(columns.target_locations_zs + index,
                    columns.target_locations_zs + source,
                    locations_xs_bytes);
        std::memcpy(columns.target_velocities_xs + index,
                    columns.target_velocities_xs + source,
                    locations_xs_bytes);
        std::memcpy(columns.target_velocities_ys + index,
                    columns.target_velocities_ys + source,
                    locations_xs_bytes);
        std::memcpy(columns.target_velocities_zs + index,
                    columns.target_velocities_zs + source,
                    locations_xs_bytes);
        std::memcpy(
            columns.health_indices + index, columns.health_indices + source, health_indices_bytes);
    }
    void swap_remove_indices(std::span<size_type const> indices) {
        auto const columns{get_data()};
        ml::soa_storage_detail::for_each_removal_run(
            num_,
            indices,
            ml::native_soa::require,
            [&](size_type index, size_type source, size_type count) {
                copy_columns(columns, index, source, count);
            });
    }
    auto ordinary_source_aliases_storage(TurretEntityDataConstView const& source) const noexcept
        -> bool {
        if (data_ == nullptr) {
            return false;
        }
        auto const allocation_begin{reinterpret_cast<std::uintptr_t>(data_)};
        auto const allocation_end{allocation_begin + layout_bytes(capacity_blocks())};
        auto const aliases = [allocation_begin, allocation_end](auto const* pointer) noexcept {
            auto const address{reinterpret_cast<std::uintptr_t>(pointer)};
            return address >= allocation_begin && address < allocation_end;
        };
        return aliases(source.entity_ids.data()) || aliases(source.integral_biases.data()) ||
               aliases(source.locations.xs) || aliases(source.locations.ys) ||
               aliases(source.locations.zs) || aliases(source.fire_point_locations.xs) ||
               aliases(source.fire_point_locations.ys) || aliases(source.fire_point_locations.zs) ||
               aliases(source.rotations.pitches.data()) || aliases(source.rotations.yaws.data()) ||
               aliases(source.rotations.rolls.data()) || aliases(source.teams.data()) ||
               aliases(source.laser_cooldowns.data()) || aliases(source.laser_damages.data()) ||
               aliases(source.target_refresh_countdowns_periods.data()) ||
               aliases(source.target_refresh_countdowns_remaining_ticks.data()) ||
               aliases(source.target_ids.data()) || aliases(source.target_locations.xs) ||
               aliases(source.target_locations.ys) || aliases(source.target_locations.zs) ||
               aliases(source.target_velocities.xs) || aliases(source.target_velocities.ys) ||
               aliases(source.target_velocities.zs) || aliases(source.health_indices.data());
    }
    template <typename Columns>
    void append_columns(Columns const& source, size_type first, size_type count) {
        auto const destination{get_data(first)};
        auto const elements_to_copy{static_cast<byte_size_type>(count)};
        auto const entity_ids_bytes{elements_to_copy * sizeof(EntityUniqueId)};
        auto const integral_biases_bytes{elements_to_copy * sizeof(std::uint32_t)};
        auto const locations_xs_bytes{elements_to_copy * sizeof(float)};
        auto const teams_bytes{elements_to_copy * sizeof(Team)};
        auto const laser_cooldowns_bytes{elements_to_copy * sizeof(std::int16_t)};
        auto const laser_damages_bytes{elements_to_copy * sizeof(std::int32_t)};
        auto const health_indices_bytes{elements_to_copy * sizeof(HealthIndex)};
        std::memcpy(destination.entity_ids, source.entity_ids.data(), entity_ids_bytes);
        std::memcpy(
            destination.integral_biases, source.integral_biases.data(), integral_biases_bytes);
        std::memcpy(destination.locations_xs, source.locations.xs, locations_xs_bytes);
        std::memcpy(destination.locations_ys, source.locations.ys, locations_xs_bytes);
        std::memcpy(destination.locations_zs, source.locations.zs, locations_xs_bytes);
        std::memcpy(destination.fire_point_locations_xs,
                    source.fire_point_locations.xs,
                    locations_xs_bytes);
        std::memcpy(destination.fire_point_locations_ys,
                    source.fire_point_locations.ys,
                    locations_xs_bytes);
        std::memcpy(destination.fire_point_locations_zs,
                    source.fire_point_locations.zs,
                    locations_xs_bytes);
        std::memcpy(
            destination.rotations_pitches, source.rotations.pitches.data(), locations_xs_bytes);
        std::memcpy(destination.rotations_yaws, source.rotations.yaws.data(), locations_xs_bytes);
        std::memcpy(destination.rotations_rolls, source.rotations.rolls.data(), locations_xs_bytes);
        std::memcpy(destination.teams, source.teams.data(), teams_bytes);
        std::memcpy(
            destination.laser_cooldowns, source.laser_cooldowns.data(), laser_cooldowns_bytes);
        std::memcpy(destination.laser_damages, source.laser_damages.data(), laser_damages_bytes);
        std::memcpy(destination.target_refresh_countdowns_periods,
                    source.target_refresh_countdowns_periods.data(),
                    laser_cooldowns_bytes);
        std::memcpy(destination.target_refresh_countdowns_remaining_ticks,
                    source.target_refresh_countdowns_remaining_ticks.data(),
                    laser_cooldowns_bytes);
        std::memcpy(destination.target_ids, source.target_ids.data(), entity_ids_bytes);
        std::memcpy(
            destination.target_locations_xs, source.target_locations.xs, locations_xs_bytes);
        std::memcpy(
            destination.target_locations_ys, source.target_locations.ys, locations_xs_bytes);
        std::memcpy(
            destination.target_locations_zs, source.target_locations.zs, locations_xs_bytes);
        std::memcpy(
            destination.target_velocities_xs, source.target_velocities.xs, locations_xs_bytes);
        std::memcpy(
            destination.target_velocities_ys, source.target_velocities.ys, locations_xs_bytes);
        std::memcpy(
            destination.target_velocities_zs, source.target_velocities.zs, locations_xs_bytes);
        std::memcpy(destination.health_indices, source.health_indices.data(), health_indices_bytes);
    }
    void reallocate(size_type const new_capacity) {
        auto* const new_data{ml::native_soa::allocate(
            layout_bytes(static_cast<byte_size_type>(new_capacity / capacity_granularity)),
            static_cast<std::uint32_t>(allocation_alignment))};
        if (num_ > 0) {
            auto const old_blocks{capacity_blocks()};
            auto const new_blocks{static_cast<byte_size_type>(new_capacity / capacity_granularity)};
            auto const source{
                make_data_unchecked(static_cast<std::byte const*>(data_), old_blocks)};
            auto const destination{make_data_unchecked(new_data, new_blocks)};
            auto const live_count{static_cast<byte_size_type>(num_)};
            auto const entity_ids_bytes{live_count * sizeof(EntityUniqueId)};
            auto const integral_biases_bytes{live_count * sizeof(std::uint32_t)};
            auto const locations_xs_bytes{live_count * sizeof(float)};
            auto const teams_bytes{live_count * sizeof(Team)};
            auto const laser_cooldowns_bytes{live_count * sizeof(std::int16_t)};
            auto const laser_damages_bytes{live_count * sizeof(std::int32_t)};
            auto const health_indices_bytes{live_count * sizeof(HealthIndex)};
            std::memcpy(destination.entity_ids, source.entity_ids, entity_ids_bytes);
            std::memcpy(destination.integral_biases, source.integral_biases, integral_biases_bytes);
            std::memcpy(destination.locations_xs, source.locations_xs, locations_xs_bytes);
            std::memcpy(destination.locations_ys, source.locations_ys, locations_xs_bytes);
            std::memcpy(destination.locations_zs, source.locations_zs, locations_xs_bytes);
            std::memcpy(destination.fire_point_locations_xs,
                        source.fire_point_locations_xs,
                        locations_xs_bytes);
            std::memcpy(destination.fire_point_locations_ys,
                        source.fire_point_locations_ys,
                        locations_xs_bytes);
            std::memcpy(destination.fire_point_locations_zs,
                        source.fire_point_locations_zs,
                        locations_xs_bytes);
            std::memcpy(
                destination.rotations_pitches, source.rotations_pitches, locations_xs_bytes);
            std::memcpy(destination.rotations_yaws, source.rotations_yaws, locations_xs_bytes);
            std::memcpy(destination.rotations_rolls, source.rotations_rolls, locations_xs_bytes);
            std::memcpy(destination.teams, source.teams, teams_bytes);
            std::memcpy(destination.laser_cooldowns, source.laser_cooldowns, laser_cooldowns_bytes);
            std::memcpy(destination.laser_damages, source.laser_damages, laser_damages_bytes);
            std::memcpy(destination.target_refresh_countdowns_periods,
                        source.target_refresh_countdowns_periods,
                        laser_cooldowns_bytes);
            std::memcpy(destination.target_refresh_countdowns_remaining_ticks,
                        source.target_refresh_countdowns_remaining_ticks,
                        laser_cooldowns_bytes);
            std::memcpy(destination.target_ids, source.target_ids, entity_ids_bytes);
            std::memcpy(
                destination.target_locations_xs, source.target_locations_xs, locations_xs_bytes);
            std::memcpy(
                destination.target_locations_ys, source.target_locations_ys, locations_xs_bytes);
            std::memcpy(
                destination.target_locations_zs, source.target_locations_zs, locations_xs_bytes);
            std::memcpy(
                destination.target_velocities_xs, source.target_velocities_xs, locations_xs_bytes);
            std::memcpy(
                destination.target_velocities_ys, source.target_velocities_ys, locations_xs_bytes);
            std::memcpy(
                destination.target_velocities_zs, source.target_velocities_zs, locations_xs_bytes);
            std::memcpy(destination.health_indices, source.health_indices, health_indices_bytes);
        }
        ml::native_soa::free(data_, allocation_alignment);
        data_ = new_data;
        capacity_ = new_capacity;
    }
};

struct TurretEntityDataSingleConstView : ml::native_soa::CompactViewState<true> {
    using Base = ml::native_soa::CompactViewState<true>;
    using Base::Base;
    using View = TurretEntityDataSingleView;
    using ConstView = TurretEntityDataSingleConstView;
    TurretEntityDataSingleConstView() = default;
    TurretEntityDataSingleConstView(TurretEntityDataSingleView const& other);
    auto get_const_view() const -> ConstView { return *this; }
    auto get_const_view(size_type offset, size_type count) const -> ConstView {
        return slice(offset, count);
    }
    auto entity_ids() const -> std::span<EntityUniqueId const> {
        return {column_data<EntityUniqueId>(
                    TurretEntityDataSingleLayout::EntityIds.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto integral_biases() const -> std::span<std::uint32_t const> {
        return {column_data<std::uint32_t>(
                    TurretEntityDataSingleLayout::IntegralBiases.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto view_locations() const -> ml::native_soa::Vector3ConstView<float> {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        auto const first{TurretEntityDataSingleLayout::LocationsXs.offset(blocks)};
        auto const stride{TurretEntityDataSingleLayout::LocationsYs.offset(blocks) - first};
        return {column_data_unchecked<float>(first), stride, count_};
    }
    auto view_fire_point_locations() const -> ml::native_soa::Vector3ConstView<float> {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        auto const first{TurretEntityDataSingleLayout::FirePointLocationsXs.offset(blocks)};
        auto const stride{TurretEntityDataSingleLayout::FirePointLocationsYs.offset(blocks) -
                          first};
        return {column_data_unchecked<float>(first), stride, count_};
    }
    auto view_rotations() const -> Rotators3fConstView {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        return Rotators3fConstView{
            {column_data_unchecked<float>(
                 TurretEntityDataSingleLayout::RotationsPitches.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<float>(
                 TurretEntityDataSingleLayout::RotationsYaws.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<float>(
                 TurretEntityDataSingleLayout::RotationsRolls.offset(blocks)),
             static_cast<std::size_t>(count_)}};
    }
    auto teams() const -> std::span<Team const> {
        return {column_data<Team>(TurretEntityDataSingleLayout::Teams.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto laser_cooldowns() const -> std::span<std::int16_t const> {
        return {column_data<std::int16_t>(
                    TurretEntityDataSingleLayout::LaserCooldowns.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto laser_damages() const -> std::span<std::int32_t const> {
        return {column_data<std::int32_t>(
                    TurretEntityDataSingleLayout::LaserDamages.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto target_refresh_countdowns_periods() const -> std::span<std::int16_t const> {
        return {column_data<std::int16_t>(
                    TurretEntityDataSingleLayout::TargetRefreshCountdownsPeriods.offset(
                        capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto target_refresh_countdowns_remaining_ticks() const -> std::span<std::int16_t const> {
        return {column_data<std::int16_t>(
                    TurretEntityDataSingleLayout::TargetRefreshCountdownsRemainingTicks.offset(
                        capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto target_ids() const -> std::span<EntityUniqueId const> {
        return {column_data<EntityUniqueId>(
                    TurretEntityDataSingleLayout::TargetIds.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto view_target_locations() const -> ml::native_soa::Vector3ConstView<float> {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        auto const first{TurretEntityDataSingleLayout::TargetLocationsXs.offset(blocks)};
        auto const stride{TurretEntityDataSingleLayout::TargetLocationsYs.offset(blocks) - first};
        return {column_data_unchecked<float>(first), stride, count_};
    }
    auto view_target_velocities() const -> ml::native_soa::Vector3ConstView<float> {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        auto const first{TurretEntityDataSingleLayout::TargetVelocitiesXs.offset(blocks)};
        auto const stride{TurretEntityDataSingleLayout::TargetVelocitiesYs.offset(blocks) - first};
        return {column_data_unchecked<float>(first), stride, count_};
    }
    auto health_indices() const -> std::span<HealthIndex const> {
        return {column_data<HealthIndex>(
                    TurretEntityDataSingleLayout::HealthIndices.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto columns() const -> TurretEntityDataConstView {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        return TurretEntityDataConstView{
            {column_data_unchecked<EntityUniqueId>(
                 TurretEntityDataSingleLayout::EntityIds.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<std::uint32_t>(
                 TurretEntityDataSingleLayout::IntegralBiases.offset(blocks)),
             static_cast<std::size_t>(count_)},
            Vectors3fConstView{{column_data_unchecked<float>(
                                    TurretEntityDataSingleLayout::LocationsXs.offset(blocks)),
                                static_cast<std::size_t>(count_)},
                               {column_data_unchecked<float>(
                                    TurretEntityDataSingleLayout::LocationsYs.offset(blocks)),
                                static_cast<std::size_t>(count_)},
                               {column_data_unchecked<float>(
                                    TurretEntityDataSingleLayout::LocationsZs.offset(blocks)),
                                static_cast<std::size_t>(count_)}},
            Vectors3fConstView{
                {column_data_unchecked<float>(
                     TurretEntityDataSingleLayout::FirePointLocationsXs.offset(blocks)),
                 static_cast<std::size_t>(count_)},
                {column_data_unchecked<float>(
                     TurretEntityDataSingleLayout::FirePointLocationsYs.offset(blocks)),
                 static_cast<std::size_t>(count_)},
                {column_data_unchecked<float>(
                     TurretEntityDataSingleLayout::FirePointLocationsZs.offset(blocks)),
                 static_cast<std::size_t>(count_)}},
            Rotators3fConstView{{column_data_unchecked<float>(
                                     TurretEntityDataSingleLayout::RotationsPitches.offset(blocks)),
                                 static_cast<std::size_t>(count_)},
                                {column_data_unchecked<float>(
                                     TurretEntityDataSingleLayout::RotationsYaws.offset(blocks)),
                                 static_cast<std::size_t>(count_)},
                                {column_data_unchecked<float>(
                                     TurretEntityDataSingleLayout::RotationsRolls.offset(blocks)),
                                 static_cast<std::size_t>(count_)}},
            {column_data_unchecked<Team>(TurretEntityDataSingleLayout::Teams.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<std::int16_t>(
                 TurretEntityDataSingleLayout::LaserCooldowns.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<std::int32_t>(
                 TurretEntityDataSingleLayout::LaserDamages.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<std::int16_t>(
                 TurretEntityDataSingleLayout::TargetRefreshCountdownsPeriods.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<std::int16_t>(
                 TurretEntityDataSingleLayout::TargetRefreshCountdownsRemainingTicks.offset(
                     blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<EntityUniqueId>(
                 TurretEntityDataSingleLayout::TargetIds.offset(blocks)),
             static_cast<std::size_t>(count_)},
            Vectors3fConstView{{column_data_unchecked<float>(
                                    TurretEntityDataSingleLayout::TargetLocationsXs.offset(blocks)),
                                static_cast<std::size_t>(count_)},
                               {column_data_unchecked<float>(
                                    TurretEntityDataSingleLayout::TargetLocationsYs.offset(blocks)),
                                static_cast<std::size_t>(count_)},
                               {column_data_unchecked<float>(
                                    TurretEntityDataSingleLayout::TargetLocationsZs.offset(blocks)),
                                static_cast<std::size_t>(count_)}},
            Vectors3fConstView{
                {column_data_unchecked<float>(
                     TurretEntityDataSingleLayout::TargetVelocitiesXs.offset(blocks)),
                 static_cast<std::size_t>(count_)},
                {column_data_unchecked<float>(
                     TurretEntityDataSingleLayout::TargetVelocitiesYs.offset(blocks)),
                 static_cast<std::size_t>(count_)},
                {column_data_unchecked<float>(
                     TurretEntityDataSingleLayout::TargetVelocitiesZs.offset(blocks)),
                 static_cast<std::size_t>(count_)}},
            {column_data_unchecked<HealthIndex>(
                 TurretEntityDataSingleLayout::HealthIndices.offset(blocks)),
             static_cast<std::size_t>(count_)}};
    }
    template <typename Func>
    void each_column(Func&& func) const {
        columns().each_column(std::forward<Func>(func));
    }
};
static_assert(sizeof(TurretEntityDataSingleConstView) == 16);
static_assert(std::is_trivially_copyable_v<TurretEntityDataSingleConstView>);
struct TurretEntityDataSingleView : ml::native_soa::CompactViewState<false> {
    using Base = ml::native_soa::CompactViewState<false>;
    using Base::Base;
    using View = TurretEntityDataSingleView;
    using ConstView = TurretEntityDataSingleConstView;
    TurretEntityDataSingleView() = default;
    auto get_const_view() const -> ConstView { return *this; }
    auto get_const_view(size_type offset, size_type count) const -> ConstView {
        return slice(offset, count);
    }
    auto entity_ids() const -> std::span<EntityUniqueId> {
        return {column_data<EntityUniqueId>(
                    TurretEntityDataSingleLayout::EntityIds.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto integral_biases() const -> std::span<std::uint32_t> {
        return {column_data<std::uint32_t>(
                    TurretEntityDataSingleLayout::IntegralBiases.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto view_locations() const -> ml::native_soa::Vector3View<float> {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        auto const first{TurretEntityDataSingleLayout::LocationsXs.offset(blocks)};
        auto const stride{TurretEntityDataSingleLayout::LocationsYs.offset(blocks) - first};
        return {column_data_unchecked<float>(first), stride, count_};
    }
    auto view_fire_point_locations() const -> ml::native_soa::Vector3View<float> {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        auto const first{TurretEntityDataSingleLayout::FirePointLocationsXs.offset(blocks)};
        auto const stride{TurretEntityDataSingleLayout::FirePointLocationsYs.offset(blocks) -
                          first};
        return {column_data_unchecked<float>(first), stride, count_};
    }
    auto view_rotations() const -> Rotators3fView {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        return Rotators3fView{{column_data_unchecked<float>(
                                   TurretEntityDataSingleLayout::RotationsPitches.offset(blocks)),
                               static_cast<std::size_t>(count_)},
                              {column_data_unchecked<float>(
                                   TurretEntityDataSingleLayout::RotationsYaws.offset(blocks)),
                               static_cast<std::size_t>(count_)},
                              {column_data_unchecked<float>(
                                   TurretEntityDataSingleLayout::RotationsRolls.offset(blocks)),
                               static_cast<std::size_t>(count_)}};
    }
    auto teams() const -> std::span<Team> {
        return {column_data<Team>(TurretEntityDataSingleLayout::Teams.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto laser_cooldowns() const -> std::span<std::int16_t> {
        return {column_data<std::int16_t>(
                    TurretEntityDataSingleLayout::LaserCooldowns.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto laser_damages() const -> std::span<std::int32_t> {
        return {column_data<std::int32_t>(
                    TurretEntityDataSingleLayout::LaserDamages.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto target_refresh_countdowns_periods() const -> std::span<std::int16_t> {
        return {column_data<std::int16_t>(
                    TurretEntityDataSingleLayout::TargetRefreshCountdownsPeriods.offset(
                        capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto target_refresh_countdowns_remaining_ticks() const -> std::span<std::int16_t> {
        return {column_data<std::int16_t>(
                    TurretEntityDataSingleLayout::TargetRefreshCountdownsRemainingTicks.offset(
                        capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto target_ids() const -> std::span<EntityUniqueId> {
        return {column_data<EntityUniqueId>(
                    TurretEntityDataSingleLayout::TargetIds.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto view_target_locations() const -> ml::native_soa::Vector3View<float> {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        auto const first{TurretEntityDataSingleLayout::TargetLocationsXs.offset(blocks)};
        auto const stride{TurretEntityDataSingleLayout::TargetLocationsYs.offset(blocks) - first};
        return {column_data_unchecked<float>(first), stride, count_};
    }
    auto view_target_velocities() const -> ml::native_soa::Vector3View<float> {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        auto const first{TurretEntityDataSingleLayout::TargetVelocitiesXs.offset(blocks)};
        auto const stride{TurretEntityDataSingleLayout::TargetVelocitiesYs.offset(blocks) - first};
        return {column_data_unchecked<float>(first), stride, count_};
    }
    auto health_indices() const -> std::span<HealthIndex> {
        return {column_data<HealthIndex>(
                    TurretEntityDataSingleLayout::HealthIndices.offset(capacity_blocks())),
                static_cast<std::size_t>(count_)};
    }
    auto columns() const -> TurretEntityDataView {
        validate();
        if (!state_ || !state_->data_) {
            return {};
        }
        auto const blocks{capacity_blocks()};
        return TurretEntityDataView{
            {column_data_unchecked<EntityUniqueId>(
                 TurretEntityDataSingleLayout::EntityIds.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<std::uint32_t>(
                 TurretEntityDataSingleLayout::IntegralBiases.offset(blocks)),
             static_cast<std::size_t>(count_)},
            Vectors3fView{{column_data_unchecked<float>(
                               TurretEntityDataSingleLayout::LocationsXs.offset(blocks)),
                           static_cast<std::size_t>(count_)},
                          {column_data_unchecked<float>(
                               TurretEntityDataSingleLayout::LocationsYs.offset(blocks)),
                           static_cast<std::size_t>(count_)},
                          {column_data_unchecked<float>(
                               TurretEntityDataSingleLayout::LocationsZs.offset(blocks)),
                           static_cast<std::size_t>(count_)}},
            Vectors3fView{{column_data_unchecked<float>(
                               TurretEntityDataSingleLayout::FirePointLocationsXs.offset(blocks)),
                           static_cast<std::size_t>(count_)},
                          {column_data_unchecked<float>(
                               TurretEntityDataSingleLayout::FirePointLocationsYs.offset(blocks)),
                           static_cast<std::size_t>(count_)},
                          {column_data_unchecked<float>(
                               TurretEntityDataSingleLayout::FirePointLocationsZs.offset(blocks)),
                           static_cast<std::size_t>(count_)}},
            Rotators3fView{{column_data_unchecked<float>(
                                TurretEntityDataSingleLayout::RotationsPitches.offset(blocks)),
                            static_cast<std::size_t>(count_)},
                           {column_data_unchecked<float>(
                                TurretEntityDataSingleLayout::RotationsYaws.offset(blocks)),
                            static_cast<std::size_t>(count_)},
                           {column_data_unchecked<float>(
                                TurretEntityDataSingleLayout::RotationsRolls.offset(blocks)),
                            static_cast<std::size_t>(count_)}},
            {column_data_unchecked<Team>(TurretEntityDataSingleLayout::Teams.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<std::int16_t>(
                 TurretEntityDataSingleLayout::LaserCooldowns.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<std::int32_t>(
                 TurretEntityDataSingleLayout::LaserDamages.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<std::int16_t>(
                 TurretEntityDataSingleLayout::TargetRefreshCountdownsPeriods.offset(blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<std::int16_t>(
                 TurretEntityDataSingleLayout::TargetRefreshCountdownsRemainingTicks.offset(
                     blocks)),
             static_cast<std::size_t>(count_)},
            {column_data_unchecked<EntityUniqueId>(
                 TurretEntityDataSingleLayout::TargetIds.offset(blocks)),
             static_cast<std::size_t>(count_)},
            Vectors3fView{{column_data_unchecked<float>(
                               TurretEntityDataSingleLayout::TargetLocationsXs.offset(blocks)),
                           static_cast<std::size_t>(count_)},
                          {column_data_unchecked<float>(
                               TurretEntityDataSingleLayout::TargetLocationsYs.offset(blocks)),
                           static_cast<std::size_t>(count_)},
                          {column_data_unchecked<float>(
                               TurretEntityDataSingleLayout::TargetLocationsZs.offset(blocks)),
                           static_cast<std::size_t>(count_)}},
            Vectors3fView{{column_data_unchecked<float>(
                               TurretEntityDataSingleLayout::TargetVelocitiesXs.offset(blocks)),
                           static_cast<std::size_t>(count_)},
                          {column_data_unchecked<float>(
                               TurretEntityDataSingleLayout::TargetVelocitiesYs.offset(blocks)),
                           static_cast<std::size_t>(count_)},
                          {column_data_unchecked<float>(
                               TurretEntityDataSingleLayout::TargetVelocitiesZs.offset(blocks)),
                           static_cast<std::size_t>(count_)}},
            {column_data_unchecked<HealthIndex>(
                 TurretEntityDataSingleLayout::HealthIndices.offset(blocks)),
             static_cast<std::size_t>(count_)}};
    }
    template <typename Func>
    void each_column(Func&& func) const {
        columns().each_column(std::forward<Func>(func));
    }
};
static_assert(sizeof(TurretEntityDataSingleView) == 16);
static_assert(std::is_trivially_copyable_v<TurretEntityDataSingleView>);
inline TurretEntityDataSingleConstView::TurretEntityDataSingleConstView(
    TurretEntityDataSingleView const& other)
    : Base{other} {}
struct SingleAllocationTurretEntityData : SingleAllocationTurretEntityDataStorage {
    SingleAllocationTurretEntityData() noexcept = default;
    SingleAllocationTurretEntityData(SingleAllocationTurretEntityData const&) = delete;
    auto operator=(SingleAllocationTurretEntityData const&)
        -> SingleAllocationTurretEntityData& = delete;
    SingleAllocationTurretEntityData(SingleAllocationTurretEntityData&&) noexcept = default;
    auto operator=(SingleAllocationTurretEntityData&&) noexcept
        -> SingleAllocationTurretEntityData& = default;
    auto get_view() & -> View { return {this, 0, num()}; }
    auto get_view(size_type offset, size_type count) & -> View { return {this, offset, count}; }
    auto slice(size_type offset, size_type count) & -> View { return get_view(offset, count); }
    auto left(size_type count) & -> View { return get_view().left(count); }
    auto right(size_type count) & -> View { return get_view().right(count); }
    auto get_view() && -> View = delete;
    auto get_view(size_type, size_type) && -> View = delete;
    auto slice(size_type, size_type) && -> View = delete;
    auto left(size_type) && -> View = delete;
    auto right(size_type) && -> View = delete;
    auto get_view() const& -> ConstView { return {this, 0, num()}; }
    auto get_view(size_type offset, size_type count) const& -> ConstView {
        return {this, offset, count};
    }
    auto slice(size_type offset, size_type count) const& -> ConstView {
        return get_view(offset, count);
    }
    auto left(size_type count) const& -> ConstView { return get_view().left(count); }
    auto right(size_type count) const& -> ConstView { return get_view().right(count); }
    auto get_view() const&& -> ConstView = delete;
    auto get_view(size_type, size_type) const&& -> ConstView = delete;
    auto slice(size_type, size_type) const&& -> ConstView = delete;
    auto left(size_type) const&& -> ConstView = delete;
    auto right(size_type) const&& -> ConstView = delete;
    auto get_const_view() const& -> ConstView { return get_view(); }
    auto get_const_view(size_type offset, size_type count) const& -> ConstView {
        return get_view(offset, count);
    }
    auto get_const_view() const&& -> ConstView = delete;
    auto get_const_view(size_type, size_type) const&& -> ConstView = delete;
};
} // namespace ioj::sim
