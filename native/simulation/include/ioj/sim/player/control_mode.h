// This file is autogenerated. Do not edit by hand.
// Edit lispb/schema and regenerate it instead.

#pragma once

#include <array>

#include <cstdint>

#include <cstddef>

#include <optional>

#include <string_view>

#include "sandbox/core/enum_traits.h"

namespace ioj::sim {
enum class SpaceShipControlMode : std::uint8_t {
    Velocity = 0,
    Power = 1,
    COUNT = 2,
};

[[nodiscard]] constexpr auto to_string(SpaceShipControlMode const value) noexcept
    -> std::string_view {
    switch (value) {
        case SpaceShipControlMode::Velocity: {
            return "Velocity";
        }
        case SpaceShipControlMode::Power: {
            return "Power";
        }
        case SpaceShipControlMode::COUNT: {
            return "COUNT";
        }
    }

    return "<invalid SpaceShipControlMode>";
}

[[nodiscard]] constexpr auto
    try_parse_space_ship_control_mode(std::string_view const value) noexcept
    -> std::optional<SpaceShipControlMode> {
    if (value == "Velocity") {
        return SpaceShipControlMode::Velocity;
    }
    if (value == "Power") {
        return SpaceShipControlMode::Power;
    }
    if (value == "COUNT") {
        return SpaceShipControlMode::COUNT;
    }

    return std::nullopt;
}

[[nodiscard]] constexpr auto to_display_string(SpaceShipControlMode const value) noexcept
    -> std::string_view {
    switch (value) {
        case SpaceShipControlMode::Velocity: {
            return "Velocity";
        }
        case SpaceShipControlMode::Power: {
            return "Power";
        }
        case SpaceShipControlMode::COUNT: {
            return "Count";
        }
    }

    return "<invalid SpaceShipControlMode>";
}

[[nodiscard]] constexpr auto to_serialized_string(SpaceShipControlMode const value) noexcept
    -> std::string_view {
    switch (value) {
        case SpaceShipControlMode::Velocity: {
            return "velocity";
        }
        case SpaceShipControlMode::Power: {
            return "power";
        }
        case SpaceShipControlMode::COUNT: {
            return "count";
        }
        default: {
            return "<invalid SpaceShipControlMode>";
        }
    }
}

[[nodiscard]] constexpr auto
    try_parse_serialized_space_ship_control_mode(std::string_view const value) noexcept
    -> std::optional<SpaceShipControlMode> {
    if (value == "velocity") {
        return SpaceShipControlMode::Velocity;
    }
    if (value == "power") {
        return SpaceShipControlMode::Power;
    }
    if (value == "count") {
        return SpaceShipControlMode::COUNT;
    }

    return std::nullopt;
}

} // namespace ioj::sim
namespace ml {
template <>
struct EnumTraits<::ioj::sim::SpaceShipControlMode> {
    inline static constexpr std::array values{
        ::ioj::sim::SpaceShipControlMode::Velocity,
        ::ioj::sim::SpaceShipControlMode::Power,
    };
    inline static constexpr std::array names{
        std::string_view{"Velocity"},
        std::string_view{"Power"},
    };
    inline static constexpr std::size_t count{values.size()};
};

} // namespace ml
