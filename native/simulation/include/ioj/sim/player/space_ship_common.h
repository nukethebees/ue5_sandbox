// This file is autogenerated. Do not edit by hand.
// Edit lispb/schema and regenerate it instead.

#pragma once

#include <array>

#include <cstdint>

#include <cstddef>

#include <optional>

#include <string_view>

#include "sandbox/core/enum_traits.h"

namespace ioj::sim::player {
enum class BoostBrakeState : std::uint8_t {
    None = 0,
    Boost = 1,
    Brake = 2,
};

[[nodiscard]] constexpr auto to_string(BoostBrakeState const value) noexcept -> std::string_view {
    switch (value) {
        case BoostBrakeState::None: {
            return "None";
        }
        case BoostBrakeState::Boost: {
            return "Boost";
        }
        case BoostBrakeState::Brake: {
            return "Brake";
        }
    }

    return "<invalid BoostBrakeState>";
}

[[nodiscard]] constexpr auto try_parse_boost_brake_state(std::string_view const value) noexcept
    -> std::optional<BoostBrakeState> {
    if (value == "None") {
        return BoostBrakeState::None;
    }
    if (value == "Boost") {
        return BoostBrakeState::Boost;
    }
    if (value == "Brake") {
        return BoostBrakeState::Brake;
    }

    return std::nullopt;
}

} // namespace ioj::sim::player
namespace ml {
template <>
struct EnumTraits<::ioj::sim::player::BoostBrakeState> {
    inline static constexpr std::array values{
        ::ioj::sim::player::BoostBrakeState::None,
        ::ioj::sim::player::BoostBrakeState::Boost,
        ::ioj::sim::player::BoostBrakeState::Brake,
    };
    inline static constexpr std::array names{
        std::string_view{"None"},
        std::string_view{"Boost"},
        std::string_view{"Brake"},
    };
    inline static constexpr std::size_t count{values.size()};
};

} // namespace ml
