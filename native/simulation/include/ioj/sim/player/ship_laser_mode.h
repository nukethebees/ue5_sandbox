// This file is autogenerated. Do not edit by hand.
// Edit lispb/schema and regenerate it instead.

#pragma once

#include <array>

#include <cstdint>

#include <cstddef>

#include <optional>

#include <string_view>

#include "sandbox/core/enum_traits.h"

namespace ioj::sim {
enum class ShipLaserMode : std::uint8_t {
    Single = 0,
    Double = 1,
    Hyper = 2,
};

[[nodiscard]] constexpr auto to_string(ShipLaserMode const value) noexcept -> std::string_view {
    switch (value) {
        case ShipLaserMode::Single: {
            return "Single";
        }
        case ShipLaserMode::Double: {
            return "Double";
        }
        case ShipLaserMode::Hyper: {
            return "Hyper";
        }
    }

    return "<invalid ShipLaserMode>";
}

[[nodiscard]] constexpr auto try_parse_ship_laser_mode(std::string_view const value) noexcept
    -> std::optional<ShipLaserMode> {
    if (value == "Single") {
        return ShipLaserMode::Single;
    }
    if (value == "Double") {
        return ShipLaserMode::Double;
    }
    if (value == "Hyper") {
        return ShipLaserMode::Hyper;
    }

    return std::nullopt;
}

[[nodiscard]] constexpr auto to_display_string(ShipLaserMode const value) noexcept
    -> std::string_view {
    switch (value) {
        case ShipLaserMode::Single: {
            return "Single";
        }
        case ShipLaserMode::Double: {
            return "Double";
        }
        case ShipLaserMode::Hyper: {
            return "Hyper";
        }
    }

    return "<invalid ShipLaserMode>";
}

[[nodiscard]] constexpr auto to_serialized_string(ShipLaserMode const value) noexcept
    -> std::string_view {
    switch (value) {
        case ShipLaserMode::Single: {
            return "single";
        }
        case ShipLaserMode::Double: {
            return "double";
        }
        case ShipLaserMode::Hyper: {
            return "hyper";
        }
        default: {
            return "<invalid ShipLaserMode>";
        }
    }
}

[[nodiscard]] constexpr auto
    try_parse_serialized_ship_laser_mode(std::string_view const value) noexcept
    -> std::optional<ShipLaserMode> {
    if (value == "single") {
        return ShipLaserMode::Single;
    }
    if (value == "double") {
        return ShipLaserMode::Double;
    }
    if (value == "hyper") {
        return ShipLaserMode::Hyper;
    }

    return std::nullopt;
}

} // namespace ioj::sim
namespace ml {
template <>
struct EnumTraits<::ioj::sim::ShipLaserMode> {
    inline static constexpr std::array values{
        ::ioj::sim::ShipLaserMode::Single,
        ::ioj::sim::ShipLaserMode::Double,
        ::ioj::sim::ShipLaserMode::Hyper,
    };
    inline static constexpr std::array names{
        std::string_view{"Single"},
        std::string_view{"Double"},
        std::string_view{"Hyper"},
    };
    inline static constexpr std::size_t count{values.size()};
};

} // namespace ml
