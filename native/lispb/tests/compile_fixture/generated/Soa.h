// This file is autogenerated. Do not edit by hand.
// Edit lispb/schema and regenerate it instead.

#pragma once

#include "CoreMinimal.h"
#include "Containers/AllowShrinking.h"
#include "Containers/Array.h"
#include "Containers/ArrayView.h"
#include "Project/CountingAllocator.h"
#include "Project/RestrictedLeaf.h"
#include "Project/Row.h"
#include "Project/Tracked.h"
#include "sandbox/core/fixed_storage.h"
#include "sandbox/core/single_allocation/removal.h"
#include "sandbox/core/soa_concepts.h"
#include "SandboxCore/container_ops.h"
#include "SandboxCore/mimalloc_storage_allocator.h"
#include "SandboxCore/single_allocation/operations.h"
#include "SandboxCore/single_allocation/vector_views.h"
#include "SandboxCore/soa_storage_ops.h"
#include "Templates/MemoryOps.h"
#include "Templates/UnrealTemplate.h"

#include <cstdint>
#include <memory>
#include <type_traits>
#include <utility>

namespace codegen_compile_fixture {
struct ApiOwner;

struct ApiPair {
    float x;

    float y;
};

enum class ApiField : uint8 {
    Values = 0,
    Count = static_cast<uint8>(Values) + 1,
};

struct ApiMask {
    inline static constexpr int32 field_count{static_cast<int32>(ApiField::Count)};
    static_assert(field_count <= 64, "Field mask exceeds 64 bits.");
    using storage_type = std::conditional_t<
        field_count <= 8,
        uint8,
        std::conditional_t<field_count <= 16,
                           uint16,
                           std::conditional_t<field_count <= 32, uint32, uint64>>>;

    constexpr ApiMask() noexcept = default;
    explicit constexpr ApiMask(storage_type const value) noexcept
        : value_{value} {}

    [[nodiscard]] constexpr auto value() const noexcept -> storage_type { return value_; }
    [[nodiscard]] constexpr auto is_empty() const noexcept -> bool { return value_ == 0; }
    [[nodiscard]] constexpr auto has(ApiField const field) const noexcept -> bool {
        return (value_ & bit(field)) != 0;
    }
    [[nodiscard]] static constexpr auto index(ApiField const field) noexcept -> int32 {
        return static_cast<int32>(field);
    }
    constexpr void set(ApiField const field) noexcept { value_ |= bit(field); }
    constexpr void set(ApiMask const fields) noexcept { value_ |= fields.value_; }
    constexpr void clear(ApiField const field) noexcept {
        value_ = static_cast<storage_type>(value_ & ~bit(field));
    }
  private:
    [[nodiscard]] static constexpr auto bit(ApiField const field) noexcept -> storage_type {
        return static_cast<storage_type>(uint64{1} << static_cast<uint8>(field));
    }

    storage_type value_{};
};
static_assert(sizeof(ApiMask) == sizeof(ApiMask::storage_type));
static_assert(std::is_trivially_copyable_v<ApiMask>);

struct ApiView;
struct ApiConstView;
struct ApiRowsSingleLayout {
    using size_type = int32;
    using byte_size_type = SIZE_T;

    inline static constexpr size_type capacity_granularity{
        ml::soa_storage::LayoutPolicy::capacity_granularity};
    inline static constexpr byte_size_type column_gap{ml::soa_storage::LayoutPolicy::column_gap};

    template <typename T>
    using ColLayout = ml::soa_storage::ColumnLayout<T>;
    inline static constexpr ml::soa_storage::ColumnLayoutStart LayoutStart{};

    inline static constexpr ColLayout<float> ValuesColumn{LayoutStart};
    inline static constexpr ColLayout<ApiMask> MasksColumn{ValuesColumn};
    inline static constexpr ColLayout<float> PositionsXsColumn{MasksColumn};
    inline static constexpr ColLayout<float> PositionsYsColumn{PositionsXsColumn};

    inline static constexpr byte_size_type allocation_alignment{
        PositionsYsColumn.allocation_alignment};

    // Conservative per-block bound for checked capacity arithmetic; gaps do not scale with
    // capacity.
    inline static constexpr byte_size_type capacity_block_bound{
        ml::soa_storage::capacity_block_bound(PositionsYsColumn)};
    inline static constexpr size_type max_capacity{
        ml::soa_storage::maximum_capacity(capacity_block_bound)};
    static constexpr auto layout_bytes(byte_size_type blocks) noexcept -> byte_size_type {
        return blocks == 0 ? 0 : PositionsYsColumn.data_end(blocks);
    }
    static_assert(
        allocation_alignment <= std::numeric_limits<uint32>::max(),
        "Single-allocation alignment must fit the allocator's 32-bit alignment argument.");
    static_assert(max_capacity >= capacity_granularity);
};

struct ApiView_positions;
struct ApiConstView_positions;
template <bool Const>
struct ApiView_positionsImpl : ml::soa_storage::CompactViewState<Const> {
    using Base = ml::soa_storage::CompactViewState<Const>;
    using Base::Base;
    using Base::validate;
    using size_type = typename Base::size_type;
    template <typename T>
    using Element = typename Base::template Element<T>;
    using View = ApiView_positions;
    using ConstView = ApiConstView_positions;
    ApiView_positionsImpl() = default;
    template <bool Enabled = Const>
    ApiView_positionsImpl(ApiView_positionsImpl<false> const& other)
        requires Enabled
        : Base{other} {}
  protected:
    using Base::capacity_blocks;
    using Base::column_data;
    using Base::column_data_unchecked;
    using Base::count_;
    using Base::offset_;
    using Base::state_;
  public:
    auto xs() const -> TArrayView<Element<float>> {
        return {this->template column_data<float>(
                    ApiRowsSingleLayout::PositionsXsColumn.offset(capacity_blocks())),
                count_};
    }
    auto ys() const -> TArrayView<Element<float>> {
        return {this->template column_data<float>(
                    ApiRowsSingleLayout::PositionsYsColumn.offset(capacity_blocks())),
                count_};
    }
    template <typename Func>
    auto apply_arrays(Func&& func) const -> decltype(auto) {
        auto column_0{xs()};
        auto column_1{ys()};
        return std::forward<Func>(func)(column_0, column_1);
    }
};
struct ApiConstView_positions : ApiView_positionsImpl<true> {
    using Base = ApiView_positionsImpl<true>;
    using Base::Base;
    using View = ApiView_positions;
    using ConstView = ApiConstView_positions;
    ApiConstView_positions() = default;
    ApiConstView_positions(ApiView_positions const& other);
    auto get_const_view() const -> ConstView { return *this; }
    auto get_const_view(size_type offset, size_type count) const -> ConstView {
        return slice(offset, count);
    }
    float x_at(std::int32_t index) const { return this->xs()[index]; }
    using equivalent_type = ApiPair;
    auto operator[](std::int32_t index) const -> equivalent_type {
        return equivalent_type{xs()[index], ys()[index]};
    }
};
static_assert(ml::soa_storage_detail::validate_compact_view<ApiConstView_positions>());
struct ApiView_positions : ApiView_positionsImpl<false> {
    using Base = ApiView_positionsImpl<false>;
    using Base::Base;
    using View = ApiView_positions;
    using ConstView = ApiConstView_positions;
    ApiView_positions() = default;
    auto get_const_view() const -> ConstView { return *this; }
    auto get_const_view(size_type offset, size_type count) const -> ConstView {
        return slice(offset, count);
    }
    float x_at(std::int32_t index) const { return this->xs()[index]; }
    void shift_x(float amount) {
        auto xs = this->xs();
        auto const count = num();
        for (std::int32_t i{}; i < count; ++i) {
            xs[i] += amount;
        }
    }
    using equivalent_type = ApiPair;
    auto operator[](std::int32_t index) const -> equivalent_type {
        return equivalent_type{xs()[index], ys()[index]};
    }
};
static_assert(ml::soa_storage_detail::validate_compact_view<ApiView_positions>());
inline ApiConstView_positions::ApiConstView_positions(ApiView_positions const& other)
    : Base{other} {}
template <bool Const>
struct ApiViewImpl : ml::soa_storage::CompactViewState<Const> {
    using Base = ml::soa_storage::CompactViewState<Const>;
    using Base::Base;
    using Base::validate;
    using size_type = typename Base::size_type;
    template <typename T>
    using Element = typename Base::template Element<T>;
    using View = ApiView;
    using ConstView = ApiConstView;
    ApiViewImpl() = default;
    template <bool Enabled = Const>
    ApiViewImpl(ApiViewImpl<false> const& other)
        requires Enabled
        : Base{other} {}
  protected:
    using Base::capacity_blocks;
    using Base::column_data;
    using Base::column_data_unchecked;
    using Base::count_;
    using Base::offset_;
    using Base::state_;
  public:
    auto values() const -> TArrayView<Element<float>> {
        return {this->template column_data<float>(
                    ApiRowsSingleLayout::ValuesColumn.offset(capacity_blocks())),
                count_};
    }
    auto masks() const -> TArrayView<Element<ApiMask>> {
        return {this->template column_data<ApiMask>(
                    ApiRowsSingleLayout::MasksColumn.offset(capacity_blocks())),
                count_};
    }
    auto view_positions() const
        -> std::conditional_t<Const, ApiConstView_positions, ApiView_positions> {
        return {state_, offset_, count_};
    }
    template <typename Func>
    auto apply_arrays(Func&& func) const -> decltype(auto) {
        auto column_0{values()};
        auto column_1{masks()};
        auto column_2{view_positions().xs()};
        auto column_3{view_positions().ys()};
        return std::forward<Func>(func)(column_0, column_1, column_2, column_3);
    }
};
struct COMPILE_FIXTURE_API ApiConstView : ApiViewImpl<true> {
    using Base = ApiViewImpl<true>;
    using Base::Base;
    using View = ApiView;
    using ConstView = ApiConstView;
    ApiConstView() = default;
    ApiConstView(ApiView const& other);
    auto get_const_view() const -> ConstView { return *this; }
    auto get_const_view(size_type offset, size_type count) const -> ConstView {
        return slice(offset, count);
    }
    using Value = float;
    float first_x() const;
};
static_assert(ml::soa_storage_detail::validate_compact_view<ApiConstView>());
struct COMPILE_FIXTURE_API ApiView : ApiViewImpl<false> {
    using Base = ApiViewImpl<false>;
    using Base::Base;
    using View = ApiView;
    using ConstView = ApiConstView;
    ApiView() = default;
    auto get_const_view() const -> ConstView { return *this; }
    auto get_const_view(size_type offset, size_type count) const -> ConstView {
        return slice(offset, count);
    }
    using Value = float;
    float first_x() const;
    void assign_first(float value) { this->values()[0] = value; }
};
static_assert(ml::soa_storage_detail::validate_compact_view<ApiView>());
inline ApiConstView::ApiConstView(ApiView const& other)
    : Base{other} {}
struct COMPILE_FIXTURE_API ApiOwner
    : protected ml::soa_storage::StorageState
    , private ml::soa_storage::StorageOperations {
    using Operations = ml::soa_storage::StorageOperations;
    using Operations::allocated_bytes;
    using Operations::append_from;
    using Operations::capacity;
    using Operations::copy_element;
    using Operations::copy_elements;
    using Operations::is_empty;
    using Operations::num;
    using Operations::reserve;
    using Operations::set_num;
    using Layout = ApiRowsSingleLayout;
    using size_type = Layout::size_type;
    using byte_size_type = Layout::byte_size_type;
    inline static constexpr auto capacity_granularity = Layout::capacity_granularity;
    inline static constexpr auto allocation_alignment = Layout::allocation_alignment;
    inline static constexpr auto capacity_block_bound = Layout::capacity_block_bound;
    inline static constexpr auto max_capacity = Layout::max_capacity;
    static constexpr auto layout_bytes(byte_size_type blocks) noexcept -> byte_size_type {
        return Layout::layout_bytes(blocks);
    }
    using View = ApiView;
    using ConstView = ApiConstView;
    template <typename Source>
    inline static constexpr bool accepts_source = requires(Source const& source) {
        { source.num() } -> std::convertible_to<size_type>;
        source.validate();
        { ml::soa_storage::source_data(source.values()) } -> std::convertible_to<float const*>;
        { ml::soa_storage::source_data(source.masks()) } -> std::convertible_to<ApiMask const*>;
        {
            ml::soa_storage::source_data(source.view_positions().xs())
        } -> std::convertible_to<float const*>;
        {
            ml::soa_storage::source_data(source.view_positions().ys())
        } -> std::convertible_to<float const*>;
    };
    /* **************************************** */
    // Lifetime
    /* **************************************** */
    ApiOwner() noexcept = default;
    ~ApiOwner() { ml::soa_storage::MimallocStorageAllocator::free(data_); }
    ApiOwner(ApiOwner const&) = delete;
    auto operator=(ApiOwner const&) -> ApiOwner& = delete;
    ApiOwner(ApiOwner&& other) noexcept
        : StorageState{std::exchange(other.data_, nullptr),
                       std::exchange(other.num_, 0),
                       std::exchange(other.capacity_, 0)} {}
    auto operator=(ApiOwner&& other) noexcept -> ApiOwner& {
        if (this != &other) {
            ml::soa_storage::MimallocStorageAllocator::free(data_);
            data_ = std::exchange(other.data_, nullptr);
            num_ = std::exchange(other.num_, 0);
            capacity_ = std::exchange(other.capacity_, 0);
        }
        return *this;
    }
  protected:
    template <typename Byte>
    struct DataPointers {
        template <typename T>
        using Element = std::conditional_t<std::is_const_v<Byte>, T const, T>;
        Element<float>* values{};
        Element<ApiMask>* masks{};
        Element<float>* positions_xs{};
        Element<float>* positions_ys{};
        auto operator+(size_type const offset) const noexcept -> DataPointers {
            if (values == nullptr) {
                return {};
            }
            return {values + offset, masks + offset, positions_xs + offset, positions_ys + offset};
        }
    };
    template <typename Self>
    auto get_data(this Self& self) noexcept {
        using Byte = std::conditional_t<std::is_const_v<Self>, std::byte const, std::byte>;
        if (self.data_ == nullptr) {
            return DataPointers<Byte>{};
        }
        return make_data_unchecked(static_cast<Byte*>(self.data_), self.capacity_blocks());
    }
    template <typename Self>
    auto get_data(this Self& self, size_type const offset) noexcept {
        return self.get_data() + offset;
    }
  private:
    friend struct ml::soa_storage::StorageOperations;
    /* **************************************** */
    // Column pointers
    /* **************************************** */
    template <typename Byte>
    static auto make_data_unchecked(Byte* const data, byte_size_type const blocks) noexcept
        -> DataPointers<Byte> {
        ml::soa_storage::LayoutCursor cursor{blocks};
        auto const pointer_at = [data](auto const& column, byte_size_type offset) noexcept {
            using Column = std::remove_cvref_t<decltype(column)>;
            using Pointer = std::conditional_t<std::is_const_v<Byte>,
                                               typename Column::const_pointer,
                                               typename Column::pointer>;
            return std::launder(reinterpret_cast<Pointer>(data + offset));
        };
        return {pointer_at(Layout::ValuesColumn, cursor.advance(Layout::ValuesColumn)),
                pointer_at(Layout::MasksColumn, cursor.advance(Layout::MasksColumn)),
                pointer_at(Layout::PositionsXsColumn, cursor.advance(Layout::PositionsXsColumn)),
                pointer_at(Layout::PositionsYsColumn, cursor.advance(Layout::PositionsYsColumn))};
    }
    auto capacity_blocks() const noexcept -> byte_size_type {
        return static_cast<byte_size_type>(capacity_ / capacity_granularity);
    }

    /* **************************************** */
    // Typed mutations and growth
    /* **************************************** */
    void default_construct_columns(size_type const first, size_type const count) {
        auto const columns{make_data_unchecked(data_, capacity_blocks()) + first};
        ml::soa_storage::default_construct_n(columns.values, count);
        ml::soa_storage::default_construct_n(columns.masks, count);
        ml::soa_storage::default_construct_n(columns.positions_xs, count);
        ml::soa_storage::default_construct_n(columns.positions_ys, count);
    }
    void swap_remove_columns(size_type const index,
                             size_type const source,
                             size_type const move_count) {
        copy_columns(get_data(), index, source, move_count);
    }
    static void copy_columns(DataPointers<std::byte> const& columns,
                             size_type index,
                             size_type source,
                             size_type move_count) {
        ml::soa_storage::copy_n(columns.values + index, columns.values + source, move_count);
        ml::soa_storage::copy_n(columns.masks + index, columns.masks + source, move_count);
        ml::soa_storage::copy_n(
            columns.positions_xs + index, columns.positions_xs + source, move_count);
        ml::soa_storage::copy_n(
            columns.positions_ys + index, columns.positions_ys + source, move_count);
    }
    void swap_remove_indices(std::span<size_type const> indices) {
        auto const columns{get_data()};
        ml::soa_storage_detail::for_each_removal_run(
            num_,
            indices,
            ml::soa_storage::require,
            [&](size_type index, size_type source, size_type count) {
                copy_columns(columns, index, source, count);
            });
    }
    template <typename Columns>
    void append_columns(Columns const& source,
                        size_type source_first,
                        size_type first,
                        size_type count) {
        auto const destination{get_data(first)};
        ml::soa_storage::copy_n(destination.values,
                                ml::soa_storage::source_data(source.values()) + source_first,
                                count);
        ml::soa_storage::copy_n(
            destination.masks, ml::soa_storage::source_data(source.masks()) + source_first, count);
        ml::soa_storage::copy_n(destination.positions_xs,
                                ml::soa_storage::source_data(source.view_positions().xs()) +
                                    source_first,
                                count);
        ml::soa_storage::copy_n(destination.positions_ys,
                                ml::soa_storage::source_data(source.view_positions().ys()) +
                                    source_first,
                                count);
    }
    template <typename Columns>
    void copy_columns_from(Columns const& source,
                           size_type source_first,
                           size_type first,
                           size_type count) {
        auto const destination{get_data(first)};
        ml::soa_storage::move_n(destination.values,
                                ml::soa_storage::source_data(source.values()) + source_first,
                                count);
        ml::soa_storage::move_n(
            destination.masks, ml::soa_storage::source_data(source.masks()) + source_first, count);
        ml::soa_storage::move_n(destination.positions_xs,
                                ml::soa_storage::source_data(source.view_positions().xs()) +
                                    source_first,
                                count);
        ml::soa_storage::move_n(destination.positions_ys,
                                ml::soa_storage::source_data(source.view_positions().ys()) +
                                    source_first,
                                count);
    }
    void reallocate(size_type const new_capacity) {
        auto* const new_data{ml::soa_storage::MimallocStorageAllocator::allocate(
            layout_bytes(static_cast<byte_size_type>(new_capacity / capacity_granularity)),
            static_cast<uint32>(allocation_alignment))};
        if (num_ > 0) {
            auto const old_blocks{capacity_blocks()};
            auto const new_blocks{static_cast<byte_size_type>(new_capacity / capacity_granularity)};
            auto const source{
                make_data_unchecked(static_cast<std::byte const*>(data_), old_blocks)};
            auto const destination{make_data_unchecked(new_data, new_blocks)};
            ml::soa_storage::copy_n(destination.values, source.values, num_);
            ml::soa_storage::copy_n(destination.masks, source.masks, num_);
            ml::soa_storage::copy_n(destination.positions_xs, source.positions_xs, num_);
            ml::soa_storage::copy_n(destination.positions_ys, source.positions_ys, num_);
        }
        ml::soa_storage::MimallocStorageAllocator::free(data_);
        data_ = new_data;
        capacity_ = new_capacity;
    }
  public:
    template <typename Self>
    using ViewFor =
        std::conditional_t<std::is_const_v<std::remove_reference_t<Self>>, ConstView, View>;
    template <typename Self>
    auto get_view(this Self&& self) -> ViewFor<Self>
        requires std::is_lvalue_reference_v<Self>
    {
        return {&self, 0, self.num()};
    }
    template <typename Self>
    auto get_view(this Self&& self, size_type offset, size_type count) -> ViewFor<Self>
        requires std::is_lvalue_reference_v<Self>
    {
        return {&self, offset, count};
    }
    template <typename Self>
    auto slice(this Self&& self, size_type offset, size_type count) -> ViewFor<Self>
        requires std::is_lvalue_reference_v<Self>
    {
        return self.get_view(offset, count);
    }
    template <typename Self>
    auto left(this Self&& self, size_type count) -> ViewFor<Self>
        requires std::is_lvalue_reference_v<Self>
    {
        return self.get_view().left(count);
    }
    template <typename Self>
    auto right(this Self&& self, size_type count) -> ViewFor<Self>
        requires std::is_lvalue_reference_v<Self>
    {
        return self.get_view().right(count);
    }
    template <typename Self>
    auto get_const_view(this Self&& self) -> ConstView
        requires std::is_lvalue_reference_v<Self>
    {
        return {&self, 0, self.num()};
    }
    template <typename Self>
    auto get_const_view(this Self&& self, size_type offset, size_type count) -> ConstView
        requires std::is_lvalue_reference_v<Self>
    {
        return {&self, offset, count};
    }
    using Value = float;
    float first_value() const { return get_view().values()[0]; }
    Value total() const;
};

struct EquivalentRowsSingleView;
struct EquivalentRowsSingleConstView;
struct EquivalentRowsSingleLayout {
    using size_type = int32;
    using byte_size_type = SIZE_T;

    inline static constexpr size_type capacity_granularity{
        ml::soa_storage::LayoutPolicy::capacity_granularity};
    inline static constexpr byte_size_type column_gap{ml::soa_storage::LayoutPolicy::column_gap};

    template <typename T>
    using ColLayout = ml::soa_storage::ColumnLayout<T>;
    inline static constexpr ml::soa_storage::ColumnLayoutStart LayoutStart{};

    inline static constexpr ColLayout<float> XsColumn{LayoutStart};
    inline static constexpr ColLayout<float> YsColumn{XsColumn};

    inline static constexpr byte_size_type allocation_alignment{YsColumn.allocation_alignment};

    // Conservative per-block bound for checked capacity arithmetic; gaps do not scale with
    // capacity.
    inline static constexpr byte_size_type capacity_block_bound{
        ml::soa_storage::capacity_block_bound(YsColumn)};
    inline static constexpr size_type max_capacity{
        ml::soa_storage::maximum_capacity(capacity_block_bound)};
    static constexpr auto layout_bytes(byte_size_type blocks) noexcept -> byte_size_type {
        return blocks == 0 ? 0 : YsColumn.data_end(blocks);
    }
    static_assert(
        allocation_alignment <= std::numeric_limits<uint32>::max(),
        "Single-allocation alignment must fit the allocator's 32-bit alignment argument.");
    static_assert(max_capacity >= capacity_granularity);
};

template <bool Const>
struct EquivalentRowsSingleViewImpl : ml::soa_storage::CompactViewState<Const> {
    using Base = ml::soa_storage::CompactViewState<Const>;
    using Base::Base;
    using Base::validate;
    using size_type = typename Base::size_type;
    template <typename T>
    using Element = typename Base::template Element<T>;
    using View = EquivalentRowsSingleView;
    using ConstView = EquivalentRowsSingleConstView;
    EquivalentRowsSingleViewImpl() = default;
    template <bool Enabled = Const>
    EquivalentRowsSingleViewImpl(EquivalentRowsSingleViewImpl<false> const& other)
        requires Enabled
        : Base{other} {}
  protected:
    using Base::capacity_blocks;
    using Base::column_data;
    using Base::column_data_unchecked;
    using Base::count_;
    using Base::offset_;
    using Base::state_;
  public:
    auto xs() const -> TArrayView<Element<float>> {
        return {this->template column_data<float>(
                    EquivalentRowsSingleLayout::XsColumn.offset(capacity_blocks())),
                count_};
    }
    auto ys() const -> TArrayView<Element<float>> {
        return {this->template column_data<float>(
                    EquivalentRowsSingleLayout::YsColumn.offset(capacity_blocks())),
                count_};
    }
    template <typename Func>
    auto apply_arrays(Func&& func) const -> decltype(auto) {
        auto column_0{xs()};
        auto column_1{ys()};
        return std::forward<Func>(func)(column_0, column_1);
    }
};
struct EquivalentRowsSingleConstView : EquivalentRowsSingleViewImpl<true> {
    using Base = EquivalentRowsSingleViewImpl<true>;
    using Base::Base;
    using View = EquivalentRowsSingleView;
    using ConstView = EquivalentRowsSingleConstView;
    EquivalentRowsSingleConstView() = default;
    EquivalentRowsSingleConstView(EquivalentRowsSingleView const& other);
    auto get_const_view() const -> ConstView { return *this; }
    auto get_const_view(size_type offset, size_type count) const -> ConstView {
        return slice(offset, count);
    }
    using equivalent_type = ApiPair;
    auto operator[](std::int32_t index) const -> equivalent_type {
        return equivalent_type{xs()[index], ys()[index]};
    }
};
static_assert(ml::soa_storage_detail::validate_compact_view<EquivalentRowsSingleConstView>());
struct EquivalentRowsSingleView : EquivalentRowsSingleViewImpl<false> {
    using Base = EquivalentRowsSingleViewImpl<false>;
    using Base::Base;
    using View = EquivalentRowsSingleView;
    using ConstView = EquivalentRowsSingleConstView;
    EquivalentRowsSingleView() = default;
    auto get_const_view() const -> ConstView { return *this; }
    auto get_const_view(size_type offset, size_type count) const -> ConstView {
        return slice(offset, count);
    }
    using equivalent_type = ApiPair;
    auto operator[](std::int32_t index) const -> equivalent_type {
        return equivalent_type{xs()[index], ys()[index]};
    }
};
static_assert(ml::soa_storage_detail::validate_compact_view<EquivalentRowsSingleView>());
inline EquivalentRowsSingleConstView::EquivalentRowsSingleConstView(
    EquivalentRowsSingleView const& other)
    : Base{other} {}
struct EquivalentOwner
    : protected ml::soa_storage::StorageState
    , private ml::soa_storage::StorageOperations {
    using Operations = ml::soa_storage::StorageOperations;
    using Operations::allocated_bytes;
    using Operations::capacity;
    using Operations::is_empty;
    using Operations::num;
    using Operations::set_num;
    using Layout = EquivalentRowsSingleLayout;
    using size_type = Layout::size_type;
    using byte_size_type = Layout::byte_size_type;
    inline static constexpr auto capacity_granularity = Layout::capacity_granularity;
    inline static constexpr auto allocation_alignment = Layout::allocation_alignment;
    inline static constexpr auto capacity_block_bound = Layout::capacity_block_bound;
    inline static constexpr auto max_capacity = Layout::max_capacity;
    static constexpr auto layout_bytes(byte_size_type blocks) noexcept -> byte_size_type {
        return Layout::layout_bytes(blocks);
    }
    using View = EquivalentRowsSingleView;
    using ConstView = EquivalentRowsSingleConstView;
    template <typename Source>
    inline static constexpr bool accepts_source = requires(Source const& source) {
        { source.num() } -> std::convertible_to<size_type>;
        source.validate();
        { ml::soa_storage::source_data(source.xs()) } -> std::convertible_to<float const*>;
        { ml::soa_storage::source_data(source.ys()) } -> std::convertible_to<float const*>;
    };
    /* **************************************** */
    // Lifetime
    /* **************************************** */
    EquivalentOwner() noexcept = default;
    ~EquivalentOwner() { ml::soa_storage::MimallocStorageAllocator::free(data_); }
    EquivalentOwner(EquivalentOwner const&) = delete;
    auto operator=(EquivalentOwner const&) -> EquivalentOwner& = delete;
    EquivalentOwner(EquivalentOwner&& other) noexcept
        : StorageState{std::exchange(other.data_, nullptr),
                       std::exchange(other.num_, 0),
                       std::exchange(other.capacity_, 0)} {}
    auto operator=(EquivalentOwner&& other) noexcept -> EquivalentOwner& {
        if (this != &other) {
            ml::soa_storage::MimallocStorageAllocator::free(data_);
            data_ = std::exchange(other.data_, nullptr);
            num_ = std::exchange(other.num_, 0);
            capacity_ = std::exchange(other.capacity_, 0);
        }
        return *this;
    }
  protected:
    template <typename Byte>
    struct DataPointers {
        template <typename T>
        using Element = std::conditional_t<std::is_const_v<Byte>, T const, T>;
        Element<float>* xs{};
        Element<float>* ys{};
        auto operator+(size_type const offset) const noexcept -> DataPointers {
            if (xs == nullptr) {
                return {};
            }
            return {xs + offset, ys + offset};
        }
    };
    template <typename Self>
    auto get_data(this Self& self) noexcept {
        using Byte = std::conditional_t<std::is_const_v<Self>, std::byte const, std::byte>;
        if (self.data_ == nullptr) {
            return DataPointers<Byte>{};
        }
        return make_data_unchecked(static_cast<Byte*>(self.data_), self.capacity_blocks());
    }
    template <typename Self>
    auto get_data(this Self& self, size_type const offset) noexcept {
        return self.get_data() + offset;
    }
  private:
    friend struct ml::soa_storage::StorageOperations;
    /* **************************************** */
    // Column pointers
    /* **************************************** */
    template <typename Byte>
    static auto make_data_unchecked(Byte* const data, byte_size_type const blocks) noexcept
        -> DataPointers<Byte> {
        ml::soa_storage::LayoutCursor cursor{blocks};
        auto const pointer_at = [data](auto const& column, byte_size_type offset) noexcept {
            using Column = std::remove_cvref_t<decltype(column)>;
            using Pointer = std::conditional_t<std::is_const_v<Byte>,
                                               typename Column::const_pointer,
                                               typename Column::pointer>;
            return std::launder(reinterpret_cast<Pointer>(data + offset));
        };
        return {pointer_at(Layout::XsColumn, cursor.advance(Layout::XsColumn)),
                pointer_at(Layout::YsColumn, cursor.advance(Layout::YsColumn))};
    }
    auto capacity_blocks() const noexcept -> byte_size_type {
        return static_cast<byte_size_type>(capacity_ / capacity_granularity);
    }

    /* **************************************** */
    // Typed mutations and growth
    /* **************************************** */
    void default_construct_columns(size_type const first, size_type const count) {
        auto const columns{make_data_unchecked(data_, capacity_blocks()) + first};
        ml::soa_storage::default_construct_n(columns.xs, count);
        ml::soa_storage::default_construct_n(columns.ys, count);
    }
    void swap_remove_columns(size_type const index,
                             size_type const source,
                             size_type const move_count) {
        copy_columns(get_data(), index, source, move_count);
    }
    static void copy_columns(DataPointers<std::byte> const& columns,
                             size_type index,
                             size_type source,
                             size_type move_count) {
        ml::soa_storage::copy_n(columns.xs + index, columns.xs + source, move_count);
        ml::soa_storage::copy_n(columns.ys + index, columns.ys + source, move_count);
    }
    void swap_remove_indices(std::span<size_type const> indices) {
        auto const columns{get_data()};
        ml::soa_storage_detail::for_each_removal_run(
            num_,
            indices,
            ml::soa_storage::require,
            [&](size_type index, size_type source, size_type count) {
                copy_columns(columns, index, source, count);
            });
    }
    template <typename Columns>
    void append_columns(Columns const& source,
                        size_type source_first,
                        size_type first,
                        size_type count) {
        auto const destination{get_data(first)};
        ml::soa_storage::copy_n(
            destination.xs, ml::soa_storage::source_data(source.xs()) + source_first, count);
        ml::soa_storage::copy_n(
            destination.ys, ml::soa_storage::source_data(source.ys()) + source_first, count);
    }
    void reallocate(size_type const new_capacity) {
        auto* const new_data{ml::soa_storage::MimallocStorageAllocator::allocate(
            layout_bytes(static_cast<byte_size_type>(new_capacity / capacity_granularity)),
            static_cast<uint32>(allocation_alignment))};
        if (num_ > 0) {
            auto const old_blocks{capacity_blocks()};
            auto const new_blocks{static_cast<byte_size_type>(new_capacity / capacity_granularity)};
            auto const source{
                make_data_unchecked(static_cast<std::byte const*>(data_), old_blocks)};
            auto const destination{make_data_unchecked(new_data, new_blocks)};
            ml::soa_storage::copy_n(destination.xs, source.xs, num_);
            ml::soa_storage::copy_n(destination.ys, source.ys, num_);
        }
        ml::soa_storage::MimallocStorageAllocator::free(data_);
        data_ = new_data;
        capacity_ = new_capacity;
    }
  public:
    template <typename Self>
    using ViewFor =
        std::conditional_t<std::is_const_v<std::remove_reference_t<Self>>, ConstView, View>;
    template <typename Self>
    auto get_view(this Self&& self) -> ViewFor<Self>
        requires std::is_lvalue_reference_v<Self>
    {
        return {&self, 0, self.num()};
    }
    template <typename Self>
    auto get_view(this Self&& self, size_type offset, size_type count) -> ViewFor<Self>
        requires std::is_lvalue_reference_v<Self>
    {
        return {&self, offset, count};
    }
    template <typename Self>
    auto slice(this Self&& self, size_type offset, size_type count) -> ViewFor<Self>
        requires std::is_lvalue_reference_v<Self>
    {
        return self.get_view(offset, count);
    }
    template <typename Self>
    auto left(this Self&& self, size_type count) -> ViewFor<Self>
        requires std::is_lvalue_reference_v<Self>
    {
        return self.get_view().left(count);
    }
    template <typename Self>
    auto right(this Self&& self, size_type count) -> ViewFor<Self>
        requires std::is_lvalue_reference_v<Self>
    {
        return self.get_view().right(count);
    }
    template <typename Self>
    auto get_const_view(this Self&& self) -> ConstView
        requires std::is_lvalue_reference_v<Self>
    {
        return {&self, 0, self.num()};
    }
    template <typename Self>
    auto get_const_view(this Self&& self, size_type offset, size_type count) -> ConstView
        requires std::is_lvalue_reference_v<Self>
    {
        return {&self, offset, count};
    }
    using equivalent_type = ApiPair;
    auto operator[](std::int32_t index) const -> equivalent_type { return get_const_view()[index]; }
};

struct ApiReference {
    codegen_compile_fixture::ApiOwner* owner;
};

struct FRowsView;
struct FRowsConstView;

struct FRowsConstView {
    using View = FRowsView;
    using ConstView = FRowsConstView;

    using equivalent_type = FRow;

    auto operator[](int32 const index) const -> FRow {
        return {ids.GetData()[index], weights.GetData()[index]};
    }
    auto at(int32 const index) const -> FRow {
        validate_array_sizes();
        check(index >= 0);
        check(index < num());
        return (*this)[index];
    }
    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.ids, self.weights);
    }

    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TConstArrayView<int32> ids;
    TConstArrayView<float> weights;
};

struct FRowsView {
    using View = FRowsView;
    using ConstView = FRowsConstView;

    using equivalent_type = FRow;

    auto operator[](int32 const index) const -> FRow {
        return {ids.GetData()[index], weights.GetData()[index]};
    }
    auto at(int32 const index) const -> FRow {
        validate_array_sizes();
        check(index >= 0);
        check(index < num());
        return (*this)[index];
    }

    void set(int32 const index, int32 const new_ids, float const new_weights) const {
        ids[index] = new_ids;
        weights[index] = new_weights;
    }
    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.ids, self.weights);
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TArrayView<int32> ids;
    TArrayView<float> weights;
};

struct FRows {
    using View = FRowsView;
    using ConstView = FRowsConstView;

    using equivalent_type = FRow;

    auto operator[](int32 const index) const -> FRow {
        return {ids.GetData()[index], weights.GetData()[index]};
    }
    auto at(int32 const index) const -> FRow {
        validate_array_sizes();
        check(index >= 0);
        check(index < num());
        return (*this)[index];
    }

    int32 first_id() { return ids[0]; }
    float weight_sum();
    int32 manual_value() const;
    void set(int32 const index, int32 const new_ids, float const new_weights) {
        ids[index] = new_ids;
        weights[index] = new_weights;
    }

    auto add(int32 const new_ids, float const new_weights) -> int32 {
        auto const index{num()};
        ids.Add(new_ids);
        weights.Add(new_weights);
        return index;
    }

    void reset();

    void reserve(int32 const count);

    void add_uninitialised(int32 const count);

    void add_defaulted(int32 const count);

    void remove_at_swap(int32 const index,
                        int32 const count,
                        EAllowShrinking const allow_shrinking) {
        ml::soa_ops::remove_at_swap(*this, index, count, allow_shrinking);
    }

    void set_num(int32 const count, EAllowShrinking const allow_shrinking);

    template <typename Other>
    void copy_element(int32 const dst_i, Other const& other, int32 const src_i) {
        ml::copy_element(ids, dst_i, other.ids, src_i);
        ml::copy_element(weights, dst_i, other.weights, src_i);
    }

    template <typename Other>
    void
        copy_elements(int32 const dst_i, Other const& other, int32 const src_i, int32 const count) {
        ml::copy_elements(ids, dst_i, other.ids, src_i, count);
        ml::copy_elements(weights, dst_i, other.weights, src_i, count);
    }

    template <typename Other>
    void copy_to_tail(Other const& other) {
        auto const count{other.num()};
        check(num() >= count);
        copy_elements(num() - count, other, 0, count);
    }

    template <typename Other>
    void append_from(Other const& other)
        requires ml::SupportsApplyArrayPairsWith<FRows, Other>
    {
        ml::append_from(ids, other.ids);
        ml::append_from(weights, other.weights);
    }

    void apply_permutation(TArrayView<int32> indices);

    template <typename Compare>
    void sort(Compare&& compare, TArrayView<int32> scratch_indices) {
        ml::soa_ops::sort(*this, std::forward<Compare>(compare), scratch_indices);
    }

    template <auto Compare>
    void sort(TArrayView<int32> scratch_indices) {
        ml::soa_ops::sort<Compare>(*this, scratch_indices);
    }

    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.ids, self.weights);
    }

    template <typename Self, typename Other, typename TFunc>
    auto apply_array_pairs(this Self&& self, Other&& other, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.ids, other.ids, self.weights, other.weights);
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TArray<int32> ids;
    TArray<float> weights;
};

struct FChildView;
struct FChildConstView;

struct FChildConstView {
    using View = FChildView;
    using ConstView = FChildConstView;
    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.values);
    }

    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TConstArrayView<int32> values;
};

struct FChildView {
    using View = FChildView;
    using ConstView = FChildConstView;

    void set(int32 const index, int32 const new_values) const { values[index] = new_values; }
    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.values);
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TArrayView<int32> values;
};

struct FChild {
    using View = FChildView;
    using ConstView = FChildConstView;
    void set(int32 const index, int32 const new_values) { values[index] = new_values; }

    auto add(int32 const new_values) -> int32 {
        auto const index{num()};
        values.Add(new_values);
        return index;
    }

    void reset();

    void reserve(int32 const count);

    void add_uninitialised(int32 const count);

    void add_defaulted(int32 const count);

    void remove_at_swap(int32 const index,
                        int32 const count,
                        EAllowShrinking const allow_shrinking) {
        ml::soa_ops::remove_at_swap(*this, index, count, allow_shrinking);
    }

    void set_num(int32 const count, EAllowShrinking const allow_shrinking);

    template <typename Other>
    void copy_element(int32 const dst_i, Other const& other, int32 const src_i) {
        ml::copy_element(values, dst_i, other.values, src_i);
    }

    template <typename Other>
    void
        copy_elements(int32 const dst_i, Other const& other, int32 const src_i, int32 const count) {
        ml::copy_elements(values, dst_i, other.values, src_i, count);
    }

    template <typename Other>
    void copy_to_tail(Other const& other) {
        auto const count{other.num()};
        check(num() >= count);
        copy_elements(num() - count, other, 0, count);
    }

    template <typename Other>
    void append_from(Other const& other)
        requires ml::SupportsApplyArrayPairsWith<FChild, Other>
    {
        ml::append_from(values, other.values);
    }

    void apply_permutation(TArrayView<int32> indices);

    template <typename Compare>
    void sort(Compare&& compare, TArrayView<int32> scratch_indices) {
        ml::soa_ops::sort(*this, std::forward<Compare>(compare), scratch_indices);
    }

    template <auto Compare>
    void sort(TArrayView<int32> scratch_indices) {
        ml::soa_ops::sort<Compare>(*this, scratch_indices);
    }

    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.values);
    }

    template <typename Self, typename Other, typename TFunc>
    auto apply_array_pairs(this Self&& self, Other&& other, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.values, other.values);
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TArray<int32> values;
};

struct FParentsView;
struct FParentsConstView;

struct FParentsConstView {
    using View = FParentsView;
    using ConstView = FParentsConstView;
    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.keys, self.children);
    }

    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TConstArrayView<int32> keys;
    FChild::ConstView children;
};

struct FParentsView {
    using View = FParentsView;
    using ConstView = FParentsConstView;
    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.keys, self.children);
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TArrayView<int32> keys;
    FChild::View children;
};

struct FParents {
    using View = FParentsView;
    using ConstView = FParentsConstView;
    void reset();

    void reserve(int32 const count);

    void add_uninitialised(int32 const count);

    void add_defaulted(int32 const count);

    void remove_at_swap(int32 const index,
                        int32 const count,
                        EAllowShrinking const allow_shrinking) {
        ml::soa_ops::remove_at_swap(*this, index, count, allow_shrinking);
    }

    void set_num(int32 const count, EAllowShrinking const allow_shrinking);

    template <typename Other>
    void copy_element(int32 const dst_i, Other const& other, int32 const src_i) {
        ml::copy_element(keys, dst_i, other.keys, src_i);
        ml::copy_element(children, dst_i, other.children, src_i);
    }

    template <typename Other>
    void
        copy_elements(int32 const dst_i, Other const& other, int32 const src_i, int32 const count) {
        ml::copy_elements(keys, dst_i, other.keys, src_i, count);
        ml::copy_elements(children, dst_i, other.children, src_i, count);
    }

    template <typename Other>
    void copy_to_tail(Other const& other) {
        auto const count{other.num()};
        check(num() >= count);
        copy_elements(num() - count, other, 0, count);
    }

    template <typename Other>
    void append_from(Other const& other)
        requires ml::SupportsApplyArrayPairsWith<FParents, Other>
    {
        ml::append_from(keys, other.keys);
        children.append_from(other.children);
    }

    void apply_permutation(TArrayView<int32> indices);

    template <typename Compare>
    void sort(Compare&& compare, TArrayView<int32> scratch_indices) {
        ml::soa_ops::sort(*this, std::forward<Compare>(compare), scratch_indices);
    }

    template <auto Compare>
    void sort(TArrayView<int32> scratch_indices) {
        ml::soa_ops::sort<Compare>(*this, scratch_indices);
    }

    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.keys, self.children);
    }

    template <typename Self, typename Other, typename TFunc>
    auto apply_array_pairs(this Self&& self, Other&& other, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.keys, other.keys, self.children, other.children);
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TArray<int32> keys;
    FChild children;
};

struct FParentsSingleView;
struct FParentsSingleConstView;
struct FParentsSingleLayout {
    using size_type = int32;
    using byte_size_type = SIZE_T;

    inline static constexpr size_type capacity_granularity{
        ml::soa_storage::LayoutPolicy::capacity_granularity};
    inline static constexpr byte_size_type column_gap{ml::soa_storage::LayoutPolicy::column_gap};

    template <typename T>
    using ColLayout = ml::soa_storage::ColumnLayout<T>;
    inline static constexpr ml::soa_storage::ColumnLayoutStart LayoutStart{};

    inline static constexpr ColLayout<int32> KeysColumn{LayoutStart};
    inline static constexpr ColLayout<int32> ChildrenValuesColumn{KeysColumn};

    inline static constexpr byte_size_type allocation_alignment{
        ChildrenValuesColumn.allocation_alignment};

    // Conservative per-block bound for checked capacity arithmetic; gaps do not scale with
    // capacity.
    inline static constexpr byte_size_type capacity_block_bound{
        ml::soa_storage::capacity_block_bound(ChildrenValuesColumn)};
    inline static constexpr size_type max_capacity{
        ml::soa_storage::maximum_capacity(capacity_block_bound)};
    static constexpr auto layout_bytes(byte_size_type blocks) noexcept -> byte_size_type {
        return blocks == 0 ? 0 : ChildrenValuesColumn.data_end(blocks);
    }
    static_assert(
        allocation_alignment <= std::numeric_limits<uint32>::max(),
        "Single-allocation alignment must fit the allocator's 32-bit alignment argument.");
    static_assert(max_capacity >= capacity_granularity);
};

struct FParentsSingleView_children;
struct FParentsSingleConstView_children;
template <bool Const>
struct FParentsSingleView_childrenImpl : ml::soa_storage::CompactViewState<Const> {
    using Base = ml::soa_storage::CompactViewState<Const>;
    using Base::Base;
    using Base::validate;
    using size_type = typename Base::size_type;
    template <typename T>
    using Element = typename Base::template Element<T>;
    using View = FParentsSingleView_children;
    using ConstView = FParentsSingleConstView_children;
    FParentsSingleView_childrenImpl() = default;
    template <bool Enabled = Const>
    FParentsSingleView_childrenImpl(FParentsSingleView_childrenImpl<false> const& other)
        requires Enabled
        : Base{other} {}
  protected:
    using Base::capacity_blocks;
    using Base::column_data;
    using Base::column_data_unchecked;
    using Base::count_;
    using Base::offset_;
    using Base::state_;
  public:
    auto values() const -> TArrayView<Element<int32>> {
        return {this->template column_data<int32>(
                    FParentsSingleLayout::ChildrenValuesColumn.offset(capacity_blocks())),
                count_};
    }
    template <typename Func>
    auto apply_arrays(Func&& func) const -> decltype(auto) {
        auto column_0{values()};
        return std::forward<Func>(func)(column_0);
    }
};
struct FParentsSingleConstView_children : FParentsSingleView_childrenImpl<true> {
    using Base = FParentsSingleView_childrenImpl<true>;
    using Base::Base;
    using View = FParentsSingleView_children;
    using ConstView = FParentsSingleConstView_children;
    FParentsSingleConstView_children() = default;
    FParentsSingleConstView_children(FParentsSingleView_children const& other);
    auto get_const_view() const -> ConstView { return *this; }
    auto get_const_view(size_type offset, size_type count) const -> ConstView {
        return slice(offset, count);
    }
};
static_assert(ml::soa_storage_detail::validate_compact_view<FParentsSingleConstView_children>());
struct FParentsSingleView_children : FParentsSingleView_childrenImpl<false> {
    using Base = FParentsSingleView_childrenImpl<false>;
    using Base::Base;
    using View = FParentsSingleView_children;
    using ConstView = FParentsSingleConstView_children;
    FParentsSingleView_children() = default;
    auto get_const_view() const -> ConstView { return *this; }
    auto get_const_view(size_type offset, size_type count) const -> ConstView {
        return slice(offset, count);
    }
};
static_assert(ml::soa_storage_detail::validate_compact_view<FParentsSingleView_children>());
inline FParentsSingleConstView_children::FParentsSingleConstView_children(
    FParentsSingleView_children const& other)
    : Base{other} {}
template <bool Const>
struct FParentsSingleViewImpl : ml::soa_storage::CompactViewState<Const> {
    using Base = ml::soa_storage::CompactViewState<Const>;
    using Base::Base;
    using Base::validate;
    using size_type = typename Base::size_type;
    template <typename T>
    using Element = typename Base::template Element<T>;
    using View = FParentsSingleView;
    using ConstView = FParentsSingleConstView;
    FParentsSingleViewImpl() = default;
    template <bool Enabled = Const>
    FParentsSingleViewImpl(FParentsSingleViewImpl<false> const& other)
        requires Enabled
        : Base{other} {}
  protected:
    using Base::capacity_blocks;
    using Base::column_data;
    using Base::column_data_unchecked;
    using Base::count_;
    using Base::offset_;
    using Base::state_;
  public:
    auto keys() const -> TArrayView<Element<int32>> {
        return {this->template column_data<int32>(
                    FParentsSingleLayout::KeysColumn.offset(capacity_blocks())),
                count_};
    }
    auto view_children() const -> std::
        conditional_t<Const, FParentsSingleConstView_children, FParentsSingleView_children> {
        return {state_, offset_, count_};
    }
    template <typename Func>
    auto apply_arrays(Func&& func) const -> decltype(auto) {
        auto column_0{keys()};
        auto column_1{view_children().values()};
        return std::forward<Func>(func)(column_0, column_1);
    }
};
struct FParentsSingleConstView : FParentsSingleViewImpl<true> {
    using Base = FParentsSingleViewImpl<true>;
    using Base::Base;
    using View = FParentsSingleView;
    using ConstView = FParentsSingleConstView;
    FParentsSingleConstView() = default;
    FParentsSingleConstView(FParentsSingleView const& other);
    auto get_const_view() const -> ConstView { return *this; }
    auto get_const_view(size_type offset, size_type count) const -> ConstView {
        return slice(offset, count);
    }
};
static_assert(ml::soa_storage_detail::validate_compact_view<FParentsSingleConstView>());
struct FParentsSingleView : FParentsSingleViewImpl<false> {
    using Base = FParentsSingleViewImpl<false>;
    using Base::Base;
    using View = FParentsSingleView;
    using ConstView = FParentsSingleConstView;
    FParentsSingleView() = default;
    auto get_const_view() const -> ConstView { return *this; }
    auto get_const_view(size_type offset, size_type count) const -> ConstView {
        return slice(offset, count);
    }
};
static_assert(ml::soa_storage_detail::validate_compact_view<FParentsSingleView>());
inline FParentsSingleConstView::FParentsSingleConstView(FParentsSingleView const& other)
    : Base{other} {}
struct SingleParents
    : protected ml::soa_storage::StorageState
    , private ml::soa_storage::StorageOperations {
    using Operations = ml::soa_storage::StorageOperations;
    using Operations::add_defaulted;
    using Operations::add_uninitialised;
    using Operations::allocated_bytes;
    using Operations::append_from;
    using Operations::capacity;
    using Operations::copy_element;
    using Operations::copy_elements;
    using Operations::is_empty;
    using Operations::num;
    using Operations::remove_at_swap;
    using Operations::reserve;
    using Operations::reset;
    using Operations::set_num;
    using Layout = FParentsSingleLayout;
    using size_type = Layout::size_type;
    using byte_size_type = Layout::byte_size_type;
    inline static constexpr auto capacity_granularity = Layout::capacity_granularity;
    inline static constexpr auto allocation_alignment = Layout::allocation_alignment;
    inline static constexpr auto capacity_block_bound = Layout::capacity_block_bound;
    inline static constexpr auto max_capacity = Layout::max_capacity;
    static constexpr auto layout_bytes(byte_size_type blocks) noexcept -> byte_size_type {
        return Layout::layout_bytes(blocks);
    }
    using View = FParentsSingleView;
    using ConstView = FParentsSingleConstView;
    template <typename Source>
    inline static constexpr bool accepts_source = requires(Source const& source) {
        { source.num() } -> std::convertible_to<size_type>;
        source.validate();
        { ml::soa_storage::source_data(source.keys()) } -> std::convertible_to<int32 const*>;
        {
            ml::soa_storage::source_data(source.view_children().values())
        } -> std::convertible_to<int32 const*>;
    };
    /* **************************************** */
    // Lifetime
    /* **************************************** */
    SingleParents() noexcept = default;
    ~SingleParents() { ml::soa_storage::MimallocStorageAllocator::free(data_); }
    SingleParents(SingleParents const&) = delete;
    auto operator=(SingleParents const&) -> SingleParents& = delete;
    SingleParents(SingleParents&& other) noexcept
        : StorageState{std::exchange(other.data_, nullptr),
                       std::exchange(other.num_, 0),
                       std::exchange(other.capacity_, 0)} {}
    auto operator=(SingleParents&& other) noexcept -> SingleParents& {
        if (this != &other) {
            ml::soa_storage::MimallocStorageAllocator::free(data_);
            data_ = std::exchange(other.data_, nullptr);
            num_ = std::exchange(other.num_, 0);
            capacity_ = std::exchange(other.capacity_, 0);
        }
        return *this;
    }
  protected:
    template <typename Byte>
    struct DataPointers {
        template <typename T>
        using Element = std::conditional_t<std::is_const_v<Byte>, T const, T>;
        Element<int32>* keys{};
        Element<int32>* children_values{};
        auto operator+(size_type const offset) const noexcept -> DataPointers {
            if (keys == nullptr) {
                return {};
            }
            return {keys + offset, children_values + offset};
        }
    };
    template <typename Self>
    auto get_data(this Self& self) noexcept {
        using Byte = std::conditional_t<std::is_const_v<Self>, std::byte const, std::byte>;
        if (self.data_ == nullptr) {
            return DataPointers<Byte>{};
        }
        return make_data_unchecked(static_cast<Byte*>(self.data_), self.capacity_blocks());
    }
    template <typename Self>
    auto get_data(this Self& self, size_type const offset) noexcept {
        return self.get_data() + offset;
    }
  private:
    friend struct ml::soa_storage::StorageOperations;
    /* **************************************** */
    // Column pointers
    /* **************************************** */
    template <typename Byte>
    static auto make_data_unchecked(Byte* const data, byte_size_type const blocks) noexcept
        -> DataPointers<Byte> {
        ml::soa_storage::LayoutCursor cursor{blocks};
        auto const pointer_at = [data](auto const& column, byte_size_type offset) noexcept {
            using Column = std::remove_cvref_t<decltype(column)>;
            using Pointer = std::conditional_t<std::is_const_v<Byte>,
                                               typename Column::const_pointer,
                                               typename Column::pointer>;
            return std::launder(reinterpret_cast<Pointer>(data + offset));
        };
        return {
            pointer_at(Layout::KeysColumn, cursor.advance(Layout::KeysColumn)),
            pointer_at(Layout::ChildrenValuesColumn, cursor.advance(Layout::ChildrenValuesColumn))};
    }
    auto capacity_blocks() const noexcept -> byte_size_type {
        return static_cast<byte_size_type>(capacity_ / capacity_granularity);
    }

    /* **************************************** */
    // Typed mutations and growth
    /* **************************************** */
    void default_construct_columns(size_type const first, size_type const count) {
        auto const columns{make_data_unchecked(data_, capacity_blocks()) + first};
        ml::soa_storage::default_construct_n(columns.keys, count);
        ml::soa_storage::default_construct_n(columns.children_values, count);
    }
    void swap_remove_columns(size_type const index,
                             size_type const source,
                             size_type const move_count) {
        copy_columns(get_data(), index, source, move_count);
    }
    static void copy_columns(DataPointers<std::byte> const& columns,
                             size_type index,
                             size_type source,
                             size_type move_count) {
        ml::soa_storage::copy_n(columns.keys + index, columns.keys + source, move_count);
        ml::soa_storage::copy_n(
            columns.children_values + index, columns.children_values + source, move_count);
    }
    void swap_remove_indices(std::span<size_type const> indices) {
        auto const columns{get_data()};
        ml::soa_storage_detail::for_each_removal_run(
            num_,
            indices,
            ml::soa_storage::require,
            [&](size_type index, size_type source, size_type count) {
                copy_columns(columns, index, source, count);
            });
    }
    template <typename Columns>
    void append_columns(Columns const& source,
                        size_type source_first,
                        size_type first,
                        size_type count) {
        auto const destination{get_data(first)};
        ml::soa_storage::copy_n(
            destination.keys, ml::soa_storage::source_data(source.keys()) + source_first, count);
        ml::soa_storage::copy_n(destination.children_values,
                                ml::soa_storage::source_data(source.view_children().values()) +
                                    source_first,
                                count);
    }
    template <typename Columns>
    void copy_columns_from(Columns const& source,
                           size_type source_first,
                           size_type first,
                           size_type count) {
        auto const destination{get_data(first)};
        ml::soa_storage::move_n(
            destination.keys, ml::soa_storage::source_data(source.keys()) + source_first, count);
        ml::soa_storage::move_n(destination.children_values,
                                ml::soa_storage::source_data(source.view_children().values()) +
                                    source_first,
                                count);
    }
    void reallocate(size_type const new_capacity) {
        auto* const new_data{ml::soa_storage::MimallocStorageAllocator::allocate(
            layout_bytes(static_cast<byte_size_type>(new_capacity / capacity_granularity)),
            static_cast<uint32>(allocation_alignment))};
        if (num_ > 0) {
            auto const old_blocks{capacity_blocks()};
            auto const new_blocks{static_cast<byte_size_type>(new_capacity / capacity_granularity)};
            auto const source{
                make_data_unchecked(static_cast<std::byte const*>(data_), old_blocks)};
            auto const destination{make_data_unchecked(new_data, new_blocks)};
            ml::soa_storage::copy_n(destination.keys, source.keys, num_);
            ml::soa_storage::copy_n(destination.children_values, source.children_values, num_);
        }
        ml::soa_storage::MimallocStorageAllocator::free(data_);
        data_ = new_data;
        capacity_ = new_capacity;
    }
  public:
    template <typename Self>
    using ViewFor =
        std::conditional_t<std::is_const_v<std::remove_reference_t<Self>>, ConstView, View>;
    template <typename Self>
    auto get_view(this Self&& self) -> ViewFor<Self>
        requires std::is_lvalue_reference_v<Self>
    {
        return {&self, 0, self.num()};
    }
    template <typename Self>
    auto get_view(this Self&& self, size_type offset, size_type count) -> ViewFor<Self>
        requires std::is_lvalue_reference_v<Self>
    {
        return {&self, offset, count};
    }
    template <typename Self>
    auto slice(this Self&& self, size_type offset, size_type count) -> ViewFor<Self>
        requires std::is_lvalue_reference_v<Self>
    {
        return self.get_view(offset, count);
    }
    template <typename Self>
    auto left(this Self&& self, size_type count) -> ViewFor<Self>
        requires std::is_lvalue_reference_v<Self>
    {
        return self.get_view().left(count);
    }
    template <typename Self>
    auto right(this Self&& self, size_type count) -> ViewFor<Self>
        requires std::is_lvalue_reference_v<Self>
    {
        return self.get_view().right(count);
    }
    template <typename Self>
    auto get_const_view(this Self&& self) -> ConstView
        requires std::is_lvalue_reference_v<Self>
    {
        return {&self, 0, self.num()};
    }
    template <typename Self>
    auto get_const_view(this Self&& self, size_type offset, size_type count) -> ConstView
        requires std::is_lvalue_reference_v<Self>
    {
        return {&self, offset, count};
    }
};

struct CountedParents
    : protected ml::soa_storage::StorageState
    , private ml::soa_storage::StorageOperations {
    using Operations = ml::soa_storage::StorageOperations;
    using Operations::add_defaulted;
    using Operations::add_uninitialised;
    using Operations::allocated_bytes;
    using Operations::append_from;
    using Operations::capacity;
    using Operations::copy_element;
    using Operations::copy_elements;
    using Operations::is_empty;
    using Operations::num;
    using Operations::remove_at_swap;
    using Operations::reserve;
    using Operations::reset;
    using Operations::set_num;
    using Layout = FParentsSingleLayout;
    using size_type = Layout::size_type;
    using byte_size_type = Layout::byte_size_type;
    inline static constexpr auto capacity_granularity = Layout::capacity_granularity;
    inline static constexpr auto allocation_alignment = Layout::allocation_alignment;
    inline static constexpr auto capacity_block_bound = Layout::capacity_block_bound;
    inline static constexpr auto max_capacity = Layout::max_capacity;
    static constexpr auto layout_bytes(byte_size_type blocks) noexcept -> byte_size_type {
        return Layout::layout_bytes(blocks);
    }
    using View = FParentsSingleView;
    using ConstView = FParentsSingleConstView;
    template <typename Source>
    inline static constexpr bool accepts_source = requires(Source const& source) {
        { source.num() } -> std::convertible_to<size_type>;
        source.validate();
        { ml::soa_storage::source_data(source.keys()) } -> std::convertible_to<int32 const*>;
        {
            ml::soa_storage::source_data(source.view_children().values())
        } -> std::convertible_to<int32 const*>;
    };
    /* **************************************** */
    // Lifetime
    /* **************************************** */
    CountedParents() noexcept = default;
    ~CountedParents() { CountingAllocator::free(data_); }
    CountedParents(CountedParents const&) = delete;
    auto operator=(CountedParents const&) -> CountedParents& = delete;
    CountedParents(CountedParents&& other) noexcept
        : StorageState{std::exchange(other.data_, nullptr),
                       std::exchange(other.num_, 0),
                       std::exchange(other.capacity_, 0)} {}
    auto operator=(CountedParents&& other) noexcept -> CountedParents& {
        if (this != &other) {
            CountingAllocator::free(data_);
            data_ = std::exchange(other.data_, nullptr);
            num_ = std::exchange(other.num_, 0);
            capacity_ = std::exchange(other.capacity_, 0);
        }
        return *this;
    }
  protected:
    template <typename Byte>
    struct DataPointers {
        template <typename T>
        using Element = std::conditional_t<std::is_const_v<Byte>, T const, T>;
        Element<int32>* keys{};
        Element<int32>* children_values{};
        auto operator+(size_type const offset) const noexcept -> DataPointers {
            if (keys == nullptr) {
                return {};
            }
            return {keys + offset, children_values + offset};
        }
    };
    template <typename Self>
    auto get_data(this Self& self) noexcept {
        using Byte = std::conditional_t<std::is_const_v<Self>, std::byte const, std::byte>;
        if (self.data_ == nullptr) {
            return DataPointers<Byte>{};
        }
        return make_data_unchecked(static_cast<Byte*>(self.data_), self.capacity_blocks());
    }
    template <typename Self>
    auto get_data(this Self& self, size_type const offset) noexcept {
        return self.get_data() + offset;
    }
  private:
    friend struct ml::soa_storage::StorageOperations;
    /* **************************************** */
    // Column pointers
    /* **************************************** */
    template <typename Byte>
    static auto make_data_unchecked(Byte* const data, byte_size_type const blocks) noexcept
        -> DataPointers<Byte> {
        ml::soa_storage::LayoutCursor cursor{blocks};
        auto const pointer_at = [data](auto const& column, byte_size_type offset) noexcept {
            using Column = std::remove_cvref_t<decltype(column)>;
            using Pointer = std::conditional_t<std::is_const_v<Byte>,
                                               typename Column::const_pointer,
                                               typename Column::pointer>;
            return std::launder(reinterpret_cast<Pointer>(data + offset));
        };
        return {
            pointer_at(Layout::KeysColumn, cursor.advance(Layout::KeysColumn)),
            pointer_at(Layout::ChildrenValuesColumn, cursor.advance(Layout::ChildrenValuesColumn))};
    }
    auto capacity_blocks() const noexcept -> byte_size_type {
        return static_cast<byte_size_type>(capacity_ / capacity_granularity);
    }

    /* **************************************** */
    // Typed mutations and growth
    /* **************************************** */
    void default_construct_columns(size_type const first, size_type const count) {
        auto const columns{make_data_unchecked(data_, capacity_blocks()) + first};
        ml::soa_storage::default_construct_n(columns.keys, count);
        ml::soa_storage::default_construct_n(columns.children_values, count);
    }
    void swap_remove_columns(size_type const index,
                             size_type const source,
                             size_type const move_count) {
        copy_columns(get_data(), index, source, move_count);
    }
    static void copy_columns(DataPointers<std::byte> const& columns,
                             size_type index,
                             size_type source,
                             size_type move_count) {
        ml::soa_storage::copy_n(columns.keys + index, columns.keys + source, move_count);
        ml::soa_storage::copy_n(
            columns.children_values + index, columns.children_values + source, move_count);
    }
    void swap_remove_indices(std::span<size_type const> indices) {
        auto const columns{get_data()};
        ml::soa_storage_detail::for_each_removal_run(
            num_,
            indices,
            ml::soa_storage::require,
            [&](size_type index, size_type source, size_type count) {
                copy_columns(columns, index, source, count);
            });
    }
    template <typename Columns>
    void append_columns(Columns const& source,
                        size_type source_first,
                        size_type first,
                        size_type count) {
        auto const destination{get_data(first)};
        ml::soa_storage::copy_n(
            destination.keys, ml::soa_storage::source_data(source.keys()) + source_first, count);
        ml::soa_storage::copy_n(destination.children_values,
                                ml::soa_storage::source_data(source.view_children().values()) +
                                    source_first,
                                count);
    }
    template <typename Columns>
    void copy_columns_from(Columns const& source,
                           size_type source_first,
                           size_type first,
                           size_type count) {
        auto const destination{get_data(first)};
        ml::soa_storage::move_n(
            destination.keys, ml::soa_storage::source_data(source.keys()) + source_first, count);
        ml::soa_storage::move_n(destination.children_values,
                                ml::soa_storage::source_data(source.view_children().values()) +
                                    source_first,
                                count);
    }
    void reallocate(size_type const new_capacity) {
        auto* const new_data{CountingAllocator::allocate(
            layout_bytes(static_cast<byte_size_type>(new_capacity / capacity_granularity)),
            static_cast<uint32>(allocation_alignment))};
        if (num_ > 0) {
            auto const old_blocks{capacity_blocks()};
            auto const new_blocks{static_cast<byte_size_type>(new_capacity / capacity_granularity)};
            auto const source{
                make_data_unchecked(static_cast<std::byte const*>(data_), old_blocks)};
            auto const destination{make_data_unchecked(new_data, new_blocks)};
            ml::soa_storage::copy_n(destination.keys, source.keys, num_);
            ml::soa_storage::copy_n(destination.children_values, source.children_values, num_);
        }
        CountingAllocator::free(data_);
        data_ = new_data;
        capacity_ = new_capacity;
    }
  public:
    template <typename Self>
    using ViewFor =
        std::conditional_t<std::is_const_v<std::remove_reference_t<Self>>, ConstView, View>;
    template <typename Self>
    auto get_view(this Self&& self) -> ViewFor<Self>
        requires std::is_lvalue_reference_v<Self>
    {
        return {&self, 0, self.num()};
    }
    template <typename Self>
    auto get_view(this Self&& self, size_type offset, size_type count) -> ViewFor<Self>
        requires std::is_lvalue_reference_v<Self>
    {
        return {&self, offset, count};
    }
    template <typename Self>
    auto slice(this Self&& self, size_type offset, size_type count) -> ViewFor<Self>
        requires std::is_lvalue_reference_v<Self>
    {
        return self.get_view(offset, count);
    }
    template <typename Self>
    auto left(this Self&& self, size_type count) -> ViewFor<Self>
        requires std::is_lvalue_reference_v<Self>
    {
        return self.get_view().left(count);
    }
    template <typename Self>
    auto right(this Self&& self, size_type count) -> ViewFor<Self>
        requires std::is_lvalue_reference_v<Self>
    {
        return self.get_view().right(count);
    }
    template <typename Self>
    auto get_const_view(this Self&& self) -> ConstView
        requires std::is_lvalue_reference_v<Self>
    {
        return {&self, 0, self.num()};
    }
    template <typename Self>
    auto get_const_view(this Self&& self, size_type offset, size_type count) -> ConstView
        requires std::is_lvalue_reference_v<Self>
    {
        return {&self, offset, count};
    }
};

enum class EField8 : uint8 {
    Values = 0,
    Count = static_cast<uint8>(Values) + (8),
};

struct FFieldMask8 {
    inline static constexpr int32 field_count{static_cast<int32>(EField8::Count)};
    static_assert(field_count <= 64, "Field mask exceeds 64 bits.");
    using storage_type = std::conditional_t<
        field_count <= 8,
        uint8,
        std::conditional_t<field_count <= 16,
                           uint16,
                           std::conditional_t<field_count <= 32, uint32, uint64>>>;

    constexpr FFieldMask8() noexcept = default;
    explicit constexpr FFieldMask8(storage_type const value) noexcept
        : value_{value} {}

    [[nodiscard]] constexpr auto value() const noexcept -> storage_type { return value_; }
    [[nodiscard]] constexpr auto is_empty() const noexcept -> bool { return value_ == 0; }
    [[nodiscard]] constexpr auto has(EField8 const field) const noexcept -> bool {
        return (value_ & bit(field)) != 0;
    }
    [[nodiscard]] static constexpr auto index(EField8 const field) noexcept -> int32 {
        return static_cast<int32>(field);
    }
    constexpr void set(EField8 const field) noexcept { value_ |= bit(field); }
    constexpr void set(FFieldMask8 const fields) noexcept { value_ |= fields.value_; }
    constexpr void clear(EField8 const field) noexcept {
        value_ = static_cast<storage_type>(value_ & ~bit(field));
    }

    [[nodiscard]] static constexpr auto values_field(int32 const value_index) noexcept -> EField8 {
        return static_cast<EField8>(static_cast<int32>(EField8::Values) + value_index);
    }
  private:
    [[nodiscard]] static constexpr auto bit(EField8 const field) noexcept -> storage_type {
        return static_cast<storage_type>(uint64{1} << static_cast<uint8>(field));
    }

    storage_type value_{};
};
static_assert(sizeof(FFieldMask8) == sizeof(FFieldMask8::storage_type));
static_assert(std::is_trivially_copyable_v<FFieldMask8>);

struct FMaskRows8View;
struct FMaskRows8ConstView;

struct FMaskRows8ConstView {
    using View = FMaskRows8View;
    using ConstView = FMaskRows8ConstView;
    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.masks, self.values);
    }

    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TConstArrayView<FFieldMask8> masks;
    TConstArrayView<int32> values;
};

struct FMaskRows8View {
    using View = FMaskRows8View;
    using ConstView = FMaskRows8ConstView;

    void set(int32 const index, FFieldMask8 const& new_masks, int32 const new_values) const {
        masks[index] = new_masks;
        values[index] = new_values;
    }
    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.masks, self.values);
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TArrayView<FFieldMask8> masks;
    TArrayView<int32> values;
};

struct FMaskRows8 {
    using View = FMaskRows8View;
    using ConstView = FMaskRows8ConstView;
    void set(int32 const index, FFieldMask8 const& new_masks, int32 const new_values) {
        masks[index] = new_masks;
        values[index] = new_values;
    }

    auto add(FFieldMask8 const& new_masks, int32 const new_values) -> int32 {
        auto const index{num()};
        masks.Add(new_masks);
        values.Add(new_values);
        return index;
    }

    void apply_permutation(TArrayView<int32> indices);

    template <typename Compare>
    void sort(Compare&& compare, TArrayView<int32> scratch_indices) {
        ml::soa_ops::sort(*this, std::forward<Compare>(compare), scratch_indices);
    }

    template <auto Compare>
    void sort(TArrayView<int32> scratch_indices) {
        ml::soa_ops::sort<Compare>(*this, scratch_indices);
    }

    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.masks, self.values);
    }

    template <typename Self, typename Other, typename TFunc>
    auto apply_array_pairs(this Self&& self, Other&& other, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.masks, other.masks, self.values, other.values);
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TArray<FFieldMask8> masks;
    TArray<int32> values;
};

enum class EField9 : uint8 {
    Values = 0,
    Tail = static_cast<uint8>(Values) + (8),
    Count = static_cast<uint8>(Tail) + 1,
};

struct FFieldMask9 {
    inline static constexpr int32 field_count{static_cast<int32>(EField9::Count)};
    static_assert(field_count <= 64, "Field mask exceeds 64 bits.");
    using storage_type = std::conditional_t<
        field_count <= 8,
        uint8,
        std::conditional_t<field_count <= 16,
                           uint16,
                           std::conditional_t<field_count <= 32, uint32, uint64>>>;

    constexpr FFieldMask9() noexcept = default;
    explicit constexpr FFieldMask9(storage_type const value) noexcept
        : value_{value} {}

    [[nodiscard]] constexpr auto value() const noexcept -> storage_type { return value_; }
    [[nodiscard]] constexpr auto is_empty() const noexcept -> bool { return value_ == 0; }
    [[nodiscard]] constexpr auto has(EField9 const field) const noexcept -> bool {
        return (value_ & bit(field)) != 0;
    }
    [[nodiscard]] static constexpr auto index(EField9 const field) noexcept -> int32 {
        return static_cast<int32>(field);
    }
    constexpr void set(EField9 const field) noexcept { value_ |= bit(field); }
    constexpr void set(FFieldMask9 const fields) noexcept { value_ |= fields.value_; }
    constexpr void clear(EField9 const field) noexcept {
        value_ = static_cast<storage_type>(value_ & ~bit(field));
    }

    [[nodiscard]] static constexpr auto values_field(int32 const value_index) noexcept -> EField9 {
        return static_cast<EField9>(static_cast<int32>(EField9::Values) + value_index);
    }
  private:
    [[nodiscard]] static constexpr auto bit(EField9 const field) noexcept -> storage_type {
        return static_cast<storage_type>(uint64{1} << static_cast<uint8>(field));
    }

    storage_type value_{};
};
static_assert(sizeof(FFieldMask9) == sizeof(FFieldMask9::storage_type));
static_assert(std::is_trivially_copyable_v<FFieldMask9>);

struct FMaskRows9View;
struct FMaskRows9ConstView;

struct FMaskRows9ConstView {
    using View = FMaskRows9View;
    using ConstView = FMaskRows9ConstView;
    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.masks, self.values, self.tail);
    }

    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TConstArrayView<FFieldMask9> masks;
    TConstArrayView<int32> values;
    TConstArrayView<int32> tail;
};

struct FMaskRows9View {
    using View = FMaskRows9View;
    using ConstView = FMaskRows9ConstView;

    void set(int32 const index,
             FFieldMask9 const& new_masks,
             int32 const new_values,
             int32 const new_tail) const {
        masks[index] = new_masks;
        values[index] = new_values;
        tail[index] = new_tail;
    }
    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.masks, self.values, self.tail);
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TArrayView<FFieldMask9> masks;
    TArrayView<int32> values;
    TArrayView<int32> tail;
};

struct FMaskRows9 {
    using View = FMaskRows9View;
    using ConstView = FMaskRows9ConstView;
    void set(int32 const index,
             FFieldMask9 const& new_masks,
             int32 const new_values,
             int32 const new_tail) {
        masks[index] = new_masks;
        values[index] = new_values;
        tail[index] = new_tail;
    }

    auto add(FFieldMask9 const& new_masks, int32 const new_values, int32 const new_tail) -> int32 {
        auto const index{num()};
        masks.Add(new_masks);
        values.Add(new_values);
        tail.Add(new_tail);
        return index;
    }

    void apply_permutation(TArrayView<int32> indices);

    template <typename Compare>
    void sort(Compare&& compare, TArrayView<int32> scratch_indices) {
        ml::soa_ops::sort(*this, std::forward<Compare>(compare), scratch_indices);
    }

    template <auto Compare>
    void sort(TArrayView<int32> scratch_indices) {
        ml::soa_ops::sort<Compare>(*this, scratch_indices);
    }

    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.masks, self.values, self.tail);
    }

    template <typename Self, typename Other, typename TFunc>
    auto apply_array_pairs(this Self&& self, Other&& other, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(
            self.masks, other.masks, self.values, other.values, self.tail, other.tail);
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TArray<FFieldMask9> masks;
    TArray<int32> values;
    TArray<int32> tail;
};

enum class EField16 : uint8 {
    Values = 0,
    Count = static_cast<uint8>(Values) + (16),
};

struct FFieldMask16 {
    inline static constexpr int32 field_count{static_cast<int32>(EField16::Count)};
    static_assert(field_count <= 64, "Field mask exceeds 64 bits.");
    using storage_type = std::conditional_t<
        field_count <= 8,
        uint8,
        std::conditional_t<field_count <= 16,
                           uint16,
                           std::conditional_t<field_count <= 32, uint32, uint64>>>;

    constexpr FFieldMask16() noexcept = default;
    explicit constexpr FFieldMask16(storage_type const value) noexcept
        : value_{value} {}

    [[nodiscard]] constexpr auto value() const noexcept -> storage_type { return value_; }
    [[nodiscard]] constexpr auto is_empty() const noexcept -> bool { return value_ == 0; }
    [[nodiscard]] constexpr auto has(EField16 const field) const noexcept -> bool {
        return (value_ & bit(field)) != 0;
    }
    [[nodiscard]] static constexpr auto index(EField16 const field) noexcept -> int32 {
        return static_cast<int32>(field);
    }
    constexpr void set(EField16 const field) noexcept { value_ |= bit(field); }
    constexpr void set(FFieldMask16 const fields) noexcept { value_ |= fields.value_; }
    constexpr void clear(EField16 const field) noexcept {
        value_ = static_cast<storage_type>(value_ & ~bit(field));
    }

    [[nodiscard]] static constexpr auto values_field(int32 const value_index) noexcept -> EField16 {
        return static_cast<EField16>(static_cast<int32>(EField16::Values) + value_index);
    }
  private:
    [[nodiscard]] static constexpr auto bit(EField16 const field) noexcept -> storage_type {
        return static_cast<storage_type>(uint64{1} << static_cast<uint8>(field));
    }

    storage_type value_{};
};
static_assert(sizeof(FFieldMask16) == sizeof(FFieldMask16::storage_type));
static_assert(std::is_trivially_copyable_v<FFieldMask16>);

struct FMaskRows16View;
struct FMaskRows16ConstView;

struct FMaskRows16ConstView {
    using View = FMaskRows16View;
    using ConstView = FMaskRows16ConstView;
    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.masks, self.values);
    }

    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TConstArrayView<FFieldMask16> masks;
    TConstArrayView<int32> values;
};

struct FMaskRows16View {
    using View = FMaskRows16View;
    using ConstView = FMaskRows16ConstView;

    void set(int32 const index, FFieldMask16 const& new_masks, int32 const new_values) const {
        masks[index] = new_masks;
        values[index] = new_values;
    }
    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.masks, self.values);
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TArrayView<FFieldMask16> masks;
    TArrayView<int32> values;
};

struct FMaskRows16 {
    using View = FMaskRows16View;
    using ConstView = FMaskRows16ConstView;
    void set(int32 const index, FFieldMask16 const& new_masks, int32 const new_values) {
        masks[index] = new_masks;
        values[index] = new_values;
    }

    auto add(FFieldMask16 const& new_masks, int32 const new_values) -> int32 {
        auto const index{num()};
        masks.Add(new_masks);
        values.Add(new_values);
        return index;
    }

    void apply_permutation(TArrayView<int32> indices);

    template <typename Compare>
    void sort(Compare&& compare, TArrayView<int32> scratch_indices) {
        ml::soa_ops::sort(*this, std::forward<Compare>(compare), scratch_indices);
    }

    template <auto Compare>
    void sort(TArrayView<int32> scratch_indices) {
        ml::soa_ops::sort<Compare>(*this, scratch_indices);
    }

    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.masks, self.values);
    }

    template <typename Self, typename Other, typename TFunc>
    auto apply_array_pairs(this Self&& self, Other&& other, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.masks, other.masks, self.values, other.values);
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TArray<FFieldMask16> masks;
    TArray<int32> values;
};

enum class EField17 : uint8 {
    Values = 0,
    Count = static_cast<uint8>(Values) + (17),
};

struct FFieldMask17 {
    inline static constexpr int32 field_count{static_cast<int32>(EField17::Count)};
    static_assert(field_count <= 64, "Field mask exceeds 64 bits.");
    using storage_type = std::conditional_t<
        field_count <= 8,
        uint8,
        std::conditional_t<field_count <= 16,
                           uint16,
                           std::conditional_t<field_count <= 32, uint32, uint64>>>;

    constexpr FFieldMask17() noexcept = default;
    explicit constexpr FFieldMask17(storage_type const value) noexcept
        : value_{value} {}

    [[nodiscard]] constexpr auto value() const noexcept -> storage_type { return value_; }
    [[nodiscard]] constexpr auto is_empty() const noexcept -> bool { return value_ == 0; }
    [[nodiscard]] constexpr auto has(EField17 const field) const noexcept -> bool {
        return (value_ & bit(field)) != 0;
    }
    [[nodiscard]] static constexpr auto index(EField17 const field) noexcept -> int32 {
        return static_cast<int32>(field);
    }
    constexpr void set(EField17 const field) noexcept { value_ |= bit(field); }
    constexpr void set(FFieldMask17 const fields) noexcept { value_ |= fields.value_; }
    constexpr void clear(EField17 const field) noexcept {
        value_ = static_cast<storage_type>(value_ & ~bit(field));
    }

    [[nodiscard]] static constexpr auto values_field(int32 const value_index) noexcept -> EField17 {
        return static_cast<EField17>(static_cast<int32>(EField17::Values) + value_index);
    }
  private:
    [[nodiscard]] static constexpr auto bit(EField17 const field) noexcept -> storage_type {
        return static_cast<storage_type>(uint64{1} << static_cast<uint8>(field));
    }

    storage_type value_{};
};
static_assert(sizeof(FFieldMask17) == sizeof(FFieldMask17::storage_type));
static_assert(std::is_trivially_copyable_v<FFieldMask17>);

struct FMaskRows17View;
struct FMaskRows17ConstView;

struct FMaskRows17ConstView {
    using View = FMaskRows17View;
    using ConstView = FMaskRows17ConstView;
    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.masks, self.values);
    }

    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TConstArrayView<FFieldMask17> masks;
    TConstArrayView<int32> values;
};

struct FMaskRows17View {
    using View = FMaskRows17View;
    using ConstView = FMaskRows17ConstView;

    void set(int32 const index, FFieldMask17 const& new_masks, int32 const new_values) const {
        masks[index] = new_masks;
        values[index] = new_values;
    }
    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.masks, self.values);
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TArrayView<FFieldMask17> masks;
    TArrayView<int32> values;
};

struct FMaskRows17 {
    using View = FMaskRows17View;
    using ConstView = FMaskRows17ConstView;
    void set(int32 const index, FFieldMask17 const& new_masks, int32 const new_values) {
        masks[index] = new_masks;
        values[index] = new_values;
    }

    auto add(FFieldMask17 const& new_masks, int32 const new_values) -> int32 {
        auto const index{num()};
        masks.Add(new_masks);
        values.Add(new_values);
        return index;
    }

    void apply_permutation(TArrayView<int32> indices);

    template <typename Compare>
    void sort(Compare&& compare, TArrayView<int32> scratch_indices) {
        ml::soa_ops::sort(*this, std::forward<Compare>(compare), scratch_indices);
    }

    template <auto Compare>
    void sort(TArrayView<int32> scratch_indices) {
        ml::soa_ops::sort<Compare>(*this, scratch_indices);
    }

    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.masks, self.values);
    }

    template <typename Self, typename Other, typename TFunc>
    auto apply_array_pairs(this Self&& self, Other&& other, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.masks, other.masks, self.values, other.values);
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TArray<FFieldMask17> masks;
    TArray<int32> values;
};

enum class EField32 : uint8 {
    Values = 0,
    Count = static_cast<uint8>(Values) + (32),
};

struct FFieldMask32 {
    inline static constexpr int32 field_count{static_cast<int32>(EField32::Count)};
    static_assert(field_count <= 64, "Field mask exceeds 64 bits.");
    using storage_type = std::conditional_t<
        field_count <= 8,
        uint8,
        std::conditional_t<field_count <= 16,
                           uint16,
                           std::conditional_t<field_count <= 32, uint32, uint64>>>;

    constexpr FFieldMask32() noexcept = default;
    explicit constexpr FFieldMask32(storage_type const value) noexcept
        : value_{value} {}

    [[nodiscard]] constexpr auto value() const noexcept -> storage_type { return value_; }
    [[nodiscard]] constexpr auto is_empty() const noexcept -> bool { return value_ == 0; }
    [[nodiscard]] constexpr auto has(EField32 const field) const noexcept -> bool {
        return (value_ & bit(field)) != 0;
    }
    [[nodiscard]] static constexpr auto index(EField32 const field) noexcept -> int32 {
        return static_cast<int32>(field);
    }
    constexpr void set(EField32 const field) noexcept { value_ |= bit(field); }
    constexpr void set(FFieldMask32 const fields) noexcept { value_ |= fields.value_; }
    constexpr void clear(EField32 const field) noexcept {
        value_ = static_cast<storage_type>(value_ & ~bit(field));
    }

    [[nodiscard]] static constexpr auto values_field(int32 const value_index) noexcept -> EField32 {
        return static_cast<EField32>(static_cast<int32>(EField32::Values) + value_index);
    }
  private:
    [[nodiscard]] static constexpr auto bit(EField32 const field) noexcept -> storage_type {
        return static_cast<storage_type>(uint64{1} << static_cast<uint8>(field));
    }

    storage_type value_{};
};
static_assert(sizeof(FFieldMask32) == sizeof(FFieldMask32::storage_type));
static_assert(std::is_trivially_copyable_v<FFieldMask32>);

struct FMaskRows32View;
struct FMaskRows32ConstView;

struct FMaskRows32ConstView {
    using View = FMaskRows32View;
    using ConstView = FMaskRows32ConstView;
    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.masks, self.values);
    }

    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TConstArrayView<FFieldMask32> masks;
    TConstArrayView<int32> values;
};

struct FMaskRows32View {
    using View = FMaskRows32View;
    using ConstView = FMaskRows32ConstView;

    void set(int32 const index, FFieldMask32 const& new_masks, int32 const new_values) const {
        masks[index] = new_masks;
        values[index] = new_values;
    }
    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.masks, self.values);
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TArrayView<FFieldMask32> masks;
    TArrayView<int32> values;
};

struct FMaskRows32 {
    using View = FMaskRows32View;
    using ConstView = FMaskRows32ConstView;
    void set(int32 const index, FFieldMask32 const& new_masks, int32 const new_values) {
        masks[index] = new_masks;
        values[index] = new_values;
    }

    auto add(FFieldMask32 const& new_masks, int32 const new_values) -> int32 {
        auto const index{num()};
        masks.Add(new_masks);
        values.Add(new_values);
        return index;
    }

    void apply_permutation(TArrayView<int32> indices);

    template <typename Compare>
    void sort(Compare&& compare, TArrayView<int32> scratch_indices) {
        ml::soa_ops::sort(*this, std::forward<Compare>(compare), scratch_indices);
    }

    template <auto Compare>
    void sort(TArrayView<int32> scratch_indices) {
        ml::soa_ops::sort<Compare>(*this, scratch_indices);
    }

    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.masks, self.values);
    }

    template <typename Self, typename Other, typename TFunc>
    auto apply_array_pairs(this Self&& self, Other&& other, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.masks, other.masks, self.values, other.values);
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TArray<FFieldMask32> masks;
    TArray<int32> values;
};

enum class EField33 : uint8 {
    Values = 0,
    Count = static_cast<uint8>(Values) + (33),
};

struct FFieldMask33 {
    inline static constexpr int32 field_count{static_cast<int32>(EField33::Count)};
    static_assert(field_count <= 64, "Field mask exceeds 64 bits.");
    using storage_type = std::conditional_t<
        field_count <= 8,
        uint8,
        std::conditional_t<field_count <= 16,
                           uint16,
                           std::conditional_t<field_count <= 32, uint32, uint64>>>;

    constexpr FFieldMask33() noexcept = default;
    explicit constexpr FFieldMask33(storage_type const value) noexcept
        : value_{value} {}

    [[nodiscard]] constexpr auto value() const noexcept -> storage_type { return value_; }
    [[nodiscard]] constexpr auto is_empty() const noexcept -> bool { return value_ == 0; }
    [[nodiscard]] constexpr auto has(EField33 const field) const noexcept -> bool {
        return (value_ & bit(field)) != 0;
    }
    [[nodiscard]] static constexpr auto index(EField33 const field) noexcept -> int32 {
        return static_cast<int32>(field);
    }
    constexpr void set(EField33 const field) noexcept { value_ |= bit(field); }
    constexpr void set(FFieldMask33 const fields) noexcept { value_ |= fields.value_; }
    constexpr void clear(EField33 const field) noexcept {
        value_ = static_cast<storage_type>(value_ & ~bit(field));
    }

    [[nodiscard]] static constexpr auto values_field(int32 const value_index) noexcept -> EField33 {
        return static_cast<EField33>(static_cast<int32>(EField33::Values) + value_index);
    }
  private:
    [[nodiscard]] static constexpr auto bit(EField33 const field) noexcept -> storage_type {
        return static_cast<storage_type>(uint64{1} << static_cast<uint8>(field));
    }

    storage_type value_{};
};
static_assert(sizeof(FFieldMask33) == sizeof(FFieldMask33::storage_type));
static_assert(std::is_trivially_copyable_v<FFieldMask33>);

struct FMaskRows33View;
struct FMaskRows33ConstView;

struct FMaskRows33ConstView {
    using View = FMaskRows33View;
    using ConstView = FMaskRows33ConstView;
    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.masks, self.values);
    }

    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TConstArrayView<FFieldMask33> masks;
    TConstArrayView<int32> values;
};

struct FMaskRows33View {
    using View = FMaskRows33View;
    using ConstView = FMaskRows33ConstView;

    void set(int32 const index, FFieldMask33 const& new_masks, int32 const new_values) const {
        masks[index] = new_masks;
        values[index] = new_values;
    }
    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.masks, self.values);
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TArrayView<FFieldMask33> masks;
    TArrayView<int32> values;
};

struct FMaskRows33 {
    using View = FMaskRows33View;
    using ConstView = FMaskRows33ConstView;
    void set(int32 const index, FFieldMask33 const& new_masks, int32 const new_values) {
        masks[index] = new_masks;
        values[index] = new_values;
    }

    auto add(FFieldMask33 const& new_masks, int32 const new_values) -> int32 {
        auto const index{num()};
        masks.Add(new_masks);
        values.Add(new_values);
        return index;
    }

    void apply_permutation(TArrayView<int32> indices);

    template <typename Compare>
    void sort(Compare&& compare, TArrayView<int32> scratch_indices) {
        ml::soa_ops::sort(*this, std::forward<Compare>(compare), scratch_indices);
    }

    template <auto Compare>
    void sort(TArrayView<int32> scratch_indices) {
        ml::soa_ops::sort<Compare>(*this, scratch_indices);
    }

    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.masks, self.values);
    }

    template <typename Self, typename Other, typename TFunc>
    auto apply_array_pairs(this Self&& self, Other&& other, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.masks, other.masks, self.values, other.values);
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TArray<FFieldMask33> masks;
    TArray<int32> values;
};

struct RestrictionRowsSingleView;
struct RestrictionRowsSingleConstView;
struct RestrictionRowsSingleLayout {
    using size_type = int32;
    using byte_size_type = SIZE_T;

    inline static constexpr size_type capacity_granularity{
        ml::soa_storage::LayoutPolicy::capacity_granularity};
    inline static constexpr byte_size_type column_gap{ml::soa_storage::LayoutPolicy::column_gap};

    template <typename T>
    using ColLayout = ml::soa_storage::ColumnLayout<T>;
    inline static constexpr ml::soa_storage::ColumnLayoutStart LayoutStart{};

    inline static constexpr ColLayout<RestrictedLeaf> RestrictedColumn{LayoutStart};

    inline static constexpr byte_size_type allocation_alignment{
        RestrictedColumn.allocation_alignment};

    // Conservative per-block bound for checked capacity arithmetic; gaps do not scale with
    // capacity.
    inline static constexpr byte_size_type capacity_block_bound{
        ml::soa_storage::capacity_block_bound(RestrictedColumn)};
    inline static constexpr size_type max_capacity{
        ml::soa_storage::maximum_capacity(capacity_block_bound)};
    static constexpr auto layout_bytes(byte_size_type blocks) noexcept -> byte_size_type {
        return blocks == 0 ? 0 : RestrictedColumn.data_end(blocks);
    }
    static_assert(
        allocation_alignment <= std::numeric_limits<uint32>::max(),
        "Single-allocation alignment must fit the allocator's 32-bit alignment argument.");
    static_assert(max_capacity >= capacity_granularity);
};

template <bool Const>
struct RestrictionRowsSingleViewImpl : ml::soa_storage::CompactViewState<Const> {
    using Base = ml::soa_storage::CompactViewState<Const>;
    using Base::Base;
    using Base::validate;
    using size_type = typename Base::size_type;
    template <typename T>
    using Element = typename Base::template Element<T>;
    using View = RestrictionRowsSingleView;
    using ConstView = RestrictionRowsSingleConstView;
    RestrictionRowsSingleViewImpl() = default;
    template <bool Enabled = Const>
    RestrictionRowsSingleViewImpl(RestrictionRowsSingleViewImpl<false> const& other)
        requires Enabled
        : Base{other} {}
  protected:
    using Base::capacity_blocks;
    using Base::column_data;
    using Base::column_data_unchecked;
    using Base::count_;
    using Base::offset_;
    using Base::state_;
  public:
    auto restricted() const -> TArrayView<Element<RestrictedLeaf>> {
        return {this->template column_data<RestrictedLeaf>(
                    RestrictionRowsSingleLayout::RestrictedColumn.offset(capacity_blocks())),
                count_};
    }
    template <typename Func>
    auto apply_arrays(Func&& func) const -> decltype(auto) {
        auto column_0{restricted()};
        return std::forward<Func>(func)(column_0);
    }
};
struct RestrictionRowsSingleConstView : RestrictionRowsSingleViewImpl<true> {
    using Base = RestrictionRowsSingleViewImpl<true>;
    using Base::Base;
    using View = RestrictionRowsSingleView;
    using ConstView = RestrictionRowsSingleConstView;
    RestrictionRowsSingleConstView() = default;
    RestrictionRowsSingleConstView(RestrictionRowsSingleView const& other);
    auto get_const_view() const -> ConstView { return *this; }
    auto get_const_view(size_type offset, size_type count) const -> ConstView {
        return slice(offset, count);
    }
};
static_assert(ml::soa_storage_detail::validate_compact_view<RestrictionRowsSingleConstView>());
struct RestrictionRowsSingleView : RestrictionRowsSingleViewImpl<false> {
    using Base = RestrictionRowsSingleViewImpl<false>;
    using Base::Base;
    using View = RestrictionRowsSingleView;
    using ConstView = RestrictionRowsSingleConstView;
    RestrictionRowsSingleView() = default;
    auto get_const_view() const -> ConstView { return *this; }
    auto get_const_view(size_type offset, size_type count) const -> ConstView {
        return slice(offset, count);
    }
};
static_assert(ml::soa_storage_detail::validate_compact_view<RestrictionRowsSingleView>());
inline RestrictionRowsSingleConstView::RestrictionRowsSingleConstView(
    RestrictionRowsSingleView const& other)
    : Base{other} {}
struct SingleRestrictionRows
    : protected ml::soa_storage::StorageState
    , private ml::soa_storage::StorageOperations {
    using Operations = ml::soa_storage::StorageOperations;
    using Operations::allocated_bytes;
    using Operations::capacity;
    using Operations::is_empty;
    using Operations::num;
    using Layout = RestrictionRowsSingleLayout;
    using size_type = Layout::size_type;
    using byte_size_type = Layout::byte_size_type;
    inline static constexpr auto capacity_granularity = Layout::capacity_granularity;
    inline static constexpr auto allocation_alignment = Layout::allocation_alignment;
    inline static constexpr auto capacity_block_bound = Layout::capacity_block_bound;
    inline static constexpr auto max_capacity = Layout::max_capacity;
    static constexpr auto layout_bytes(byte_size_type blocks) noexcept -> byte_size_type {
        return Layout::layout_bytes(blocks);
    }
    using View = RestrictionRowsSingleView;
    using ConstView = RestrictionRowsSingleConstView;
    template <typename Source>
    inline static constexpr bool accepts_source = requires(Source const& source) {
        { source.num() } -> std::convertible_to<size_type>;
        source.validate();
        {
            ml::soa_storage::source_data(source.restricted())
        } -> std::convertible_to<RestrictedLeaf const*>;
    };
    /* **************************************** */
    // Lifetime
    /* **************************************** */
    SingleRestrictionRows() noexcept = default;
    ~SingleRestrictionRows() { ml::soa_storage::MimallocStorageAllocator::free(data_); }
    SingleRestrictionRows(SingleRestrictionRows const&) = delete;
    auto operator=(SingleRestrictionRows const&) -> SingleRestrictionRows& = delete;
    SingleRestrictionRows(SingleRestrictionRows&& other) noexcept
        : StorageState{std::exchange(other.data_, nullptr),
                       std::exchange(other.num_, 0),
                       std::exchange(other.capacity_, 0)} {}
    auto operator=(SingleRestrictionRows&& other) noexcept -> SingleRestrictionRows& {
        if (this != &other) {
            ml::soa_storage::MimallocStorageAllocator::free(data_);
            data_ = std::exchange(other.data_, nullptr);
            num_ = std::exchange(other.num_, 0);
            capacity_ = std::exchange(other.capacity_, 0);
        }
        return *this;
    }
  protected:
    template <typename Byte>
    struct DataPointers {
        template <typename T>
        using Element = std::conditional_t<std::is_const_v<Byte>, T const, T>;
        Element<RestrictedLeaf>* restricted{};
        auto operator+(size_type const offset) const noexcept -> DataPointers {
            if (restricted == nullptr) {
                return {};
            }
            return {restricted + offset};
        }
    };
    template <typename Self>
    auto get_data(this Self& self) noexcept {
        using Byte = std::conditional_t<std::is_const_v<Self>, std::byte const, std::byte>;
        if (self.data_ == nullptr) {
            return DataPointers<Byte>{};
        }
        return make_data_unchecked(static_cast<Byte*>(self.data_), self.capacity_blocks());
    }
    template <typename Self>
    auto get_data(this Self& self, size_type const offset) noexcept {
        return self.get_data() + offset;
    }
  private:
    friend struct ml::soa_storage::StorageOperations;
    /* **************************************** */
    // Column pointers
    /* **************************************** */
    template <typename Byte>
    static auto make_data_unchecked(Byte* const data, byte_size_type const blocks) noexcept
        -> DataPointers<Byte> {
        ml::soa_storage::LayoutCursor cursor{blocks};
        auto const pointer_at = [data](auto const& column, byte_size_type offset) noexcept {
            using Column = std::remove_cvref_t<decltype(column)>;
            using Pointer = std::conditional_t<std::is_const_v<Byte>,
                                               typename Column::const_pointer,
                                               typename Column::pointer>;
            return std::launder(reinterpret_cast<Pointer>(data + offset));
        };
        return {pointer_at(Layout::RestrictedColumn, cursor.advance(Layout::RestrictedColumn))};
    }
    auto capacity_blocks() const noexcept -> byte_size_type {
        return static_cast<byte_size_type>(capacity_ / capacity_granularity);
    }

    /* **************************************** */
    // Typed mutations and growth
    /* **************************************** */
    void default_construct_columns(size_type const first, size_type const count) {
        auto const columns{make_data_unchecked(data_, capacity_blocks()) + first};
        ml::soa_storage::default_construct_n(columns.restricted, count);
    }
    void swap_remove_columns(size_type const index,
                             size_type const source,
                             size_type const move_count) {
        copy_columns(get_data(), index, source, move_count);
    }
    static void copy_columns(DataPointers<std::byte> const& columns,
                             size_type index,
                             size_type source,
                             size_type move_count) {
        ml::soa_storage::copy_n(
            columns.restricted + index, columns.restricted + source, move_count);
    }
    void swap_remove_indices(std::span<size_type const> indices) {
        auto const columns{get_data()};
        ml::soa_storage_detail::for_each_removal_run(
            num_,
            indices,
            ml::soa_storage::require,
            [&](size_type index, size_type source, size_type count) {
                copy_columns(columns, index, source, count);
            });
    }
    template <typename Columns>
    void append_columns(Columns const& source,
                        size_type source_first,
                        size_type first,
                        size_type count) {
        auto const destination{get_data(first)};
        ml::soa_storage::copy_n(destination.restricted,
                                ml::soa_storage::source_data(source.restricted()) + source_first,
                                count);
    }
    void reallocate(size_type const new_capacity) {
        auto* const new_data{ml::soa_storage::MimallocStorageAllocator::allocate(
            layout_bytes(static_cast<byte_size_type>(new_capacity / capacity_granularity)),
            static_cast<uint32>(allocation_alignment))};
        if (num_ > 0) {
            auto const old_blocks{capacity_blocks()};
            auto const new_blocks{static_cast<byte_size_type>(new_capacity / capacity_granularity)};
            auto const source{
                make_data_unchecked(static_cast<std::byte const*>(data_), old_blocks)};
            auto const destination{make_data_unchecked(new_data, new_blocks)};
            ml::soa_storage::copy_n(destination.restricted, source.restricted, num_);
        }
        ml::soa_storage::MimallocStorageAllocator::free(data_);
        data_ = new_data;
        capacity_ = new_capacity;
    }
  public:
    template <typename Self>
    using ViewFor =
        std::conditional_t<std::is_const_v<std::remove_reference_t<Self>>, ConstView, View>;
    template <typename Self>
    auto get_view(this Self&& self) -> ViewFor<Self>
        requires std::is_lvalue_reference_v<Self>
    {
        return {&self, 0, self.num()};
    }
    template <typename Self>
    auto get_view(this Self&& self, size_type offset, size_type count) -> ViewFor<Self>
        requires std::is_lvalue_reference_v<Self>
    {
        return {&self, offset, count};
    }
    template <typename Self>
    auto slice(this Self&& self, size_type offset, size_type count) -> ViewFor<Self>
        requires std::is_lvalue_reference_v<Self>
    {
        return self.get_view(offset, count);
    }
    template <typename Self>
    auto left(this Self&& self, size_type count) -> ViewFor<Self>
        requires std::is_lvalue_reference_v<Self>
    {
        return self.get_view().left(count);
    }
    template <typename Self>
    auto right(this Self&& self, size_type count) -> ViewFor<Self>
        requires std::is_lvalue_reference_v<Self>
    {
        return self.get_view().right(count);
    }
    template <typename Self>
    auto get_const_view(this Self&& self) -> ConstView
        requires std::is_lvalue_reference_v<Self>
    {
        return {&self, 0, self.num()};
    }
    template <typename Self>
    auto get_const_view(this Self&& self, size_type offset, size_type count) -> ConstView
        requires std::is_lvalue_reference_v<Self>
    {
        return {&self, offset, count};
    }
};

struct FFixedChildView;
struct FFixedChildConstView;

struct FFixedChildConstView {
    using View = FFixedChildView;
    using ConstView = FFixedChildConstView;
    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.tracked);
    }

    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TConstArrayView<FTracked> tracked;
};

struct FFixedChildView {
    using View = FFixedChildView;
    using ConstView = FFixedChildConstView;

    void set(int32 const index, FTracked const& new_tracked) const { tracked[index] = new_tracked; }
    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.tracked);
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TArrayView<FTracked> tracked;
};

struct FFixedChild {
    using View = FFixedChildView;
    using ConstView = FFixedChildConstView;
    void set(int32 const index, FTracked const& new_tracked) { tracked[index] = new_tracked; }

    auto add(FTracked const& new_tracked) -> int32 {
        auto const index{num()};
        tracked.Add(new_tracked);
        return index;
    }

    void apply_permutation(TArrayView<int32> indices);

    template <typename Compare>
    void sort(Compare&& compare, TArrayView<int32> scratch_indices) {
        ml::soa_ops::sort(*this, std::forward<Compare>(compare), scratch_indices);
    }

    template <auto Compare>
    void sort(TArrayView<int32> scratch_indices) {
        ml::soa_ops::sort<Compare>(*this, scratch_indices);
    }

    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.tracked);
    }

    template <typename Self, typename Other, typename TFunc>
    auto apply_array_pairs(this Self&& self, Other&& other, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.tracked, other.tracked);
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TArray<FTracked> tracked;
};

template <int32 Capacity>
    requires (Capacity >= 0)
struct TFixedChildStorage {
    using View = FFixedChildView;
    using ConstView = FFixedChildConstView;

    auto get_view(int32 const offset, int32 const count) -> View {
        return View{TArrayView<FTracked>{tracked_.data() + offset, count}};
    }
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView {
        return ConstView{TConstArrayView<FTracked>{tracked_.data() + offset, count}};
    }

    template <typename TArg0>
    void construct_at(int32 const index, TArg0&& new_tracked) {
        tracked_.construct_at(index, std::forward<TArg0>(new_tracked));
    }

    void default_construct_at(int32 const index) { tracked_.construct_at(index); }

    void copy_construct_at(int32 const index,
                           TFixedChildStorage const& other,
                           int32 const other_index) {
        tracked_.construct_at(index, other.tracked_[other_index]);
    }

    void move_construct_at(int32 const index, TFixedChildStorage& other, int32 const other_index) {
        tracked_.construct_at(index, MoveTemp(other.tracked_[other_index]));
    }

    template <typename SourceView>
    void construct_from_view_at(int32 const index,
                                SourceView const& source,
                                int32 const source_index) {
        tracked_.construct_at(index, source.tracked[source_index]);
    }

    template <typename SourceView>
    void copy_assign_from_view_at(int32 const dst_index,
                                  SourceView const& source,
                                  int32 const source_index) {
        tracked_[dst_index] = source.tracked[source_index];
    }

    void
        move_assign_at(int32 const dst_index, TFixedChildStorage& other, int32 const source_index) {
        tracked_[dst_index] = MoveTemp(other.tracked_[source_index]);
    }

    void destroy_at(int32 const index) noexcept { tracked_.destroy_at(index); }

    ml::TFixedStorage<FTracked, Capacity> tracked_;
};

struct FFixedRowsView;
struct FFixedRowsConstView;

struct FFixedRowsConstView {
    using View = FFixedRowsView;
    using ConstView = FFixedRowsConstView;
    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.ids, self.children);
    }

    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TConstArrayView<int32> ids;
    FFixedChild::ConstView children;
};

struct FFixedRowsView {
    using View = FFixedRowsView;
    using ConstView = FFixedRowsConstView;
    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.ids, self.children);
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TArrayView<int32> ids;
    FFixedChild::View children;
};

struct FFixedRows {
    using View = FFixedRowsView;
    using ConstView = FFixedRowsConstView;
    void apply_permutation(TArrayView<int32> indices);

    template <typename Compare>
    void sort(Compare&& compare, TArrayView<int32> scratch_indices) {
        ml::soa_ops::sort(*this, std::forward<Compare>(compare), scratch_indices);
    }

    template <auto Compare>
    void sort(TArrayView<int32> scratch_indices) {
        ml::soa_ops::sort<Compare>(*this, scratch_indices);
    }

    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.ids, self.children);
    }

    template <typename Self, typename Other, typename TFunc>
    auto apply_array_pairs(this Self&& self, Other&& other, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(self.ids, other.ids, self.children, other.children);
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TArray<int32> ids;
    FFixedChild children;
};

template <int32 Capacity>
    requires (Capacity >= 0)
struct TFixedRowsStorage {
    using View = FFixedRowsView;
    using ConstView = FFixedRowsConstView;

    auto get_view(int32 const offset, int32 const count) -> View {
        return View{TArrayView<int32>{ids_.data() + offset, count},
                    children_.get_view(offset, count)};
    }
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView {
        return ConstView{TConstArrayView<int32>{ids_.data() + offset, count},
                         children_.get_const_view(offset, count)};
    }

    template <typename TArg0, typename TArg1>
    void construct_at(int32 const index, TArg0&& new_ids, TArg1&& new_children_tracked) {
        ids_.construct_at(index, std::forward<TArg0>(new_ids));
        children_.construct_at(index, std::forward<TArg1>(new_children_tracked));
    }

    void default_construct_at(int32 const index) {
        ids_.construct_at(index);
        children_.default_construct_at(index);
    }

    void copy_construct_at(int32 const index,
                           TFixedRowsStorage const& other,
                           int32 const other_index) {
        ids_.construct_at(index, other.ids_[other_index]);
        children_.copy_construct_at(index, other.children_, other_index);
    }

    void move_construct_at(int32 const index, TFixedRowsStorage& other, int32 const other_index) {
        ids_.construct_at(index, MoveTemp(other.ids_[other_index]));
        children_.move_construct_at(index, other.children_, other_index);
    }

    template <typename SourceView>
    void construct_from_view_at(int32 const index,
                                SourceView const& source,
                                int32 const source_index) {
        ids_.construct_at(index, source.ids[source_index]);
        children_.construct_from_view_at(index, source.children, source_index);
    }

    template <typename SourceView>
    void copy_assign_from_view_at(int32 const dst_index,
                                  SourceView const& source,
                                  int32 const source_index) {
        ids_[dst_index] = source.ids[source_index];
        children_.copy_assign_from_view_at(dst_index, source.children, source_index);
    }

    void move_assign_at(int32 const dst_index, TFixedRowsStorage& other, int32 const source_index) {
        ids_[dst_index] = MoveTemp(other.ids_[source_index]);
        children_.move_assign_at(dst_index, other.children_, source_index);
    }

    void destroy_at(int32 const index) noexcept {
        children_.destroy_at(index);
        ids_.destroy_at(index);
    }

    ml::TFixedStorage<int32, Capacity> ids_;
    TFixedChildStorage<Capacity> children_;
};

template <int32 Capacity>
    requires (Capacity >= 0)
struct TFixedRows {
    using size_type = int32;
    using View = FFixedRowsView;
    using ConstView = FFixedRowsConstView;

    static constexpr size_type capacity_value{Capacity};

    inline static constexpr bool supports_copy_construction{std::is_copy_constructible_v<int32> &&
                                                            std::is_copy_constructible_v<FTracked>};
    inline static constexpr bool supports_move_construction{std::is_move_constructible_v<int32> &&
                                                            std::is_move_constructible_v<FTracked>};
    inline static constexpr bool supports_nothrow_move_construction{
        std::is_nothrow_move_constructible_v<int32> &&
        std::is_nothrow_move_constructible_v<FTracked>};
    inline static constexpr bool supports_default_construction{
        std::is_default_constructible_v<int32> && std::is_default_constructible_v<FTracked>};
    inline static constexpr bool supports_uninitialised_storage{
        std::is_trivially_copyable_v<int32> && std::is_trivially_destructible_v<int32> &&
        std::is_trivially_copyable_v<FTracked> && std::is_trivially_destructible_v<FTracked>};
    template <typename TArg0, typename TArg1>
    inline static constexpr bool supports_element_construction_from{
        std::is_constructible_v<int32, TArg0&&> && std::is_constructible_v<FTracked, TArg1&&>};

    TFixedRows() noexcept = default;
    TFixedRows(TFixedRows const& other)
        requires supports_copy_construction
    {
        for (size_type i{}; i < other.size_; ++i) {
            storage_.copy_construct_at(size_, other.storage_, i);
            ++size_;
        }
    }
    TFixedRows(TFixedRows const&)
        requires (!supports_copy_construction)
    = delete;
    TFixedRows(TFixedRows&& other) noexcept(supports_nothrow_move_construction)
        requires supports_move_construction
    {
        for (size_type i{}; i < other.size_; ++i) {
            storage_.move_construct_at(size_, other.storage_, i);
            ++size_;
        }
        other.reset();
    }
    TFixedRows(TFixedRows&&)
        requires (!supports_move_construction)
    = delete;
    ~TFixedRows() { reset(); }

    auto operator=(TFixedRows const& other) -> TFixedRows&
        requires supports_copy_construction
    {
        if (this != std::addressof(other)) {
            reset();
            append_from(other.get_const_view());
        }
        return *this;
    }
    auto operator=(TFixedRows const&) -> TFixedRows&
        requires (!supports_copy_construction)
    = delete;
    auto operator=(TFixedRows&& other) noexcept(supports_nothrow_move_construction) -> TFixedRows&
        requires supports_move_construction
    {
        if (this != std::addressof(other)) {
            reset();
            for (size_type i{}; i < other.size_; ++i) {
                storage_.move_construct_at(size_, other.storage_, i);
                ++size_;
            }
            other.reset();
        }
        return *this;
    }
    auto operator=(TFixedRows&&) -> TFixedRows&
        requires (!supports_move_construction)
    = delete;

    static constexpr auto capacity() noexcept -> size_type { return capacity_value; }
    auto num() const noexcept -> size_type { return size_; }
    auto is_empty() const noexcept -> bool { return size_ == 0; }
    auto is_full() const noexcept -> bool { return size_ == capacity(); }

    auto get_view() -> View { return storage_.get_view(0, size_); }
    auto get_view(size_type const offset, size_type const count) -> View {
        check_view_range(offset, count);
        return storage_.get_view(offset, count);
    }
    auto get_view() const -> ConstView { return get_const_view(); }
    auto get_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view(offset, count);
    }
    auto get_const_view() const -> ConstView { return storage_.get_const_view(0, size_); }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        check_view_range(offset, count);
        return storage_.get_const_view(offset, count);
    }
    auto slice(size_type const offset, size_type const count) -> View {
        return get_view(offset, count);
    }
    auto left(size_type const count) -> View { return slice(0, count); }
    auto right(size_type const count) -> View { return slice(size_ - count, count); }
    auto slice(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view(offset, count);
    }
    auto left(size_type const count) const -> ConstView { return slice(0, count); }
    auto right(size_type const count) const -> ConstView { return slice(size_ - count, count); }

    template <typename TFunc>
    auto apply_arrays(TFunc&& func) -> decltype(auto) {
        auto view{get_view()};
        return view.apply_arrays(std::forward<TFunc>(func));
    }
    template <typename TFunc>
    auto apply_arrays(TFunc&& func) const -> decltype(auto) {
        auto view{get_const_view()};
        return view.apply_arrays(std::forward<TFunc>(func));
    }

    template <typename TArg0, typename TArg1>
        requires supports_element_construction_from<TArg0, TArg1>
    auto emplace_back(TArg0&& new_ids, TArg1&& new_children_tracked) -> size_type {
        check_has_sufficient_capacity(1);
        auto const index{size_};
        storage_.construct_at(
            index, std::forward<TArg0>(new_ids), std::forward<TArg1>(new_children_tracked));
        ++size_;
        return index;
    }
    template <typename TArg0, typename TArg1>
        requires supports_element_construction_from<TArg0, TArg1>
    auto add(TArg0&& new_ids, TArg1&& new_children_tracked) -> size_type {
        return emplace_back(std::forward<TArg0>(new_ids),
                            std::forward<TArg1>(new_children_tracked));
    }

    void add_defaulted(size_type const count = 1)
        requires supports_default_construction
    {
        check_has_sufficient_capacity(count);
        for (size_type i{}; i < count; ++i) {
            storage_.default_construct_at(size_);
            ++size_;
        }
    }
    void set_num(size_type const new_size)
        requires supports_default_construction
    {
        check(new_size >= 0);
        check(new_size <= capacity());
        if (new_size < size_) {
            destroy_from(new_size);
            return;
        }
        add_defaulted(new_size - size_);
    }
    void set_num(size_type const new_size, EAllowShrinking const)
        requires supports_default_construction
    {
        set_num(new_size);
    }
    auto capacity_view() -> View
        requires supports_uninitialised_storage
    {
        return storage_.get_view(0, capacity());
    }
    void set_num_uninitialised(size_type const new_size)
        requires supports_uninitialised_storage
    {
        check(new_size >= 0);
        check(new_size <= capacity());
        size_ = new_size;
    }
    void add_uninitialised(size_type const count)
        requires supports_uninitialised_storage
    {
        check_has_sufficient_capacity(count);
        size_ += count;
    }

    void pop() {
        check(!is_empty());
        --size_;
        storage_.destroy_at(size_);
    }
    void reset() noexcept { destroy_from(0); }
    void empty() noexcept { reset(); }
    void reserve(size_type const requested_capacity) const {
        check(requested_capacity >= 0);
        check(requested_capacity <= capacity());
    }
    void remove_at_swap(size_type const index, size_type const count, EAllowShrinking const) {
        check(index >= 0);
        check(count >= 0);
        check(index + count <= size_);
        if (count == 0) {
            return;
        }
        auto const available_tail{size_ - (index + count)};
        auto const move_count{count < available_tail ? count : available_tail};
        auto const source_begin{size_ - move_count};
        for (size_type i{}; i < move_count; ++i) {
            storage_.move_assign_at(index + i, storage_, source_begin + i);
        }
        destroy_from(size_ - count);
    }
    template <typename Other>
    void copy_element(size_type const dst_index, Other const& other, size_type const source_index) {
        check_index(dst_index);
        auto const source{other.get_const_view()};
        check(source_index >= 0);
        check(source_index < source.num());
        storage_.copy_assign_from_view_at(dst_index, source, source_index);
    }
    template <typename Other>
    void copy_elements(size_type const dst_index,
                       Other const& other,
                       size_type const source_index,
                       size_type const count) {
        check(dst_index >= 0);
        check(source_index >= 0);
        check(count >= 0);
        check(dst_index + count <= size_);
        auto const source{other.get_const_view()};
        check(source_index + count <= source.num());
        for (size_type i{}; i < count; ++i) {
            storage_.copy_assign_from_view_at(dst_index + i, source, source_index + i);
        }
    }
    template <typename Other>
    void append_from(Other const& other) {
        auto const source{other.get_const_view()};
        auto const count{source.num()};
        check_has_sufficient_capacity(count);
        for (size_type i{}; i < count; ++i) {
            storage_.construct_from_view_at(size_, source, i);
            ++size_;
        }
    }
  private:
    void check_index(size_type const index) const {
        check(index >= 0);
        check(index < size_);
    }
    void check_view_range(size_type const offset, size_type const count) const {
        check(offset >= 0);
        check(count >= 0);
        check(offset + count <= size_);
    }
    void check_has_sufficient_capacity(size_type const count) const {
        check(count >= 0);
        check(count <= capacity() - size_);
    }
    void destroy_from(size_type const first_index) noexcept {
        for (size_type i{size_}; i > first_index; --i) {
            storage_.destroy_at(i - 1);
        }
        size_ = first_index;
    }

    TFixedRowsStorage<Capacity> storage_;
    size_type size_{};
};

template <int32 Capacity>
    requires (Capacity >= 0)
struct TFixedRowsAlt {
    using size_type = int32;
    using View = FFixedRowsView;
    using ConstView = FFixedRowsConstView;

    static constexpr size_type capacity_value{Capacity};

    inline static constexpr bool supports_copy_construction{std::is_copy_constructible_v<int32> &&
                                                            std::is_copy_constructible_v<FTracked>};
    inline static constexpr bool supports_move_construction{std::is_move_constructible_v<int32> &&
                                                            std::is_move_constructible_v<FTracked>};
    inline static constexpr bool supports_nothrow_move_construction{
        std::is_nothrow_move_constructible_v<int32> &&
        std::is_nothrow_move_constructible_v<FTracked>};
    inline static constexpr bool supports_default_construction{
        std::is_default_constructible_v<int32> && std::is_default_constructible_v<FTracked>};
    inline static constexpr bool supports_uninitialised_storage{
        std::is_trivially_copyable_v<int32> && std::is_trivially_destructible_v<int32> &&
        std::is_trivially_copyable_v<FTracked> && std::is_trivially_destructible_v<FTracked>};
    template <typename TArg0, typename TArg1>
    inline static constexpr bool supports_element_construction_from{
        std::is_constructible_v<int32, TArg0&&> && std::is_constructible_v<FTracked, TArg1&&>};

    TFixedRowsAlt() noexcept = default;
    TFixedRowsAlt(TFixedRowsAlt const& other)
        requires supports_copy_construction
    {
        for (size_type i{}; i < other.size_; ++i) {
            storage_.copy_construct_at(size_, other.storage_, i);
            ++size_;
        }
    }
    TFixedRowsAlt(TFixedRowsAlt const&)
        requires (!supports_copy_construction)
    = delete;
    TFixedRowsAlt(TFixedRowsAlt&& other) noexcept(supports_nothrow_move_construction)
        requires supports_move_construction
    {
        for (size_type i{}; i < other.size_; ++i) {
            storage_.move_construct_at(size_, other.storage_, i);
            ++size_;
        }
        other.reset();
    }
    TFixedRowsAlt(TFixedRowsAlt&&)
        requires (!supports_move_construction)
    = delete;
    ~TFixedRowsAlt() { reset(); }

    auto operator=(TFixedRowsAlt const& other) -> TFixedRowsAlt&
        requires supports_copy_construction
    {
        if (this != std::addressof(other)) {
            reset();
            append_from(other.get_const_view());
        }
        return *this;
    }
    auto operator=(TFixedRowsAlt const&) -> TFixedRowsAlt&
        requires (!supports_copy_construction)
    = delete;
    auto operator=(TFixedRowsAlt&& other) noexcept(supports_nothrow_move_construction)
        -> TFixedRowsAlt&
        requires supports_move_construction
    {
        if (this != std::addressof(other)) {
            reset();
            for (size_type i{}; i < other.size_; ++i) {
                storage_.move_construct_at(size_, other.storage_, i);
                ++size_;
            }
            other.reset();
        }
        return *this;
    }
    auto operator=(TFixedRowsAlt&&) -> TFixedRowsAlt&
        requires (!supports_move_construction)
    = delete;

    static constexpr auto capacity() noexcept -> size_type { return capacity_value; }
    auto num() const noexcept -> size_type { return size_; }
    auto is_empty() const noexcept -> bool { return size_ == 0; }
    auto is_full() const noexcept -> bool { return size_ == capacity(); }

    auto get_view() -> View { return storage_.get_view(0, size_); }
    auto get_view(size_type const offset, size_type const count) -> View {
        check_view_range(offset, count);
        return storage_.get_view(offset, count);
    }
    auto get_view() const -> ConstView { return get_const_view(); }
    auto get_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view(offset, count);
    }
    auto get_const_view() const -> ConstView { return storage_.get_const_view(0, size_); }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        check_view_range(offset, count);
        return storage_.get_const_view(offset, count);
    }
    auto slice(size_type const offset, size_type const count) -> View {
        return get_view(offset, count);
    }
    auto left(size_type const count) -> View { return slice(0, count); }
    auto right(size_type const count) -> View { return slice(size_ - count, count); }
    auto slice(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view(offset, count);
    }
    auto left(size_type const count) const -> ConstView { return slice(0, count); }
    auto right(size_type const count) const -> ConstView { return slice(size_ - count, count); }

    template <typename TFunc>
    auto apply_arrays(TFunc&& func) -> decltype(auto) {
        auto view{get_view()};
        return view.apply_arrays(std::forward<TFunc>(func));
    }
    template <typename TFunc>
    auto apply_arrays(TFunc&& func) const -> decltype(auto) {
        auto view{get_const_view()};
        return view.apply_arrays(std::forward<TFunc>(func));
    }

    template <typename TArg0, typename TArg1>
        requires supports_element_construction_from<TArg0, TArg1>
    auto emplace_back(TArg0&& new_ids, TArg1&& new_children_tracked) -> size_type {
        check_has_sufficient_capacity(1);
        auto const index{size_};
        storage_.construct_at(
            index, std::forward<TArg0>(new_ids), std::forward<TArg1>(new_children_tracked));
        ++size_;
        return index;
    }
    template <typename TArg0, typename TArg1>
        requires supports_element_construction_from<TArg0, TArg1>
    auto add(TArg0&& new_ids, TArg1&& new_children_tracked) -> size_type {
        return emplace_back(std::forward<TArg0>(new_ids),
                            std::forward<TArg1>(new_children_tracked));
    }

    void add_defaulted(size_type const count = 1)
        requires supports_default_construction
    {
        check_has_sufficient_capacity(count);
        for (size_type i{}; i < count; ++i) {
            storage_.default_construct_at(size_);
            ++size_;
        }
    }
    void set_num(size_type const new_size)
        requires supports_default_construction
    {
        check(new_size >= 0);
        check(new_size <= capacity());
        if (new_size < size_) {
            destroy_from(new_size);
            return;
        }
        add_defaulted(new_size - size_);
    }
    void set_num(size_type const new_size, EAllowShrinking const)
        requires supports_default_construction
    {
        set_num(new_size);
    }
    auto capacity_view() -> View
        requires supports_uninitialised_storage
    {
        return storage_.get_view(0, capacity());
    }
    void set_num_uninitialised(size_type const new_size)
        requires supports_uninitialised_storage
    {
        check(new_size >= 0);
        check(new_size <= capacity());
        size_ = new_size;
    }
    void add_uninitialised(size_type const count)
        requires supports_uninitialised_storage
    {
        check_has_sufficient_capacity(count);
        size_ += count;
    }

    void pop() {
        check(!is_empty());
        --size_;
        storage_.destroy_at(size_);
    }
    void reset() noexcept { destroy_from(0); }
    void empty() noexcept { reset(); }
    void reserve(size_type const requested_capacity) const {
        check(requested_capacity >= 0);
        check(requested_capacity <= capacity());
    }
    void remove_at_swap(size_type const index, size_type const count, EAllowShrinking const) {
        check(index >= 0);
        check(count >= 0);
        check(index + count <= size_);
        if (count == 0) {
            return;
        }
        auto const available_tail{size_ - (index + count)};
        auto const move_count{count < available_tail ? count : available_tail};
        auto const source_begin{size_ - move_count};
        for (size_type i{}; i < move_count; ++i) {
            storage_.move_assign_at(index + i, storage_, source_begin + i);
        }
        destroy_from(size_ - count);
    }
    template <typename Other>
    void copy_element(size_type const dst_index, Other const& other, size_type const source_index) {
        check_index(dst_index);
        auto const source{other.get_const_view()};
        check(source_index >= 0);
        check(source_index < source.num());
        storage_.copy_assign_from_view_at(dst_index, source, source_index);
    }
    template <typename Other>
    void copy_elements(size_type const dst_index,
                       Other const& other,
                       size_type const source_index,
                       size_type const count) {
        check(dst_index >= 0);
        check(source_index >= 0);
        check(count >= 0);
        check(dst_index + count <= size_);
        auto const source{other.get_const_view()};
        check(source_index + count <= source.num());
        for (size_type i{}; i < count; ++i) {
            storage_.copy_assign_from_view_at(dst_index + i, source, source_index + i);
        }
    }
    template <typename Other>
    void append_from(Other const& other) {
        auto const source{other.get_const_view()};
        auto const count{source.num()};
        check_has_sufficient_capacity(count);
        for (size_type i{}; i < count; ++i) {
            storage_.construct_from_view_at(size_, source, i);
            ++size_;
        }
    }
  private:
    void check_index(size_type const index) const {
        check(index >= 0);
        check(index < size_);
    }
    void check_view_range(size_type const offset, size_type const count) const {
        check(offset >= 0);
        check(count >= 0);
        check(offset + count <= size_);
    }
    void check_has_sufficient_capacity(size_type const count) const {
        check(count >= 0);
        check(count <= capacity() - size_);
    }
    void destroy_from(size_type const first_index) noexcept {
        for (size_type i{size_}; i > first_index; --i) {
            storage_.destroy_at(i - 1);
        }
        size_ = first_index;
    }

    TFixedRowsStorage<Capacity> storage_;
    size_type size_{};
};
} // namespace codegen_compile_fixture
