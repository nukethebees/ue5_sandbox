// This file is autogenerated. Do not edit by hand.
// Edit lispb/schema and regenerate it instead.

#pragma once

#include "CoreMinimal.h"
#include "Containers/AllowShrinking.h"
#include "Containers/Array.h"
#include "Containers/ArrayView.h"
#include "Project/Point2f.h"
#include "Project/Vector2d.h"
#include "Project/Vector2f.h"
#include "SandboxCore/array_utils.h"

#include <type_traits>
#include <utility>

namespace codegen_compile_fixture {
template <typename T>
struct TValuesEquivalentType;

template <>
struct TValuesEquivalentType<float> {
    using type = FVector2f;
};

template <>
struct TValuesEquivalentType<double> {
    using type = FVector2d;
};

template <typename T>
struct TValuesView {
    using size_type = TArrayView<T>::SizeType;
    using value_type = std::remove_const_t<T>;
    using View = TValuesView<T>;
    using ConstView = TValuesView<value_type const>;
    using equivalent_type = typename TValuesEquivalentType<value_type>::type;

    TArrayView<T> xs;
    TArrayView<T> ys;

    auto get_view() -> View { return View{xs, ys}; }
    auto get_view(size_type const offset, size_type const count) -> View {
        return get_view().slice(offset, count);
    }
    auto get_view() const -> ConstView { return ConstView{xs, ys}; }
    auto get_view(size_type const offset, size_type const count) const -> ConstView {
        return get_view().slice(offset, count);
    }
    auto get_const_view() const -> ConstView { return ConstView{xs, ys}; }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    template <typename TFunc>
    auto apply_arrays(TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(xs, ys);
    }
    template <typename TFunc>
    auto apply_arrays(TFunc&& func) const -> decltype(auto) {
        return std::forward<TFunc>(func)(xs, ys);
    }
    auto num() const -> size_type { return xs.Num(); }
    auto is_empty() const -> bool { return num() == 0; }
    auto slice(size_type const offset, size_type const count) const -> TValuesView {
        return TValuesView{xs.Slice(offset, count), ys.Slice(offset, count)};
    }
    auto left(size_type const count) const -> TValuesView {
        return TValuesView{xs.Left(count), ys.Left(count)};
    }
    auto right(size_type const count) const -> TValuesView {
        return TValuesView{xs.Right(count), ys.Right(count)};
    }
    void set(size_type const index, value_type const x, value_type const y) const
        requires (!std::is_const_v<T>)
    {
        xs[index] = x;
        ys[index] = y;
    }
    void set(size_type const index, equivalent_type const& value) const
        requires (!std::is_const_v<T>)
    {
        set(index, value.X, value.Y);
    }
    auto operator[](size_type const index) const -> equivalent_type {
        return {xs.GetData()[index], ys.GetData()[index]};
    }
    auto at(size_type const index) const -> equivalent_type {
        check(index >= 0);
        check(index < num());
        return (*this)[index];
    }
};

struct FValuesf {
    using value_type = float;
    using equivalent_type = FVector2f;
    using size_type = TArray<value_type>::SizeType;
    using View = TValuesView<value_type>;
    using ConstView = TValuesView<value_type const>;

    struct Data {
        value_type* xs;
        value_type* ys;
    };

    struct ConstData {
        value_type const* xs;
        value_type const* ys;
    };

    TArray<value_type> xs;
    TArray<value_type> ys;

    auto get_data() -> Data { return Data{xs.GetData(), ys.GetData()}; }
    auto get_data() const -> ConstData { return ConstData{xs.GetData(), ys.GetData()}; }
    auto get_view() -> View { return View{xs, ys}; }
    auto get_view(size_type const offset, size_type const count) -> View {
        return get_view().slice(offset, count);
    }
    auto get_view() const -> ConstView { return ConstView{xs, ys}; }
    auto get_view(size_type const offset, size_type const count) const -> ConstView {
        return get_view().slice(offset, count);
    }
    auto get_const_view() const -> ConstView { return ConstView{xs, ys}; }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    template <typename TFunc>
    auto apply_arrays(TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(xs, ys);
    }
    template <typename TFunc>
    auto apply_arrays(TFunc&& func) const -> decltype(auto) {
        return std::forward<TFunc>(func)(xs, ys);
    }
    auto num() const -> size_type { return xs.Num(); }
    void validate_array_sizes() const { check(ys.Num() == xs.Num()); }
    auto is_empty() const -> bool { return num() == 0; }
    template <typename Other>
    auto copy_element(size_type const dst_i, Other const& src, size_type const src_i) -> void {
        xs[dst_i] = src.xs[src_i];
        ys[dst_i] = src.ys[src_i];
    }
    template <typename Other>
    auto copy_elements(size_type const dst_i,
                       Other const& src,
                       size_type const src_i,
                       size_type const count) -> void {
        for (auto i{0}; i < count; ++i) {
            xs[dst_i + i] = src.xs[src_i + i];
            ys[dst_i + i] = src.ys[src_i + i];
        }
    }
    template <typename Other>
    auto copy_to_tail(Other const& src) -> void {
        auto const count{src.num()};
        check(num() >= count);
        copy_elements(num() - count, src, 0, count);
    }
    template <typename Other>
    void append_from(Other const& other) {
        xs.Append(other.xs);
        ys.Append(other.ys);
    }
    void apply_permutation(TArrayView<int32> indices);
    template <typename Compare>
    void sort(Compare&& compare, TArrayView<int32> scratch_indices) {
        validate_array_sizes();
        auto const n{num()};
        check(scratch_indices.Num() == n);
        ml::fill_indices(scratch_indices);
        scratch_indices.Sort([this, &compare](int32 const lhs, int32 const rhs) {
            return compare(*this, lhs, rhs);
        });
        apply_permutation(scratch_indices);
    }
    template <auto Compare>
    void sort(TArrayView<int32> scratch_indices) {
        validate_array_sizes();
        auto const n{num()};
        check(scratch_indices.Num() == n);
        ml::fill_indices(scratch_indices);
        scratch_indices.Sort(
            [this](int32 const lhs, int32 const rhs) { return Compare(*this, lhs, rhs); });
        apply_permutation(scratch_indices);
    }
    auto operator[](size_type const index) const -> equivalent_type {
        return {xs.GetData()[index], ys.GetData()[index]};
    }
    auto at(size_type const index) const -> equivalent_type {
        validate_array_sizes();
        check(index >= 0);
        check(index < num());
        return (*this)[index];
    }
    void set(size_type const index, value_type const x, value_type const y) {
        xs[index] = x;
        ys[index] = y;
    }
    auto add(value_type const x, value_type const y) -> size_type {
        auto const index{xs.Add(x)};
        ys.Add(y);
        return index;
    }
    void set(size_type const index, FVector2f const& value) { set(index, value.X, value.Y); }
    auto add(FVector2f const& value) -> size_type { return add(value.X, value.Y); }
    void set(size_type const index, FPoint2f const& value) { set(index, value.X, value.Y); }
    auto add(FPoint2f const& value) -> size_type { return add(value.X, value.Y); }
    auto reset() -> void {
        xs.Reset();
        ys.Reset();
    }
    auto empty() -> void {
        xs.Empty();
        ys.Empty();
    }
    auto reserve(size_type const count) -> void {
        xs.Reserve(count);
        ys.Reserve(count);
    }
    auto set_num(size_type const count, EAllowShrinking const allow_shrinking) -> void {
        xs.SetNum(count, allow_shrinking);
        ys.SetNum(count, allow_shrinking);
    }
    auto set_num_uninitialised(size_type const count) -> void {
        xs.SetNumUninitialized(count);
        ys.SetNumUninitialized(count);
    }
    auto add_uninitialised(size_type const count) -> void {
        xs.AddUninitialized(count);
        ys.AddUninitialized(count);
    }
    auto remove_at_swap(size_type const index,
                        size_type const count,
                        EAllowShrinking const allow_shrinking) -> void {
        xs.RemoveAtSwap(index, count, allow_shrinking);
        ys.RemoveAtSwap(index, count, allow_shrinking);
    }
    auto add_zeroed(size_type const count) -> void {
        xs.AddZeroed(count);
        ys.AddZeroed(count);
    }
    auto add_defaulted(size_type const count) -> void {
        xs.AddDefaulted(count);
        ys.AddDefaulted(count);
    }
};

struct FValuesd {
    using value_type = double;
    using equivalent_type = FVector2d;
    using size_type = TArray<value_type>::SizeType;
    using View = TValuesView<value_type>;
    using ConstView = TValuesView<value_type const>;

    struct Data {
        value_type* xs;
        value_type* ys;
    };

    struct ConstData {
        value_type const* xs;
        value_type const* ys;
    };

    TArray<value_type> xs;
    TArray<value_type> ys;

    auto get_data() -> Data { return Data{xs.GetData(), ys.GetData()}; }
    auto get_data() const -> ConstData { return ConstData{xs.GetData(), ys.GetData()}; }
    auto get_view() -> View { return View{xs, ys}; }
    auto get_view(size_type const offset, size_type const count) -> View {
        return get_view().slice(offset, count);
    }
    auto get_view() const -> ConstView { return ConstView{xs, ys}; }
    auto get_view(size_type const offset, size_type const count) const -> ConstView {
        return get_view().slice(offset, count);
    }
    auto get_const_view() const -> ConstView { return ConstView{xs, ys}; }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    template <typename TFunc>
    auto apply_arrays(TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(xs, ys);
    }
    template <typename TFunc>
    auto apply_arrays(TFunc&& func) const -> decltype(auto) {
        return std::forward<TFunc>(func)(xs, ys);
    }
    auto num() const -> size_type { return xs.Num(); }
    void validate_array_sizes() const { check(ys.Num() == xs.Num()); }
    auto is_empty() const -> bool { return num() == 0; }
    template <typename Other>
    auto copy_element(size_type const dst_i, Other const& src, size_type const src_i) -> void {
        xs[dst_i] = src.xs[src_i];
        ys[dst_i] = src.ys[src_i];
    }
    template <typename Other>
    auto copy_elements(size_type const dst_i,
                       Other const& src,
                       size_type const src_i,
                       size_type const count) -> void {
        for (auto i{0}; i < count; ++i) {
            xs[dst_i + i] = src.xs[src_i + i];
            ys[dst_i + i] = src.ys[src_i + i];
        }
    }
    template <typename Other>
    auto copy_to_tail(Other const& src) -> void {
        auto const count{src.num()};
        check(num() >= count);
        copy_elements(num() - count, src, 0, count);
    }
    template <typename Other>
    void append_from(Other const& other) {
        xs.Append(other.xs);
        ys.Append(other.ys);
    }
    void apply_permutation(TArrayView<int32> indices);
    template <typename Compare>
    void sort(Compare&& compare, TArrayView<int32> scratch_indices) {
        validate_array_sizes();
        auto const n{num()};
        check(scratch_indices.Num() == n);
        ml::fill_indices(scratch_indices);
        scratch_indices.Sort([this, &compare](int32 const lhs, int32 const rhs) {
            return compare(*this, lhs, rhs);
        });
        apply_permutation(scratch_indices);
    }
    template <auto Compare>
    void sort(TArrayView<int32> scratch_indices) {
        validate_array_sizes();
        auto const n{num()};
        check(scratch_indices.Num() == n);
        ml::fill_indices(scratch_indices);
        scratch_indices.Sort(
            [this](int32 const lhs, int32 const rhs) { return Compare(*this, lhs, rhs); });
        apply_permutation(scratch_indices);
    }
    auto operator[](size_type const index) const -> equivalent_type {
        return {xs.GetData()[index], ys.GetData()[index]};
    }
    auto at(size_type const index) const -> equivalent_type {
        validate_array_sizes();
        check(index >= 0);
        check(index < num());
        return (*this)[index];
    }
    void set(size_type const index, value_type const x, value_type const y) {
        xs[index] = x;
        ys[index] = y;
    }
    auto add(value_type const x, value_type const y) -> size_type {
        auto const index{xs.Add(x)};
        ys.Add(y);
        return index;
    }
    auto reset() -> void {
        xs.Reset();
        ys.Reset();
    }
    auto empty() -> void {
        xs.Empty();
        ys.Empty();
    }
    auto reserve(size_type const count) -> void {
        xs.Reserve(count);
        ys.Reserve(count);
    }
    auto set_num(size_type const count, EAllowShrinking const allow_shrinking) -> void {
        xs.SetNum(count, allow_shrinking);
        ys.SetNum(count, allow_shrinking);
    }
    auto set_num_uninitialised(size_type const count) -> void {
        xs.SetNumUninitialized(count);
        ys.SetNumUninitialized(count);
    }
    auto add_uninitialised(size_type const count) -> void {
        xs.AddUninitialized(count);
        ys.AddUninitialized(count);
    }
    auto remove_at_swap(size_type const index,
                        size_type const count,
                        EAllowShrinking const allow_shrinking) -> void {
        xs.RemoveAtSwap(index, count, allow_shrinking);
        ys.RemoveAtSwap(index, count, allow_shrinking);
    }
    auto add_zeroed(size_type const count) -> void {
        xs.AddZeroed(count);
        ys.AddZeroed(count);
    }
    auto add_defaulted(size_type const count) -> void {
        xs.AddDefaulted(count);
        ys.AddDefaulted(count);
    }
};

template <typename T>
struct TScalarView {
    using size_type = TArrayView<T>::SizeType;
    using value_type = std::remove_const_t<T>;
    using View = TScalarView<T>;
    using ConstView = TScalarView<value_type const>;

    TArrayView<T> values;

    auto get_view() -> View { return View{values}; }
    auto get_view(size_type const offset, size_type const count) -> View {
        return get_view().slice(offset, count);
    }
    auto get_view() const -> ConstView { return ConstView{values}; }
    auto get_view(size_type const offset, size_type const count) const -> ConstView {
        return get_view().slice(offset, count);
    }
    auto get_const_view() const -> ConstView { return ConstView{values}; }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    template <typename TFunc>
    auto apply_arrays(TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(values);
    }
    template <typename TFunc>
    auto apply_arrays(TFunc&& func) const -> decltype(auto) {
        return std::forward<TFunc>(func)(values);
    }
    auto num() const -> size_type { return values.Num(); }
    auto is_empty() const -> bool { return num() == 0; }
    auto slice(size_type const offset, size_type const count) const -> TScalarView {
        return TScalarView{values.Slice(offset, count)};
    }
    auto left(size_type const count) const -> TScalarView {
        return TScalarView{values.Left(count)};
    }
    auto right(size_type const count) const -> TScalarView {
        return TScalarView{values.Right(count)};
    }
    void set(size_type const index, value_type const v) const
        requires (!std::is_const_v<T>)
    {
        values[index] = v;
    }
};

struct FScalari {
    using value_type = int32;
    using size_type = TArray<value_type>::SizeType;
    using View = TScalarView<value_type>;
    using ConstView = TScalarView<value_type const>;

    struct Data {
        value_type* values;
    };

    struct ConstData {
        value_type const* values;
    };

    TArray<value_type> values;

    auto get_data() -> Data { return Data{values.GetData()}; }
    auto get_data() const -> ConstData { return ConstData{values.GetData()}; }
    auto get_view() -> View { return View{values}; }
    auto get_view(size_type const offset, size_type const count) -> View {
        return get_view().slice(offset, count);
    }
    auto get_view() const -> ConstView { return ConstView{values}; }
    auto get_view(size_type const offset, size_type const count) const -> ConstView {
        return get_view().slice(offset, count);
    }
    auto get_const_view() const -> ConstView { return ConstView{values}; }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView {
        return get_const_view().slice(offset, count);
    }
    template <typename TFunc>
    auto apply_arrays(TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(values);
    }
    template <typename TFunc>
    auto apply_arrays(TFunc&& func) const -> decltype(auto) {
        return std::forward<TFunc>(func)(values);
    }
    auto num() const -> size_type { return values.Num(); }
    void validate_array_sizes() const {}
    auto is_empty() const -> bool { return num() == 0; }
    template <typename Other>
    auto copy_element(size_type const dst_i, Other const& src, size_type const src_i) -> void {
        values[dst_i] = src.values[src_i];
    }
    template <typename Other>
    auto copy_elements(size_type const dst_i,
                       Other const& src,
                       size_type const src_i,
                       size_type const count) -> void {
        for (auto i{0}; i < count; ++i) {
            values[dst_i + i] = src.values[src_i + i];
        }
    }
    template <typename Other>
    auto copy_to_tail(Other const& src) -> void {
        auto const count{src.num()};
        check(num() >= count);
        copy_elements(num() - count, src, 0, count);
    }
    template <typename Other>
    void append_from(Other const& other) {
        values.Append(other.values);
    }
    void apply_permutation(TArrayView<int32> indices);
    template <typename Compare>
    void sort(Compare&& compare, TArrayView<int32> scratch_indices) {
        validate_array_sizes();
        auto const n{num()};
        check(scratch_indices.Num() == n);
        ml::fill_indices(scratch_indices);
        scratch_indices.Sort([this, &compare](int32 const lhs, int32 const rhs) {
            return compare(*this, lhs, rhs);
        });
        apply_permutation(scratch_indices);
    }
    template <auto Compare>
    void sort(TArrayView<int32> scratch_indices) {
        validate_array_sizes();
        auto const n{num()};
        check(scratch_indices.Num() == n);
        ml::fill_indices(scratch_indices);
        scratch_indices.Sort(
            [this](int32 const lhs, int32 const rhs) { return Compare(*this, lhs, rhs); });
        apply_permutation(scratch_indices);
    }
    void set(size_type const index, value_type const v) { values[index] = v; }
    auto add(value_type const v) -> size_type {
        auto const index{values.Add(v)};
        return index;
    }
    auto reset() -> void { values.Reset(); }
    auto empty() -> void { values.Empty(); }
    auto reserve(size_type const count) -> void { values.Reserve(count); }
    auto set_num(size_type const count, EAllowShrinking const allow_shrinking) -> void {
        values.SetNum(count, allow_shrinking);
    }
    auto set_num_uninitialised(size_type const count) -> void { values.SetNumUninitialized(count); }
    auto add_uninitialised(size_type const count) -> void { values.AddUninitialized(count); }
    auto remove_at_swap(size_type const index,
                        size_type const count,
                        EAllowShrinking const allow_shrinking) -> void {
        values.RemoveAtSwap(index, count, allow_shrinking);
    }
    auto add_zeroed(size_type const count) -> void { values.AddZeroed(count); }
    auto add_defaulted(size_type const count) -> void { values.AddDefaulted(count); }
};
} // namespace codegen_compile_fixture
