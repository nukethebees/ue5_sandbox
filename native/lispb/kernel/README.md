# SandboxCore kernel DSL

The kernel DSL declares fixed, typed operations for generated SandboxCore C++ kernels. Run the
matching repository generation target after changing a kernel module.

See [ARCHITECTURE.md](ARCHITECTURE.md) for the language model, emission profiles, and generated
output contract.
