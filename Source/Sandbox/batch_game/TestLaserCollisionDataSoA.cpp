// clang-format off
// This file is autogenerated. Do not edit by hand.
// Edit Codegen/manifest.py and regenerate it instead.

#include "TestLaserCollisionDataSoA.h"

#include "SandboxCore/array_checks.h"
#include "SandboxCore/container_ops.h"

namespace ml::test_lasers {
auto ComponentHitRangesConstView::get_view() const -> ConstView {
    return get_view(0, num());
}

auto ComponentHitRangesConstView::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<UPrimitiveComponent const*>{components}.Slice(offset, count),
        TConstArrayView<int32>{counts}.Slice(offset, count),
        TConstArrayView<int32>{offsets}.Slice(offset, count),
        TConstArrayView<int32>{next_write_indices}.Slice(offset, count),
    };
}

auto ComponentHitRangesConstView::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto ComponentHitRangesConstView::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<UPrimitiveComponent const*>{components}.Slice(offset, count),
        TConstArrayView<int32>{counts}.Slice(offset, count),
        TConstArrayView<int32>{offsets}.Slice(offset, count),
        TConstArrayView<int32>{next_write_indices}.Slice(offset, count),
    };
}

auto ComponentHitRangesConstView::num() const noexcept -> int32 {
    return ml::num(components);
}

void ComponentHitRangesConstView::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(components),
        ml::num(counts),
        ml::num(offsets),
        ml::num(next_write_indices),
    });
}

auto ComponentHitRangesConstView::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto ComponentHitRangesConstView::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto ComponentHitRangesConstView::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}

auto ComponentHitRangesView::get_view() -> View {
    return get_view(0, num());
}

auto ComponentHitRangesView::get_view(int32 const offset, int32 const count) -> View {
    return View{
        TArrayView<UPrimitiveComponent const*>{components}.Slice(offset, count),
        TArrayView<int32>{counts}.Slice(offset, count),
        TArrayView<int32>{offsets}.Slice(offset, count),
        TArrayView<int32>{next_write_indices}.Slice(offset, count),
    };
}

auto ComponentHitRangesView::get_view() const -> ConstView {
    return get_view(0, num());
}

auto ComponentHitRangesView::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<UPrimitiveComponent const*>{components}.Slice(offset, count),
        TConstArrayView<int32>{counts}.Slice(offset, count),
        TConstArrayView<int32>{offsets}.Slice(offset, count),
        TConstArrayView<int32>{next_write_indices}.Slice(offset, count),
    };
}

auto ComponentHitRangesView::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto ComponentHitRangesView::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<UPrimitiveComponent const*>{components}.Slice(offset, count),
        TConstArrayView<int32>{counts}.Slice(offset, count),
        TConstArrayView<int32>{offsets}.Slice(offset, count),
        TConstArrayView<int32>{next_write_indices}.Slice(offset, count),
    };
}

auto ComponentHitRangesView::num() const noexcept -> int32 {
    return ml::num(components);
}

void ComponentHitRangesView::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(components),
        ml::num(counts),
        ml::num(offsets),
        ml::num(next_write_indices),
    });
}

auto ComponentHitRangesView::slice(int32 const offset, int32 const count) -> View {
    return get_view(offset, count);
}

auto ComponentHitRangesView::left(int32 const count) -> View {
    return slice(0, count);
}

auto ComponentHitRangesView::right(int32 const count) -> View {
    return slice(num() - count, count);
}

auto ComponentHitRangesView::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto ComponentHitRangesView::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto ComponentHitRangesView::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}

void ComponentHitRanges::reset() {
    ml::reset(components);
    ml::reset(counts);
    ml::reset(offsets);
    ml::reset(next_write_indices);
}

auto ComponentHitRanges::get_view() -> View {
    return get_view(0, num());
}

auto ComponentHitRanges::get_view(int32 const offset, int32 const count) -> View {
    return View{
        TArrayView<UPrimitiveComponent const*>{components}.Slice(offset, count),
        TArrayView<int32>{counts}.Slice(offset, count),
        TArrayView<int32>{offsets}.Slice(offset, count),
        TArrayView<int32>{next_write_indices}.Slice(offset, count),
    };
}

auto ComponentHitRanges::get_view() const -> ConstView {
    return get_view(0, num());
}

auto ComponentHitRanges::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<UPrimitiveComponent const*>{components}.Slice(offset, count),
        TConstArrayView<int32>{counts}.Slice(offset, count),
        TConstArrayView<int32>{offsets}.Slice(offset, count),
        TConstArrayView<int32>{next_write_indices}.Slice(offset, count),
    };
}

auto ComponentHitRanges::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto ComponentHitRanges::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<UPrimitiveComponent const*>{components}.Slice(offset, count),
        TConstArrayView<int32>{counts}.Slice(offset, count),
        TConstArrayView<int32>{offsets}.Slice(offset, count),
        TConstArrayView<int32>{next_write_indices}.Slice(offset, count),
    };
}

auto ComponentHitRanges::num() const noexcept -> int32 {
    return ml::num(components);
}

void ComponentHitRanges::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(components),
        ml::num(counts),
        ml::num(offsets),
        ml::num(next_write_indices),
    });
}

auto ComponentHitRanges::slice(int32 const offset, int32 const count) -> View {
    return get_view(offset, count);
}

auto ComponentHitRanges::left(int32 const count) -> View {
    return slice(0, count);
}

auto ComponentHitRanges::right(int32 const count) -> View {
    return slice(num() - count, count);
}

auto ComponentHitRanges::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto ComponentHitRanges::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto ComponentHitRanges::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}
} // namespace ml::test_lasers
// clang-format on
