// clang-format off
// This file is autogenerated. Do not edit by hand.
// Edit Codegen/manifest.py and regenerate it instead.

#include "TestCapitalShipFighterSpawnQueueSoA.h"

void TestCapitalShipFighterSpawnQueue::reset() {
    ml::reset(locations);
    ml::reset(rotations);
    ml::reset(teams);
    ml::reset(targets);
}

void TestCapitalShipFighterSpawnQueue::reserve(int32 const count) {
    ml::reserve(locations, count);
    ml::reserve(rotations, count);
    ml::reserve(teams, count);
    ml::reserve(targets, count);
}

void TestCapitalShipFighterSpawnQueue::add_uninitialised(int32 const count) {
    ml::add_uninitialised(locations, count);
    ml::add_uninitialised(rotations, count);
    ml::add_uninitialised(teams, count);
    ml::add_uninitialised(targets, count);
}

void TestCapitalShipFighterSpawnQueue::add_defaulted(int32 const count) {
    ml::add_defaulted(locations, count);
    ml::add_defaulted(rotations, count);
    ml::add_defaulted(teams, count);
    ml::add_defaulted(targets, count);
}

void TestCapitalShipFighterSpawnQueue::set_num(int32 const count, EAllowShrinking const allow_shrinking) {
    ml::set_num(locations, count, allow_shrinking);
    ml::set_num(rotations, count, allow_shrinking);
    ml::set_num(teams, count, allow_shrinking);
    ml::set_num(targets, count, allow_shrinking);
}
// clang-format on
