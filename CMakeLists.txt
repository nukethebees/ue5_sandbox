cmake_minimum_required(VERSION 4.4.2)

include(cmake/machine_activity_integration.cmake)
sandbox_configure_machine_activity()

project(SandboxUnrealBuild LANGUAGES CXX)
sandbox_add_machine_activity_status_target()

enable_testing()

add_subdirectory(cmake/compiler_warnings)
add_subdirectory(native/third_party/handmade_math)

find_package(Python3 REQUIRED COMPONENTS Interpreter)

option(SANDBOX_CODEGEN_ONLY "Configure only the standalone code generator" OFF)
option(SANDBOX_NATIVE_CORE_ONLY "Configure only the standalone native core library" OFF)
option(SANDBOX_NATIVE_MEMORY_ONLY "Configure only the standalone native memory library" OFF)
option(SANDBOX_NATIVE_SIMULATION_ONLY "Configure only the standalone native simulation library" OFF)
option(SANDBOX_NATIVE_MESH_ONLY "Configure only the standalone native mesh generator" OFF)
option(SANDBOX_NATIVE_S7_ONLY "Configure only the standalone native S7 library" OFF)
option(SANDBOX_KERNEL_BENCHMARKS "Build native generated-kernel benchmarks" OFF)
option(SANDBOX_NATIVE_SOA_BENCHMARKS "Build standalone experimental SoA benchmarks" OFF)
option(SANDBOX_KERNEL_BENCHMARK_PLOTS "Generate native kernel benchmark plots" OFF)

set(CACHE{UE_PLATFORM}
  TYPE STRING
  HELP "Unreal target platform passed to UnrealBuildTool"
  VALUE Win64
)

set(CACHE{UE_CONFIGURATION}
  TYPE STRING
  HELP "Unreal target configuration and native artifact configuration"
  VALUE Development
)
set_property(CACHE UE_CONFIGURATION PROPERTY STRINGS
  Debug
  DebugGame
  Development
  Shipping
  Test
)

if(SANDBOX_NATIVE_CORE_ONLY)
  add_subdirectory(native/core)
  return()
endif()

if(SANDBOX_NATIVE_MEMORY_ONLY)
  add_subdirectory(native/memory)
  return()
endif()

if(SANDBOX_NATIVE_SIMULATION_ONLY)
  add_subdirectory(native/simulation)
  return()
endif()

if(SANDBOX_NATIVE_MESH_ONLY)
  add_subdirectory(native/mesh_gen)
  return()
endif()

if(SANDBOX_NATIVE_S7_ONLY)
  add_subdirectory(native/s7)
  return()
endif()

add_subdirectory(native/sbx_mimalloc)
add_subdirectory(native/lispb)

if(SANDBOX_CODEGEN_ONLY)
  return()
endif()

set(CACHE{UE_ROOT}
  TYPE PATH
  HELP "Root directory of the Unreal Engine installation"
  VALUE "$ENV{UE_ROOT}"
)

add_subdirectory(native/memory)
add_subdirectory(native/s7)
add_subdirectory(native/core)
add_subdirectory(native/simulation)

set(CACHE{SANDBOX_TEST_SUITE}
  TYPE STRING
  HELP "Low-level test suite registered with CTest"
  VALUE default
)
set_property(CACHE SANDBOX_TEST_SUITE PROPERTY STRINGS
  default
  benchmark
  editor-only
)

set(CACHE{SANDBOX_ISMC_BENCHMARK_SECONDS}
  TYPE STRING
  HELP "Duration of the offscreen SandboxISMC paired benchmark"
  VALUE 10
)
set(CACHE{SANDBOX_ISMC_BENCHMARK_INSTANCES}
  TYPE STRING
  HELP "Instances per renderer in the offscreen SandboxISMC paired benchmark"
  VALUE 40000
)
set(CACHE{SANDBOX_ISMC_BENCHMARK_UPDATE_PERCENT}
  TYPE STRING
  HELP "Percentage of instances updated per frame in the offscreen SandboxISMC benchmark"
  VALUE 100
)
set(SANDBOX_ISMC_BENCHMARK_CHURN 0 CACHE STRING "Enable deterministic population churn (0 or 1)")
set(SANDBOX_ISMC_BENCHMARK_MIN_INSTANCES 1000 CACHE STRING "Minimum live population during churn")
set(SANDBOX_ISMC_BENCHMARK_HALF_CYCLE_UPDATES 120 CACHE STRING "Updates per grow/shrink half-cycle")
set(SANDBOX_ISMC_BENCHMARK_REPLACEMENT_PERCENT 5 CACHE STRING "Percentage of survivors replaced per update")
set(SANDBOX_ISMC_BENCHMARK_WARMUP_UPDATES 0 CACHE STRING "Initial updates excluded from benchmark samples")
set(CACHE{SANDBOX_ISMC_BENCHMARK_MODE}
  TYPE STRING
  HELP "SandboxISMC benchmark renderer mode: paired, custom, or engine_ismc"
  VALUE paired
)
set_property(CACHE SANDBOX_ISMC_BENCHMARK_MODE PROPERTY STRINGS
  paired
  custom
  engine_ismc
)
set(CACHE{SANDBOX_ISMC_BENCHMARK_VISIBILITY}
  TYPE STRING
  HELP "SandboxISMC benchmark visibility distribution: all, half, or none"
  VALUE all
)
set_property(CACHE SANDBOX_ISMC_BENCHMARK_VISIBILITY PROPERTY STRINGS
  all
  half
  none
)
set(CACHE{SANDBOX_ISMC_BENCHMARK_CUSTOM_DATA}
  TYPE STRING
  HELP "SandboxISMC benchmark custom data: none, static, or animated"
  VALUE none
)
set_property(CACHE SANDBOX_ISMC_BENCHMARK_CUSTOM_DATA PROPERTY STRINGS
  none
  static
  animated
)
set(CACHE{SANDBOX_ISMC_BENCHMARK_SHADOWS}
  TYPE STRING
  HELP "Whether the offscreen SandboxISMC benchmark casts shadows (0 or 1)"
  VALUE 0
)

set(CACHE{SANDBOX_SPARK_BENCHMARK_SECONDS}
  TYPE STRING
  HELP "Measured duration of the offscreen spark benchmark"
  VALUE 10
)
set(CACHE{SANDBOX_SPARK_BENCHMARK_CAPACITY}
  TYPE STRING
  HELP "Particle pool capacity used by the offscreen spark benchmark"
  VALUE 50000
)
set(CACHE{SANDBOX_SPARK_BENCHMARK_SPARKS_PER_HIT}
  TYPE STRING
  HELP "Sparks generated by each benchmark impact"
  VALUE 96
)
set(CACHE{SANDBOX_SPARK_BENCHMARK_IMPACTS_PER_FRAME}
  TYPE STRING
  HELP "Impact bursts submitted per frame by the offscreen spark benchmark"
  VALUE 100
)
set(CACHE{SANDBOX_SPARK_BENCHMARK_WARMUP_FRAMES}
  TYPE STRING
  HELP "Spark benchmark frames excluded before measurement"
  VALUE 60
)

set(CACHE{SANDBOX_TELEMETRY_BENCHMARK_SAMPLES}
  TYPE STRING
  HELP "Measured samples for the level telemetry storage benchmark"
  VALUE 7
)

set(CACHE{SANDBOX_FRAME_MEMORY_LEVEL_BENCHMARK_SECONDS}
  TYPE STRING
  HELP "Simulation duration of the frame-memory authored-level benchmark"
  VALUE 20
)

set(CACHE{SANDBOX_SPACE_GAME_TEST_TIME_SCALE}
  TYPE STRING
  HELP "Optional positive time scale for level-backed SpaceGame automation"
  VALUE ""
)

if(NOT CMAKE_HOST_WIN32)
  message(FATAL_ERROR
    "This initial CMake build layer supports Windows hosts only. "
    "It invokes Unreal's Engine/Build/BatchFiles/RunUBT.bat script.")
endif()

find_program(POWERSHELL_EXECUTABLE NAMES pwsh powershell REQUIRED)

if(NOT UE_ROOT)
  message(FATAL_ERROR
    "UE_ROOT is required. Set the UE_ROOT environment variable, pass "
    "-DUE_ROOT=<engine-root>, or set UE_ROOT in CMakeUserPresets.json.")
endif()

cmake_path(ABSOLUTE_PATH UE_ROOT NORMALIZE OUTPUT_VARIABLE UE_ROOT_ABSOLUTE)
file(REAL_PATH "${UE_ROOT_ABSOLUTE}" UE_ROOT_CANONICAL)

if(NOT DEFINED ENV{TEMP} OR "$ENV{TEMP}" STREQUAL "")
  message(FATAL_ERROR
    "The TEMP environment variable is required for the cross-worktree Unreal build lock.")
endif()

string(TOLOWER "${UE_ROOT_CANONICAL}" UE_BUILD_LOCK_IDENTITY)
string(SHA256 UE_BUILD_LOCK_HASH "${UE_BUILD_LOCK_IDENTITY}")
cmake_path(SET UE_BUILD_LOCK_PATH NORMALIZE
  "$ENV{TEMP}/SandboxUnrealBuild/locks/ue-${UE_BUILD_LOCK_HASH}.lock"
)

set(UE_LOCKED_COMMAND_PREFIX
  "${CMAKE_COMMAND}"
  "-DUNREAL_LOCK_PATH=${UE_BUILD_LOCK_PATH}"
  "-DUNREAL_ENGINE_ROOT=${UE_ROOT_CANONICAL}"
  "-DUNREAL_WORKING_DIRECTORY=${CMAKE_CURRENT_SOURCE_DIR}"
  "-DMACHINE_ACTIVITY_MODULE=${SANDBOX_MACHINE_ACTIVITY_MODULE}"
  -P "${CMAKE_CURRENT_SOURCE_DIR}/cmake/run_unreal_locked.cmake"
  --
)

cmake_path(APPEND UE_ROOT_ABSOLUTE Engine Build BatchFiles RunUBT.bat
  OUTPUT_VARIABLE UE_BUILD_SCRIPT
)
if(NOT EXISTS "${UE_BUILD_SCRIPT}")
  message(FATAL_ERROR
    "UE_ROOT='${UE_ROOT_ABSOLUTE}' is not a usable Unreal Engine installation. "
    "Expected to find '${UE_BUILD_SCRIPT}'.")
endif()

cmake_path(APPEND UE_ROOT_ABSOLUTE Engine Build BatchFiles GenerateProjectFiles.bat
  OUTPUT_VARIABLE UE_GENERATE_PROJECT_FILES_SCRIPT
)
if(NOT EXISTS "${UE_GENERATE_PROJECT_FILES_SCRIPT}")
  message(FATAL_ERROR
    "UE_ROOT='${UE_ROOT_ABSOLUTE}' is not a usable Unreal Engine installation. "
    "Expected to find '${UE_GENERATE_PROJECT_FILES_SCRIPT}'.")
endif()

if(UE_CONFIGURATION STREQUAL "Development")
  set(UE_EDITOR_NAME UnrealEditor.exe)
  set(UE_EDITOR_CMD_NAME UnrealEditor-Cmd.exe)
else()
  set(UE_EDITOR_NAME "UnrealEditor-Win64-${UE_CONFIGURATION}.exe")
  set(UE_EDITOR_CMD_NAME "UnrealEditor-Win64-${UE_CONFIGURATION}-Cmd.exe")
endif()

cmake_path(APPEND UE_ROOT_ABSOLUTE Engine Binaries Win64 "${UE_EDITOR_NAME}"
  OUTPUT_VARIABLE UE_EDITOR_EXE
)
if(NOT EXISTS "${UE_EDITOR_EXE}")
  message(FATAL_ERROR
    "UE_ROOT='${UE_ROOT_ABSOLUTE}' is not a usable Unreal Engine installation. "
    "Expected to find '${UE_EDITOR_EXE}'.")
endif()

cmake_path(APPEND UE_ROOT_ABSOLUTE Engine Binaries Win64 "${UE_EDITOR_CMD_NAME}"
  OUTPUT_VARIABLE UE_EDITOR_CMD_EXE
)
if(NOT EXISTS "${UE_EDITOR_CMD_EXE}")
  message(FATAL_ERROR
    "UE_ROOT='${UE_ROOT_ABSOLUTE}' is not a usable Unreal Engine installation. "
    "Expected to find '${UE_EDITOR_CMD_EXE}'.")
endif()

set(SANDBOX_UPROJECT "${CMAKE_CURRENT_SOURCE_DIR}/Sandbox.uproject")
set(SANDBOX_LOCAL_DDC_DIR "${CMAKE_CURRENT_BINARY_DIR}/local-derived-data-cache")
set(SANDBOX_LIVE_CODING_DISABLED_ARGUMENT
  "-ini:EditorPerProjectUserSettings:[/Script/LiveCoding.LiveCodingSettings]:bEnabled=False"
)
file(MAKE_DIRECTORY "${SANDBOX_LOCAL_DDC_DIR}")

add_custom_target(format-code
  COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/Scripts/format-cpp.py" --changed
  WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
  COMMENT "Formatting Git-changed project source files"
  USES_TERMINAL
  VERBATIM
)

add_custom_target(format-all-code
  COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/Scripts/format-cpp.py"
  WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
  COMMENT "Formatting all project source files"
  USES_TERMINAL
  VERBATIM
)

add_custom_target(generate-project-files
  COMMAND "${POWERSHELL_EXECUTABLE}" -NoProfile -File
    "${CMAKE_CURRENT_SOURCE_DIR}/PowerShell/Set-LiveCodingDisabled.ps1"
    -settings_path
    "${CMAKE_CURRENT_SOURCE_DIR}/Saved/Config/WindowsEditor/EditorPerProjectUserSettings.ini"
  COMMAND ${UE_LOCKED_COMMAND_PREFIX} "${UE_GENERATE_PROJECT_FILES_SCRIPT}"
    "-Project=${SANDBOX_UPROJECT}"
    -Game
    -WaitMutex
  WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
  COMMENT "Generating Visual Studio project files for ${SANDBOX_UPROJECT}"
  USES_TERMINAL
  VERBATIM
)
include(cmake/unreal.cmake)
include(cmake/catch2.cmake)

add_subdirectory(native/image)
add_subdirectory(native/mesh_gen)

set(UI_GLOW_MATERIAL_SOURCE
  "${CMAKE_CURRENT_SOURCE_DIR}/Plugins/SandboxUI/Source/SandboxUI/Private/materials/UiGlowComposite.lispb"
)
set(UI_GLOW_MATERIAL_ARTIFACT
  "${CMAKE_CURRENT_SOURCE_DIR}/Intermediate/MaterialGen/UiGlowComposite.smat"
)
set(UI_GLOW_MATERIAL_DEPFILE
  "${CMAKE_CURRENT_BINARY_DIR}/UiGlowComposite.smat.d"
)
add_custom_command(
  OUTPUT "${UI_GLOW_MATERIAL_ARTIFACT}"
  COMMAND "${CMAKE_COMMAND}" -E make_directory
    "${CMAKE_CURRENT_SOURCE_DIR}/Intermediate/MaterialGen"
  COMMAND $<TARGET_FILE:lispb> generate
    --project "${CMAKE_CURRENT_SOURCE_DIR}/lispb/project.lispb"
    --build-root "${CMAKE_BINARY_DIR}"
    --target ui-glow-material
    --depfile "${UI_GLOW_MATERIAL_DEPFILE}"
  DEPENDS lispb "${CMAKE_CURRENT_SOURCE_DIR}/lispb/project.lispb" "${UI_GLOW_MATERIAL_SOURCE}"
  DEPFILE "${UI_GLOW_MATERIAL_DEPFILE}"
  WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
  COMMENT "Compiling UiGlow material IR"
  VERBATIM
)
add_custom_target(compile-ui-glow-material DEPENDS "${UI_GLOW_MATERIAL_ARTIFACT}")

set(WORLD_SOFT_TARGET_MATERIAL_SOURCE
  "${CMAKE_CURRENT_SOURCE_DIR}/Plugins/SpaceGame/Source/SpaceGamePresentation/Private/materials/SoftTargetWorld.lispb"
)
set(WORLD_SOFT_TARGET_MATERIAL_ARTIFACT
  "${CMAKE_CURRENT_SOURCE_DIR}/Intermediate/MaterialGen/SoftTargetWorld.smat"
)
set(WORLD_SOFT_TARGET_MATERIAL_DEPFILE
  "${CMAKE_CURRENT_BINARY_DIR}/SoftTargetWorld.smat.d"
)
add_custom_command(
  OUTPUT "${WORLD_SOFT_TARGET_MATERIAL_ARTIFACT}"
  COMMAND "${CMAKE_COMMAND}" -E make_directory
    "${CMAKE_CURRENT_SOURCE_DIR}/Intermediate/MaterialGen"
  COMMAND $<TARGET_FILE:lispb> generate
    --project "${CMAKE_CURRENT_SOURCE_DIR}/lispb/project.lispb"
    --build-root "${CMAKE_BINARY_DIR}"
    --target world-soft-target-material
    --depfile "${WORLD_SOFT_TARGET_MATERIAL_DEPFILE}"
  DEPENDS lispb "${CMAKE_CURRENT_SOURCE_DIR}/lispb/project.lispb" "${WORLD_SOFT_TARGET_MATERIAL_SOURCE}"
  DEPFILE "${WORLD_SOFT_TARGET_MATERIAL_DEPFILE}"
  WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
  COMMENT "Compiling world soft-target material IR"
  VERBATIM
)
add_custom_target(compile-world-soft-target-material
  DEPENDS "${WORLD_SOFT_TARGET_MATERIAL_ARTIFACT}"
)

set(MIGRATED_MATERIAL_ARTIFACTS)
function(add_migrated_material_ir name target source artifact_name)
  set(source_path "${CMAKE_CURRENT_SOURCE_DIR}/${source}")
  set(artifact_path
    "${CMAKE_CURRENT_SOURCE_DIR}/Intermediate/MaterialGen/${artifact_name}.smat"
  )
  set(depfile_path "${CMAKE_CURRENT_BINARY_DIR}/${artifact_name}.smat.d")
  add_custom_command(
    OUTPUT "${artifact_path}"
    COMMAND "${CMAKE_COMMAND}" -E make_directory
      "${CMAKE_CURRENT_SOURCE_DIR}/Intermediate/MaterialGen"
    COMMAND $<TARGET_FILE:lispb> generate
      --project "${CMAKE_CURRENT_SOURCE_DIR}/lispb/project.lispb"
      --build-root "${CMAKE_BINARY_DIR}"
      --target "${target}"
      --depfile "${depfile_path}"
    DEPENDS lispb "${CMAKE_CURRENT_SOURCE_DIR}/lispb/project.lispb" "${source_path}"
    DEPFILE "${depfile_path}"
    WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
    COMMENT "Compiling ${name} material IR"
    VERBATIM
  )
  list(APPEND MIGRATED_MATERIAL_ARTIFACTS "${artifact_path}")
  set(MIGRATED_MATERIAL_ARTIFACTS "${MIGRATED_MATERIAL_ARTIFACTS}" PARENT_SCOPE)
endfunction()

set(SHADER_MATERIAL_SOURCE_ROOT
  "Plugins/SandboxShaders/Source/SbxShadersExperiments/Private/materials"
)
add_migrated_material_ir("radar display" radar-display-material
  "${SHADER_MATERIAL_SOURCE_ROOT}/RadarDisplay.lispb" RadarDisplay)
add_migrated_material_ir("energy shield" energy-shield-material
  "${SHADER_MATERIAL_SOURCE_ROOT}/EnergyShield.lispb" EnergyShield)
add_migrated_material_ir("space energy field" space-energy-field-material
  "${SHADER_MATERIAL_SOURCE_ROOT}/SpaceEnergyFieldDisplay.lispb" SpaceEnergyField)
add_migrated_material_ir("glowing cross" glowing-cross-material
  "Source/Sandbox/materials/GlowingCross.lispb" GlowingCross)
add_migrated_material_ir("vertex ripple" vertex-ripple-material
  "${SHADER_MATERIAL_SOURCE_ROOT}/VertexRipple.lispb" VertexRipple)
add_migrated_material_ir("construction spawn" construction-spawn-material
  "${SHADER_MATERIAL_SOURCE_ROOT}/ConstructionSpawn.lispb" ConstructionSpawn)
add_migrated_material_ir("planet atmosphere" planet-atmosphere-material
  "${SHADER_MATERIAL_SOURCE_ROOT}/PlanetAtmosphere.lispb" PlanetAtmosphere)
add_migrated_material_ir("planet surface" planet-surface-material
  "${SHADER_MATERIAL_SOURCE_ROOT}/PlanetSurface.lispb" PlanetSurface)
add_migrated_material_ir("tactical scan" tactical-scan-material
  "${SHADER_MATERIAL_SOURCE_ROOT}/TacticalScan.lispb" TacticalScan)
add_migrated_material_ir("celestial analytic" celestial-analytic-material
  "Plugins/SandboxShaders/Source/SandboxCelestials/Private/materials/CelestialAnalytic.lispb"
  CelestialAnalytic)
add_custom_target(compile-migrated-materials DEPENDS ${MIGRATED_MATERIAL_ARTIFACTS})

add_unreal_target(editor SandboxEditor)
add_unreal_target(game Sandbox)
add_custom_target(run-editor
  COMMAND ${standard_activity_command} "${UE_EDITOR_EXE}" "${SANDBOX_UPROJECT}"
    "${SANDBOX_LIVE_CODING_DISABLED_ARGUMENT}"
  DEPENDS editor
  WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
  COMMENT "Launching ${SANDBOX_UPROJECT} with Live Coding disabled"
  USES_TERMINAL
  VERBATIM
)
add_custom_target(run-editor-debug
  COMMAND ${standard_activity_command} "${UE_EDITOR_EXE}" "${SANDBOX_UPROJECT}"
    "${SANDBOX_LIVE_CODING_DISABLED_ARGUMENT}"
    -WaitForDebugger
  DEPENDS editor
  WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
  COMMENT "Launching ${SANDBOX_UPROJECT} with Live Coding disabled and waiting for a debugger"
  USES_TERMINAL
  VERBATIM
)
add_custom_target(check-space-game-layers
  COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/Scripts/check_space_game_layers.py"
  COMMENT "Checking SpaceGame module boundaries"
  VERBATIM
)
add_test(NAME SpaceGame.Architecture
  COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/Scripts/check_space_game_layers.py")
add_test(NAME SpaceGame.ArchitectureChecker
  COMMAND "${Python3_EXECUTABLE}" -m unittest Scripts.test_space_game_layers)
add_test(NAME Sandbox.LiveCodingConfig
  COMMAND "${POWERSHELL_EXECUTABLE}" -NoProfile -File
    "${CMAKE_CURRENT_SOURCE_DIR}/PowerShell/Set-LiveCodingDisabled.Tests.ps1")
set_tests_properties(
  SpaceGame.Architecture
  SpaceGame.ArchitectureChecker
  Sandbox.LiveCodingConfig
  PROPERTIES
  LABELS "unit;all"
  WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
)
add_dependencies(editor
  sandbox-image
  sandbox-mesh-gen
  check-space-game-layers
  check-generated-code
  check-generated-slate-code
  compile-ui-glow-material
  compile-world-soft-target-material
  compile-migrated-materials
  sandbox-material-gen
)
add_dependencies(game check-space-game-layers)
add_dependencies(game check-generated-code)

add_custom_target(worktree-dependencies
  COMMAND "${POWERSHELL_EXECUTABLE}" -NoProfile -File
    "${CMAKE_CURRENT_SOURCE_DIR}/PowerShell/Set-LiveCodingDisabled.ps1"
    -settings_path
    "${CMAKE_CURRENT_SOURCE_DIR}/Saved/Config/WindowsEditor/EditorPerProjectUserSettings.ini"
  DEPENDS
    native-core
    native-memory
    sbx-mimalloc
    native-simulation
    sandbox-s7
    sandbox-image
    sandbox-material-gen
    sandbox-mesh-gen
    check-generated-code
    check-generated-slate-code
    compile-ui-glow-material
  COMMENT "Preparing worktree dependencies and disabling Live Coding"
  VERBATIM
)

add_unreal_commandlet_target(generate-lab-images
  COMMANDLET GenerateSandboxImages
  COMMENT "Regenerating SandboxImages Lab PNGs"
)

add_unreal_commandlet_target(generate-slate-dsl-smoke-asset
  COMMANDLET GenerateSlateDslSmokeAsset
  COMMENT "Generating the Slate DSL smoke Editor Utility Widget asset"
)

add_unreal_commandlet_target(generate-scripted-level-assets
  COMMANDLET GenerateScriptedLevelAssets
  COMMENT "Generating scripted-level runtime and menu assets"
)

add_unreal_commandlet_target(import-game-audio
  COMMANDLET ImportGameAudio
  COMMENT "Importing optional local game audio"
)

add_unreal_commandlet_target(generate-lab-mesh-box
  COMMANDLET GenerateSandboxMeshBox
  COMMENT "Generating the SandboxMesh Lab box"
)

add_unreal_commandlet_target(generate-lab-mesh-cylinder
  COMMANDLET GenerateSandboxMeshCylinder
  COMMENT "Generating the SandboxMesh Lab cylinder"
)

add_unreal_commandlet_target(generate-lab-mesh-sphere
  COMMANDLET GenerateSandboxMeshSphere
  COMMENT "Generating the SandboxMesh Lab sphere"
)

add_unreal_commandlet_target(generate-lab-mesh-cone
  COMMANDLET GenerateSandboxMeshCone
  COMMENT "Generating the SandboxMesh Lab cone"
)

add_unreal_commandlet_target(generate-lab-mesh-hex-frame
  COMMANDLET GenerateSandboxMeshHexFrame
  COMMENT "Generating the SandboxMesh Lab hex frame"
)

add_unreal_commandlet_target(generate-lab-mesh-hex-tile
  COMMANDLET GenerateSandboxMeshHexTile
  COMMENT "Generating the SandboxMesh Lab hex tile"
)

add_unreal_commandlet_target(generate-lab-mesh-honeycomb-panel
  COMMANDLET GenerateSandboxMeshHoneycombPanel
  COMMENT "Generating the SandboxMesh Lab honeycomb panel"
)

add_unreal_commandlet_target(generate-lab-mesh-assemblies
  COMMANDLET GenerateSandboxMeshAssemblies
  COMMENT "Generating SandboxMesh Lab JSON assemblies"
)

add_unreal_benchmark_commandlet_target(heatmap-benchmark HeatmapBenchmark
  -Resolutions=32,64,128,256,512 -Warmup=10 -Iterations=100
  -Output=Saved/Benchmarks/HeatmapBenchmark.csv)
add_unreal_benchmark_commandlet_target(radar-3d-benchmark Radar3DBenchmark
  -ContactCounts=32,128,256,512 -Warmup=10 -Iterations=100
  -Output=Saved/Benchmarks/Radar3DBenchmark.csv)
add_unreal_benchmark_commandlet_target(scatter-3d-benchmark Scatter3DBenchmark
  -PointCounts=1,64,1024,16384,65536 -Warmup=20 -Iterations=100
  -Output=Saved/Benchmarks/Scatter3DBenchmark.csv)
add_unreal_benchmark_commandlet_target(volume-heatmap-3d-benchmark
  VolumeHeatmap3DBenchmark -d3d12)

set(SANDBOX_UNIT_AUTOMATION_FILTERS
  Sandbox.enum
  Sandbox.fighters
  Sandbox.geometry
  Sandbox.levels
  Sandbox.UnitTests
  USFLoader.UnitTests
  SandboxShaders.UnitTests
  SandboxCoreEngine.UnitTests
  SandboxGpuTutorials.UnitTests
  SandboxImages.UnitTests
  SandboxISMC.UnitTests
  SandboxMesh.UnitTests
  SandboxUI.UnitTests
)
set(SANDBOX_LEVEL_AUTOMATION_FILTERS
  Sandbox.LevelTests
  SandboxCoreEngine.LevelTests
)

add_unreal_automation_test(
  Sandbox.UnitTests
  FILTERS ${SANDBOX_UNIT_AUTOMATION_FILTERS}
  LABELS unit
)
add_unreal_automation_test(
  Sandbox.LevelTests
  FILTERS ${SANDBOX_LEVEL_AUTOMATION_FILTERS}
  LABELS level
)
add_unreal_automation_test(
  Sandbox.InputSmokeTests
  FILTERS Sandbox.LevelTests.PlayerInputSmoke
  LABELS input-smoke
)
add_unreal_automation_test(
  Sandbox.AllAutomationTests
  FILTERS
    ${SANDBOX_UNIT_AUTOMATION_FILTERS}
    ${SANDBOX_LEVEL_AUTOMATION_FILTERS}
  LABELS all
)

add_unreal_editor_test(SandboxISMC.RenderTests
  ARGUMENTS
    "-ExecCmds=Automation RunTests StartsWith:SandboxISMC.RenderTests; Quit"
    -RenderOffscreen
  LABELS level all
  TIMEOUT 900
)

add_unreal_editor_test(Sandbox.SparkRenderTests
  ARGUMENTS
    "-ExecCmds=Automation RunTests StartsWith:Sandbox.SparkRenderTests; Quit"
    -RenderOffscreen
  LABELS level all
  TIMEOUT 900
)

add_unreal_editor_test(SandboxBenchmarks.SparkBenchmark
  ACTIVITY BENCHMARK
  ARGUMENTS
    "-ExecCmds=Automation RunTests StartsWith:SandboxBenchmarks.SparkBenchmark; Quit"
    "-SandboxSparkBenchmarkSeconds=${SANDBOX_SPARK_BENCHMARK_SECONDS}"
    "-SandboxSparkBenchmarkCapacity=${SANDBOX_SPARK_BENCHMARK_CAPACITY}"
    "-SandboxSparkBenchmarkSparksPerHit=${SANDBOX_SPARK_BENCHMARK_SPARKS_PER_HIT}"
    "-SandboxSparkBenchmarkImpactsPerFrame=${SANDBOX_SPARK_BENCHMARK_IMPACTS_PER_FRAME}"
    "-SandboxSparkBenchmarkWarmupFrames=${SANDBOX_SPARK_BENCHMARK_WARMUP_FRAMES}"
    -RenderOffscreen
    -FullStdOutLogOutput
  LABELS spark-benchmark
  TIMEOUT 1200
)

add_unreal_editor_test(SandboxBenchmarks.LevelTelemetryStorage
  ACTIVITY BENCHMARK
  ARGUMENTS
    "-ExecCmds=Automation RunTests SandboxBenchmarks.LevelTelemetryStorage; Quit"
    "-SandboxTelemetryBenchmarkSamples=${SANDBOX_TELEMETRY_BENCHMARK_SAMPLES}"
    -FullStdOutLogOutput
  LABELS telemetry-benchmark
  TIMEOUT 1200
)

add_unreal_editor_test(SandboxBenchmarks.LevelTelemetryIntegration
  ACTIVITY BENCHMARK
  ARGUMENTS
    "-ExecCmds=Automation RunTests SandboxBenchmarks.TelemetryBenchmark; Quit"
    -FullStdOutLogOutput
  LABELS telemetry-benchmark
  TIMEOUT 1200
)

add_unreal_editor_test(SandboxBenchmarks.FrameMemoryLevel
  ACTIVITY BENCHMARK
  ARGUMENTS
    "-ExecCmds=Automation RunTests SandboxBenchmarks.FrameMemoryLevel; Quit"
    "-SandboxFrameMemoryLevelBenchmarkSeconds=${SANDBOX_FRAME_MEMORY_LEVEL_BENCHMARK_SECONDS}"
    -nullrhi
    -FullStdOutLogOutput
  LABELS frame-memory-level-benchmark
  TIMEOUT 1200
)

add_unreal_editor_test(SandboxISMC.RemoteBenchmark
  ACTIVITY BENCHMARK
  ARGUMENTS
    "-ExecCmds=Automation RunTests SandboxISMC.RemoteBenchmark; Quit"
    "-SandboxISMCBenchmarkSeconds=${SANDBOX_ISMC_BENCHMARK_SECONDS}"
    "-SandboxISMCBenchmarkInstances=${SANDBOX_ISMC_BENCHMARK_INSTANCES}"
    "-SandboxISMCBenchmarkChurn=${SANDBOX_ISMC_BENCHMARK_CHURN}"
    "-SandboxISMCBenchmarkMinInstances=${SANDBOX_ISMC_BENCHMARK_MIN_INSTANCES}"
    "-SandboxISMCBenchmarkHalfCycleUpdates=${SANDBOX_ISMC_BENCHMARK_HALF_CYCLE_UPDATES}"
    "-SandboxISMCBenchmarkReplacementPercent=${SANDBOX_ISMC_BENCHMARK_REPLACEMENT_PERCENT}"
    "-SandboxISMCBenchmarkWarmupUpdates=${SANDBOX_ISMC_BENCHMARK_WARMUP_UPDATES}"
    "-SandboxISMCBenchmarkUpdatePercent=${SANDBOX_ISMC_BENCHMARK_UPDATE_PERCENT}"
    "-SandboxISMCBenchmarkMode=${SANDBOX_ISMC_BENCHMARK_MODE}"
    "-SandboxISMCBenchmarkVisibility=${SANDBOX_ISMC_BENCHMARK_VISIBILITY}"
    "-SandboxISMCBenchmarkCustomData=${SANDBOX_ISMC_BENCHMARK_CUSTOM_DATA}"
    "-SandboxISMCBenchmarkShadows=${SANDBOX_ISMC_BENCHMARK_SHADOWS}"
    -SandboxISMCBenchmarkEndPIE
    -RenderOffscreen
    -FullStdOutLogOutput
  LABELS sandbox-ismc-benchmark
  TIMEOUT 600
)

add_custom_target(dev-core
  DEPENDS
    native-core-tests
    sandbox-image-tests
    mesh-gen-tests
    editor
    codegen-tests
    kernel-codegen-tests
    native-memory-tests
    sbx-mimalloc-tests
    sandbox-s7-tests
    slate-codegen-tests
)

if(SANDBOX_TEST_SUITE STREQUAL "benchmark")
  set(SANDBOX_SOA_SPACING_CPU_GROUP "0" CACHE STRING "Processor group for the pinned SoA confirmation")
  set(SANDBOX_SOA_SPACING_CPU "2" CACHE STRING "Logical CPU within the group for the pinned SoA confirmation")
  add_low_level_test_suite(benchmarks SandboxCoreBenchmarks SandboxCore
    ACTIVITY BENCHMARK
    LABELS benchmark
  )
  add_dependencies(benchmarks check-generated-code)
  add_custom_target(single-allocation-soa-spacing
    COMMAND ${benchmark_activity_command} uv run
      "${CMAKE_CURRENT_SOURCE_DIR}/Scripts/run-soa-spacing-experiment.py"
    WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
    USES_TERMINAL
    VERBATIM
  )
  add_custom_target(single-allocation-soa-spacing-confirmation
    COMMAND ${benchmark_activity_command} uv run
      "${CMAKE_CURRENT_SOURCE_DIR}/Scripts/run-soa-spacing-experiment.py" --suite confirmation
      --cpu-group "${SANDBOX_SOA_SPACING_CPU_GROUP}" --cpu "${SANDBOX_SOA_SPACING_CPU}"
    WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
    USES_TERMINAL
    VERBATIM
  )
  add_test(NAME SandboxCore.SingleAllocation.Spacing.ConfirmationSmoke
    COMMAND uv run "${CMAKE_CURRENT_SOURCE_DIR}/Scripts/run-soa-spacing-experiment.py"
      --suite confirmation --smoke --output-dir "${CMAKE_BINARY_DIR}/spacing-confirmation-smoke"
      --cpu-group "${SANDBOX_SOA_SPACING_CPU_GROUP}" --cpu "${SANDBOX_SOA_SPACING_CPU}"
  )
  set_tests_properties(SandboxCore.SingleAllocation.Spacing.ConfirmationSmoke PROPERTIES TIMEOUT 300 LABELS "soa-spacing-smoke")
  add_test(NAME SandboxCore.SingleAllocation.Spacing.ConfirmationReportTests
    COMMAND uv run --no-project python "${CMAKE_CURRENT_SOURCE_DIR}/Scripts/test_soa_spacing_confirmation.py"
  )
  set_tests_properties(SandboxCore.SingleAllocation.Spacing.ConfirmationReportTests PROPERTIES LABELS "soa-spacing-smoke")
  add_test(NAME SandboxCore.SingleAllocation.Spacing.PipelineSmoke
    COMMAND uv run "${CMAKE_CURRENT_SOURCE_DIR}/Scripts/run-soa-spacing-experiment.py"
      --smoke --output-dir "${CMAKE_BINARY_DIR}/spacing-smoke"
  )
  set_tests_properties(SandboxCore.SingleAllocation.Spacing.PipelineSmoke PROPERTIES TIMEOUT 300 LABELS "soa-spacing-smoke")
  add_custom_target(single-allocation-soa-plots
    COMMAND uv run "${CMAKE_CURRENT_SOURCE_DIR}/Scripts/plot-single-allocation-soa-benchmarks.py"
      --input "${CMAKE_BINARY_DIR}/Testing/Temporary/LastTest.log"
      --output-dir "${CMAKE_BINARY_DIR}/single-allocation-soa-plots"
    WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
    COMMENT "Plotting the single-allocation SoA comparison"
    VERBATIM
    USES_TERMINAL
  )
elseif(SANDBOX_TEST_SUITE STREQUAL "default")
  add_low_level_test_suite(core-tests SandboxCoreTests SandboxCore
    LABELS low-level-core)
  add_low_level_test_suite(native-tests SandboxNativeTests SandboxNative
    LABELS low-level-native)

  add_dependencies(core-tests check-generated-code)
  add_dependencies(native-tests check-generated-code)

  add_dependencies(dev-core
    core-tests
    native-tests
  )
elseif(SANDBOX_TEST_SUITE STREQUAL "editor-only")
  # Editor automation tests are registered above and need no low-level test executable.
else()
  message(FATAL_ERROR
    "SANDBOX_TEST_SUITE must be one of: default, benchmark, editor-only. "
    "Got '${SANDBOX_TEST_SUITE}'."
  )
endif()

add_custom_target(resave-assets
  COMMAND ${standard_activity_command} "${UE_EDITOR_CMD_EXE}" "${SANDBOX_UPROJECT}"
    -run=ResavePackages
    -projectonly
    -fixupredirects
    -unattended
  DEPENDS editor
  WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
  COMMENT "Resaving project assets and fixing redirectors"
  USES_TERMINAL
  VERBATIM
)

add_custom_target(generate-ui-glow-material
  COMMAND ${standard_activity_command} "${UE_EDITOR_CMD_EXE}" "${SANDBOX_UPROJECT}"
    -run=MaterialSynth
    "-Artifact=${UI_GLOW_MATERIAL_ARTIFACT}"
    -Generate -RenderOffscreen -AllowCommandletRendering
    -unattended -nop4 -nosplash -nosound -stdout
  DEPENDS editor
  WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
  USES_TERMINAL
  VERBATIM
)

add_custom_target(validate-ui-glow-material-unreal
  COMMAND ${standard_activity_command} "${UE_EDITOR_CMD_EXE}" "${SANDBOX_UPROJECT}"
    -run=MaterialSynth
    "-Artifact=${UI_GLOW_MATERIAL_ARTIFACT}"
    -Validate -unattended -nop4 -nosplash -nullrhi -nosound -stdout
  DEPENDS editor compile-ui-glow-material
  WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
  USES_TERMINAL
  VERBATIM
)

add_custom_target(validate-ui-glow-material
  COMMAND $<TARGET_FILE:lispb> validate
    --project "${CMAKE_CURRENT_SOURCE_DIR}/lispb/project.lispb"
    --build-root "${CMAKE_BINARY_DIR}"
    --target ui-glow-material
  DEPENDS lispb
  WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
  USES_TERMINAL
  VERBATIM
)
add_custom_target(generate-world-soft-target-assets
  COMMAND ${standard_activity_command} "${UE_EDITOR_CMD_EXE}" "${SANDBOX_UPROJECT}"
    -run=MaterialSynth
    "-Artifact=${WORLD_SOFT_TARGET_MATERIAL_ARTIFACT}"
    -Generate -RenderOffscreen -AllowCommandletRendering
    -unattended -nop4 -nosplash -nosound -stdout
  COMMAND ${standard_activity_command} "${UE_EDITOR_CMD_EXE}" "${SANDBOX_UPROJECT}"
    -run=GenerateWorldSoftTargetAssets -RenderOffscreen -AllowCommandletRendering
    -unattended -nop4 -nosplash -nosound -stdout
  DEPENDS editor
  WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
  USES_TERMINAL
  VERBATIM
)

add_custom_target(generate-migrated-materials
  COMMAND ${standard_activity_command} "${UE_EDITOR_CMD_EXE}" "${SANDBOX_UPROJECT}"
    -run=MaterialSynth
    "-ArtifactDirectory=${CMAKE_CURRENT_SOURCE_DIR}/Intermediate/MaterialGen"
    "-ArtifactList=UiGlowComposite,RadarDisplay,EnergyShield,SpaceEnergyField,GlowingCross,VertexRipple,ConstructionSpawn,PlanetAtmosphere,PlanetSurface,TacticalScan,CelestialAnalytic"
    -GenerateAll -RenderOffscreen -AllowCommandletRendering
    -unattended -nop4 -nosplash -nosound -stdout
  DEPENDS editor compile-migrated-materials
  WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
  USES_TERMINAL
  VERBATIM
)

add_custom_target(generate-celestial-analytic-material
  COMMAND "${UE_EDITOR_CMD_EXE}" "${SANDBOX_UPROJECT}"
    -run=MaterialSynth
    "-Artifact=${CMAKE_CURRENT_SOURCE_DIR}/Intermediate/MaterialGen/CelestialAnalytic.smat"
    -Generate -RenderOffscreen -AllowCommandletRendering
    -unattended -nop4 -nosplash -nosound -stdout
  DEPENDS editor
    "${CMAKE_CURRENT_SOURCE_DIR}/Intermediate/MaterialGen/CelestialAnalytic.smat"
  WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
  USES_TERMINAL
  VERBATIM
)

add_custom_target(validate-world-soft-target-material
  COMMAND $<TARGET_FILE:lispb> validate
    --project "${CMAKE_CURRENT_SOURCE_DIR}/lispb/project.lispb"
    --build-root "${CMAKE_BINARY_DIR}"
    --target world-soft-target-material
  DEPENDS lispb
  WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
  USES_TERMINAL
  VERBATIM
)

add_test(
  NAME SandboxShaders.ShaderSmoke
  COMMAND ${standard_activity_command} "${UE_EDITOR_CMD_EXE}" "${SANDBOX_UPROJECT}"
    "-ExecCmds=Automation RunTests StartsWith:SandboxShaders.ShaderSmoke; Quit"
    -AllowCommandletRendering
    -unattended
    -DDC-ForceMemoryCache
    -nop4
    -nosplash
    -nosound
    -stdout
)

add_test(
  NAME USFLoader.ShaderSmoke
  COMMAND ${standard_activity_command} "${UE_EDITOR_CMD_EXE}" "${SANDBOX_UPROJECT}"
    "-ExecCmds=Automation RunTests StartsWith:USFLoader.ShaderSmoke; Quit"
    -AllowCommandletRendering
    -unattended
    -DDC-ForceMemoryCache
    -nop4
    -nosplash
    -nosound
    -stdout
)

add_test(
  NAME SandboxEditor.MaterialSynth
  COMMAND ${standard_activity_command} "${UE_EDITOR_CMD_EXE}" "${SANDBOX_UPROJECT}"
    "-ExecCmds=Automation RunTests SandboxEditor.MaterialSynth; Quit"
    -RenderOffscreen
    -AllowCommandletRendering
    -DDC-ForceMemoryCache
    -unattended
    -nop4
    -nosplash
    -nosound
    -stdout
)
set_tests_properties(SandboxEditor.MaterialSynth PROPERTIES
  LABELS "material-synth;all"
  TIMEOUT 900
  WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
)
set_tests_properties(SandboxShaders.ShaderSmoke PROPERTIES
  LABELS all
  TIMEOUT 900
  WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
)
set_tests_properties(USFLoader.ShaderSmoke PROPERTIES
  LABELS all
  TIMEOUT 900
  WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
)

add_test(
  NAME SandboxUI.EntityOverlayBenchmark
  COMMAND ${benchmark_activity_command} "${UE_EDITOR_CMD_EXE}" "${SANDBOX_UPROJECT}"
    -run=EntityOverlayBenchmark
    -RenderOffscreen
    -AllowCommandletRendering
    -DDC-ForceMemoryCache
    -unattended
    -nop4
    -nosplash
    -nosound
    -stdout
)
set_tests_properties(SandboxUI.EntityOverlayBenchmark PROPERTIES
  LABELS entity-overlay-benchmark
  TIMEOUT 1200
  WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
)

add_test(
  NAME SandboxShaders.GpuStarfieldBenchmark
  COMMAND ${benchmark_activity_command} "${Python3_EXECUTABLE}"
    "${CMAKE_CURRENT_SOURCE_DIR}/Scripts/run-gpu-starfield-benchmark.py"
    "--editor=${UE_EDITOR_CMD_EXE}"
    "--project=${SANDBOX_UPROJECT}"
    "--output=${CMAKE_CURRENT_SOURCE_DIR}/Saved/Benchmarks/GpuStarfield"
)
set_tests_properties(SandboxShaders.GpuStarfieldBenchmark PROPERTIES
  LABELS gpu-benchmark
  TIMEOUT 2400
  WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
)

add_test(
  NAME SandboxShaders.GpuStarfieldMatrix
  COMMAND ${benchmark_activity_command} "${Python3_EXECUTABLE}"
    "${CMAKE_CURRENT_SOURCE_DIR}/Scripts/run-gpu-starfield-benchmark.py"
    "--editor=${UE_EDITOR_CMD_EXE}"
    "--project=${SANDBOX_UPROJECT}"
    "--output=${CMAKE_CURRENT_SOURCE_DIR}/Saved/Benchmarks/GpuStarfield"
    "--counts=100000,1000000"
    "--resolutions=1920x1080,3840x2160"
    "--size-multipliers=1,4"
    "--camera-modes=stationary"
)
set_tests_properties(SandboxShaders.GpuStarfieldMatrix PROPERTIES
  LABELS gpu-starfield-matrix
  TIMEOUT 3600
  WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
)

add_custom_target(gpu-tutorial-shader-tests
  COMMAND ${standard_activity_command} "${UE_EDITOR_CMD_EXE}" "${SANDBOX_UPROJECT}"
    "-ExecCmds=Automation RunTests StartsWith:SandboxGpuTutorials.UnitTests; Quit"
    -unattended
    -nop4
    -nosplash
    -nosound
    -DDC-ForceMemoryCache
    -stdout
  WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
  COMMENT "Compiling and validating SandboxGpuTutorials materials with the graphics RHI"
  VERBATIM
)
