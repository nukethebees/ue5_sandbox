set(CACHE{UE_ROOT}
  TYPE PATH
  HELP "Root directory of the Unreal Engine installation"
  VALUE "$ENV{UE_ROOT}"
)

set(CACHE{SANDBOX_TEST_SUITE}
  TYPE STRING
  HELP "Low-level test suite registered with CTest"
  VALUE default
)
set_property(CACHE SANDBOX_TEST_SUITE PROPERTY STRINGS
  default
  benchmark
  editor-only
)

set(CACHE{SANDBOX_ISMC_BENCHMARK_SECONDS}
  TYPE STRING
  HELP "Duration of the offscreen SandboxISMC paired benchmark"
  VALUE 10
)
set(CACHE{SANDBOX_ISMC_BENCHMARK_INSTANCES}
  TYPE STRING
  HELP "Instances per renderer in the offscreen SandboxISMC paired benchmark"
  VALUE 40000
)
set(CACHE{SANDBOX_ISMC_BENCHMARK_UPDATE_PERCENT}
  TYPE STRING
  HELP "Percentage of instances updated per frame in the offscreen SandboxISMC benchmark"
  VALUE 100
)
set(SANDBOX_ISMC_BENCHMARK_CHURN 0 CACHE STRING "Enable deterministic population churn (0 or 1)")
set(SANDBOX_ISMC_BENCHMARK_MIN_INSTANCES 1000 CACHE STRING "Minimum live population during churn")
set(SANDBOX_ISMC_BENCHMARK_HALF_CYCLE_UPDATES 120 CACHE STRING "Updates per grow/shrink half-cycle")
set(SANDBOX_ISMC_BENCHMARK_REPLACEMENT_PERCENT 5 CACHE STRING "Percentage of survivors replaced per update")
set(SANDBOX_ISMC_BENCHMARK_WARMUP_UPDATES 0 CACHE STRING "Initial updates excluded from benchmark samples")
set(CACHE{SANDBOX_ISMC_BENCHMARK_MODE}
  TYPE STRING
  HELP "SandboxISMC benchmark renderer mode: paired, custom, or engine_ismc"
  VALUE paired
)
set_property(CACHE SANDBOX_ISMC_BENCHMARK_MODE PROPERTY STRINGS
  paired
  custom
  engine_ismc
)
set(CACHE{SANDBOX_ISMC_BENCHMARK_VISIBILITY}
  TYPE STRING
  HELP "SandboxISMC benchmark visibility distribution: all, half, or none"
  VALUE all
)
set_property(CACHE SANDBOX_ISMC_BENCHMARK_VISIBILITY PROPERTY STRINGS
  all
  half
  none
)
set(CACHE{SANDBOX_ISMC_BENCHMARK_CUSTOM_DATA}
  TYPE STRING
  HELP "SandboxISMC benchmark custom data: none, static, or animated"
  VALUE none
)
set_property(CACHE SANDBOX_ISMC_BENCHMARK_CUSTOM_DATA PROPERTY STRINGS
  none
  static
  animated
)
set(CACHE{SANDBOX_ISMC_BENCHMARK_SHADOWS}
  TYPE STRING
  HELP "Whether the offscreen SandboxISMC benchmark casts shadows (0 or 1)"
  VALUE 0
)

set(CACHE{SANDBOX_SPARK_BENCHMARK_SECONDS}
  TYPE STRING
  HELP "Measured duration of the offscreen spark benchmark"
  VALUE 10
)
set(CACHE{SANDBOX_SPARK_BENCHMARK_CAPACITY}
  TYPE STRING
  HELP "Particle pool capacity used by the offscreen spark benchmark"
  VALUE 50000
)
set(CACHE{SANDBOX_SPARK_BENCHMARK_SPARKS_PER_HIT}
  TYPE STRING
  HELP "Sparks generated by each benchmark impact"
  VALUE 96
)
set(CACHE{SANDBOX_SPARK_BENCHMARK_IMPACTS_PER_FRAME}
  TYPE STRING
  HELP "Impact bursts submitted per frame by the offscreen spark benchmark"
  VALUE 100
)
set(CACHE{SANDBOX_SPARK_BENCHMARK_WARMUP_FRAMES}
  TYPE STRING
  HELP "Spark benchmark frames excluded before measurement"
  VALUE 60
)

set(CACHE{SANDBOX_TELEMETRY_BENCHMARK_SAMPLES}
  TYPE STRING
  HELP "Measured samples for the level telemetry storage benchmark"
  VALUE 7
)

set(CACHE{SANDBOX_SPACE_GAME_TEST_TIME_SCALE}
  TYPE STRING
  HELP "Optional positive time scale for level-backed SpaceGame automation"
  VALUE ""
)

if(NOT CMAKE_HOST_WIN32)
  message(FATAL_ERROR
    "This initial CMake build layer supports Windows hosts only. "
    "It invokes Unreal's Engine/Build/BatchFiles/RunUBT.bat script.")
endif()

find_program(POWERSHELL_EXECUTABLE NAMES pwsh powershell REQUIRED)

if(NOT UE_ROOT)
  message(FATAL_ERROR
    "UE_ROOT is required. Set the UE_ROOT environment variable, pass "
    "-DUE_ROOT=<engine-root>, or set UE_ROOT in CMakeUserPresets.json.")
endif()

include("${PROJECT_SOURCE_DIR}/cmake/unreal_paths.cmake")
sandbox_resolve_unreal_root(UE_ROOT_ABSOLUTE UE_ROOT_CANONICAL
  "${UE_ROOT}" "${PROJECT_SOURCE_DIR}")

include("${PROJECT_SOURCE_DIR}/cmake/unreal_jobserver.cmake")
sandbox_configure_unreal_jobserver("${UE_ROOT_CANONICAL}")
sandbox_unreal_jobserver_command(unreal_standard_activity_command STANDARD "Unreal command")
sandbox_unreal_jobserver_command(unreal_benchmark_activity_command BENCHMARK "Unreal benchmark")

sandbox_get_unreal_editor_names(UE_EDITOR_NAME UE_EDITOR_CMD_NAME "${UE_CONFIGURATION}")
sandbox_configure_unreal_engine_paths(UE "${UE_ROOT_ABSOLUTE}"
  "${UE_EDITOR_NAME}" "${UE_EDITOR_CMD_NAME}")

if(NOT EXISTS "${UE_BUILD_SCRIPT}")
  message(FATAL_ERROR
    "UE_ROOT='${UE_ROOT_ABSOLUTE}' is not a usable Unreal Engine installation. "
    "Expected to find '${UE_BUILD_SCRIPT}'.")
endif()

if(NOT EXISTS "${UE_RUN_UAT_SCRIPT}")
  message(FATAL_ERROR
    "UE_ROOT='${UE_ROOT_ABSOLUTE}' is not a usable Unreal Engine installation. "
    "Expected to find '${UE_RUN_UAT_SCRIPT}'.")
endif()

if(NOT EXISTS "${UE_GENERATE_PROJECT_FILES_SCRIPT}")
  message(FATAL_ERROR
    "UE_ROOT='${UE_ROOT_ABSOLUTE}' is not a usable Unreal Engine installation. "
    "Expected to find '${UE_GENERATE_PROJECT_FILES_SCRIPT}'.")
endif()

if(NOT EXISTS "${UE_EDITOR_EXE}")
  message(FATAL_ERROR
    "UE_ROOT='${UE_ROOT_ABSOLUTE}' is not a usable Unreal Engine installation. "
    "Expected to find '${UE_EDITOR_EXE}'.")
endif()

if(NOT EXISTS "${UE_EDITOR_CMD_EXE}")
  message(FATAL_ERROR
    "UE_ROOT='${UE_ROOT_ABSOLUTE}' is not a usable Unreal Engine installation. "
    "Expected to find '${UE_EDITOR_CMD_EXE}'.")
endif()

if(NOT EXISTS "${UE_UNREAL_PAK_EXE}")
  message(FATAL_ERROR
    "UE_ROOT='${UE_ROOT_ABSOLUTE}' is not a usable Unreal Engine installation. "
    "Expected to find '${UE_UNREAL_PAK_EXE}'.")
endif()

set(SANDBOX_UPROJECT "${PROJECT_SOURCE_DIR}/Sandbox.uproject")
set(SANDBOX_LOCAL_DDC_DIR "${PROJECT_BINARY_DIR}/local-derived-data-cache")
if(DEFINED ENV{SANDBOX_GAME_ARTIFACT_ROOT} AND
    NOT "$ENV{SANDBOX_GAME_ARTIFACT_ROOT}" STREQUAL "")
  set(sandbox_game_artifact_root_default "$ENV{SANDBOX_GAME_ARTIFACT_ROOT}")
else()
  set(sandbox_game_artifact_root_default "${PROJECT_SOURCE_DIR}/out/game-package")
endif()
set(SANDBOX_GAME_ARTIFACT_ROOT "${sandbox_game_artifact_root_default}" CACHE PATH
  "Root directory for cooked, staged, and archived game artifacts")
sandbox_configure_game_artifact_paths(SANDBOX_GAME
  "${SANDBOX_GAME_ARTIFACT_ROOT}" "${PROJECT_SOURCE_DIR}"
  "${UE_PLATFORM}" "${UE_CONFIGURATION}")
set(SANDBOX_GAME_ARTIFACT_ROOT_ABSOLUTE "${SANDBOX_GAME_ROOT}")
set(SANDBOX_GAME_PLATFORM_ROOT "${SANDBOX_GAME_ROOT}/${UE_PLATFORM}")
set(SANDBOX_GAME_CONFIGURATION_ROOT
  "${SANDBOX_GAME_PLATFORM_ROOT}/${UE_CONFIGURATION}")
set(SANDBOX_LIVE_CODING_DISABLED_ARGUMENT
  "-ini:EditorPerProjectUserSettings:[/Script/LiveCoding.LiveCodingSettings]:bEnabled=False"
)
set(SANDBOX_SET_LIVE_CODING_DISABLED_TOOL
  "${PROJECT_SOURCE_DIR}/tools/bin/SetLiveCodingDisabled.exe"
)
set(SANDBOX_UNREAL_BUILD_TOOLS
  "${PROJECT_SOURCE_DIR}/tools/bin/UnrealBuildTools.exe"
)
sandbox_add_staged_csharp_tool_preflight(sandbox-unreal-build-tools-preflight
  "UnrealBuildTools.exe" "${SANDBOX_UNREAL_BUILD_TOOLS}")
sandbox_add_staged_csharp_tool_preflight(sandbox-set-live-coding-disabled-preflight
  "SetLiveCodingDisabled.exe" "${SANDBOX_SET_LIVE_CODING_DISABLED_TOOL}")
file(MAKE_DIRECTORY "${SANDBOX_LOCAL_DDC_DIR}")

include("${PROJECT_SOURCE_DIR}/cmake/unreal.cmake")
include("${PROJECT_SOURCE_DIR}/cmake/unreal_packaging.cmake")
include("${PROJECT_SOURCE_DIR}/cmake/catch2.cmake")

add_subdirectory(maintenance)
add_subdirectory(build)

add_unreal_commandlet_target(generate-lab-images
  COMMANDLET GenerateSandboxImages
  COMMENT "Regenerating SandboxImages Lab PNGs"
)

add_unreal_commandlet_target(generate-slate-dsl-smoke-asset
  COMMANDLET GenerateSlateDslSmokeAsset
  COMMENT "Generating the Slate DSL smoke Editor Utility Widget asset"
)

add_unreal_commandlet_target(generate-scripted-level-assets
  COMMANDLET GenerateScriptedLevelAssets
  COMMENT "Generating scripted-level runtime and menu assets"
)

add_unreal_commandlet_target(import-game-audio
  COMMANDLET ImportGameAudio
  COMMENT "Importing optional local game audio"
)

add_unreal_commandlet_target(generate-lab-mesh-box
  COMMANDLET GenerateSandboxMeshBox
  COMMENT "Generating the SandboxMesh Lab box"
)

add_unreal_commandlet_target(generate-lab-mesh-cylinder
  COMMANDLET GenerateSandboxMeshCylinder
  COMMENT "Generating the SandboxMesh Lab cylinder"
)

add_unreal_commandlet_target(generate-lab-mesh-sphere
  COMMANDLET GenerateSandboxMeshSphere
  COMMENT "Generating the SandboxMesh Lab sphere"
)

add_unreal_commandlet_target(generate-lab-mesh-cone
  COMMANDLET GenerateSandboxMeshCone
  COMMENT "Generating the SandboxMesh Lab cone"
)

add_unreal_commandlet_target(generate-lab-mesh-hex-frame
  COMMANDLET GenerateSandboxMeshHexFrame
  COMMENT "Generating the SandboxMesh Lab hex frame"
)

add_unreal_commandlet_target(generate-lab-mesh-hex-tile
  COMMANDLET GenerateSandboxMeshHexTile
  COMMENT "Generating the SandboxMesh Lab hex tile"
)

add_unreal_commandlet_target(generate-lab-mesh-honeycomb-panel
  COMMANDLET GenerateSandboxMeshHoneycombPanel
  COMMENT "Generating the SandboxMesh Lab honeycomb panel"
)

add_unreal_commandlet_target(generate-lab-mesh-assemblies
  COMMANDLET GenerateSandboxMeshAssemblies
  COMMENT "Generating SandboxMesh Lab JSON assemblies"
)

add_unreal_benchmark_commandlet_target(heatmap-benchmark HeatmapBenchmark
  -Resolutions=32,64,128,256,512 -Warmup=10 -Iterations=100
  -Output=Saved/Benchmarks/HeatmapBenchmark.csv)
add_unreal_benchmark_commandlet_target(radar-3d-benchmark Radar3DBenchmark
  -ContactCounts=32,128,256,512 -Warmup=10 -Iterations=100
  -Output=Saved/Benchmarks/Radar3DBenchmark.csv)
add_unreal_benchmark_commandlet_target(scatter-3d-benchmark Scatter3DBenchmark
  -PointCounts=1,64,1024,16384,65536 -Warmup=20 -Iterations=100
  -Output=Saved/Benchmarks/Scatter3DBenchmark.csv)
add_unreal_benchmark_commandlet_target(volume-heatmap-3d-benchmark
  VolumeHeatmap3DBenchmark -d3d12)

set(SANDBOX_UNIT_AUTOMATION_FILTERS
  Sandbox.enum
  Sandbox.fighters
  Sandbox.geometry
  Sandbox.levels
  Sandbox.UnitTests
  SandboxShaders.UnitTests
  SandboxCoreEngine.UnitTests
  SandboxGpuTutorials.UnitTests
  SandboxImages.UnitTests
  SandboxISMC.UnitTests
  SandboxMesh.UnitTests
  SandboxUI.UnitTests
)
set(SANDBOX_LEVEL_AUTOMATION_FILTERS
  Sandbox.LevelTests
  SandboxCoreEngine.LevelTests
)

add_unreal_automation_test(
  Sandbox.ExportSimulationFixture
  FILTERS Sandbox.FixtureTools.SimulationFixtureExport
  LABELS fixture-tools
)

add_unreal_automation_test(
  Sandbox.UnitTests
  FILTERS ${SANDBOX_UNIT_AUTOMATION_FILTERS}
  LABELS unit
)
add_unreal_automation_test(
  Sandbox.LevelTests
  FILTERS ${SANDBOX_LEVEL_AUTOMATION_FILTERS}
  LABELS level
)
add_unreal_automation_test(
  Sandbox.InputSmokeTests
  FILTERS Sandbox.LevelTests.PlayerInputSmoke
  LABELS input-smoke
)
add_unreal_automation_test(
  Sandbox.AllAutomationTests
  FILTERS
    ${SANDBOX_UNIT_AUTOMATION_FILTERS}
    ${SANDBOX_LEVEL_AUTOMATION_FILTERS}
  LABELS all
)
add_unreal_automation_test(
  USFLoader.UnitTests
  FILTERS USFLoader.UnitTests
  LABELS usfloader
)

add_unreal_editor_test(SandboxISMC.RenderTests
  ARGUMENTS
    "-ExecCmds=Automation Now; Automation RunTests StartsWith:SandboxISMC.RenderTests; Quit"
    -RenderOffscreen
  LABELS level all
  TIMEOUT 900
)

add_unreal_editor_test(Sandbox.SparkRenderTests
  ARGUMENTS
    "-ExecCmds=Automation Now; Automation RunTests StartsWith:Sandbox.SparkRenderTests; Quit"
    -RenderOffscreen
  LABELS level all
  TIMEOUT 900
)

add_unreal_editor_test(SandboxBenchmarks.SparkBenchmark
  ACTIVITY BENCHMARK
  ARGUMENTS
    "-ExecCmds=Automation Now; Automation RunTests StartsWith:SandboxBenchmarks.SparkBenchmark; Quit"
    "-SandboxSparkBenchmarkSeconds=${SANDBOX_SPARK_BENCHMARK_SECONDS}"
    "-SandboxSparkBenchmarkCapacity=${SANDBOX_SPARK_BENCHMARK_CAPACITY}"
    "-SandboxSparkBenchmarkSparksPerHit=${SANDBOX_SPARK_BENCHMARK_SPARKS_PER_HIT}"
    "-SandboxSparkBenchmarkImpactsPerFrame=${SANDBOX_SPARK_BENCHMARK_IMPACTS_PER_FRAME}"
    "-SandboxSparkBenchmarkWarmupFrames=${SANDBOX_SPARK_BENCHMARK_WARMUP_FRAMES}"
    -RenderOffscreen
    -FullStdOutLogOutput
  LABELS spark-benchmark
  TIMEOUT 1200
)

add_unreal_editor_test(SandboxBenchmarks.LevelTelemetryStorage
  ACTIVITY BENCHMARK
  ARGUMENTS
    "-ExecCmds=Automation Now; Automation RunTests SandboxBenchmarks.LevelTelemetryStorage; Quit"
    "-SandboxTelemetryBenchmarkSamples=${SANDBOX_TELEMETRY_BENCHMARK_SAMPLES}"
    -FullStdOutLogOutput
  LABELS telemetry-benchmark
  TIMEOUT 1200
)

add_unreal_editor_test(SandboxISMC.RemoteBenchmark
  ACTIVITY BENCHMARK
  ARGUMENTS
    "-ExecCmds=Automation Now; Automation RunTests SandboxISMC.RemoteBenchmark; Quit"
    "-SandboxISMCBenchmarkSeconds=${SANDBOX_ISMC_BENCHMARK_SECONDS}"
    "-SandboxISMCBenchmarkInstances=${SANDBOX_ISMC_BENCHMARK_INSTANCES}"
    "-SandboxISMCBenchmarkChurn=${SANDBOX_ISMC_BENCHMARK_CHURN}"
    "-SandboxISMCBenchmarkMinInstances=${SANDBOX_ISMC_BENCHMARK_MIN_INSTANCES}"
    "-SandboxISMCBenchmarkHalfCycleUpdates=${SANDBOX_ISMC_BENCHMARK_HALF_CYCLE_UPDATES}"
    "-SandboxISMCBenchmarkReplacementPercent=${SANDBOX_ISMC_BENCHMARK_REPLACEMENT_PERCENT}"
    "-SandboxISMCBenchmarkWarmupUpdates=${SANDBOX_ISMC_BENCHMARK_WARMUP_UPDATES}"
    "-SandboxISMCBenchmarkUpdatePercent=${SANDBOX_ISMC_BENCHMARK_UPDATE_PERCENT}"
    "-SandboxISMCBenchmarkMode=${SANDBOX_ISMC_BENCHMARK_MODE}"
    "-SandboxISMCBenchmarkVisibility=${SANDBOX_ISMC_BENCHMARK_VISIBILITY}"
    "-SandboxISMCBenchmarkCustomData=${SANDBOX_ISMC_BENCHMARK_CUSTOM_DATA}"
    "-SandboxISMCBenchmarkShadows=${SANDBOX_ISMC_BENCHMARK_SHADOWS}"
    -SandboxISMCBenchmarkEndPIE
    -RenderOffscreen
    -FullStdOutLogOutput
  LABELS sandbox-ismc-benchmark
  TIMEOUT 600
)

add_custom_target(dev-core
  DEPENDS
    native-core-tests
    native-simulation-tests
    native-simulation-benchmark-tests
    sandbox-image-tests
    mesh-gen-tests
    editor
    codegen-tests
    kernel-codegen-tests
    native-memory-tests
    sbx-mimalloc-tests
    sandbox-s7-tests
    slate-codegen-tests
    jobserver-tests
    check-cmake-presets
)

if(SANDBOX_TEST_SUITE STREQUAL "benchmark")
  set(SANDBOX_SOA_SPACING_CPU_GROUP "0" CACHE STRING "Processor group for the pinned SoA confirmation")
  set(SANDBOX_SOA_SPACING_CPU "2" CACHE STRING "Logical CPU within the group for the pinned SoA confirmation")
  add_low_level_test_suite(benchmarks SandboxCoreBenchmarks SandboxCore
    ACTIVITY BENCHMARK
    LABELS benchmark
  )
  add_dependencies(benchmarks check-generated-code)
  add_custom_target(single-allocation-soa-spacing
    COMMAND ${benchmark_activity_command} uv run
      "${PROJECT_SOURCE_DIR}/Scripts/run-soa-spacing-experiment.py"
    WORKING_DIRECTORY "${PROJECT_SOURCE_DIR}"
    USES_TERMINAL
    VERBATIM
  )
  add_custom_target(single-allocation-soa-spacing-confirmation
    COMMAND ${benchmark_activity_command} uv run
      "${PROJECT_SOURCE_DIR}/Scripts/run-soa-spacing-experiment.py" --suite confirmation
      --cpu-group "${SANDBOX_SOA_SPACING_CPU_GROUP}" --cpu "${SANDBOX_SOA_SPACING_CPU}"
    WORKING_DIRECTORY "${PROJECT_SOURCE_DIR}"
    USES_TERMINAL
    VERBATIM
  )
  add_test(NAME SandboxCore.SingleAllocation.Spacing.ConfirmationSmoke
    COMMAND uv run "${PROJECT_SOURCE_DIR}/Scripts/run-soa-spacing-experiment.py"
      --suite confirmation --smoke --output-dir "${CMAKE_BINARY_DIR}/spacing-confirmation-smoke"
      --cpu-group "${SANDBOX_SOA_SPACING_CPU_GROUP}" --cpu "${SANDBOX_SOA_SPACING_CPU}"
  )
  set_tests_properties(SandboxCore.SingleAllocation.Spacing.ConfirmationSmoke PROPERTIES WORKING_DIRECTORY "${PROJECT_BINARY_DIR}" TIMEOUT 300 LABELS "soa-spacing-smoke")
  add_test(NAME SandboxCore.SingleAllocation.Spacing.ConfirmationReportTests
    COMMAND uv run --no-project python "${PROJECT_SOURCE_DIR}/Scripts/test_soa_spacing_confirmation.py"
  )
  set_tests_properties(SandboxCore.SingleAllocation.Spacing.ConfirmationReportTests PROPERTIES WORKING_DIRECTORY "${PROJECT_BINARY_DIR}" LABELS "soa-spacing-smoke")
  add_test(NAME SandboxCore.SingleAllocation.Spacing.PipelineSmoke
    COMMAND uv run "${PROJECT_SOURCE_DIR}/Scripts/run-soa-spacing-experiment.py"
      --smoke --output-dir "${CMAKE_BINARY_DIR}/spacing-smoke"
  )
  set_tests_properties(SandboxCore.SingleAllocation.Spacing.PipelineSmoke PROPERTIES WORKING_DIRECTORY "${PROJECT_BINARY_DIR}" TIMEOUT 300 LABELS "soa-spacing-smoke")
  add_custom_target(single-allocation-soa-plots
    COMMAND uv run "${PROJECT_SOURCE_DIR}/Scripts/plot-single-allocation-soa-benchmarks.py"
      --input "${CMAKE_BINARY_DIR}/Testing/Temporary/LastTest.log"
      --output-dir "${CMAKE_BINARY_DIR}/single-allocation-soa-plots"
    WORKING_DIRECTORY "${PROJECT_SOURCE_DIR}"
    COMMENT "Plotting the single-allocation SoA comparison"
    VERBATIM
    USES_TERMINAL
  )
elseif(SANDBOX_TEST_SUITE STREQUAL "default")
  add_low_level_test_suite(core-tests SandboxCoreTests SandboxCore
    AGGREGATE
    LABELS low-level-core)
  add_custom_target(native-tests
    DEPENDS
      native-level-authoring-tests
      native-simulation-tests
      native-simulation-benchmark-tests
  )

  add_dependencies(core-tests check-generated-code)
  add_dependencies(native-tests check-generated-code)

  add_dependencies(dev-core
    core-tests
    native-tests
  )
elseif(SANDBOX_TEST_SUITE STREQUAL "editor-only")
  # Editor automation tests are registered above and need no low-level test executable.
else()
  message(FATAL_ERROR
    "SANDBOX_TEST_SUITE must be one of: default, benchmark, editor-only. "
    "Got '${SANDBOX_TEST_SUITE}'."
  )
endif()

add_unreal_editor_target(resave-assets
  ARGUMENTS
    -run=ResavePackages
    -projectonly
    -fixupredirects
    -unattended
  DEPENDS editor
  COMMENT "Resaving project assets and fixing redirectors"
  FOLDER "Sandbox/Unreal/Maintenance"
)

add_unreal_editor_target(generate-ui-glow-material
  ARGUMENTS
    -run=MaterialSynth
    "-Artifact=${UI_GLOW_MATERIAL_ARTIFACT}"
    -Generate -RenderOffscreen -AllowCommandletRendering
    -unattended -nop4 -nosplash -nosound -stdout
  DEPENDS editor
  COMMENT "Generating the UI glow material"
  FOLDER "Sandbox/Unreal/Maintenance"
)

add_unreal_editor_target(validate-ui-glow-material-unreal
  ARGUMENTS
    -run=MaterialSynth
    "-Artifact=${UI_GLOW_MATERIAL_ARTIFACT}"
    -Validate -unattended -nop4 -nosplash -nullrhi -nosound -stdout
  DEPENDS editor compile-ui-glow-material
  COMMENT "Validating the UI glow material in Unreal"
  FOLDER "Sandbox/Unreal/Maintenance"
)

add_custom_target(generate-world-soft-target-assets
  COMMAND ${unreal_standard_activity_command} "${UE_EDITOR_CMD_EXE}" "${SANDBOX_UPROJECT}"
    -run=MaterialSynth
    "-Artifact=${WORLD_SOFT_TARGET_MATERIAL_ARTIFACT}"
    -Generate -RenderOffscreen -AllowCommandletRendering
    -unattended -nop4 -nosplash -nosound -stdout
  COMMAND ${unreal_standard_activity_command} "${UE_EDITOR_CMD_EXE}" "${SANDBOX_UPROJECT}"
    -run=GenerateWorldSoftTargetAssets -RenderOffscreen -AllowCommandletRendering
    -unattended -nop4 -nosplash -nosound -stdout
  DEPENDS editor
  WORKING_DIRECTORY "${PROJECT_SOURCE_DIR}"
  USES_TERMINAL
  VERBATIM
)

add_unreal_editor_target(generate-migrated-materials
  ARGUMENTS
    -run=MaterialSynth
    "-ArtifactDirectory=${PROJECT_SOURCE_DIR}/Intermediate/MaterialGen"
    "-ArtifactList=UiGlowComposite,RadarDisplay,EnergyShield,SpaceEnergyField,GlowingCross,VertexRipple,ConstructionSpawn,PlanetAtmosphere,PlanetSurface,TacticalScan,CelestialAnalytic"
    -GenerateAll -RenderOffscreen -AllowCommandletRendering
    -unattended -nop4 -nosplash -nosound -stdout
  DEPENDS editor compile-migrated-materials
  COMMENT "Generating migrated materials"
  FOLDER "Sandbox/Unreal/Maintenance"
)

add_unreal_editor_target(generate-celestial-analytic-material
  ARGUMENTS
    -run=MaterialSynth
    "-Artifact=${PROJECT_SOURCE_DIR}/Intermediate/MaterialGen/CelestialAnalytic.smat"
    -Generate -RenderOffscreen -AllowCommandletRendering
    -unattended -nop4 -nosplash -nosound -stdout
  DEPENDS editor
    "${PROJECT_SOURCE_DIR}/Intermediate/MaterialGen/CelestialAnalytic.smat"
  COMMENT "Generating the celestial analytic material"
  FOLDER "Sandbox/Unreal/Maintenance"
)

add_unreal_editor_test(SandboxShaders.MaterialCompilation
  NO_LOCAL_DDC
  ARGUMENTS
    "-ExecCmds=Automation Now; Automation RunTests StartsWith:SandboxShaders.MaterialCompilation; Quit"
    -AllowCommandletRendering
    -DDC-ForceMemoryCache
  LABELS focused-material all
  TIMEOUT 900
)

add_unreal_editor_test(USFLoader.IncludedFunctionCompilation
  NO_LOCAL_DDC
  ARGUMENTS
    "-ExecCmds=Automation Now; Automation RunTests StartsWith:USFLoader.IncludedFunctionCompilation; Quit"
    -AllowCommandletRendering
    -DDC-ForceMemoryCache
  LABELS usfloader
  TIMEOUT 900
)

add_unreal_editor_test(SandboxEditor.MaterialSynth
  NO_LOCAL_DDC
  ARGUMENTS
    "-ExecCmds=Automation Now; Automation RunTests SandboxEditor.MaterialSynth; Quit"
    -RenderOffscreen
    -AllowCommandletRendering
    -DDC-ForceMemoryCache
  LABELS "material-synth;all"
  TIMEOUT 900
)

add_unreal_editor_test(SandboxUI.EntityOverlayBenchmark
  NO_LOCAL_DDC
  ACTIVITY BENCHMARK
  ARGUMENTS
    -run=EntityOverlayBenchmark
    -RenderOffscreen
    -AllowCommandletRendering
    -DDC-ForceMemoryCache
  LABELS entity-overlay-benchmark
  TIMEOUT 1200
)

add_test(
  NAME SandboxShaders.GpuStarfieldBenchmark
  COMMAND ${unreal_benchmark_activity_command} "${Python3_EXECUTABLE}"
    "${PROJECT_SOURCE_DIR}/Scripts/run-gpu-starfield-benchmark.py"
    "--editor=${UE_EDITOR_CMD_EXE}"
    "--project=${SANDBOX_UPROJECT}"
    "--output=${PROJECT_SOURCE_DIR}/Saved/Benchmarks/GpuStarfield"
)
set_tests_properties(SandboxShaders.GpuStarfieldBenchmark PROPERTIES
  LABELS gpu-benchmark
  TIMEOUT 2400
  WORKING_DIRECTORY "${PROJECT_SOURCE_DIR}"
)

add_test(
  NAME SandboxShaders.GpuStarfieldMatrix
  COMMAND ${unreal_benchmark_activity_command} "${Python3_EXECUTABLE}"
    "${PROJECT_SOURCE_DIR}/Scripts/run-gpu-starfield-benchmark.py"
    "--editor=${UE_EDITOR_CMD_EXE}"
    "--project=${SANDBOX_UPROJECT}"
    "--output=${PROJECT_SOURCE_DIR}/Saved/Benchmarks/GpuStarfield"
    "--counts=100000,1000000"
    "--resolutions=1920x1080,3840x2160"
    "--size-multipliers=1,4"
    "--camera-modes=stationary"
)
set_tests_properties(SandboxShaders.GpuStarfieldMatrix PROPERTIES
  LABELS gpu-starfield-matrix
  TIMEOUT 3600
  WORKING_DIRECTORY "${PROJECT_SOURCE_DIR}"
)

add_custom_target(gpu-tutorial-shader-tests
  COMMAND ${unreal_standard_activity_command} "${UE_EDITOR_CMD_EXE}" "${SANDBOX_UPROJECT}"
    "-ExecCmds=Automation Now; Automation RunTests StartsWith:SandboxGpuTutorials.UnitTests; Quit"
    -unattended
    -nop4
    -nosplash
    -nosound
    -DDC-ForceMemoryCache
    -stdout
  WORKING_DIRECTORY "${PROJECT_SOURCE_DIR}"
  COMMENT "Compiling and validating SandboxGpuTutorials materials with the graphics RHI"
  VERBATIM
)
