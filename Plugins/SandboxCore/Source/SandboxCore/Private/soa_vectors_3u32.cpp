// clang-format off
// This file is autogenerated. Do not edit by hand.
// Edit Codegen/manifest.py and regenerate it instead.

#include "SandboxCore/soa_vectors_3u32.h"

#include "SandboxCore/array_checks.h"
#include "SandboxCore/container_ops.h"
#include "SandboxCore/soa_permutation.h"

#include "Containers/AllowShrinking.h"
#include "Containers/ArrayView.h"
#include "CoreMinimal.h"

auto FVectors3u32ConstView::get_view() const -> ConstView {
    return get_view(0, num());
}

auto FVectors3u32ConstView::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<uint32>{xs}.Slice(offset, count),
        TConstArrayView<uint32>{ys}.Slice(offset, count),
        TConstArrayView<uint32>{zs}.Slice(offset, count),
    };
}

auto FVectors3u32ConstView::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto FVectors3u32ConstView::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<uint32>{xs}.Slice(offset, count),
        TConstArrayView<uint32>{ys}.Slice(offset, count),
        TConstArrayView<uint32>{zs}.Slice(offset, count),
    };
}

auto FVectors3u32ConstView::num() const noexcept -> int32 {
    return ml::num(xs);
}

auto FVectors3u32ConstView::is_empty() const noexcept -> bool {
    return num() == 0;
}

void FVectors3u32ConstView::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(xs),
        ml::num(ys),
        ml::num(zs),
    });
}

auto FVectors3u32ConstView::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto FVectors3u32ConstView::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto FVectors3u32ConstView::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}

auto FVectors3u32View::get_view() -> View {
    return get_view(0, num());
}

auto FVectors3u32View::get_view(int32 const offset, int32 const count) -> View {
    return View{
        TArrayView<uint32>{xs}.Slice(offset, count),
        TArrayView<uint32>{ys}.Slice(offset, count),
        TArrayView<uint32>{zs}.Slice(offset, count),
    };
}

auto FVectors3u32View::get_view() const -> ConstView {
    return get_view(0, num());
}

auto FVectors3u32View::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<uint32>{xs}.Slice(offset, count),
        TConstArrayView<uint32>{ys}.Slice(offset, count),
        TConstArrayView<uint32>{zs}.Slice(offset, count),
    };
}

auto FVectors3u32View::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto FVectors3u32View::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<uint32>{xs}.Slice(offset, count),
        TConstArrayView<uint32>{ys}.Slice(offset, count),
        TConstArrayView<uint32>{zs}.Slice(offset, count),
    };
}

auto FVectors3u32View::num() const noexcept -> int32 {
    return ml::num(xs);
}

auto FVectors3u32View::is_empty() const noexcept -> bool {
    return num() == 0;
}

void FVectors3u32View::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(xs),
        ml::num(ys),
        ml::num(zs),
    });
}

auto FVectors3u32View::slice(int32 const offset, int32 const count) -> View {
    return get_view(offset, count);
}

auto FVectors3u32View::left(int32 const count) -> View {
    return slice(0, count);
}

auto FVectors3u32View::right(int32 const count) -> View {
    return slice(num() - count, count);
}

auto FVectors3u32View::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto FVectors3u32View::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto FVectors3u32View::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}

void FVectors3u32::reset() {
    ml::reset(xs);
    ml::reset(ys);
    ml::reset(zs);
}

void FVectors3u32::reserve(int32 const count) {
    ml::reserve(xs, count);
    ml::reserve(ys, count);
    ml::reserve(zs, count);
}

void FVectors3u32::add_uninitialised(int32 const count) {
    ml::add_uninitialised(xs, count);
    ml::add_uninitialised(ys, count);
    ml::add_uninitialised(zs, count);
}

void FVectors3u32::add_defaulted(int32 const count) {
    ml::add_defaulted(xs, count);
    ml::add_defaulted(ys, count);
    ml::add_defaulted(zs, count);
}

void FVectors3u32::set_num(int32 const count, EAllowShrinking const allow_shrinking) {
    ml::set_num(xs, count, allow_shrinking);
    ml::set_num(ys, count, allow_shrinking);
    ml::set_num(zs, count, allow_shrinking);
}

void FVectors3u32::apply_permutation(TArrayView<int32> indices) {
    validate_array_sizes();
    check(indices.Num() == num());
    ml::apply_permutation(xs, indices);
    ml::apply_permutation(ys, indices);
    ml::apply_permutation(zs, indices);
}

auto FVectors3u32::get_view() -> View {
    return get_view(0, num());
}

auto FVectors3u32::get_view(int32 const offset, int32 const count) -> View {
    return View{
        TArrayView<uint32>{xs}.Slice(offset, count),
        TArrayView<uint32>{ys}.Slice(offset, count),
        TArrayView<uint32>{zs}.Slice(offset, count),
    };
}

auto FVectors3u32::get_view() const -> ConstView {
    return get_view(0, num());
}

auto FVectors3u32::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<uint32>{xs}.Slice(offset, count),
        TConstArrayView<uint32>{ys}.Slice(offset, count),
        TConstArrayView<uint32>{zs}.Slice(offset, count),
    };
}

auto FVectors3u32::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto FVectors3u32::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<uint32>{xs}.Slice(offset, count),
        TConstArrayView<uint32>{ys}.Slice(offset, count),
        TConstArrayView<uint32>{zs}.Slice(offset, count),
    };
}

auto FVectors3u32::num() const noexcept -> int32 {
    return ml::num(xs);
}

auto FVectors3u32::is_empty() const noexcept -> bool {
    return num() == 0;
}

void FVectors3u32::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(xs),
        ml::num(ys),
        ml::num(zs),
    });
}

auto FVectors3u32::slice(int32 const offset, int32 const count) -> View {
    return get_view(offset, count);
}

auto FVectors3u32::left(int32 const count) -> View {
    return slice(0, count);
}

auto FVectors3u32::right(int32 const count) -> View {
    return slice(num() - count, count);
}

auto FVectors3u32::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto FVectors3u32::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto FVectors3u32::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}
// clang-format on
