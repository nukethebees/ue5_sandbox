// clang-format off
// This file is autogenerated. Do not edit by hand.
// Edit Codegen/manifest.py and regenerate it instead.

#include "SandboxCore/soa_vectors_2d.h"

#include "SandboxCore/array_checks.h"
#include "SandboxCore/container_ops.h"
#include "SandboxCore/soa_permutation.h"

#include "Containers/AllowShrinking.h"
#include "Containers/ArrayView.h"
#include "CoreMinimal.h"

auto FVectors2dConstView::get_view() const -> ConstView {
    return get_view(0, num());
}

auto FVectors2dConstView::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<double>{xs}.Slice(offset, count),
        TConstArrayView<double>{ys}.Slice(offset, count),
    };
}

auto FVectors2dConstView::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto FVectors2dConstView::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<double>{xs}.Slice(offset, count),
        TConstArrayView<double>{ys}.Slice(offset, count),
    };
}

auto FVectors2dConstView::num() const noexcept -> int32 {
    return ml::num(xs);
}

auto FVectors2dConstView::is_empty() const noexcept -> bool {
    return num() == 0;
}

void FVectors2dConstView::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(xs),
        ml::num(ys),
    });
}

auto FVectors2dConstView::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto FVectors2dConstView::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto FVectors2dConstView::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}

auto FVectors2dView::get_view() -> View {
    return get_view(0, num());
}

auto FVectors2dView::get_view(int32 const offset, int32 const count) -> View {
    return View{
        TArrayView<double>{xs}.Slice(offset, count),
        TArrayView<double>{ys}.Slice(offset, count),
    };
}

auto FVectors2dView::get_view() const -> ConstView {
    return get_view(0, num());
}

auto FVectors2dView::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<double>{xs}.Slice(offset, count),
        TConstArrayView<double>{ys}.Slice(offset, count),
    };
}

auto FVectors2dView::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto FVectors2dView::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<double>{xs}.Slice(offset, count),
        TConstArrayView<double>{ys}.Slice(offset, count),
    };
}

auto FVectors2dView::num() const noexcept -> int32 {
    return ml::num(xs);
}

auto FVectors2dView::is_empty() const noexcept -> bool {
    return num() == 0;
}

void FVectors2dView::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(xs),
        ml::num(ys),
    });
}

auto FVectors2dView::slice(int32 const offset, int32 const count) -> View {
    return get_view(offset, count);
}

auto FVectors2dView::left(int32 const count) -> View {
    return slice(0, count);
}

auto FVectors2dView::right(int32 const count) -> View {
    return slice(num() - count, count);
}

auto FVectors2dView::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto FVectors2dView::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto FVectors2dView::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}

void FVectors2d::reset() {
    ml::reset(xs);
    ml::reset(ys);
}

void FVectors2d::reserve(int32 const count) {
    ml::reserve(xs, count);
    ml::reserve(ys, count);
}

void FVectors2d::add_uninitialised(int32 const count) {
    ml::add_uninitialised(xs, count);
    ml::add_uninitialised(ys, count);
}

void FVectors2d::add_defaulted(int32 const count) {
    ml::add_defaulted(xs, count);
    ml::add_defaulted(ys, count);
}

void FVectors2d::set_num(int32 const count, EAllowShrinking const allow_shrinking) {
    ml::set_num(xs, count, allow_shrinking);
    ml::set_num(ys, count, allow_shrinking);
}

void FVectors2d::apply_permutation(TArrayView<int32> indices) {
    validate_array_sizes();
    check(indices.Num() == num());
    ml::apply_permutation(xs, indices);
    ml::apply_permutation(ys, indices);
}

auto FVectors2d::get_view() -> View {
    return get_view(0, num());
}

auto FVectors2d::get_view(int32 const offset, int32 const count) -> View {
    return View{
        TArrayView<double>{xs}.Slice(offset, count),
        TArrayView<double>{ys}.Slice(offset, count),
    };
}

auto FVectors2d::get_view() const -> ConstView {
    return get_view(0, num());
}

auto FVectors2d::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<double>{xs}.Slice(offset, count),
        TConstArrayView<double>{ys}.Slice(offset, count),
    };
}

auto FVectors2d::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto FVectors2d::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<double>{xs}.Slice(offset, count),
        TConstArrayView<double>{ys}.Slice(offset, count),
    };
}

auto FVectors2d::num() const noexcept -> int32 {
    return ml::num(xs);
}

auto FVectors2d::is_empty() const noexcept -> bool {
    return num() == 0;
}

void FVectors2d::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(xs),
        ml::num(ys),
    });
}

auto FVectors2d::slice(int32 const offset, int32 const count) -> View {
    return get_view(offset, count);
}

auto FVectors2d::left(int32 const count) -> View {
    return slice(0, count);
}

auto FVectors2d::right(int32 const count) -> View {
    return slice(num() - count, count);
}

auto FVectors2d::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto FVectors2d::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto FVectors2d::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}
// clang-format on
