// clang-format off
// This file is autogenerated. Do not edit by hand.
// Edit Codegen/manifest.py and regenerate it instead.

#include "SandboxCore/soa_vectors_3d.h"

#include "SandboxCore/array_checks.h"
#include "SandboxCore/container_ops.h"
#include "SandboxCore/soa_permutation.h"

#include "Containers/AllowShrinking.h"
#include "Containers/ArrayView.h"
#include "CoreMinimal.h"

auto FVectors3dConstView::operator[](int32 const index) const -> FVector3d {
    validate_array_sizes();
    return {xs[index], ys[index], zs[index]};
}

auto FVectors3dConstView::get_view() const -> ConstView {
    return get_view(0, num());
}

auto FVectors3dConstView::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<double>{xs}.Slice(offset, count),
        TConstArrayView<double>{ys}.Slice(offset, count),
        TConstArrayView<double>{zs}.Slice(offset, count),
    };
}

auto FVectors3dConstView::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto FVectors3dConstView::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<double>{xs}.Slice(offset, count),
        TConstArrayView<double>{ys}.Slice(offset, count),
        TConstArrayView<double>{zs}.Slice(offset, count),
    };
}

auto FVectors3dConstView::num() const noexcept -> int32 {
    return ml::num(xs);
}

auto FVectors3dConstView::is_empty() const noexcept -> bool {
    return num() == 0;
}

void FVectors3dConstView::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(xs),
        ml::num(ys),
        ml::num(zs),
    });
}

auto FVectors3dConstView::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto FVectors3dConstView::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto FVectors3dConstView::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}

auto FVectors3dView::get_view() -> View {
    return get_view(0, num());
}

auto FVectors3dView::get_view(int32 const offset, int32 const count) -> View {
    return View{
        TArrayView<double>{xs}.Slice(offset, count),
        TArrayView<double>{ys}.Slice(offset, count),
        TArrayView<double>{zs}.Slice(offset, count),
    };
}

auto FVectors3dView::operator[](int32 const index) const -> FVector3d {
    validate_array_sizes();
    return {xs[index], ys[index], zs[index]};
}

auto FVectors3dView::get_view() const -> ConstView {
    return get_view(0, num());
}

auto FVectors3dView::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<double>{xs}.Slice(offset, count),
        TConstArrayView<double>{ys}.Slice(offset, count),
        TConstArrayView<double>{zs}.Slice(offset, count),
    };
}

auto FVectors3dView::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto FVectors3dView::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<double>{xs}.Slice(offset, count),
        TConstArrayView<double>{ys}.Slice(offset, count),
        TConstArrayView<double>{zs}.Slice(offset, count),
    };
}

auto FVectors3dView::num() const noexcept -> int32 {
    return ml::num(xs);
}

auto FVectors3dView::is_empty() const noexcept -> bool {
    return num() == 0;
}

void FVectors3dView::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(xs),
        ml::num(ys),
        ml::num(zs),
    });
}

auto FVectors3dView::slice(int32 const offset, int32 const count) -> View {
    return get_view(offset, count);
}

auto FVectors3dView::left(int32 const count) -> View {
    return slice(0, count);
}

auto FVectors3dView::right(int32 const count) -> View {
    return slice(num() - count, count);
}

auto FVectors3dView::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto FVectors3dView::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto FVectors3dView::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}

void FVectors3d::reset() {
    ml::reset(xs);
    ml::reset(ys);
    ml::reset(zs);
}

void FVectors3d::reserve(int32 const count) {
    ml::reserve(xs, count);
    ml::reserve(ys, count);
    ml::reserve(zs, count);
}

void FVectors3d::add_uninitialised(int32 const count) {
    ml::add_uninitialised(xs, count);
    ml::add_uninitialised(ys, count);
    ml::add_uninitialised(zs, count);
}

void FVectors3d::add_defaulted(int32 const count) {
    ml::add_defaulted(xs, count);
    ml::add_defaulted(ys, count);
    ml::add_defaulted(zs, count);
}

void FVectors3d::set_num(int32 const count, EAllowShrinking const allow_shrinking) {
    ml::set_num(xs, count, allow_shrinking);
    ml::set_num(ys, count, allow_shrinking);
    ml::set_num(zs, count, allow_shrinking);
}

void FVectors3d::apply_permutation(TArrayView<int32> indices) {
    validate_array_sizes();
    check(indices.Num() == num());
    ml::apply_permutation(xs, indices);
    ml::apply_permutation(ys, indices);
    ml::apply_permutation(zs, indices);
}

auto FVectors3d::get_view() -> View {
    return get_view(0, num());
}

auto FVectors3d::get_view(int32 const offset, int32 const count) -> View {
    return View{
        TArrayView<double>{xs}.Slice(offset, count),
        TArrayView<double>{ys}.Slice(offset, count),
        TArrayView<double>{zs}.Slice(offset, count),
    };
}

auto FVectors3d::operator[](int32 const index) const -> FVector3d {
    validate_array_sizes();
    return {xs[index], ys[index], zs[index]};
}

auto FVectors3d::get_view() const -> ConstView {
    return get_view(0, num());
}

auto FVectors3d::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<double>{xs}.Slice(offset, count),
        TConstArrayView<double>{ys}.Slice(offset, count),
        TConstArrayView<double>{zs}.Slice(offset, count),
    };
}

auto FVectors3d::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto FVectors3d::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<double>{xs}.Slice(offset, count),
        TConstArrayView<double>{ys}.Slice(offset, count),
        TConstArrayView<double>{zs}.Slice(offset, count),
    };
}

auto FVectors3d::num() const noexcept -> int32 {
    return ml::num(xs);
}

auto FVectors3d::is_empty() const noexcept -> bool {
    return num() == 0;
}

void FVectors3d::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(xs),
        ml::num(ys),
        ml::num(zs),
    });
}

auto FVectors3d::slice(int32 const offset, int32 const count) -> View {
    return get_view(offset, count);
}

auto FVectors3d::left(int32 const count) -> View {
    return slice(0, count);
}

auto FVectors3d::right(int32 const count) -> View {
    return slice(num() - count, count);
}

auto FVectors3d::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto FVectors3d::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto FVectors3d::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}
// clang-format on
