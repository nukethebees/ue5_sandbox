// clang-format off
// This file is autogenerated. Do not edit by hand.
// Edit Codegen/manifests and regenerate it instead.

#include "SandboxCore/countdown_timers.h"

#include "SandboxCore/array_checks.h"
#include "SandboxCore/array_math.h"
#include "SandboxCore/container_ops.h"
#include "SandboxCore/soa_permutation.h"

#include "Containers/AllowShrinking.h"
#include "Containers/ArrayView.h"
#include "CoreMinimal.h"

void FCountdownTimers::tick(float const dt) noexcept {
    ml::subtract_in_place(remaining_times, dt);
}

auto FCountdownTimersConstView::get_view() const -> ConstView {
    return get_view(0, num());
}

auto FCountdownTimersConstView::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<float>{remaining_times}.Slice(offset, count),
    };
}

auto FCountdownTimersConstView::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto FCountdownTimersConstView::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<float>{remaining_times}.Slice(offset, count),
    };
}

auto FCountdownTimersConstView::num() const noexcept -> int32 {
    return ml::num(remaining_times);
}

auto FCountdownTimersConstView::is_empty() const noexcept -> bool {
    return num() == 0;
}

void FCountdownTimersConstView::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(remaining_times),
    });
}

auto FCountdownTimersConstView::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto FCountdownTimersConstView::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto FCountdownTimersConstView::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}

auto FCountdownTimersView::get_view() -> View {
    return get_view(0, num());
}

auto FCountdownTimersView::get_view(int32 const offset, int32 const count) -> View {
    return View{
        TArrayView<float>{remaining_times}.Slice(offset, count),
    };
}

auto FCountdownTimersView::get_view() const -> ConstView {
    return get_view(0, num());
}

auto FCountdownTimersView::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<float>{remaining_times}.Slice(offset, count),
    };
}

auto FCountdownTimersView::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto FCountdownTimersView::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<float>{remaining_times}.Slice(offset, count),
    };
}

auto FCountdownTimersView::num() const noexcept -> int32 {
    return ml::num(remaining_times);
}

auto FCountdownTimersView::is_empty() const noexcept -> bool {
    return num() == 0;
}

void FCountdownTimersView::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(remaining_times),
    });
}

auto FCountdownTimersView::slice(int32 const offset, int32 const count) -> View {
    return get_view(offset, count);
}

auto FCountdownTimersView::left(int32 const count) -> View {
    return slice(0, count);
}

auto FCountdownTimersView::right(int32 const count) -> View {
    return slice(num() - count, count);
}

auto FCountdownTimersView::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto FCountdownTimersView::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto FCountdownTimersView::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}

void FCountdownTimers::reset() {
    ml::reset(remaining_times);
}

void FCountdownTimers::reserve(int32 const count) {
    ml::reserve(remaining_times, count);
}

void FCountdownTimers::add_uninitialised(int32 const count) {
    ml::add_uninitialised(remaining_times, count);
}

void FCountdownTimers::add_defaulted(int32 const count) {
    ml::add_defaulted(remaining_times, count);
}

void FCountdownTimers::set_num(int32 const count, EAllowShrinking const allow_shrinking) {
    ml::set_num(remaining_times, count, allow_shrinking);
}

void FCountdownTimers::apply_permutation(TArrayView<int32> indices) {
    validate_array_sizes();
    check(indices.Num() == num());
    ml::apply_permutation(remaining_times, indices);
}

auto FCountdownTimers::get_view() -> View {
    return get_view(0, num());
}

auto FCountdownTimers::get_view(int32 const offset, int32 const count) -> View {
    return View{
        TArrayView<float>{remaining_times}.Slice(offset, count),
    };
}

auto FCountdownTimers::get_view() const -> ConstView {
    return get_view(0, num());
}

auto FCountdownTimers::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<float>{remaining_times}.Slice(offset, count),
    };
}

auto FCountdownTimers::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto FCountdownTimers::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<float>{remaining_times}.Slice(offset, count),
    };
}

auto FCountdownTimers::num() const noexcept -> int32 {
    return ml::num(remaining_times);
}

auto FCountdownTimers::is_empty() const noexcept -> bool {
    return num() == 0;
}

void FCountdownTimers::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(remaining_times),
    });
}

auto FCountdownTimers::slice(int32 const offset, int32 const count) -> View {
    return get_view(offset, count);
}

auto FCountdownTimers::left(int32 const count) -> View {
    return slice(0, count);
}

auto FCountdownTimers::right(int32 const count) -> View {
    return slice(num() - count, count);
}

auto FCountdownTimers::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto FCountdownTimers::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto FCountdownTimers::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}
// clang-format on
