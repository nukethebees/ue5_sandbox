// clang-format off
// This file is autogenerated. Do not edit by hand.
#include "SandboxCore/array_math_kernels.h"

#include "CoreMinimal.h"

#include <cstddef>
#include <cstdint>

namespace {

auto ranges_overlap(void const* const lhs, std::size_t const lhs_size,
                    void const* const rhs, std::size_t const rhs_size) noexcept -> bool {
    if (lhs_size == 0 || rhs_size == 0) {
        return false;
    }

    auto const lhs_address{reinterpret_cast<std::uintptr_t>(lhs)};
    auto const rhs_address{reinterpret_cast<std::uintptr_t>(rhs)};
    return lhs_address <= rhs_address ? rhs_address - lhs_address < lhs_size
                                  : lhs_address - rhs_address < rhs_size;
}

void add_int32_array_scalar_in_place_raw(int32* RESTRICT data, int32 const value, int32 const count) noexcept {
    for (int32 i{0}; i < count; ++i) {
        data[i] = (data[i] + value);
    }
}

void add_float_array_scalar_in_place_raw(float* RESTRICT data, float const value, int32 const count) noexcept {
    for (int32 i{0}; i < count; ++i) {
        data[i] = (data[i] + value);
    }
}

void add_double_array_scalar_in_place_raw(double* RESTRICT data, double const value, int32 const count) noexcept {
    for (int32 i{0}; i < count; ++i) {
        data[i] = (data[i] + value);
    }
}

void subtract_int32_array_scalar_in_place_raw(int32* RESTRICT data, int32 const value, int32 const count) noexcept {
    for (int32 i{0}; i < count; ++i) {
        data[i] = (data[i] - value);
    }
}

void subtract_float_array_scalar_in_place_raw(float* RESTRICT data, float const value, int32 const count) noexcept {
    for (int32 i{0}; i < count; ++i) {
        data[i] = (data[i] - value);
    }
}

void subtract_double_array_scalar_in_place_raw(double* RESTRICT data, double const value, int32 const count) noexcept {
    for (int32 i{0}; i < count; ++i) {
        data[i] = (data[i] - value);
    }
}

void multiply_int32_array_array_raw(int32 const* lhs, int32 const* rhs, int32* RESTRICT out, int32 const count) noexcept {
    for (int32 i{0}; i < count; ++i) {
        out[i] = (lhs[i] * rhs[i]);
    }
}

void multiply_int32_array_scalar_raw(int32 const* lhs, int32 const rhs, int32* RESTRICT out, int32 const count) noexcept {
    for (int32 i{0}; i < count; ++i) {
        out[i] = (lhs[i] * rhs);
    }
}

void multiply_float_array_array_raw(float const* lhs, float const* rhs, float* RESTRICT out, int32 const count) noexcept {
    for (int32 i{0}; i < count; ++i) {
        out[i] = (lhs[i] * rhs[i]);
    }
}

void multiply_float_array_scalar_raw(float const* lhs, float const rhs, float* RESTRICT out, int32 const count) noexcept {
    for (int32 i{0}; i < count; ++i) {
        out[i] = (lhs[i] * rhs);
    }
}

void multiply_double_array_array_raw(double const* lhs, double const* rhs, double* RESTRICT out, int32 const count) noexcept {
    for (int32 i{0}; i < count; ++i) {
        out[i] = (lhs[i] * rhs[i]);
    }
}

void multiply_double_array_scalar_raw(double const* lhs, double const rhs, double* RESTRICT out, int32 const count) noexcept {
    for (int32 i{0}; i < count; ++i) {
        out[i] = (lhs[i] * rhs);
    }
}

void multiply_int32_array_array_in_place_raw(int32* RESTRICT lhs, int32 const* rhs, int32 const count) noexcept {
    for (int32 i{0}; i < count; ++i) {
        lhs[i] = (lhs[i] * rhs[i]);
    }
}

void multiply_int32_array_scalar_in_place_raw(int32* RESTRICT lhs, int32 const rhs, int32 const count) noexcept {
    for (int32 i{0}; i < count; ++i) {
        lhs[i] = (lhs[i] * rhs);
    }
}

void multiply_float_array_array_in_place_raw(float* RESTRICT lhs, float const* rhs, int32 const count) noexcept {
    for (int32 i{0}; i < count; ++i) {
        lhs[i] = (lhs[i] * rhs[i]);
    }
}

void multiply_float_array_scalar_in_place_raw(float* RESTRICT lhs, float const rhs, int32 const count) noexcept {
    for (int32 i{0}; i < count; ++i) {
        lhs[i] = (lhs[i] * rhs);
    }
}

void multiply_double_array_array_in_place_raw(double* RESTRICT lhs, double const* rhs, int32 const count) noexcept {
    for (int32 i{0}; i < count; ++i) {
        lhs[i] = (lhs[i] * rhs[i]);
    }
}

void multiply_double_array_scalar_in_place_raw(double* RESTRICT lhs, double const rhs, int32 const count) noexcept {
    for (int32 i{0}; i < count; ++i) {
        lhs[i] = (lhs[i] * rhs);
    }
}

void divide_int32_array_scalar_in_place_raw(int32* RESTRICT data, int32 const value, int32 const count) noexcept {
    for (int32 i{0}; i < count; ++i) {
        data[i] = (data[i] / value);
    }
}

void divide_float_array_scalar_in_place_raw(float* RESTRICT data, float const value, int32 const count) noexcept {
    for (int32 i{0}; i < count; ++i) {
        data[i] = (data[i] / value);
    }
}

void divide_double_array_scalar_in_place_raw(double* RESTRICT data, double const value, int32 const count) noexcept {
    for (int32 i{0}; i < count; ++i) {
        data[i] = (data[i] / value);
    }
}

}

namespace ml {

void add_in_place(TArrayView<int32> const data, int32 const value) noexcept {
    int32 const count{data.Num()};
    add_int32_array_scalar_in_place_raw(data.GetData(), value, count);
}

void add_in_place(TArray<int32>& data, int32 const value) noexcept {
    add_in_place(TArrayView<int32>{data}, value);
}

void add_in_place(TArrayView<float> const data, float const value) noexcept {
    int32 const count{data.Num()};
    add_float_array_scalar_in_place_raw(data.GetData(), value, count);
}

void add_in_place(TArray<float>& data, float const value) noexcept {
    add_in_place(TArrayView<float>{data}, value);
}

void add_in_place(TArrayView<double> const data, double const value) noexcept {
    int32 const count{data.Num()};
    add_double_array_scalar_in_place_raw(data.GetData(), value, count);
}

void add_in_place(TArray<double>& data, double const value) noexcept {
    add_in_place(TArrayView<double>{data}, value);
}

void subtract_in_place(TArrayView<int32> const data, int32 const value) noexcept {
    int32 const count{data.Num()};
    subtract_int32_array_scalar_in_place_raw(data.GetData(), value, count);
}

void subtract_in_place(TArray<int32>& data, int32 const value) noexcept {
    subtract_in_place(TArrayView<int32>{data}, value);
}

void subtract_in_place(TArrayView<float> const data, float const value) noexcept {
    int32 const count{data.Num()};
    subtract_float_array_scalar_in_place_raw(data.GetData(), value, count);
}

void subtract_in_place(TArray<float>& data, float const value) noexcept {
    subtract_in_place(TArrayView<float>{data}, value);
}

void subtract_in_place(TArrayView<double> const data, double const value) noexcept {
    int32 const count{data.Num()};
    subtract_double_array_scalar_in_place_raw(data.GetData(), value, count);
}

void subtract_in_place(TArray<double>& data, double const value) noexcept {
    subtract_in_place(TArrayView<double>{data}, value);
}

void multiply(TConstArrayView<int32> const lhs, TConstArrayView<int32> const rhs, TArrayView<int32> const out) noexcept {
    int32 const count{lhs.Num()};
    checkf(rhs.Num() == count, TEXT("multiply: rhs and lhs must have equal lengths"));
    checkf(out.Num() == count, TEXT("multiply: out and lhs must have equal lengths"));
    checkf(!ranges_overlap(out.GetData(), static_cast<std::size_t>(out.Num()) * sizeof(int32), lhs.GetData(), static_cast<std::size_t>(lhs.Num()) * sizeof(int32)),
           TEXT("multiply: out and lhs must not overlap"));
    checkf(!ranges_overlap(out.GetData(), static_cast<std::size_t>(out.Num()) * sizeof(int32), rhs.GetData(), static_cast<std::size_t>(rhs.Num()) * sizeof(int32)),
           TEXT("multiply: out and rhs must not overlap"));
    multiply_int32_array_array_raw(lhs.GetData(), rhs.GetData(), out.GetData(), count);
}

void multiply(TConstArrayView<int32> const lhs, int32 const rhs, TArrayView<int32> const out) noexcept {
    int32 const count{lhs.Num()};
    checkf(out.Num() == count, TEXT("multiply: out and lhs must have equal lengths"));
    checkf(!ranges_overlap(out.GetData(), static_cast<std::size_t>(out.Num()) * sizeof(int32), lhs.GetData(), static_cast<std::size_t>(lhs.Num()) * sizeof(int32)),
           TEXT("multiply: out and lhs must not overlap"));
    multiply_int32_array_scalar_raw(lhs.GetData(), rhs, out.GetData(), count);
}

void multiply(TConstArrayView<float> const lhs, TConstArrayView<float> const rhs, TArrayView<float> const out) noexcept {
    int32 const count{lhs.Num()};
    checkf(rhs.Num() == count, TEXT("multiply: rhs and lhs must have equal lengths"));
    checkf(out.Num() == count, TEXT("multiply: out and lhs must have equal lengths"));
    checkf(!ranges_overlap(out.GetData(), static_cast<std::size_t>(out.Num()) * sizeof(float), lhs.GetData(), static_cast<std::size_t>(lhs.Num()) * sizeof(float)),
           TEXT("multiply: out and lhs must not overlap"));
    checkf(!ranges_overlap(out.GetData(), static_cast<std::size_t>(out.Num()) * sizeof(float), rhs.GetData(), static_cast<std::size_t>(rhs.Num()) * sizeof(float)),
           TEXT("multiply: out and rhs must not overlap"));
    multiply_float_array_array_raw(lhs.GetData(), rhs.GetData(), out.GetData(), count);
}

void multiply(TConstArrayView<float> const lhs, float const rhs, TArrayView<float> const out) noexcept {
    int32 const count{lhs.Num()};
    checkf(out.Num() == count, TEXT("multiply: out and lhs must have equal lengths"));
    checkf(!ranges_overlap(out.GetData(), static_cast<std::size_t>(out.Num()) * sizeof(float), lhs.GetData(), static_cast<std::size_t>(lhs.Num()) * sizeof(float)),
           TEXT("multiply: out and lhs must not overlap"));
    multiply_float_array_scalar_raw(lhs.GetData(), rhs, out.GetData(), count);
}

void multiply(TConstArrayView<double> const lhs, TConstArrayView<double> const rhs, TArrayView<double> const out) noexcept {
    int32 const count{lhs.Num()};
    checkf(rhs.Num() == count, TEXT("multiply: rhs and lhs must have equal lengths"));
    checkf(out.Num() == count, TEXT("multiply: out and lhs must have equal lengths"));
    checkf(!ranges_overlap(out.GetData(), static_cast<std::size_t>(out.Num()) * sizeof(double), lhs.GetData(), static_cast<std::size_t>(lhs.Num()) * sizeof(double)),
           TEXT("multiply: out and lhs must not overlap"));
    checkf(!ranges_overlap(out.GetData(), static_cast<std::size_t>(out.Num()) * sizeof(double), rhs.GetData(), static_cast<std::size_t>(rhs.Num()) * sizeof(double)),
           TEXT("multiply: out and rhs must not overlap"));
    multiply_double_array_array_raw(lhs.GetData(), rhs.GetData(), out.GetData(), count);
}

void multiply(TConstArrayView<double> const lhs, double const rhs, TArrayView<double> const out) noexcept {
    int32 const count{lhs.Num()};
    checkf(out.Num() == count, TEXT("multiply: out and lhs must have equal lengths"));
    checkf(!ranges_overlap(out.GetData(), static_cast<std::size_t>(out.Num()) * sizeof(double), lhs.GetData(), static_cast<std::size_t>(lhs.Num()) * sizeof(double)),
           TEXT("multiply: out and lhs must not overlap"));
    multiply_double_array_scalar_raw(lhs.GetData(), rhs, out.GetData(), count);
}

void multiply_in_place(TArrayView<int32> const lhs, TConstArrayView<int32> const rhs) noexcept {
    int32 const count{lhs.Num()};
    checkf(rhs.Num() == count, TEXT("multiply_in_place: rhs and lhs must have equal lengths"));
    checkf(!ranges_overlap(lhs.GetData(), static_cast<std::size_t>(lhs.Num()) * sizeof(int32), rhs.GetData(), static_cast<std::size_t>(rhs.Num()) * sizeof(int32)),
           TEXT("multiply_in_place: lhs and rhs must not overlap"));
    multiply_int32_array_array_in_place_raw(lhs.GetData(), rhs.GetData(), count);
}

void multiply_in_place(TArray<int32>& lhs, TConstArrayView<int32> const rhs) noexcept {
    multiply_in_place(TArrayView<int32>{lhs}, rhs);
}

void multiply_in_place(TArrayView<int32> const lhs, int32 const rhs) noexcept {
    int32 const count{lhs.Num()};
    multiply_int32_array_scalar_in_place_raw(lhs.GetData(), rhs, count);
}

void multiply_in_place(TArray<int32>& lhs, int32 const rhs) noexcept {
    multiply_in_place(TArrayView<int32>{lhs}, rhs);
}

void multiply_in_place(TArrayView<float> const lhs, TConstArrayView<float> const rhs) noexcept {
    int32 const count{lhs.Num()};
    checkf(rhs.Num() == count, TEXT("multiply_in_place: rhs and lhs must have equal lengths"));
    checkf(!ranges_overlap(lhs.GetData(), static_cast<std::size_t>(lhs.Num()) * sizeof(float), rhs.GetData(), static_cast<std::size_t>(rhs.Num()) * sizeof(float)),
           TEXT("multiply_in_place: lhs and rhs must not overlap"));
    multiply_float_array_array_in_place_raw(lhs.GetData(), rhs.GetData(), count);
}

void multiply_in_place(TArray<float>& lhs, TConstArrayView<float> const rhs) noexcept {
    multiply_in_place(TArrayView<float>{lhs}, rhs);
}

void multiply_in_place(TArrayView<float> const lhs, float const rhs) noexcept {
    int32 const count{lhs.Num()};
    multiply_float_array_scalar_in_place_raw(lhs.GetData(), rhs, count);
}

void multiply_in_place(TArray<float>& lhs, float const rhs) noexcept {
    multiply_in_place(TArrayView<float>{lhs}, rhs);
}

void multiply_in_place(TArrayView<double> const lhs, TConstArrayView<double> const rhs) noexcept {
    int32 const count{lhs.Num()};
    checkf(rhs.Num() == count, TEXT("multiply_in_place: rhs and lhs must have equal lengths"));
    checkf(!ranges_overlap(lhs.GetData(), static_cast<std::size_t>(lhs.Num()) * sizeof(double), rhs.GetData(), static_cast<std::size_t>(rhs.Num()) * sizeof(double)),
           TEXT("multiply_in_place: lhs and rhs must not overlap"));
    multiply_double_array_array_in_place_raw(lhs.GetData(), rhs.GetData(), count);
}

void multiply_in_place(TArray<double>& lhs, TConstArrayView<double> const rhs) noexcept {
    multiply_in_place(TArrayView<double>{lhs}, rhs);
}

void multiply_in_place(TArrayView<double> const lhs, double const rhs) noexcept {
    int32 const count{lhs.Num()};
    multiply_double_array_scalar_in_place_raw(lhs.GetData(), rhs, count);
}

void multiply_in_place(TArray<double>& lhs, double const rhs) noexcept {
    multiply_in_place(TArrayView<double>{lhs}, rhs);
}

void divide_in_place(TArrayView<int32> const data, int32 const value) noexcept {
    int32 const count{data.Num()};
    divide_int32_array_scalar_in_place_raw(data.GetData(), value, count);
}

void divide_in_place(TArray<int32>& data, int32 const value) noexcept {
    divide_in_place(TArrayView<int32>{data}, value);
}

void divide_in_place(TArrayView<float> const data, float const value) noexcept {
    int32 const count{data.Num()};
    divide_float_array_scalar_in_place_raw(data.GetData(), value, count);
}

void divide_in_place(TArray<float>& data, float const value) noexcept {
    divide_in_place(TArrayView<float>{data}, value);
}

void divide_in_place(TArrayView<double> const data, double const value) noexcept {
    int32 const count{data.Num()};
    divide_double_array_scalar_in_place_raw(data.GetData(), value, count);
}

void divide_in_place(TArray<double>& data, double const value) noexcept {
    divide_in_place(TArrayView<double>{data}, value);
}

}
