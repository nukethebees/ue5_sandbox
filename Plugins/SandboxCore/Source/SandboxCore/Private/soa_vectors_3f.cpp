// clang-format off
// This file is autogenerated. Do not edit by hand.
// Edit Codegen/manifests and regenerate it instead.

#include "SandboxCore/soa_vectors_3f.h"

#include "SandboxCore/array_checks.h"
#include "SandboxCore/container_ops.h"
#include "SandboxCore/soa_permutation.h"

#include "Containers/AllowShrinking.h"
#include "Containers/ArrayView.h"
#include "CoreMinimal.h"

auto FVectors3fConstView::get_view() const -> ConstView {
    return get_view(0, num());
}

auto FVectors3fConstView::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<float>{xs}.Slice(offset, count),
        TConstArrayView<float>{ys}.Slice(offset, count),
        TConstArrayView<float>{zs}.Slice(offset, count),
    };
}

auto FVectors3fConstView::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto FVectors3fConstView::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<float>{xs}.Slice(offset, count),
        TConstArrayView<float>{ys}.Slice(offset, count),
        TConstArrayView<float>{zs}.Slice(offset, count),
    };
}

auto FVectors3fConstView::num() const noexcept -> int32 {
    return ml::num(xs);
}

auto FVectors3fConstView::is_empty() const noexcept -> bool {
    return num() == 0;
}

void FVectors3fConstView::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(xs),
        ml::num(ys),
        ml::num(zs),
    });
}

auto FVectors3fConstView::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto FVectors3fConstView::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto FVectors3fConstView::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}

auto FVectors3fView::get_view() -> View {
    return get_view(0, num());
}

auto FVectors3fView::get_view(int32 const offset, int32 const count) -> View {
    return View{
        TArrayView<float>{xs}.Slice(offset, count),
        TArrayView<float>{ys}.Slice(offset, count),
        TArrayView<float>{zs}.Slice(offset, count),
    };
}

auto FVectors3fView::get_view() const -> ConstView {
    return get_view(0, num());
}

auto FVectors3fView::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<float>{xs}.Slice(offset, count),
        TConstArrayView<float>{ys}.Slice(offset, count),
        TConstArrayView<float>{zs}.Slice(offset, count),
    };
}

auto FVectors3fView::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto FVectors3fView::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<float>{xs}.Slice(offset, count),
        TConstArrayView<float>{ys}.Slice(offset, count),
        TConstArrayView<float>{zs}.Slice(offset, count),
    };
}

auto FVectors3fView::num() const noexcept -> int32 {
    return ml::num(xs);
}

auto FVectors3fView::is_empty() const noexcept -> bool {
    return num() == 0;
}

void FVectors3fView::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(xs),
        ml::num(ys),
        ml::num(zs),
    });
}

auto FVectors3fView::slice(int32 const offset, int32 const count) -> View {
    return get_view(offset, count);
}

auto FVectors3fView::left(int32 const count) -> View {
    return slice(0, count);
}

auto FVectors3fView::right(int32 const count) -> View {
    return slice(num() - count, count);
}

auto FVectors3fView::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto FVectors3fView::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto FVectors3fView::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}

void FVectors3f::reset() {
    ml::reset(xs);
    ml::reset(ys);
    ml::reset(zs);
}

void FVectors3f::reserve(int32 const count) {
    ml::reserve(xs, count);
    ml::reserve(ys, count);
    ml::reserve(zs, count);
}

void FVectors3f::add_uninitialised(int32 const count) {
    ml::add_uninitialised(xs, count);
    ml::add_uninitialised(ys, count);
    ml::add_uninitialised(zs, count);
}

void FVectors3f::add_defaulted(int32 const count) {
    ml::add_defaulted(xs, count);
    ml::add_defaulted(ys, count);
    ml::add_defaulted(zs, count);
}

void FVectors3f::set_num(int32 const count, EAllowShrinking const allow_shrinking) {
    ml::set_num(xs, count, allow_shrinking);
    ml::set_num(ys, count, allow_shrinking);
    ml::set_num(zs, count, allow_shrinking);
}

void FVectors3f::apply_permutation(TArrayView<int32> indices) {
    validate_array_sizes();
    check(indices.Num() == num());
    ml::apply_permutation(xs, indices);
    ml::apply_permutation(ys, indices);
    ml::apply_permutation(zs, indices);
}

auto FVectors3f::get_view() -> View {
    return get_view(0, num());
}

auto FVectors3f::get_view(int32 const offset, int32 const count) -> View {
    return View{
        TArrayView<float>{xs}.Slice(offset, count),
        TArrayView<float>{ys}.Slice(offset, count),
        TArrayView<float>{zs}.Slice(offset, count),
    };
}

auto FVectors3f::get_view() const -> ConstView {
    return get_view(0, num());
}

auto FVectors3f::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<float>{xs}.Slice(offset, count),
        TConstArrayView<float>{ys}.Slice(offset, count),
        TConstArrayView<float>{zs}.Slice(offset, count),
    };
}

auto FVectors3f::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto FVectors3f::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<float>{xs}.Slice(offset, count),
        TConstArrayView<float>{ys}.Slice(offset, count),
        TConstArrayView<float>{zs}.Slice(offset, count),
    };
}

auto FVectors3f::num() const noexcept -> int32 {
    return ml::num(xs);
}

auto FVectors3f::is_empty() const noexcept -> bool {
    return num() == 0;
}

void FVectors3f::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(xs),
        ml::num(ys),
        ml::num(zs),
    });
}

auto FVectors3f::slice(int32 const offset, int32 const count) -> View {
    return get_view(offset, count);
}

auto FVectors3f::left(int32 const count) -> View {
    return slice(0, count);
}

auto FVectors3f::right(int32 const count) -> View {
    return slice(num() - count, count);
}

auto FVectors3f::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto FVectors3f::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto FVectors3f::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}
// clang-format on
