// clang-format off
// This file is autogenerated. Do not edit by hand.
// Edit Codegen/manifests and regenerate it instead.

#pragma once

#include "SandboxCore/array_utils.h"
#include "SandboxCore/fixed_storage.h"

#include "Containers/AllowShrinking.h"
#include "Containers/Array.h"
#include "Containers/ArrayView.h"
#include "CoreMinimal.h"
#include "Templates/SharedPointer.h"
#include "Templates/UnrealTemplate.h"

#include <memory>
#include <type_traits>
#include <utility>

namespace ml::fixed_soa_tests {
struct FTestFixedChildView;
struct FTestFixedChildConstView;

struct FTestFixedChildConstView {
    using View = FTestFixedChildView;
    using ConstView = FTestFixedChildConstView;

    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(
            self.names,
            self.references
        );
    }

    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TConstArrayView<FString> names;
    TConstArrayView<TSharedPtr<int32>> references;
};

struct FTestFixedChildView {
    using View = FTestFixedChildView;
    using ConstView = FTestFixedChildConstView;

    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(
            self.names,
            self.references
        );
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TArrayView<FString> names;
    TArrayView<TSharedPtr<int32>> references;
};

struct FTestFixedChild {
    using View = FTestFixedChildView;
    using ConstView = FTestFixedChildConstView;

    void apply_permutation(TArrayView<int32> indices);

    template <typename Compare>
    void sort(Compare&& compare, TArrayView<int32> scratch_indices) {
        validate_array_sizes();
        auto const n{num()};
        check(scratch_indices.Num() == n);
        ml::fill_indices(scratch_indices);
        // indices[new_index] is the old row index that belongs at new_index.
        scratch_indices.Sort([this, &compare](int32 const lhs, int32 const rhs) {
            return compare(*this, lhs, rhs);
        });
        apply_permutation(scratch_indices);
    }

    template <auto Compare>
    void sort(TArrayView<int32> scratch_indices) {
        validate_array_sizes();
        auto const n{num()};
        check(scratch_indices.Num() == n);
        ml::fill_indices(scratch_indices);
        // indices[new_index] is the old row index that belongs at new_index.
        scratch_indices.Sort([this](int32 const lhs, int32 const rhs) {
            return Compare(*this, lhs, rhs);
        });
        apply_permutation(scratch_indices);
    }

    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(
            self.names,
            self.references
        );
    }

    template <typename Self, typename Other, typename TFunc>
    auto apply_array_pairs(this Self&& self, Other&& other, TFunc&& func)
        -> decltype(auto) {
        return std::forward<TFunc>(func)(
            self.names, other.names,
            self.references, other.references
        );
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TArray<FString> names;
    TArray<TSharedPtr<int32>> references;
};

template <int32 Capacity>
    requires (Capacity >= 0)
struct TTestFixedChildStorage {
    using View = FTestFixedChildView;
    using ConstView = FTestFixedChildConstView;

    auto get_view(int32 const offset, int32 const count) -> View {
        return View{TArrayView<FString>{names_.data() + offset, count}, TArrayView<TSharedPtr<int32>>{references_.data() + offset, count}};
    }
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView {
        return ConstView{TConstArrayView<FString>{names_.data() + offset, count}, TConstArrayView<TSharedPtr<int32>>{references_.data() + offset, count}};
    }

    template <typename TArg0, typename TArg1>
    void construct_at(int32 const index, TArg0&& new_names, TArg1&& new_references) {
        names_.construct_at(index, std::forward<TArg0>(new_names));
        references_.construct_at(index, std::forward<TArg1>(new_references));
    }

    void default_construct_at(int32 const index) {
        names_.construct_at(index);
        references_.construct_at(index);
    }

    void copy_construct_at(int32 const index, TTestFixedChildStorage const& other, int32 const other_index) {
        names_.construct_at(index, other.names_[other_index]);
        references_.construct_at(index, other.references_[other_index]);
    }

    void move_construct_at(int32 const index, TTestFixedChildStorage& other, int32 const other_index) {
        names_.construct_at(index, MoveTemp(other.names_[other_index]));
        references_.construct_at(index, MoveTemp(other.references_[other_index]));
    }

    template <typename SourceView>
    void construct_from_view_at(int32 const index, SourceView const& source, int32 const source_index) {
        names_.construct_at(index, source.names[source_index]);
        references_.construct_at(index, source.references[source_index]);
    }

    template <typename SourceView>
    void copy_assign_from_view_at(int32 const dst_index, SourceView const& source, int32 const source_index) {
        names_[dst_index] = source.names[source_index];
        references_[dst_index] = source.references[source_index];
    }

    void move_assign_at(int32 const dst_index, TTestFixedChildStorage& other, int32 const source_index) {
        names_[dst_index] = MoveTemp(other.names_[source_index]);
        references_[dst_index] = MoveTemp(other.references_[source_index]);
    }

    void destroy_at(int32 const index) noexcept {
        references_.destroy_at(index);
        names_.destroy_at(index);
    }

    ml::TFixedStorage<FString, Capacity> names_;
    ml::TFixedStorage<TSharedPtr<int32>, Capacity> references_;
};

struct FTestFixedRowsView;
struct FTestFixedRowsConstView;

struct FTestFixedRowsConstView {
    using View = FTestFixedRowsView;
    using ConstView = FTestFixedRowsConstView;

    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(
            self.children,
            self.ids
        );
    }

    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    FTestFixedChild::ConstView children;
    TConstArrayView<int32> ids;
};

struct FTestFixedRowsView {
    using View = FTestFixedRowsView;
    using ConstView = FTestFixedRowsConstView;

    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(
            self.children,
            self.ids
        );
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    FTestFixedChild::View children;
    TArrayView<int32> ids;
};

struct FTestFixedRows {
    using View = FTestFixedRowsView;
    using ConstView = FTestFixedRowsConstView;

    void apply_permutation(TArrayView<int32> indices);

    template <typename Compare>
    void sort(Compare&& compare, TArrayView<int32> scratch_indices) {
        validate_array_sizes();
        auto const n{num()};
        check(scratch_indices.Num() == n);
        ml::fill_indices(scratch_indices);
        // indices[new_index] is the old row index that belongs at new_index.
        scratch_indices.Sort([this, &compare](int32 const lhs, int32 const rhs) {
            return compare(*this, lhs, rhs);
        });
        apply_permutation(scratch_indices);
    }

    template <auto Compare>
    void sort(TArrayView<int32> scratch_indices) {
        validate_array_sizes();
        auto const n{num()};
        check(scratch_indices.Num() == n);
        ml::fill_indices(scratch_indices);
        // indices[new_index] is the old row index that belongs at new_index.
        scratch_indices.Sort([this](int32 const lhs, int32 const rhs) {
            return Compare(*this, lhs, rhs);
        });
        apply_permutation(scratch_indices);
    }

    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(
            self.children,
            self.ids
        );
    }

    template <typename Self, typename Other, typename TFunc>
    auto apply_array_pairs(this Self&& self, Other&& other, TFunc&& func)
        -> decltype(auto) {
        return std::forward<TFunc>(func)(
            self.children, other.children,
            self.ids, other.ids
        );
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    FTestFixedChild children;
    TArray<int32> ids;
};

template <int32 Capacity>
    requires (Capacity >= 0)
struct TTestFixedRowsStorage {
    using View = FTestFixedRowsView;
    using ConstView = FTestFixedRowsConstView;

    auto get_view(int32 const offset, int32 const count) -> View {
        return View{children_.get_view(offset, count), TArrayView<int32>{ids_.data() + offset, count}};
    }
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView {
        return ConstView{children_.get_const_view(offset, count), TConstArrayView<int32>{ids_.data() + offset, count}};
    }

    template <typename TArg0, typename TArg1, typename TArg2>
    void construct_at(int32 const index, TArg0&& new_children_names, TArg1&& new_children_references, TArg2&& new_ids) {
        children_.construct_at(index, std::forward<TArg0>(new_children_names), std::forward<TArg1>(new_children_references));
        ids_.construct_at(index, std::forward<TArg2>(new_ids));
    }

    void default_construct_at(int32 const index) {
        children_.default_construct_at(index);
        ids_.construct_at(index);
    }

    void copy_construct_at(int32 const index, TTestFixedRowsStorage const& other, int32 const other_index) {
        children_.copy_construct_at(index, other.children_, other_index);
        ids_.construct_at(index, other.ids_[other_index]);
    }

    void move_construct_at(int32 const index, TTestFixedRowsStorage& other, int32 const other_index) {
        children_.move_construct_at(index, other.children_, other_index);
        ids_.construct_at(index, MoveTemp(other.ids_[other_index]));
    }

    template <typename SourceView>
    void construct_from_view_at(int32 const index, SourceView const& source, int32 const source_index) {
        children_.construct_from_view_at(index, source.children, source_index);
        ids_.construct_at(index, source.ids[source_index]);
    }

    template <typename SourceView>
    void copy_assign_from_view_at(int32 const dst_index, SourceView const& source, int32 const source_index) {
        children_.copy_assign_from_view_at(dst_index, source.children, source_index);
        ids_[dst_index] = source.ids[source_index];
    }

    void move_assign_at(int32 const dst_index, TTestFixedRowsStorage& other, int32 const source_index) {
        children_.move_assign_at(dst_index, other.children_, source_index);
        ids_[dst_index] = MoveTemp(other.ids_[source_index]);
    }

    void destroy_at(int32 const index) noexcept {
        ids_.destroy_at(index);
        children_.destroy_at(index);
    }

    TTestFixedChildStorage<Capacity> children_;
    ml::TFixedStorage<int32, Capacity> ids_;
};

template <int32 Capacity>
    requires (Capacity >= 0)
struct TTestFixedRowsArray {
    using size_type = int32;
    using View = FTestFixedRowsView;
    using ConstView = FTestFixedRowsConstView;

    static constexpr size_type capacity_value{Capacity};

    TTestFixedRowsArray() noexcept = default;
    TTestFixedRowsArray(TTestFixedRowsArray const& other)
        requires (std::is_copy_constructible_v<FString> && std::is_copy_constructible_v<TSharedPtr<int32>> && std::is_copy_constructible_v<int32>)
    {
        for (size_type i{}; i < other.size_; ++i) {
            storage_.copy_construct_at(size_, other.storage_, i);
            ++size_;
        }
    }
    TTestFixedRowsArray(TTestFixedRowsArray const&)
        requires (!(std::is_copy_constructible_v<FString> && std::is_copy_constructible_v<TSharedPtr<int32>> && std::is_copy_constructible_v<int32>))
    = delete;
    TTestFixedRowsArray(TTestFixedRowsArray&& other) noexcept(std::is_nothrow_move_constructible_v<FString> && std::is_nothrow_move_constructible_v<TSharedPtr<int32>> && std::is_nothrow_move_constructible_v<int32>)
        requires (std::is_move_constructible_v<FString> && std::is_move_constructible_v<TSharedPtr<int32>> && std::is_move_constructible_v<int32>)
    {
        for (size_type i{}; i < other.size_; ++i) {
            storage_.move_construct_at(size_, other.storage_, i);
            ++size_;
        }
        other.reset();
    }
    TTestFixedRowsArray(TTestFixedRowsArray&&)
        requires (!(std::is_move_constructible_v<FString> && std::is_move_constructible_v<TSharedPtr<int32>> && std::is_move_constructible_v<int32>))
    = delete;
    ~TTestFixedRowsArray() { reset(); }

    auto operator=(TTestFixedRowsArray const& other) -> TTestFixedRowsArray&
        requires (std::is_copy_constructible_v<FString> && std::is_copy_constructible_v<TSharedPtr<int32>> && std::is_copy_constructible_v<int32>)
    {
        if (this != std::addressof(other)) {
            reset();
            append_from(other.get_const_view());
        }
        return *this;
    }
    auto operator=(TTestFixedRowsArray const&) -> TTestFixedRowsArray&
        requires (!(std::is_copy_constructible_v<FString> && std::is_copy_constructible_v<TSharedPtr<int32>> && std::is_copy_constructible_v<int32>))
    = delete;
    auto operator=(TTestFixedRowsArray&& other) noexcept(std::is_nothrow_move_constructible_v<FString> && std::is_nothrow_move_constructible_v<TSharedPtr<int32>> && std::is_nothrow_move_constructible_v<int32>) -> TTestFixedRowsArray&
        requires (std::is_move_constructible_v<FString> && std::is_move_constructible_v<TSharedPtr<int32>> && std::is_move_constructible_v<int32>)
    {
        if (this != std::addressof(other)) {
            reset();
            for (size_type i{}; i < other.size_; ++i) {
                storage_.move_construct_at(size_, other.storage_, i);
                ++size_;
            }
            other.reset();
        }
        return *this;
    }
    auto operator=(TTestFixedRowsArray&&) -> TTestFixedRowsArray&
        requires (!(std::is_move_constructible_v<FString> && std::is_move_constructible_v<TSharedPtr<int32>> && std::is_move_constructible_v<int32>))
    = delete;

    static constexpr auto capacity() noexcept -> size_type { return capacity_value; }
    auto num() const noexcept -> size_type { return size_; }
    auto is_empty() const noexcept -> bool { return size_ == 0; }
    auto is_full() const noexcept -> bool { return size_ == capacity(); }

    auto get_view() -> View { return storage_.get_view(0, size_); }
    auto get_view(size_type const offset, size_type const count) -> View { check_view_range(offset, count); return storage_.get_view(offset, count); }
    auto get_view() const -> ConstView { return get_const_view(); }
    auto get_view(size_type const offset, size_type const count) const -> ConstView { return get_const_view(offset, count); }
    auto get_const_view() const -> ConstView { return storage_.get_const_view(0, size_); }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView { check_view_range(offset, count); return storage_.get_const_view(offset, count); }
    auto slice(size_type const offset, size_type const count) -> View { return get_view(offset, count); }
    auto left(size_type const count) -> View { return slice(0, count); }
    auto right(size_type const count) -> View { return slice(size_ - count, count); }
    auto slice(size_type const offset, size_type const count) const -> ConstView { return get_const_view(offset, count); }
    auto left(size_type const count) const -> ConstView { return slice(0, count); }
    auto right(size_type const count) const -> ConstView { return slice(size_ - count, count); }

    template <typename TFunc> auto apply_arrays(TFunc&& func) -> decltype(auto) { auto view{get_view()}; return view.apply_arrays(std::forward<TFunc>(func)); }
    template <typename TFunc> auto apply_arrays(TFunc&& func) const -> decltype(auto) { auto view{get_const_view()}; return view.apply_arrays(std::forward<TFunc>(func)); }

    template <typename TArg0, typename TArg1, typename TArg2>
        requires (std::is_constructible_v<FString, TArg0&&> && std::is_constructible_v<TSharedPtr<int32>, TArg1&&> && std::is_constructible_v<int32, TArg2&&>)
    auto emplace_back(TArg0&& new_children_names, TArg1&& new_children_references, TArg2&& new_ids) -> size_type {
        check_has_sufficient_capacity(1);
        auto const index{size_};
        storage_.construct_at(index, std::forward<TArg0>(new_children_names), std::forward<TArg1>(new_children_references), std::forward<TArg2>(new_ids));
        ++size_;
        return index;
    }
    template <typename TArg0, typename TArg1, typename TArg2>
        requires (std::is_constructible_v<FString, TArg0&&> && std::is_constructible_v<TSharedPtr<int32>, TArg1&&> && std::is_constructible_v<int32, TArg2&&>)
    auto add(TArg0&& new_children_names, TArg1&& new_children_references, TArg2&& new_ids) -> size_type { return emplace_back(std::forward<TArg0>(new_children_names), std::forward<TArg1>(new_children_references), std::forward<TArg2>(new_ids)); }

    void add_defaulted(size_type const count = 1) requires (std::is_default_constructible_v<FString> && std::is_default_constructible_v<TSharedPtr<int32>> && std::is_default_constructible_v<int32>) { check_has_sufficient_capacity(count); for (size_type i{}; i < count; ++i) { storage_.default_construct_at(size_); ++size_; } }
    void set_num(size_type const new_size) requires (std::is_default_constructible_v<FString> && std::is_default_constructible_v<TSharedPtr<int32>> && std::is_default_constructible_v<int32>) { check(new_size >= 0); check(new_size <= capacity()); if (new_size < size_) { destroy_from(new_size); return; } add_defaulted(new_size - size_); }
    void set_num(size_type const new_size, EAllowShrinking const) requires (std::is_default_constructible_v<FString> && std::is_default_constructible_v<TSharedPtr<int32>> && std::is_default_constructible_v<int32>) { set_num(new_size); }
    auto capacity_view() -> View requires ((std::is_trivially_copyable_v<FString> && std::is_trivially_destructible_v<FString>) && (std::is_trivially_copyable_v<TSharedPtr<int32>> && std::is_trivially_destructible_v<TSharedPtr<int32>>) && (std::is_trivially_copyable_v<int32> && std::is_trivially_destructible_v<int32>)) { return storage_.get_view(0, capacity()); }
    void set_num_uninitialised(size_type const new_size) requires ((std::is_trivially_copyable_v<FString> && std::is_trivially_destructible_v<FString>) && (std::is_trivially_copyable_v<TSharedPtr<int32>> && std::is_trivially_destructible_v<TSharedPtr<int32>>) && (std::is_trivially_copyable_v<int32> && std::is_trivially_destructible_v<int32>)) { check(new_size >= 0); check(new_size <= capacity()); size_ = new_size; }
    void add_uninitialised(size_type const count) requires ((std::is_trivially_copyable_v<FString> && std::is_trivially_destructible_v<FString>) && (std::is_trivially_copyable_v<TSharedPtr<int32>> && std::is_trivially_destructible_v<TSharedPtr<int32>>) && (std::is_trivially_copyable_v<int32> && std::is_trivially_destructible_v<int32>)) { check_has_sufficient_capacity(count); size_ += count; }

    void pop() { check(!is_empty()); --size_; storage_.destroy_at(size_); }
    void reset() noexcept { destroy_from(0); }
    void empty() noexcept { reset(); }
    void reserve(size_type const requested_capacity) const { check(requested_capacity >= 0); check(requested_capacity <= capacity()); }
    void remove_at_swap(size_type const index, size_type const count, EAllowShrinking const) {
        check(index >= 0); check(count >= 0); check(index + count <= size_);
        if (count == 0) { return; }
        auto const available_tail{size_ - (index + count)};
        auto const move_count{count < available_tail ? count : available_tail};
        auto const source_begin{size_ - move_count};
        for (size_type i{}; i < move_count; ++i) { storage_.move_assign_at(index + i, storage_, source_begin + i); }
        destroy_from(size_ - count);
    }
    template <typename Other> void copy_element(size_type const dst_index, Other const& other, size_type const source_index) { check_index(dst_index); auto const source{other.get_const_view()}; check(source_index >= 0); check(source_index < source.num()); storage_.copy_assign_from_view_at(dst_index, source, source_index); }
    template <typename Other> void copy_elements(size_type const dst_index, Other const& other, size_type const source_index, size_type const count) { check(dst_index >= 0); check(source_index >= 0); check(count >= 0); check(dst_index + count <= size_); auto const source{other.get_const_view()}; check(source_index + count <= source.num()); for (size_type i{}; i < count; ++i) { storage_.copy_assign_from_view_at(dst_index + i, source, source_index + i); } }
    template <typename Other> void append_from(Other const& other) { auto const source{other.get_const_view()}; auto const count{source.num()}; check_has_sufficient_capacity(count); for (size_type i{}; i < count; ++i) { storage_.construct_from_view_at(size_, source, i); ++size_; } }

  private:
    void check_index(size_type const index) const { check(index >= 0); check(index < size_); }
    void check_view_range(size_type const offset, size_type const count) const { check(offset >= 0); check(count >= 0); check(offset + count <= size_); }
    void check_has_sufficient_capacity(size_type const count) const { check(count >= 0); check(count <= capacity() - size_); }
    void destroy_from(size_type const first_index) noexcept { for (size_type i{size_}; i > first_index; --i) { storage_.destroy_at(i - 1); } size_ = first_index; }

    TTestFixedRowsStorage<Capacity> storage_;
    size_type size_{};
};

template <int32 Capacity>
    requires (Capacity >= 0)
struct TTestFixedRowsArrayAlternate {
    using size_type = int32;
    using View = FTestFixedRowsView;
    using ConstView = FTestFixedRowsConstView;

    static constexpr size_type capacity_value{Capacity};

    TTestFixedRowsArrayAlternate() noexcept = default;
    TTestFixedRowsArrayAlternate(TTestFixedRowsArrayAlternate const& other)
        requires (std::is_copy_constructible_v<FString> && std::is_copy_constructible_v<TSharedPtr<int32>> && std::is_copy_constructible_v<int32>)
    {
        for (size_type i{}; i < other.size_; ++i) {
            storage_.copy_construct_at(size_, other.storage_, i);
            ++size_;
        }
    }
    TTestFixedRowsArrayAlternate(TTestFixedRowsArrayAlternate const&)
        requires (!(std::is_copy_constructible_v<FString> && std::is_copy_constructible_v<TSharedPtr<int32>> && std::is_copy_constructible_v<int32>))
    = delete;
    TTestFixedRowsArrayAlternate(TTestFixedRowsArrayAlternate&& other) noexcept(std::is_nothrow_move_constructible_v<FString> && std::is_nothrow_move_constructible_v<TSharedPtr<int32>> && std::is_nothrow_move_constructible_v<int32>)
        requires (std::is_move_constructible_v<FString> && std::is_move_constructible_v<TSharedPtr<int32>> && std::is_move_constructible_v<int32>)
    {
        for (size_type i{}; i < other.size_; ++i) {
            storage_.move_construct_at(size_, other.storage_, i);
            ++size_;
        }
        other.reset();
    }
    TTestFixedRowsArrayAlternate(TTestFixedRowsArrayAlternate&&)
        requires (!(std::is_move_constructible_v<FString> && std::is_move_constructible_v<TSharedPtr<int32>> && std::is_move_constructible_v<int32>))
    = delete;
    ~TTestFixedRowsArrayAlternate() { reset(); }

    auto operator=(TTestFixedRowsArrayAlternate const& other) -> TTestFixedRowsArrayAlternate&
        requires (std::is_copy_constructible_v<FString> && std::is_copy_constructible_v<TSharedPtr<int32>> && std::is_copy_constructible_v<int32>)
    {
        if (this != std::addressof(other)) {
            reset();
            append_from(other.get_const_view());
        }
        return *this;
    }
    auto operator=(TTestFixedRowsArrayAlternate const&) -> TTestFixedRowsArrayAlternate&
        requires (!(std::is_copy_constructible_v<FString> && std::is_copy_constructible_v<TSharedPtr<int32>> && std::is_copy_constructible_v<int32>))
    = delete;
    auto operator=(TTestFixedRowsArrayAlternate&& other) noexcept(std::is_nothrow_move_constructible_v<FString> && std::is_nothrow_move_constructible_v<TSharedPtr<int32>> && std::is_nothrow_move_constructible_v<int32>) -> TTestFixedRowsArrayAlternate&
        requires (std::is_move_constructible_v<FString> && std::is_move_constructible_v<TSharedPtr<int32>> && std::is_move_constructible_v<int32>)
    {
        if (this != std::addressof(other)) {
            reset();
            for (size_type i{}; i < other.size_; ++i) {
                storage_.move_construct_at(size_, other.storage_, i);
                ++size_;
            }
            other.reset();
        }
        return *this;
    }
    auto operator=(TTestFixedRowsArrayAlternate&&) -> TTestFixedRowsArrayAlternate&
        requires (!(std::is_move_constructible_v<FString> && std::is_move_constructible_v<TSharedPtr<int32>> && std::is_move_constructible_v<int32>))
    = delete;

    static constexpr auto capacity() noexcept -> size_type { return capacity_value; }
    auto num() const noexcept -> size_type { return size_; }
    auto is_empty() const noexcept -> bool { return size_ == 0; }
    auto is_full() const noexcept -> bool { return size_ == capacity(); }

    auto get_view() -> View { return storage_.get_view(0, size_); }
    auto get_view(size_type const offset, size_type const count) -> View { check_view_range(offset, count); return storage_.get_view(offset, count); }
    auto get_view() const -> ConstView { return get_const_view(); }
    auto get_view(size_type const offset, size_type const count) const -> ConstView { return get_const_view(offset, count); }
    auto get_const_view() const -> ConstView { return storage_.get_const_view(0, size_); }
    auto get_const_view(size_type const offset, size_type const count) const -> ConstView { check_view_range(offset, count); return storage_.get_const_view(offset, count); }
    auto slice(size_type const offset, size_type const count) -> View { return get_view(offset, count); }
    auto left(size_type const count) -> View { return slice(0, count); }
    auto right(size_type const count) -> View { return slice(size_ - count, count); }
    auto slice(size_type const offset, size_type const count) const -> ConstView { return get_const_view(offset, count); }
    auto left(size_type const count) const -> ConstView { return slice(0, count); }
    auto right(size_type const count) const -> ConstView { return slice(size_ - count, count); }

    template <typename TFunc> auto apply_arrays(TFunc&& func) -> decltype(auto) { auto view{get_view()}; return view.apply_arrays(std::forward<TFunc>(func)); }
    template <typename TFunc> auto apply_arrays(TFunc&& func) const -> decltype(auto) { auto view{get_const_view()}; return view.apply_arrays(std::forward<TFunc>(func)); }

    template <typename TArg0, typename TArg1, typename TArg2>
        requires (std::is_constructible_v<FString, TArg0&&> && std::is_constructible_v<TSharedPtr<int32>, TArg1&&> && std::is_constructible_v<int32, TArg2&&>)
    auto emplace_back(TArg0&& new_children_names, TArg1&& new_children_references, TArg2&& new_ids) -> size_type {
        check_has_sufficient_capacity(1);
        auto const index{size_};
        storage_.construct_at(index, std::forward<TArg0>(new_children_names), std::forward<TArg1>(new_children_references), std::forward<TArg2>(new_ids));
        ++size_;
        return index;
    }
    template <typename TArg0, typename TArg1, typename TArg2>
        requires (std::is_constructible_v<FString, TArg0&&> && std::is_constructible_v<TSharedPtr<int32>, TArg1&&> && std::is_constructible_v<int32, TArg2&&>)
    auto add(TArg0&& new_children_names, TArg1&& new_children_references, TArg2&& new_ids) -> size_type { return emplace_back(std::forward<TArg0>(new_children_names), std::forward<TArg1>(new_children_references), std::forward<TArg2>(new_ids)); }

    void add_defaulted(size_type const count = 1) requires (std::is_default_constructible_v<FString> && std::is_default_constructible_v<TSharedPtr<int32>> && std::is_default_constructible_v<int32>) { check_has_sufficient_capacity(count); for (size_type i{}; i < count; ++i) { storage_.default_construct_at(size_); ++size_; } }
    void set_num(size_type const new_size) requires (std::is_default_constructible_v<FString> && std::is_default_constructible_v<TSharedPtr<int32>> && std::is_default_constructible_v<int32>) { check(new_size >= 0); check(new_size <= capacity()); if (new_size < size_) { destroy_from(new_size); return; } add_defaulted(new_size - size_); }
    void set_num(size_type const new_size, EAllowShrinking const) requires (std::is_default_constructible_v<FString> && std::is_default_constructible_v<TSharedPtr<int32>> && std::is_default_constructible_v<int32>) { set_num(new_size); }
    auto capacity_view() -> View requires ((std::is_trivially_copyable_v<FString> && std::is_trivially_destructible_v<FString>) && (std::is_trivially_copyable_v<TSharedPtr<int32>> && std::is_trivially_destructible_v<TSharedPtr<int32>>) && (std::is_trivially_copyable_v<int32> && std::is_trivially_destructible_v<int32>)) { return storage_.get_view(0, capacity()); }
    void set_num_uninitialised(size_type const new_size) requires ((std::is_trivially_copyable_v<FString> && std::is_trivially_destructible_v<FString>) && (std::is_trivially_copyable_v<TSharedPtr<int32>> && std::is_trivially_destructible_v<TSharedPtr<int32>>) && (std::is_trivially_copyable_v<int32> && std::is_trivially_destructible_v<int32>)) { check(new_size >= 0); check(new_size <= capacity()); size_ = new_size; }
    void add_uninitialised(size_type const count) requires ((std::is_trivially_copyable_v<FString> && std::is_trivially_destructible_v<FString>) && (std::is_trivially_copyable_v<TSharedPtr<int32>> && std::is_trivially_destructible_v<TSharedPtr<int32>>) && (std::is_trivially_copyable_v<int32> && std::is_trivially_destructible_v<int32>)) { check_has_sufficient_capacity(count); size_ += count; }

    void pop() { check(!is_empty()); --size_; storage_.destroy_at(size_); }
    void reset() noexcept { destroy_from(0); }
    void empty() noexcept { reset(); }
    void reserve(size_type const requested_capacity) const { check(requested_capacity >= 0); check(requested_capacity <= capacity()); }
    void remove_at_swap(size_type const index, size_type const count, EAllowShrinking const) {
        check(index >= 0); check(count >= 0); check(index + count <= size_);
        if (count == 0) { return; }
        auto const available_tail{size_ - (index + count)};
        auto const move_count{count < available_tail ? count : available_tail};
        auto const source_begin{size_ - move_count};
        for (size_type i{}; i < move_count; ++i) { storage_.move_assign_at(index + i, storage_, source_begin + i); }
        destroy_from(size_ - count);
    }
    template <typename Other> void copy_element(size_type const dst_index, Other const& other, size_type const source_index) { check_index(dst_index); auto const source{other.get_const_view()}; check(source_index >= 0); check(source_index < source.num()); storage_.copy_assign_from_view_at(dst_index, source, source_index); }
    template <typename Other> void copy_elements(size_type const dst_index, Other const& other, size_type const source_index, size_type const count) { check(dst_index >= 0); check(source_index >= 0); check(count >= 0); check(dst_index + count <= size_); auto const source{other.get_const_view()}; check(source_index + count <= source.num()); for (size_type i{}; i < count; ++i) { storage_.copy_assign_from_view_at(dst_index + i, source, source_index + i); } }
    template <typename Other> void append_from(Other const& other) { auto const source{other.get_const_view()}; auto const count{source.num()}; check_has_sufficient_capacity(count); for (size_type i{}; i < count; ++i) { storage_.construct_from_view_at(size_, source, i); ++size_; } }

  private:
    void check_index(size_type const index) const { check(index >= 0); check(index < size_); }
    void check_view_range(size_type const offset, size_type const count) const { check(offset >= 0); check(count >= 0); check(offset + count <= size_); }
    void check_has_sufficient_capacity(size_type const count) const { check(count >= 0); check(count <= capacity() - size_); }
    void destroy_from(size_type const first_index) noexcept { for (size_type i{size_}; i > first_index; --i) { storage_.destroy_at(i - 1); } size_ = first_index; }

    TTestFixedRowsStorage<Capacity> storage_;
    size_type size_{};
};
} // namespace ml::fixed_soa_tests
// clang-format on
