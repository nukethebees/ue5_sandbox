// clang-format off
// This file is autogenerated. Do not edit by hand.
#include "generated/add_scaled_avx2_lab.h"

#include <immintrin.h>

namespace ml::kernel_benchmark {

void add_scaled_autovec_avx2(float const* RESTRICT base, float const* RESTRICT value, float const scale, float* RESTRICT out, int32 const count) noexcept {
    for (int32 i{0}; i < count; ++i) {
        out[i] = (base[i] + (value[i] * scale));
    }
}

void add_scaled_avx2(float const* RESTRICT base, float const* RESTRICT value, float const scale, float* RESTRICT out, int32 const count) noexcept {
    auto const scale_vector{_mm256_set1_ps(scale)};
    int32 i{};
    int32 const vectorized_count{count - (count % 8)};
    for (; i < vectorized_count; i += 8) {
        auto const vector_0{_mm256_loadu_ps(base + i)};
        auto const vector_1{_mm256_loadu_ps(value + i)};
        auto const vector_2{_mm256_mul_ps(vector_1, scale_vector)};
        auto const vector_3{_mm256_add_ps(vector_0, vector_2)};
        _mm256_storeu_ps(out + i, vector_3);
    }

    switch (count - i) {
    case 7:
        out[i + 6] = (base[i + 6] + (value[i + 6] * scale));
        [[fallthrough]];
    case 6:
        out[i + 5] = (base[i + 5] + (value[i + 5] * scale));
        [[fallthrough]];
    case 5:
        out[i + 4] = (base[i + 4] + (value[i + 4] * scale));
        [[fallthrough]];
    case 4:
        out[i + 3] = (base[i + 3] + (value[i + 3] * scale));
        [[fallthrough]];
    case 3:
        out[i + 2] = (base[i + 2] + (value[i + 2] * scale));
        [[fallthrough]];
    case 2:
        out[i + 1] = (base[i + 1] + (value[i + 1] * scale));
        [[fallthrough]];
    case 1:
        out[i] = (base[i] + (value[i] * scale));
        [[fallthrough]];
    case 0:
        break;
    default:
        break;
    }
}

void add_scaled_avx2_unrolled(float const* RESTRICT base, float const* RESTRICT value, float const scale, float* RESTRICT out, int32 const count) noexcept {
    auto const scale_vector{_mm256_set1_ps(scale)};
    int32 i{};
    int32 const unrolled_count{count - (count % 32)};
    for (; i < unrolled_count; i += 32) {
        {
            auto const vector_0{_mm256_loadu_ps(base + i)};
            auto const vector_1{_mm256_loadu_ps(value + i)};
            auto const vector_2{_mm256_mul_ps(vector_1, scale_vector)};
            auto const vector_3{_mm256_add_ps(vector_0, vector_2)};
            _mm256_storeu_ps(out + i, vector_3);
        }
        {
            auto const vector_0{_mm256_loadu_ps(base + i + 8)};
            auto const vector_1{_mm256_loadu_ps(value + i + 8)};
            auto const vector_2{_mm256_mul_ps(vector_1, scale_vector)};
            auto const vector_3{_mm256_add_ps(vector_0, vector_2)};
            _mm256_storeu_ps(out + i + 8, vector_3);
        }
        {
            auto const vector_0{_mm256_loadu_ps(base + i + 16)};
            auto const vector_1{_mm256_loadu_ps(value + i + 16)};
            auto const vector_2{_mm256_mul_ps(vector_1, scale_vector)};
            auto const vector_3{_mm256_add_ps(vector_0, vector_2)};
            _mm256_storeu_ps(out + i + 16, vector_3);
        }
        {
            auto const vector_0{_mm256_loadu_ps(base + i + 24)};
            auto const vector_1{_mm256_loadu_ps(value + i + 24)};
            auto const vector_2{_mm256_mul_ps(vector_1, scale_vector)};
            auto const vector_3{_mm256_add_ps(vector_0, vector_2)};
            _mm256_storeu_ps(out + i + 24, vector_3);
        }
    }

    int32 const vectorized_count{count - (count % 8)};
    for (; i < vectorized_count; i += 8) {
        auto const vector_0{_mm256_loadu_ps(base + i)};
        auto const vector_1{_mm256_loadu_ps(value + i)};
        auto const vector_2{_mm256_mul_ps(vector_1, scale_vector)};
        auto const vector_3{_mm256_add_ps(vector_0, vector_2)};
        _mm256_storeu_ps(out + i, vector_3);
    }

    switch (count - i) {
    case 7:
        out[i + 6] = (base[i + 6] + (value[i + 6] * scale));
        [[fallthrough]];
    case 6:
        out[i + 5] = (base[i + 5] + (value[i + 5] * scale));
        [[fallthrough]];
    case 5:
        out[i + 4] = (base[i + 4] + (value[i + 4] * scale));
        [[fallthrough]];
    case 4:
        out[i + 3] = (base[i + 3] + (value[i + 3] * scale));
        [[fallthrough]];
    case 3:
        out[i + 2] = (base[i + 2] + (value[i + 2] * scale));
        [[fallthrough]];
    case 2:
        out[i + 1] = (base[i + 1] + (value[i + 1] * scale));
        [[fallthrough]];
    case 1:
        out[i] = (base[i] + (value[i] * scale));
        [[fallthrough]];
    case 0:
        break;
    default:
        break;
    }
}

}
