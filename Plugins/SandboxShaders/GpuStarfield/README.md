# GPU Starfield

GPU Starfield is a deterministic, camera-relative background renderer. Configure its actor settings
and run the CMake benchmark or matrix workflow for target-hardware evidence.

See [ARCHITECTURE.md](ARCHITECTURE.md) for the scene-proxy pipeline, shader contract, culling limits,
and performance model.
