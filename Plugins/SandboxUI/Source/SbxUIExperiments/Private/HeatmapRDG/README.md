# RDG Heatmap Experiment

This experiment displays a caller-owned dense scalar grid through one RDG compute pass and one Slate
image. Launch the Editor Utility Widget from `SandboxUI/Examples` to view deterministic patterns.

See [ARCHITECTURE.md](ARCHITECTURE.md) for the CPU-to-GPU pipeline, threading/lifetime contract, and
benchmark boundaries.
