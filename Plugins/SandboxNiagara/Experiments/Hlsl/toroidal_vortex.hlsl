float3(-Particles.Position.x * Particles.Position.z / max(length(float2(Particles.Position.x, Particles.Position.y)), 1.0f), -Particles.Position.y * Particles.Position.z / max(length(float2(Particles.Position.x, Particles.Position.y)), 1.0f), length(float2(Particles.Position.x, Particles.Position.y)) - User.MajorRadius) * User.PoloidalSpeed + float3(-Particles.Position.y, Particles.Position.x, 0.0f) * (User.ToroidalSpeed / max(length(float2(Particles.Position.x, Particles.Position.y)), 1.0f))
