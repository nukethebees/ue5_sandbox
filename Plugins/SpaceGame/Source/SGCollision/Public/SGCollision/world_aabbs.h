// clang-format off
// This file is autogenerated. Do not edit by hand.
// Edit Codegen/manifests and regenerate it instead.

#pragma once

#include "SandboxCore/array_utils.h"
#include "SandboxCore/container_ops.h"
#include "SandboxCore/soa_concepts.h"

#include "Containers/AllowShrinking.h"
#include "Containers/Array.h"
#include "Containers/ArrayView.h"
#include "CoreMinimal.h"

#include <utility>

namespace ml {
struct WorldAABBsView;
struct WorldAABBsConstView;

struct SGCOLLISION_API WorldAABBsConstView {
    using View = WorldAABBsView;
    using ConstView = WorldAABBsConstView;

    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(
            self.min_xs,
            self.min_ys,
            self.min_zs,
            self.max_xs,
            self.max_ys,
            self.max_zs
        );
    }

    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TConstArrayView<float> min_xs;
    TConstArrayView<float> min_ys;
    TConstArrayView<float> min_zs;
    TConstArrayView<float> max_xs;
    TConstArrayView<float> max_ys;
    TConstArrayView<float> max_zs;
};

struct SGCOLLISION_API WorldAABBsView {
    using View = WorldAABBsView;
    using ConstView = WorldAABBsConstView;

    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(
            self.min_xs,
            self.min_ys,
            self.min_zs,
            self.max_xs,
            self.max_ys,
            self.max_zs
        );
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TArrayView<float> min_xs;
    TArrayView<float> min_ys;
    TArrayView<float> min_zs;
    TArrayView<float> max_xs;
    TArrayView<float> max_ys;
    TArrayView<float> max_zs;
};

struct SGCOLLISION_API WorldAABBs {
    using View = WorldAABBsView;
    using ConstView = WorldAABBsConstView;

    void reset();

    void reserve(int32 const count);

    void add_uninitialised(int32 const count);

    void add_defaulted(int32 const count);

    void remove_at_swap(int32 const index, int32 const count, EAllowShrinking const allow_shrinking) {
        min_xs.RemoveAtSwap(index, count, allow_shrinking);
        min_ys.RemoveAtSwap(index, count, allow_shrinking);
        min_zs.RemoveAtSwap(index, count, allow_shrinking);
        max_xs.RemoveAtSwap(index, count, allow_shrinking);
        max_ys.RemoveAtSwap(index, count, allow_shrinking);
        max_zs.RemoveAtSwap(index, count, allow_shrinking);
    }

    void set_num(int32 const count, EAllowShrinking const allow_shrinking);

    template <typename Other>
    void copy_element(int32 const dst_i, Other const& other, int32 const src_i) {
        ml::copy_element(min_xs, dst_i, other.min_xs, src_i);
        ml::copy_element(min_ys, dst_i, other.min_ys, src_i);
        ml::copy_element(min_zs, dst_i, other.min_zs, src_i);
        ml::copy_element(max_xs, dst_i, other.max_xs, src_i);
        ml::copy_element(max_ys, dst_i, other.max_ys, src_i);
        ml::copy_element(max_zs, dst_i, other.max_zs, src_i);
    }

    template <typename Other>
    void copy_elements(int32 const dst_i, Other const& other, int32 const src_i, int32 const count) {
        ml::copy_elements(min_xs, dst_i, other.min_xs, src_i, count);
        ml::copy_elements(min_ys, dst_i, other.min_ys, src_i, count);
        ml::copy_elements(min_zs, dst_i, other.min_zs, src_i, count);
        ml::copy_elements(max_xs, dst_i, other.max_xs, src_i, count);
        ml::copy_elements(max_ys, dst_i, other.max_ys, src_i, count);
        ml::copy_elements(max_zs, dst_i, other.max_zs, src_i, count);
    }

    template <typename Other>
    void copy_to_tail(Other const& other) {
        auto const count{other.num()};
        check(num() >= count);
        copy_elements(num() - count, other, 0, count);
    }

    template <typename Other>
    void append_from(Other const& other)
        requires ml::SupportsApplyArrayPairsWith<WorldAABBs, Other> {
        ml::append_from(min_xs, other.min_xs);
        ml::append_from(min_ys, other.min_ys);
        ml::append_from(min_zs, other.min_zs);
        ml::append_from(max_xs, other.max_xs);
        ml::append_from(max_ys, other.max_ys);
        ml::append_from(max_zs, other.max_zs);
    }

    void apply_permutation(TArrayView<int32> indices);

    template <typename Compare>
    void sort(Compare&& compare, TArrayView<int32> scratch_indices) {
        validate_array_sizes();
        auto const n{num()};
        check(scratch_indices.Num() == n);
        ml::fill_indices(scratch_indices);
        // indices[new_index] is the old row index that belongs at new_index.
        scratch_indices.Sort([this, &compare](int32 const lhs, int32 const rhs) {
            return compare(*this, lhs, rhs);
        });
        apply_permutation(scratch_indices);
    }

    template <auto Compare>
    void sort(TArrayView<int32> scratch_indices) {
        validate_array_sizes();
        auto const n{num()};
        check(scratch_indices.Num() == n);
        ml::fill_indices(scratch_indices);
        // indices[new_index] is the old row index that belongs at new_index.
        scratch_indices.Sort([this](int32 const lhs, int32 const rhs) {
            return Compare(*this, lhs, rhs);
        });
        apply_permutation(scratch_indices);
    }

    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(
            self.min_xs,
            self.min_ys,
            self.min_zs,
            self.max_xs,
            self.max_ys,
            self.max_zs
        );
    }

    template <typename Self, typename Other, typename TFunc>
    auto apply_array_pairs(this Self&& self, Other&& other, TFunc&& func)
        -> decltype(auto) {
        return std::forward<TFunc>(func)(
            self.min_xs, other.min_xs,
            self.min_ys, other.min_ys,
            self.min_zs, other.min_zs,
            self.max_xs, other.max_xs,
            self.max_ys, other.max_ys,
            self.max_zs, other.max_zs
        );
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TArray<float> min_xs;
    TArray<float> min_ys;
    TArray<float> min_zs;
    TArray<float> max_xs;
    TArray<float> max_ys;
    TArray<float> max_zs;
};
} // namespace ml
// clang-format on
