// clang-format off
// This file is autogenerated. Do not edit by hand.
// Edit Codegen/manifests and regenerate it instead.

#include "SpaceGame/ships/capital/TestCapitalShipsSoA.h"

#include "SandboxCore/array_checks.h"
#include "SandboxCore/container_ops.h"
#include "SandboxCore/soa_permutation.h"

#include "Containers/AllowShrinking.h"
#include "Containers/ArrayView.h"
#include "CoreMinimal.h"

namespace ml::test_capital_ships {
auto SpawnDataConstView::get_view() const -> ConstView {
    return get_view(0, num());
}

auto SpawnDataConstView::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<FRegistryEntityHandle>{target_handles}.Slice(offset, count),
        locations.get_const_view(offset, count),
        rotations.get_const_view(offset, count),
        TConstArrayView<ETestTeam>{teams}.Slice(offset, count),
        TConstArrayView<int32>{healths}.Slice(offset, count),
        TConstArrayView<float>{initial_spawn_delays}.Slice(offset, count),
        TConstArrayView<float>{spawn_cooldowns}.Slice(offset, count),
    };
}

auto SpawnDataConstView::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto SpawnDataConstView::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<FRegistryEntityHandle>{target_handles}.Slice(offset, count),
        locations.get_const_view(offset, count),
        rotations.get_const_view(offset, count),
        TConstArrayView<ETestTeam>{teams}.Slice(offset, count),
        TConstArrayView<int32>{healths}.Slice(offset, count),
        TConstArrayView<float>{initial_spawn_delays}.Slice(offset, count),
        TConstArrayView<float>{spawn_cooldowns}.Slice(offset, count),
    };
}

auto SpawnDataConstView::num() const noexcept -> int32 {
    return ml::num(target_handles);
}

auto SpawnDataConstView::is_empty() const noexcept -> bool {
    return num() == 0;
}

void SpawnDataConstView::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(target_handles),
        ml::num(locations),
        ml::num(rotations),
        ml::num(teams),
        ml::num(healths),
        ml::num(initial_spawn_delays),
        ml::num(spawn_cooldowns),
    });
}

auto SpawnDataConstView::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto SpawnDataConstView::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto SpawnDataConstView::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}

auto SpawnDataView::get_view() -> View {
    return get_view(0, num());
}

auto SpawnDataView::get_view(int32 const offset, int32 const count) -> View {
    return View{
        TArrayView<FRegistryEntityHandle>{target_handles}.Slice(offset, count),
        locations.get_view(offset, count),
        rotations.get_view(offset, count),
        TArrayView<ETestTeam>{teams}.Slice(offset, count),
        TArrayView<int32>{healths}.Slice(offset, count),
        TArrayView<float>{initial_spawn_delays}.Slice(offset, count),
        TArrayView<float>{spawn_cooldowns}.Slice(offset, count),
    };
}

auto SpawnDataView::get_view() const -> ConstView {
    return get_view(0, num());
}

auto SpawnDataView::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<FRegistryEntityHandle>{target_handles}.Slice(offset, count),
        locations.get_const_view(offset, count),
        rotations.get_const_view(offset, count),
        TConstArrayView<ETestTeam>{teams}.Slice(offset, count),
        TConstArrayView<int32>{healths}.Slice(offset, count),
        TConstArrayView<float>{initial_spawn_delays}.Slice(offset, count),
        TConstArrayView<float>{spawn_cooldowns}.Slice(offset, count),
    };
}

auto SpawnDataView::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto SpawnDataView::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<FRegistryEntityHandle>{target_handles}.Slice(offset, count),
        locations.get_const_view(offset, count),
        rotations.get_const_view(offset, count),
        TConstArrayView<ETestTeam>{teams}.Slice(offset, count),
        TConstArrayView<int32>{healths}.Slice(offset, count),
        TConstArrayView<float>{initial_spawn_delays}.Slice(offset, count),
        TConstArrayView<float>{spawn_cooldowns}.Slice(offset, count),
    };
}

auto SpawnDataView::num() const noexcept -> int32 {
    return ml::num(target_handles);
}

auto SpawnDataView::is_empty() const noexcept -> bool {
    return num() == 0;
}

void SpawnDataView::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(target_handles),
        ml::num(locations),
        ml::num(rotations),
        ml::num(teams),
        ml::num(healths),
        ml::num(initial_spawn_delays),
        ml::num(spawn_cooldowns),
    });
}

auto SpawnDataView::slice(int32 const offset, int32 const count) -> View {
    return get_view(offset, count);
}

auto SpawnDataView::left(int32 const count) -> View {
    return slice(0, count);
}

auto SpawnDataView::right(int32 const count) -> View {
    return slice(num() - count, count);
}

auto SpawnDataView::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto SpawnDataView::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto SpawnDataView::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}

void SpawnData::reset() {
    ml::reset(target_handles);
    ml::reset(locations);
    ml::reset(rotations);
    ml::reset(teams);
    ml::reset(healths);
    ml::reset(initial_spawn_delays);
    ml::reset(spawn_cooldowns);
}

void SpawnData::reserve(int32 const count) {
    ml::reserve(target_handles, count);
    ml::reserve(locations, count);
    ml::reserve(rotations, count);
    ml::reserve(teams, count);
    ml::reserve(healths, count);
    ml::reserve(initial_spawn_delays, count);
    ml::reserve(spawn_cooldowns, count);
}

void SpawnData::add_uninitialised(int32 const count) {
    ml::add_uninitialised(target_handles, count);
    ml::add_uninitialised(locations, count);
    ml::add_uninitialised(rotations, count);
    ml::add_uninitialised(teams, count);
    ml::add_uninitialised(healths, count);
    ml::add_uninitialised(initial_spawn_delays, count);
    ml::add_uninitialised(spawn_cooldowns, count);
}

void SpawnData::add_defaulted(int32 const count) {
    ml::add_defaulted(target_handles, count);
    ml::add_defaulted(locations, count);
    ml::add_defaulted(rotations, count);
    ml::add_defaulted(teams, count);
    ml::add_defaulted(healths, count);
    ml::add_defaulted(initial_spawn_delays, count);
    ml::add_defaulted(spawn_cooldowns, count);
}

void SpawnData::set_num(int32 const count, EAllowShrinking const allow_shrinking) {
    ml::set_num(target_handles, count, allow_shrinking);
    ml::set_num(locations, count, allow_shrinking);
    ml::set_num(rotations, count, allow_shrinking);
    ml::set_num(teams, count, allow_shrinking);
    ml::set_num(healths, count, allow_shrinking);
    ml::set_num(initial_spawn_delays, count, allow_shrinking);
    ml::set_num(spawn_cooldowns, count, allow_shrinking);
}

void SpawnData::apply_permutation(TArrayView<int32> indices) {
    validate_array_sizes();
    check(indices.Num() == num());
    ml::apply_permutation(target_handles, indices);
    ml::apply_permutation(locations, indices);
    ml::apply_permutation(rotations, indices);
    ml::apply_permutation(teams, indices);
    ml::apply_permutation(healths, indices);
    ml::apply_permutation(initial_spawn_delays, indices);
    ml::apply_permutation(spawn_cooldowns, indices);
}

auto SpawnData::get_view() -> View {
    return get_view(0, num());
}

auto SpawnData::get_view(int32 const offset, int32 const count) -> View {
    return View{
        TArrayView<FRegistryEntityHandle>{target_handles}.Slice(offset, count),
        locations.get_view(offset, count),
        rotations.get_view(offset, count),
        TArrayView<ETestTeam>{teams}.Slice(offset, count),
        TArrayView<int32>{healths}.Slice(offset, count),
        TArrayView<float>{initial_spawn_delays}.Slice(offset, count),
        TArrayView<float>{spawn_cooldowns}.Slice(offset, count),
    };
}

auto SpawnData::get_view() const -> ConstView {
    return get_view(0, num());
}

auto SpawnData::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<FRegistryEntityHandle>{target_handles}.Slice(offset, count),
        locations.get_const_view(offset, count),
        rotations.get_const_view(offset, count),
        TConstArrayView<ETestTeam>{teams}.Slice(offset, count),
        TConstArrayView<int32>{healths}.Slice(offset, count),
        TConstArrayView<float>{initial_spawn_delays}.Slice(offset, count),
        TConstArrayView<float>{spawn_cooldowns}.Slice(offset, count),
    };
}

auto SpawnData::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto SpawnData::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<FRegistryEntityHandle>{target_handles}.Slice(offset, count),
        locations.get_const_view(offset, count),
        rotations.get_const_view(offset, count),
        TConstArrayView<ETestTeam>{teams}.Slice(offset, count),
        TConstArrayView<int32>{healths}.Slice(offset, count),
        TConstArrayView<float>{initial_spawn_delays}.Slice(offset, count),
        TConstArrayView<float>{spawn_cooldowns}.Slice(offset, count),
    };
}

auto SpawnData::num() const noexcept -> int32 {
    return ml::num(target_handles);
}

auto SpawnData::is_empty() const noexcept -> bool {
    return num() == 0;
}

void SpawnData::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(target_handles),
        ml::num(locations),
        ml::num(rotations),
        ml::num(teams),
        ml::num(healths),
        ml::num(initial_spawn_delays),
        ml::num(spawn_cooldowns),
    });
}

auto SpawnData::slice(int32 const offset, int32 const count) -> View {
    return get_view(offset, count);
}

auto SpawnData::left(int32 const count) -> View {
    return slice(0, count);
}

auto SpawnData::right(int32 const count) -> View {
    return slice(num() - count, count);
}

auto SpawnData::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto SpawnData::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto SpawnData::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}

auto EntityTickDataConstView::get_view() const -> ConstView {
    return get_view(0, num());
}

auto EntityTickDataConstView::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<int32>{ships_ready_to_spawn_fighters_buffer}.Slice(offset, count),
        fighter_queue.get_const_view(offset, count),
    };
}

auto EntityTickDataConstView::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto EntityTickDataConstView::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<int32>{ships_ready_to_spawn_fighters_buffer}.Slice(offset, count),
        fighter_queue.get_const_view(offset, count),
    };
}

auto EntityTickDataConstView::num() const noexcept -> int32 {
    return ml::num(ships_ready_to_spawn_fighters_buffer);
}

auto EntityTickDataConstView::is_empty() const noexcept -> bool {
    return num() == 0;
}

void EntityTickDataConstView::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(ships_ready_to_spawn_fighters_buffer),
        ml::num(fighter_queue),
    });
}

auto EntityTickDataConstView::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto EntityTickDataConstView::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto EntityTickDataConstView::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}

auto EntityTickDataView::get_view() -> View {
    return get_view(0, num());
}

auto EntityTickDataView::get_view(int32 const offset, int32 const count) -> View {
    return View{
        TArrayView<int32>{ships_ready_to_spawn_fighters_buffer}.Slice(offset, count),
        fighter_queue.get_view(offset, count),
    };
}

auto EntityTickDataView::get_view() const -> ConstView {
    return get_view(0, num());
}

auto EntityTickDataView::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<int32>{ships_ready_to_spawn_fighters_buffer}.Slice(offset, count),
        fighter_queue.get_const_view(offset, count),
    };
}

auto EntityTickDataView::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto EntityTickDataView::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<int32>{ships_ready_to_spawn_fighters_buffer}.Slice(offset, count),
        fighter_queue.get_const_view(offset, count),
    };
}

auto EntityTickDataView::num() const noexcept -> int32 {
    return ml::num(ships_ready_to_spawn_fighters_buffer);
}

auto EntityTickDataView::is_empty() const noexcept -> bool {
    return num() == 0;
}

void EntityTickDataView::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(ships_ready_to_spawn_fighters_buffer),
        ml::num(fighter_queue),
    });
}

auto EntityTickDataView::slice(int32 const offset, int32 const count) -> View {
    return get_view(offset, count);
}

auto EntityTickDataView::left(int32 const count) -> View {
    return slice(0, count);
}

auto EntityTickDataView::right(int32 const count) -> View {
    return slice(num() - count, count);
}

auto EntityTickDataView::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto EntityTickDataView::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto EntityTickDataView::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}

void EntityTickData::reset() {
    ml::reset(ships_ready_to_spawn_fighters_buffer);
    ml::reset(fighter_queue);
}

void EntityTickData::reserve(int32 const count) {
    ml::reserve(ships_ready_to_spawn_fighters_buffer, count);
    ml::reserve(fighter_queue, count);
}

void EntityTickData::add_uninitialised(int32 const count) {
    ml::add_uninitialised(ships_ready_to_spawn_fighters_buffer, count);
    ml::add_uninitialised(fighter_queue, count);
}

void EntityTickData::add_defaulted(int32 const count) {
    ml::add_defaulted(ships_ready_to_spawn_fighters_buffer, count);
    ml::add_defaulted(fighter_queue, count);
}

void EntityTickData::set_num(int32 const count, EAllowShrinking const allow_shrinking) {
    ml::set_num(ships_ready_to_spawn_fighters_buffer, count, allow_shrinking);
    ml::set_num(fighter_queue, count, allow_shrinking);
}

void EntityTickData::apply_permutation(TArrayView<int32> indices) {
    validate_array_sizes();
    check(indices.Num() == num());
    ml::apply_permutation(ships_ready_to_spawn_fighters_buffer, indices);
    ml::apply_permutation(fighter_queue, indices);
}

auto EntityTickData::get_view() -> View {
    return get_view(0, num());
}

auto EntityTickData::get_view(int32 const offset, int32 const count) -> View {
    return View{
        TArrayView<int32>{ships_ready_to_spawn_fighters_buffer}.Slice(offset, count),
        fighter_queue.get_view(offset, count),
    };
}

auto EntityTickData::get_view() const -> ConstView {
    return get_view(0, num());
}

auto EntityTickData::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<int32>{ships_ready_to_spawn_fighters_buffer}.Slice(offset, count),
        fighter_queue.get_const_view(offset, count),
    };
}

auto EntityTickData::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto EntityTickData::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<int32>{ships_ready_to_spawn_fighters_buffer}.Slice(offset, count),
        fighter_queue.get_const_view(offset, count),
    };
}

auto EntityTickData::num() const noexcept -> int32 {
    return ml::num(ships_ready_to_spawn_fighters_buffer);
}

auto EntityTickData::is_empty() const noexcept -> bool {
    return num() == 0;
}

void EntityTickData::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(ships_ready_to_spawn_fighters_buffer),
        ml::num(fighter_queue),
    });
}

auto EntityTickData::slice(int32 const offset, int32 const count) -> View {
    return get_view(offset, count);
}

auto EntityTickData::left(int32 const count) -> View {
    return slice(0, count);
}

auto EntityTickData::right(int32 const count) -> View {
    return slice(num() - count, count);
}

auto EntityTickData::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto EntityTickData::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto EntityTickData::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}

auto EntityDataConstView::get_view() const -> ConstView {
    return get_view(0, num());
}

auto EntityDataConstView::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<FRegistryEntityHandle>{handles}.Slice(offset, count),
        locations.get_const_view(offset, count),
        rotations.get_const_view(offset, count),
        fighter_spawn_timers.get_const_view(offset, count),
        TConstArrayView<float>{fighter_spawn_cooldowns}.Slice(offset, count),
        TConstArrayView<ETestTeam>{teams}.Slice(offset, count),
        TConstArrayView<int32>{healths}.Slice(offset, count),
        TConstArrayView<FIndexSpan>{capital_fighter_handle_spans}.Slice(offset, count),
        TConstArrayView<FRegistryEntityHandle>{target_handles}.Slice(offset, count),
    };
}

auto EntityDataConstView::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto EntityDataConstView::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<FRegistryEntityHandle>{handles}.Slice(offset, count),
        locations.get_const_view(offset, count),
        rotations.get_const_view(offset, count),
        fighter_spawn_timers.get_const_view(offset, count),
        TConstArrayView<float>{fighter_spawn_cooldowns}.Slice(offset, count),
        TConstArrayView<ETestTeam>{teams}.Slice(offset, count),
        TConstArrayView<int32>{healths}.Slice(offset, count),
        TConstArrayView<FIndexSpan>{capital_fighter_handle_spans}.Slice(offset, count),
        TConstArrayView<FRegistryEntityHandle>{target_handles}.Slice(offset, count),
    };
}

auto EntityDataConstView::num() const noexcept -> int32 {
    return ml::num(handles);
}

auto EntityDataConstView::is_empty() const noexcept -> bool {
    return num() == 0;
}

void EntityDataConstView::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(handles),
        ml::num(locations),
        ml::num(rotations),
        ml::num(fighter_spawn_timers),
        ml::num(fighter_spawn_cooldowns),
        ml::num(teams),
        ml::num(healths),
        ml::num(capital_fighter_handle_spans),
        ml::num(target_handles),
    });
}

auto EntityDataConstView::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto EntityDataConstView::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto EntityDataConstView::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}

auto EntityDataView::get_view() -> View {
    return get_view(0, num());
}

auto EntityDataView::get_view(int32 const offset, int32 const count) -> View {
    return View{
        TArrayView<FRegistryEntityHandle>{handles}.Slice(offset, count),
        locations.get_view(offset, count),
        rotations.get_view(offset, count),
        fighter_spawn_timers.get_view(offset, count),
        TArrayView<float>{fighter_spawn_cooldowns}.Slice(offset, count),
        TArrayView<ETestTeam>{teams}.Slice(offset, count),
        TArrayView<int32>{healths}.Slice(offset, count),
        TArrayView<FIndexSpan>{capital_fighter_handle_spans}.Slice(offset, count),
        TArrayView<FRegistryEntityHandle>{target_handles}.Slice(offset, count),
    };
}

auto EntityDataView::get_view() const -> ConstView {
    return get_view(0, num());
}

auto EntityDataView::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<FRegistryEntityHandle>{handles}.Slice(offset, count),
        locations.get_const_view(offset, count),
        rotations.get_const_view(offset, count),
        fighter_spawn_timers.get_const_view(offset, count),
        TConstArrayView<float>{fighter_spawn_cooldowns}.Slice(offset, count),
        TConstArrayView<ETestTeam>{teams}.Slice(offset, count),
        TConstArrayView<int32>{healths}.Slice(offset, count),
        TConstArrayView<FIndexSpan>{capital_fighter_handle_spans}.Slice(offset, count),
        TConstArrayView<FRegistryEntityHandle>{target_handles}.Slice(offset, count),
    };
}

auto EntityDataView::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto EntityDataView::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<FRegistryEntityHandle>{handles}.Slice(offset, count),
        locations.get_const_view(offset, count),
        rotations.get_const_view(offset, count),
        fighter_spawn_timers.get_const_view(offset, count),
        TConstArrayView<float>{fighter_spawn_cooldowns}.Slice(offset, count),
        TConstArrayView<ETestTeam>{teams}.Slice(offset, count),
        TConstArrayView<int32>{healths}.Slice(offset, count),
        TConstArrayView<FIndexSpan>{capital_fighter_handle_spans}.Slice(offset, count),
        TConstArrayView<FRegistryEntityHandle>{target_handles}.Slice(offset, count),
    };
}

auto EntityDataView::num() const noexcept -> int32 {
    return ml::num(handles);
}

auto EntityDataView::is_empty() const noexcept -> bool {
    return num() == 0;
}

void EntityDataView::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(handles),
        ml::num(locations),
        ml::num(rotations),
        ml::num(fighter_spawn_timers),
        ml::num(fighter_spawn_cooldowns),
        ml::num(teams),
        ml::num(healths),
        ml::num(capital_fighter_handle_spans),
        ml::num(target_handles),
    });
}

auto EntityDataView::slice(int32 const offset, int32 const count) -> View {
    return get_view(offset, count);
}

auto EntityDataView::left(int32 const count) -> View {
    return slice(0, count);
}

auto EntityDataView::right(int32 const count) -> View {
    return slice(num() - count, count);
}

auto EntityDataView::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto EntityDataView::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto EntityDataView::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}

void EntityData::reset() {
    ml::reset(handles);
    ml::reset(locations);
    ml::reset(rotations);
    ml::reset(fighter_spawn_timers);
    ml::reset(fighter_spawn_cooldowns);
    ml::reset(teams);
    ml::reset(healths);
    ml::reset(capital_fighter_handle_spans);
    ml::reset(target_handles);
}

void EntityData::reserve(int32 const count) {
    ml::reserve(handles, count);
    ml::reserve(locations, count);
    ml::reserve(rotations, count);
    ml::reserve(fighter_spawn_timers, count);
    ml::reserve(fighter_spawn_cooldowns, count);
    ml::reserve(teams, count);
    ml::reserve(healths, count);
    ml::reserve(capital_fighter_handle_spans, count);
    ml::reserve(target_handles, count);
}

void EntityData::add_uninitialised(int32 const count) {
    ml::add_uninitialised(handles, count);
    ml::add_uninitialised(locations, count);
    ml::add_uninitialised(rotations, count);
    ml::add_uninitialised(fighter_spawn_timers, count);
    ml::add_uninitialised(fighter_spawn_cooldowns, count);
    ml::add_uninitialised(teams, count);
    ml::add_uninitialised(healths, count);
    ml::add_uninitialised(capital_fighter_handle_spans, count);
    ml::add_uninitialised(target_handles, count);
}

void EntityData::add_defaulted(int32 const count) {
    ml::add_defaulted(handles, count);
    ml::add_defaulted(locations, count);
    ml::add_defaulted(rotations, count);
    ml::add_defaulted(fighter_spawn_timers, count);
    ml::add_defaulted(fighter_spawn_cooldowns, count);
    ml::add_defaulted(teams, count);
    ml::add_defaulted(healths, count);
    ml::add_defaulted(capital_fighter_handle_spans, count);
    ml::add_defaulted(target_handles, count);
}

void EntityData::set_num(int32 const count, EAllowShrinking const allow_shrinking) {
    ml::set_num(handles, count, allow_shrinking);
    ml::set_num(locations, count, allow_shrinking);
    ml::set_num(rotations, count, allow_shrinking);
    ml::set_num(fighter_spawn_timers, count, allow_shrinking);
    ml::set_num(fighter_spawn_cooldowns, count, allow_shrinking);
    ml::set_num(teams, count, allow_shrinking);
    ml::set_num(healths, count, allow_shrinking);
    ml::set_num(capital_fighter_handle_spans, count, allow_shrinking);
    ml::set_num(target_handles, count, allow_shrinking);
}

void EntityData::apply_permutation(TArrayView<int32> indices) {
    validate_array_sizes();
    check(indices.Num() == num());
    ml::apply_permutation(handles, indices);
    ml::apply_permutation(locations, indices);
    ml::apply_permutation(rotations, indices);
    ml::apply_permutation(fighter_spawn_timers, indices);
    ml::apply_permutation(fighter_spawn_cooldowns, indices);
    ml::apply_permutation(teams, indices);
    ml::apply_permutation(healths, indices);
    ml::apply_permutation(capital_fighter_handle_spans, indices);
    ml::apply_permutation(target_handles, indices);
}

auto EntityData::get_view() -> View {
    return get_view(0, num());
}

auto EntityData::get_view(int32 const offset, int32 const count) -> View {
    return View{
        TArrayView<FRegistryEntityHandle>{handles}.Slice(offset, count),
        locations.get_view(offset, count),
        rotations.get_view(offset, count),
        fighter_spawn_timers.get_view(offset, count),
        TArrayView<float>{fighter_spawn_cooldowns}.Slice(offset, count),
        TArrayView<ETestTeam>{teams}.Slice(offset, count),
        TArrayView<int32>{healths}.Slice(offset, count),
        TArrayView<FIndexSpan>{capital_fighter_handle_spans}.Slice(offset, count),
        TArrayView<FRegistryEntityHandle>{target_handles}.Slice(offset, count),
    };
}

auto EntityData::get_view() const -> ConstView {
    return get_view(0, num());
}

auto EntityData::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<FRegistryEntityHandle>{handles}.Slice(offset, count),
        locations.get_const_view(offset, count),
        rotations.get_const_view(offset, count),
        fighter_spawn_timers.get_const_view(offset, count),
        TConstArrayView<float>{fighter_spawn_cooldowns}.Slice(offset, count),
        TConstArrayView<ETestTeam>{teams}.Slice(offset, count),
        TConstArrayView<int32>{healths}.Slice(offset, count),
        TConstArrayView<FIndexSpan>{capital_fighter_handle_spans}.Slice(offset, count),
        TConstArrayView<FRegistryEntityHandle>{target_handles}.Slice(offset, count),
    };
}

auto EntityData::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto EntityData::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<FRegistryEntityHandle>{handles}.Slice(offset, count),
        locations.get_const_view(offset, count),
        rotations.get_const_view(offset, count),
        fighter_spawn_timers.get_const_view(offset, count),
        TConstArrayView<float>{fighter_spawn_cooldowns}.Slice(offset, count),
        TConstArrayView<ETestTeam>{teams}.Slice(offset, count),
        TConstArrayView<int32>{healths}.Slice(offset, count),
        TConstArrayView<FIndexSpan>{capital_fighter_handle_spans}.Slice(offset, count),
        TConstArrayView<FRegistryEntityHandle>{target_handles}.Slice(offset, count),
    };
}

auto EntityData::num() const noexcept -> int32 {
    return ml::num(handles);
}

auto EntityData::is_empty() const noexcept -> bool {
    return num() == 0;
}

void EntityData::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(handles),
        ml::num(locations),
        ml::num(rotations),
        ml::num(fighter_spawn_timers),
        ml::num(fighter_spawn_cooldowns),
        ml::num(teams),
        ml::num(healths),
        ml::num(capital_fighter_handle_spans),
        ml::num(target_handles),
    });
}

auto EntityData::slice(int32 const offset, int32 const count) -> View {
    return get_view(offset, count);
}

auto EntityData::left(int32 const count) -> View {
    return slice(0, count);
}

auto EntityData::right(int32 const count) -> View {
    return slice(num() - count, count);
}

auto EntityData::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto EntityData::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto EntityData::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}

auto FighterReassignmentConstView::get_view() const -> ConstView {
    return get_view(0, num());
}

auto FighterReassignmentConstView::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<FRegistryEntityHandle>{capital_handles}.Slice(offset, count),
        TConstArrayView<FRegistryEntityHandle>{fighter_handles}.Slice(offset, count),
    };
}

auto FighterReassignmentConstView::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto FighterReassignmentConstView::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<FRegistryEntityHandle>{capital_handles}.Slice(offset, count),
        TConstArrayView<FRegistryEntityHandle>{fighter_handles}.Slice(offset, count),
    };
}

auto FighterReassignmentConstView::num() const noexcept -> int32 {
    return ml::num(capital_handles);
}

auto FighterReassignmentConstView::is_empty() const noexcept -> bool {
    return num() == 0;
}

void FighterReassignmentConstView::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(capital_handles),
        ml::num(fighter_handles),
    });
}

auto FighterReassignmentConstView::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto FighterReassignmentConstView::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto FighterReassignmentConstView::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}

auto FighterReassignmentView::get_view() -> View {
    return get_view(0, num());
}

auto FighterReassignmentView::get_view(int32 const offset, int32 const count) -> View {
    return View{
        TArrayView<FRegistryEntityHandle>{capital_handles}.Slice(offset, count),
        TArrayView<FRegistryEntityHandle>{fighter_handles}.Slice(offset, count),
    };
}

auto FighterReassignmentView::get_view() const -> ConstView {
    return get_view(0, num());
}

auto FighterReassignmentView::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<FRegistryEntityHandle>{capital_handles}.Slice(offset, count),
        TConstArrayView<FRegistryEntityHandle>{fighter_handles}.Slice(offset, count),
    };
}

auto FighterReassignmentView::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto FighterReassignmentView::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<FRegistryEntityHandle>{capital_handles}.Slice(offset, count),
        TConstArrayView<FRegistryEntityHandle>{fighter_handles}.Slice(offset, count),
    };
}

auto FighterReassignmentView::num() const noexcept -> int32 {
    return ml::num(capital_handles);
}

auto FighterReassignmentView::is_empty() const noexcept -> bool {
    return num() == 0;
}

void FighterReassignmentView::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(capital_handles),
        ml::num(fighter_handles),
    });
}

auto FighterReassignmentView::slice(int32 const offset, int32 const count) -> View {
    return get_view(offset, count);
}

auto FighterReassignmentView::left(int32 const count) -> View {
    return slice(0, count);
}

auto FighterReassignmentView::right(int32 const count) -> View {
    return slice(num() - count, count);
}

auto FighterReassignmentView::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto FighterReassignmentView::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto FighterReassignmentView::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}

void FighterReassignment::reset() {
    ml::reset(capital_handles);
    ml::reset(fighter_handles);
}

void FighterReassignment::reserve(int32 const count) {
    ml::reserve(capital_handles, count);
    ml::reserve(fighter_handles, count);
}

void FighterReassignment::add_uninitialised(int32 const count) {
    ml::add_uninitialised(capital_handles, count);
    ml::add_uninitialised(fighter_handles, count);
}

void FighterReassignment::add_defaulted(int32 const count) {
    ml::add_defaulted(capital_handles, count);
    ml::add_defaulted(fighter_handles, count);
}

void FighterReassignment::set_num(int32 const count, EAllowShrinking const allow_shrinking) {
    ml::set_num(capital_handles, count, allow_shrinking);
    ml::set_num(fighter_handles, count, allow_shrinking);
}

void FighterReassignment::apply_permutation(TArrayView<int32> indices) {
    validate_array_sizes();
    check(indices.Num() == num());
    ml::apply_permutation(capital_handles, indices);
    ml::apply_permutation(fighter_handles, indices);
}

auto FighterReassignment::get_view() -> View {
    return get_view(0, num());
}

auto FighterReassignment::get_view(int32 const offset, int32 const count) -> View {
    return View{
        TArrayView<FRegistryEntityHandle>{capital_handles}.Slice(offset, count),
        TArrayView<FRegistryEntityHandle>{fighter_handles}.Slice(offset, count),
    };
}

auto FighterReassignment::get_view() const -> ConstView {
    return get_view(0, num());
}

auto FighterReassignment::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<FRegistryEntityHandle>{capital_handles}.Slice(offset, count),
        TConstArrayView<FRegistryEntityHandle>{fighter_handles}.Slice(offset, count),
    };
}

auto FighterReassignment::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto FighterReassignment::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<FRegistryEntityHandle>{capital_handles}.Slice(offset, count),
        TConstArrayView<FRegistryEntityHandle>{fighter_handles}.Slice(offset, count),
    };
}

auto FighterReassignment::num() const noexcept -> int32 {
    return ml::num(capital_handles);
}

auto FighterReassignment::is_empty() const noexcept -> bool {
    return num() == 0;
}

void FighterReassignment::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(capital_handles),
        ml::num(fighter_handles),
    });
}

auto FighterReassignment::slice(int32 const offset, int32 const count) -> View {
    return get_view(offset, count);
}

auto FighterReassignment::left(int32 const count) -> View {
    return slice(0, count);
}

auto FighterReassignment::right(int32 const count) -> View {
    return slice(num() - count, count);
}

auto FighterReassignment::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto FighterReassignment::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto FighterReassignment::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}
} // namespace ml::test_capital_ships
// clang-format on
