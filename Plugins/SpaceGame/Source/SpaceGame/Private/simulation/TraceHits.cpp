// clang-format off
// This file is autogenerated. Do not edit by hand.
// Edit Codegen/manifests and regenerate it instead.

#include "SpaceGame/simulation/TraceHits.h"

#include "SandboxCore/array_checks.h"
#include "SandboxCore/container_ops.h"
#include "SandboxCore/soa_permutation.h"

#include "Containers/AllowShrinking.h"
#include "Containers/ArrayView.h"
#include "CoreMinimal.h"

namespace ml {
auto FTraceHitsConstView::get_view() const -> ConstView {
    return get_view(0, num());
}

auto FTraceHitsConstView::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        locations.get_const_view(offset, count),
        TConstArrayView<FRegistryEntityHandle>{entities}.Slice(offset, count),
        TConstArrayView<uint8>{hits}.Slice(offset, count),
    };
}

auto FTraceHitsConstView::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto FTraceHitsConstView::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        locations.get_const_view(offset, count),
        TConstArrayView<FRegistryEntityHandle>{entities}.Slice(offset, count),
        TConstArrayView<uint8>{hits}.Slice(offset, count),
    };
}

auto FTraceHitsConstView::num() const noexcept -> int32 {
    return ml::num(locations);
}

auto FTraceHitsConstView::is_empty() const noexcept -> bool {
    return num() == 0;
}

void FTraceHitsConstView::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(locations),
        ml::num(entities),
        ml::num(hits),
    });
}

auto FTraceHitsConstView::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto FTraceHitsConstView::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto FTraceHitsConstView::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}

auto FTraceHitsView::get_view() -> View {
    return get_view(0, num());
}

auto FTraceHitsView::get_view(int32 const offset, int32 const count) -> View {
    return View{
        locations.get_view(offset, count),
        TArrayView<FRegistryEntityHandle>{entities}.Slice(offset, count),
        TArrayView<uint8>{hits}.Slice(offset, count),
    };
}

auto FTraceHitsView::get_view() const -> ConstView {
    return get_view(0, num());
}

auto FTraceHitsView::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        locations.get_const_view(offset, count),
        TConstArrayView<FRegistryEntityHandle>{entities}.Slice(offset, count),
        TConstArrayView<uint8>{hits}.Slice(offset, count),
    };
}

auto FTraceHitsView::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto FTraceHitsView::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        locations.get_const_view(offset, count),
        TConstArrayView<FRegistryEntityHandle>{entities}.Slice(offset, count),
        TConstArrayView<uint8>{hits}.Slice(offset, count),
    };
}

auto FTraceHitsView::num() const noexcept -> int32 {
    return ml::num(locations);
}

auto FTraceHitsView::is_empty() const noexcept -> bool {
    return num() == 0;
}

void FTraceHitsView::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(locations),
        ml::num(entities),
        ml::num(hits),
    });
}

auto FTraceHitsView::slice(int32 const offset, int32 const count) -> View {
    return get_view(offset, count);
}

auto FTraceHitsView::left(int32 const count) -> View {
    return slice(0, count);
}

auto FTraceHitsView::right(int32 const count) -> View {
    return slice(num() - count, count);
}

auto FTraceHitsView::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto FTraceHitsView::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto FTraceHitsView::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}

void FTraceHits::reset() {
    ml::reset(locations);
    ml::reset(entities);
    ml::reset(hits);
}

void FTraceHits::reserve(int32 const count) {
    ml::reserve(locations, count);
    ml::reserve(entities, count);
    ml::reserve(hits, count);
}

void FTraceHits::add_uninitialised(int32 const count) {
    ml::add_uninitialised(locations, count);
    ml::add_uninitialised(entities, count);
    ml::add_uninitialised(hits, count);
}

void FTraceHits::add_defaulted(int32 const count) {
    ml::add_defaulted(locations, count);
    ml::add_defaulted(entities, count);
    ml::add_defaulted(hits, count);
}

void FTraceHits::set_num(int32 const count, EAllowShrinking const allow_shrinking) {
    ml::set_num(locations, count, allow_shrinking);
    ml::set_num(entities, count, allow_shrinking);
    ml::set_num(hits, count, allow_shrinking);
}

void FTraceHits::apply_permutation(TArrayView<int32> indices) {
    validate_array_sizes();
    check(indices.Num() == num());
    ml::apply_permutation(locations, indices);
    ml::apply_permutation(entities, indices);
    ml::apply_permutation(hits, indices);
}

auto FTraceHits::get_view() -> View {
    return get_view(0, num());
}

auto FTraceHits::get_view(int32 const offset, int32 const count) -> View {
    return View{
        locations.get_view(offset, count),
        TArrayView<FRegistryEntityHandle>{entities}.Slice(offset, count),
        TArrayView<uint8>{hits}.Slice(offset, count),
    };
}

auto FTraceHits::get_view() const -> ConstView {
    return get_view(0, num());
}

auto FTraceHits::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        locations.get_const_view(offset, count),
        TConstArrayView<FRegistryEntityHandle>{entities}.Slice(offset, count),
        TConstArrayView<uint8>{hits}.Slice(offset, count),
    };
}

auto FTraceHits::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto FTraceHits::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        locations.get_const_view(offset, count),
        TConstArrayView<FRegistryEntityHandle>{entities}.Slice(offset, count),
        TConstArrayView<uint8>{hits}.Slice(offset, count),
    };
}

auto FTraceHits::num() const noexcept -> int32 {
    return ml::num(locations);
}

auto FTraceHits::is_empty() const noexcept -> bool {
    return num() == 0;
}

void FTraceHits::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(locations),
        ml::num(entities),
        ml::num(hits),
    });
}

auto FTraceHits::slice(int32 const offset, int32 const count) -> View {
    return get_view(offset, count);
}

auto FTraceHits::left(int32 const count) -> View {
    return slice(0, count);
}

auto FTraceHits::right(int32 const count) -> View {
    return slice(num() - count, count);
}

auto FTraceHits::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto FTraceHits::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto FTraceHits::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}
} // namespace ml
// clang-format on
