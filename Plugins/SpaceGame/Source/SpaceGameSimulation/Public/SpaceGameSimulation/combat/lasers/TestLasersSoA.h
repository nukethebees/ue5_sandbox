// clang-format off
// This file is autogenerated. Do not edit by hand.
// Edit lispb/schema and regenerate it instead.

#pragma once

#include "SandboxCore/array_utils.h"
#include "SandboxCore/container_ops.h"
#include "SandboxCore/soa_concepts.h"
#include "SandboxCore/soa_rotators.h"
#include "SandboxCore/soa_vectors_3f.h"

#include "Containers/AllowShrinking.h"
#include "Containers/Array.h"
#include "Containers/ArrayView.h"
#include "CoreMinimal.h"
#include "SandboxNative/RegistryEntityHandle.h"
#include "sandbox/simulation/laser_source.h"

#include <utility>

namespace ml::test_lasers {
struct SpawnRequestsView;
struct SpawnRequestsConstView;

struct SPACEGAMESIMULATION_API SpawnRequestsConstView {
    using View = SpawnRequestsView;
    using ConstView = SpawnRequestsConstView;

    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(
            self.locations,
            self.rotations,
            self.base_velocities,
            self.damages,
            self.speeds,
            self.max_distances,
            self.instigator_handles,
            self.sources
        );
    }

    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    FVectors3f::ConstView locations;
    FRotatorsf::ConstView rotations;
    FVectors3f::ConstView base_velocities;
    TConstArrayView<int32> damages;
    TConstArrayView<float> speeds;
    TConstArrayView<float> max_distances;
    TConstArrayView<FRegistryEntityHandle> instigator_handles;
    TConstArrayView<ml::simulation::LaserSource> sources;
};

struct SPACEGAMESIMULATION_API SpawnRequestsView {
    using View = SpawnRequestsView;
    using ConstView = SpawnRequestsConstView;

    void set(int32 const index, FVectors3f::equivalent_type const new_locations, FRotatorsf::equivalent_type const new_rotations, FVectors3f::equivalent_type const new_base_velocities, int32 const new_damages, float const new_speeds, float const new_max_distances, FRegistryEntityHandle const new_instigator_handles, ml::simulation::LaserSource const new_sources) const {
        locations.set(index, new_locations);
        rotations.set(index, new_rotations);
        base_velocities.set(index, new_base_velocities);
        damages[index] = new_damages;
        speeds[index] = new_speeds;
        max_distances[index] = new_max_distances;
        instigator_handles[index] = new_instigator_handles;
        sources[index] = new_sources;
    }

    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(
            self.locations,
            self.rotations,
            self.base_velocities,
            self.damages,
            self.speeds,
            self.max_distances,
            self.instigator_handles,
            self.sources
        );
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    FVectors3f::View locations;
    FRotatorsf::View rotations;
    FVectors3f::View base_velocities;
    TArrayView<int32> damages;
    TArrayView<float> speeds;
    TArrayView<float> max_distances;
    TArrayView<FRegistryEntityHandle> instigator_handles;
    TArrayView<ml::simulation::LaserSource> sources;
};

struct SPACEGAMESIMULATION_API SpawnRequests {
    using View = SpawnRequestsView;
    using ConstView = SpawnRequestsConstView;

    void set_damages(int32 const value);

    void set_speeds(float const value);

    void set_max_distances(float const value);

    void set_sources(ml::simulation::LaserSource const value);

    void set(int32 const index, FVectors3f::equivalent_type const new_locations, FRotatorsf::equivalent_type const new_rotations, FVectors3f::equivalent_type const new_base_velocities, int32 const new_damages, float const new_speeds, float const new_max_distances, FRegistryEntityHandle const new_instigator_handles, ml::simulation::LaserSource const new_sources) {
        locations.set(index, new_locations);
        rotations.set(index, new_rotations);
        base_velocities.set(index, new_base_velocities);
        damages[index] = new_damages;
        speeds[index] = new_speeds;
        max_distances[index] = new_max_distances;
        instigator_handles[index] = new_instigator_handles;
        sources[index] = new_sources;
    }

    auto add(FVectors3f::equivalent_type const new_locations, FRotatorsf::equivalent_type const new_rotations, FVectors3f::equivalent_type const new_base_velocities, int32 const new_damages, float const new_speeds, float const new_max_distances, FRegistryEntityHandle const new_instigator_handles, ml::simulation::LaserSource const new_sources) -> int32 {
        auto const index{num()};
        locations.add(new_locations);
        rotations.add(new_rotations);
        base_velocities.add(new_base_velocities);
        damages.Add(new_damages);
        speeds.Add(new_speeds);
        max_distances.Add(new_max_distances);
        instigator_handles.Add(new_instigator_handles);
        sources.Add(new_sources);
        return index;
    }

    void reset();

    void reserve(int32 const count);

    void add_uninitialised(int32 const count);

    void add_defaulted(int32 const count);

    void remove_at_swap(int32 const index, int32 const count, EAllowShrinking const allow_shrinking) {
        locations.remove_at_swap(index, count, allow_shrinking);
        rotations.remove_at_swap(index, count, allow_shrinking);
        base_velocities.remove_at_swap(index, count, allow_shrinking);
        damages.RemoveAtSwap(index, count, allow_shrinking);
        speeds.RemoveAtSwap(index, count, allow_shrinking);
        max_distances.RemoveAtSwap(index, count, allow_shrinking);
        instigator_handles.RemoveAtSwap(index, count, allow_shrinking);
        sources.RemoveAtSwap(index, count, allow_shrinking);
    }

    void set_num(int32 const count, EAllowShrinking const allow_shrinking);

    template <typename Other>
    void copy_element(int32 const dst_i, Other const& other, int32 const src_i) {
        ml::copy_element(locations, dst_i, other.locations, src_i);
        ml::copy_element(rotations, dst_i, other.rotations, src_i);
        ml::copy_element(base_velocities, dst_i, other.base_velocities, src_i);
        ml::copy_element(damages, dst_i, other.damages, src_i);
        ml::copy_element(speeds, dst_i, other.speeds, src_i);
        ml::copy_element(max_distances, dst_i, other.max_distances, src_i);
        ml::copy_element(instigator_handles, dst_i, other.instigator_handles, src_i);
        ml::copy_element(sources, dst_i, other.sources, src_i);
    }

    template <typename Other>
    void copy_elements(int32 const dst_i, Other const& other, int32 const src_i, int32 const count) {
        ml::copy_elements(locations, dst_i, other.locations, src_i, count);
        ml::copy_elements(rotations, dst_i, other.rotations, src_i, count);
        ml::copy_elements(base_velocities, dst_i, other.base_velocities, src_i, count);
        ml::copy_elements(damages, dst_i, other.damages, src_i, count);
        ml::copy_elements(speeds, dst_i, other.speeds, src_i, count);
        ml::copy_elements(max_distances, dst_i, other.max_distances, src_i, count);
        ml::copy_elements(instigator_handles, dst_i, other.instigator_handles, src_i, count);
        ml::copy_elements(sources, dst_i, other.sources, src_i, count);
    }

    template <typename Other>
    void copy_to_tail(Other const& other) {
        auto const count{other.num()};
        check(num() >= count);
        copy_elements(num() - count, other, 0, count);
    }

    template <typename Other>
    void append_from(Other const& other)
        requires ml::SupportsApplyArrayPairsWith<SpawnRequests, Other> {
        ml::append_from(locations, other.locations);
        ml::append_from(rotations, other.rotations);
        ml::append_from(base_velocities, other.base_velocities);
        ml::append_from(damages, other.damages);
        ml::append_from(speeds, other.speeds);
        ml::append_from(max_distances, other.max_distances);
        ml::append_from(instigator_handles, other.instigator_handles);
        ml::append_from(sources, other.sources);
    }

    void apply_permutation(TArrayView<int32> indices);

    template <typename Compare>
    void sort(Compare&& compare, TArrayView<int32> scratch_indices) {
        validate_array_sizes();
        auto const n{num()};
        check(scratch_indices.Num() == n);
        ml::fill_indices(scratch_indices);
        // indices[new_index] is the old row index that belongs at new_index.
        scratch_indices.Sort([this, &compare](int32 const lhs, int32 const rhs) {
            return compare(*this, lhs, rhs);
        });
        apply_permutation(scratch_indices);
    }

    template <auto Compare>
    void sort(TArrayView<int32> scratch_indices) {
        validate_array_sizes();
        auto const n{num()};
        check(scratch_indices.Num() == n);
        ml::fill_indices(scratch_indices);
        // indices[new_index] is the old row index that belongs at new_index.
        scratch_indices.Sort([this](int32 const lhs, int32 const rhs) {
            return Compare(*this, lhs, rhs);
        });
        apply_permutation(scratch_indices);
    }

    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(
            self.locations,
            self.rotations,
            self.base_velocities,
            self.damages,
            self.speeds,
            self.max_distances,
            self.instigator_handles,
            self.sources
        );
    }

    template <typename Self, typename Other, typename TFunc>
    auto apply_array_pairs(this Self&& self, Other&& other, TFunc&& func)
        -> decltype(auto) {
        return std::forward<TFunc>(func)(
            self.locations, other.locations,
            self.rotations, other.rotations,
            self.base_velocities, other.base_velocities,
            self.damages, other.damages,
            self.speeds, other.speeds,
            self.max_distances, other.max_distances,
            self.instigator_handles, other.instigator_handles,
            self.sources, other.sources
        );
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    FVectors3f locations;
    FRotatorsf rotations;
    FVectors3f base_velocities;
    TArray<int32> damages;
    TArray<float> speeds;
    TArray<float> max_distances;
    TArray<FRegistryEntityHandle> instigator_handles;
    TArray<ml::simulation::LaserSource> sources;
};

struct EntitiesView;
struct EntitiesConstView;

struct SPACEGAMESIMULATION_API EntitiesConstView {
    using View = EntitiesView;
    using ConstView = EntitiesConstView;

    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(
            self.sources,
            self.locations,
            self.rotations,
            self.velocities,
            self.damages,
            self.lifetimes_remaining,
            self.instigator_handles,
            self.initial_lifetimes,
            self.spawn_times
        );
    }

    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TConstArrayView<ml::simulation::LaserSource> sources;
    FVectors3f::ConstView locations;
    FRotatorsf::ConstView rotations;
    FVectors3f::ConstView velocities;
    TConstArrayView<int32> damages;
    TConstArrayView<float> lifetimes_remaining;
    TConstArrayView<FRegistryEntityHandle> instigator_handles;
    TConstArrayView<float> initial_lifetimes;
    TConstArrayView<float> spawn_times;
};

struct SPACEGAMESIMULATION_API EntitiesView {
    using View = EntitiesView;
    using ConstView = EntitiesConstView;

    void set(int32 const index, ml::simulation::LaserSource const new_sources, FVectors3f::equivalent_type const new_locations, FRotatorsf::equivalent_type const new_rotations, FVectors3f::equivalent_type const new_velocities, int32 const new_damages, float const new_lifetimes_remaining, FRegistryEntityHandle const new_instigator_handles, float const new_initial_lifetimes, float const new_spawn_times) const {
        sources[index] = new_sources;
        locations.set(index, new_locations);
        rotations.set(index, new_rotations);
        velocities.set(index, new_velocities);
        damages[index] = new_damages;
        lifetimes_remaining[index] = new_lifetimes_remaining;
        instigator_handles[index] = new_instigator_handles;
        initial_lifetimes[index] = new_initial_lifetimes;
        spawn_times[index] = new_spawn_times;
    }

    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(
            self.sources,
            self.locations,
            self.rotations,
            self.velocities,
            self.damages,
            self.lifetimes_remaining,
            self.instigator_handles,
            self.initial_lifetimes,
            self.spawn_times
        );
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TArrayView<ml::simulation::LaserSource> sources;
    FVectors3f::View locations;
    FRotatorsf::View rotations;
    FVectors3f::View velocities;
    TArrayView<int32> damages;
    TArrayView<float> lifetimes_remaining;
    TArrayView<FRegistryEntityHandle> instigator_handles;
    TArrayView<float> initial_lifetimes;
    TArrayView<float> spawn_times;
};

struct SPACEGAMESIMULATION_API Entities {
    using View = EntitiesView;
    using ConstView = EntitiesConstView;

    void set(int32 const index, ml::simulation::LaserSource const new_sources, FVectors3f::equivalent_type const new_locations, FRotatorsf::equivalent_type const new_rotations, FVectors3f::equivalent_type const new_velocities, int32 const new_damages, float const new_lifetimes_remaining, FRegistryEntityHandle const new_instigator_handles, float const new_initial_lifetimes, float const new_spawn_times) {
        sources[index] = new_sources;
        locations.set(index, new_locations);
        rotations.set(index, new_rotations);
        velocities.set(index, new_velocities);
        damages[index] = new_damages;
        lifetimes_remaining[index] = new_lifetimes_remaining;
        instigator_handles[index] = new_instigator_handles;
        initial_lifetimes[index] = new_initial_lifetimes;
        spawn_times[index] = new_spawn_times;
    }

    auto add(ml::simulation::LaserSource const new_sources, FVectors3f::equivalent_type const new_locations, FRotatorsf::equivalent_type const new_rotations, FVectors3f::equivalent_type const new_velocities, int32 const new_damages, float const new_lifetimes_remaining, FRegistryEntityHandle const new_instigator_handles, float const new_initial_lifetimes, float const new_spawn_times) -> int32 {
        auto const index{num()};
        sources.Add(new_sources);
        locations.add(new_locations);
        rotations.add(new_rotations);
        velocities.add(new_velocities);
        damages.Add(new_damages);
        lifetimes_remaining.Add(new_lifetimes_remaining);
        instigator_handles.Add(new_instigator_handles);
        initial_lifetimes.Add(new_initial_lifetimes);
        spawn_times.Add(new_spawn_times);
        return index;
    }

    void reset();

    void reserve(int32 const count);

    void add_uninitialised(int32 const count);

    void add_defaulted(int32 const count);

    void remove_at_swap(int32 const index, int32 const count, EAllowShrinking const allow_shrinking) {
        sources.RemoveAtSwap(index, count, allow_shrinking);
        locations.remove_at_swap(index, count, allow_shrinking);
        rotations.remove_at_swap(index, count, allow_shrinking);
        velocities.remove_at_swap(index, count, allow_shrinking);
        damages.RemoveAtSwap(index, count, allow_shrinking);
        lifetimes_remaining.RemoveAtSwap(index, count, allow_shrinking);
        instigator_handles.RemoveAtSwap(index, count, allow_shrinking);
        initial_lifetimes.RemoveAtSwap(index, count, allow_shrinking);
        spawn_times.RemoveAtSwap(index, count, allow_shrinking);
    }

    void set_num(int32 const count, EAllowShrinking const allow_shrinking);

    template <typename Other>
    void copy_element(int32 const dst_i, Other const& other, int32 const src_i) {
        ml::copy_element(sources, dst_i, other.sources, src_i);
        ml::copy_element(locations, dst_i, other.locations, src_i);
        ml::copy_element(rotations, dst_i, other.rotations, src_i);
        ml::copy_element(velocities, dst_i, other.velocities, src_i);
        ml::copy_element(damages, dst_i, other.damages, src_i);
        ml::copy_element(lifetimes_remaining, dst_i, other.lifetimes_remaining, src_i);
        ml::copy_element(instigator_handles, dst_i, other.instigator_handles, src_i);
        ml::copy_element(initial_lifetimes, dst_i, other.initial_lifetimes, src_i);
        ml::copy_element(spawn_times, dst_i, other.spawn_times, src_i);
    }

    template <typename Other>
    void copy_elements(int32 const dst_i, Other const& other, int32 const src_i, int32 const count) {
        ml::copy_elements(sources, dst_i, other.sources, src_i, count);
        ml::copy_elements(locations, dst_i, other.locations, src_i, count);
        ml::copy_elements(rotations, dst_i, other.rotations, src_i, count);
        ml::copy_elements(velocities, dst_i, other.velocities, src_i, count);
        ml::copy_elements(damages, dst_i, other.damages, src_i, count);
        ml::copy_elements(lifetimes_remaining, dst_i, other.lifetimes_remaining, src_i, count);
        ml::copy_elements(instigator_handles, dst_i, other.instigator_handles, src_i, count);
        ml::copy_elements(initial_lifetimes, dst_i, other.initial_lifetimes, src_i, count);
        ml::copy_elements(spawn_times, dst_i, other.spawn_times, src_i, count);
    }

    template <typename Other>
    void copy_to_tail(Other const& other) {
        auto const count{other.num()};
        check(num() >= count);
        copy_elements(num() - count, other, 0, count);
    }

    template <typename Other>
    void append_from(Other const& other)
        requires ml::SupportsApplyArrayPairsWith<Entities, Other> {
        ml::append_from(sources, other.sources);
        ml::append_from(locations, other.locations);
        ml::append_from(rotations, other.rotations);
        ml::append_from(velocities, other.velocities);
        ml::append_from(damages, other.damages);
        ml::append_from(lifetimes_remaining, other.lifetimes_remaining);
        ml::append_from(instigator_handles, other.instigator_handles);
        ml::append_from(initial_lifetimes, other.initial_lifetimes);
        ml::append_from(spawn_times, other.spawn_times);
    }

    void apply_permutation(TArrayView<int32> indices);

    template <typename Compare>
    void sort(Compare&& compare, TArrayView<int32> scratch_indices) {
        validate_array_sizes();
        auto const n{num()};
        check(scratch_indices.Num() == n);
        ml::fill_indices(scratch_indices);
        // indices[new_index] is the old row index that belongs at new_index.
        scratch_indices.Sort([this, &compare](int32 const lhs, int32 const rhs) {
            return compare(*this, lhs, rhs);
        });
        apply_permutation(scratch_indices);
    }

    template <auto Compare>
    void sort(TArrayView<int32> scratch_indices) {
        validate_array_sizes();
        auto const n{num()};
        check(scratch_indices.Num() == n);
        ml::fill_indices(scratch_indices);
        // indices[new_index] is the old row index that belongs at new_index.
        scratch_indices.Sort([this](int32 const lhs, int32 const rhs) {
            return Compare(*this, lhs, rhs);
        });
        apply_permutation(scratch_indices);
    }

    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(
            self.sources,
            self.locations,
            self.rotations,
            self.velocities,
            self.damages,
            self.lifetimes_remaining,
            self.instigator_handles,
            self.initial_lifetimes,
            self.spawn_times
        );
    }

    template <typename Self, typename Other, typename TFunc>
    auto apply_array_pairs(this Self&& self, Other&& other, TFunc&& func)
        -> decltype(auto) {
        return std::forward<TFunc>(func)(
            self.sources, other.sources,
            self.locations, other.locations,
            self.rotations, other.rotations,
            self.velocities, other.velocities,
            self.damages, other.damages,
            self.lifetimes_remaining, other.lifetimes_remaining,
            self.instigator_handles, other.instigator_handles,
            self.initial_lifetimes, other.initial_lifetimes,
            self.spawn_times, other.spawn_times
        );
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    TArray<ml::simulation::LaserSource> sources;
    FVectors3f locations;
    FRotatorsf rotations;
    FVectors3f velocities;
    TArray<int32> damages;
    TArray<float> lifetimes_remaining;
    TArray<FRegistryEntityHandle> instigator_handles;
    TArray<float> initial_lifetimes;
    TArray<float> spawn_times;
};

struct HitDetailsView;
struct HitDetailsConstView;

struct SPACEGAMESIMULATION_API HitDetailsConstView {
    using View = HitDetailsView;
    using ConstView = HitDetailsConstView;

    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(
            self.locations,
            self.emission_directions,
            self.sources
        );
    }

    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    FVectors3f::ConstView locations;
    FVectors3f::ConstView emission_directions;
    TConstArrayView<ml::simulation::LaserSource> sources;
};

struct SPACEGAMESIMULATION_API HitDetailsView {
    using View = HitDetailsView;
    using ConstView = HitDetailsConstView;

    void set(int32 const index, FVectors3f::equivalent_type const new_locations, FVectors3f::equivalent_type const new_emission_directions, ml::simulation::LaserSource const new_sources) const {
        locations.set(index, new_locations);
        emission_directions.set(index, new_emission_directions);
        sources[index] = new_sources;
    }

    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(
            self.locations,
            self.emission_directions,
            self.sources
        );
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    FVectors3f::View locations;
    FVectors3f::View emission_directions;
    TArrayView<ml::simulation::LaserSource> sources;
};

struct SPACEGAMESIMULATION_API HitDetails {
    using View = HitDetailsView;
    using ConstView = HitDetailsConstView;

    void set(int32 const index, FVectors3f::equivalent_type const new_locations, FVectors3f::equivalent_type const new_emission_directions, ml::simulation::LaserSource const new_sources) {
        locations.set(index, new_locations);
        emission_directions.set(index, new_emission_directions);
        sources[index] = new_sources;
    }

    auto add(FVectors3f::equivalent_type const new_locations, FVectors3f::equivalent_type const new_emission_directions, ml::simulation::LaserSource const new_sources) -> int32 {
        auto const index{num()};
        locations.add(new_locations);
        emission_directions.add(new_emission_directions);
        sources.Add(new_sources);
        return index;
    }

    void reset();

    void reserve(int32 const count);

    void add_uninitialised(int32 const count);

    void add_defaulted(int32 const count);

    void remove_at_swap(int32 const index, int32 const count, EAllowShrinking const allow_shrinking) {
        locations.remove_at_swap(index, count, allow_shrinking);
        emission_directions.remove_at_swap(index, count, allow_shrinking);
        sources.RemoveAtSwap(index, count, allow_shrinking);
    }

    void set_num(int32 const count, EAllowShrinking const allow_shrinking);

    template <typename Other>
    void copy_element(int32 const dst_i, Other const& other, int32 const src_i) {
        ml::copy_element(locations, dst_i, other.locations, src_i);
        ml::copy_element(emission_directions, dst_i, other.emission_directions, src_i);
        ml::copy_element(sources, dst_i, other.sources, src_i);
    }

    template <typename Other>
    void copy_elements(int32 const dst_i, Other const& other, int32 const src_i, int32 const count) {
        ml::copy_elements(locations, dst_i, other.locations, src_i, count);
        ml::copy_elements(emission_directions, dst_i, other.emission_directions, src_i, count);
        ml::copy_elements(sources, dst_i, other.sources, src_i, count);
    }

    template <typename Other>
    void copy_to_tail(Other const& other) {
        auto const count{other.num()};
        check(num() >= count);
        copy_elements(num() - count, other, 0, count);
    }

    template <typename Other>
    void append_from(Other const& other)
        requires ml::SupportsApplyArrayPairsWith<HitDetails, Other> {
        ml::append_from(locations, other.locations);
        ml::append_from(emission_directions, other.emission_directions);
        ml::append_from(sources, other.sources);
    }

    void apply_permutation(TArrayView<int32> indices);

    template <typename Compare>
    void sort(Compare&& compare, TArrayView<int32> scratch_indices) {
        validate_array_sizes();
        auto const n{num()};
        check(scratch_indices.Num() == n);
        ml::fill_indices(scratch_indices);
        // indices[new_index] is the old row index that belongs at new_index.
        scratch_indices.Sort([this, &compare](int32 const lhs, int32 const rhs) {
            return compare(*this, lhs, rhs);
        });
        apply_permutation(scratch_indices);
    }

    template <auto Compare>
    void sort(TArrayView<int32> scratch_indices) {
        validate_array_sizes();
        auto const n{num()};
        check(scratch_indices.Num() == n);
        ml::fill_indices(scratch_indices);
        // indices[new_index] is the old row index that belongs at new_index.
        scratch_indices.Sort([this](int32 const lhs, int32 const rhs) {
            return Compare(*this, lhs, rhs);
        });
        apply_permutation(scratch_indices);
    }

    template <typename TFunc>
    auto apply_arrays(this auto&& self, TFunc&& func) -> decltype(auto) {
        return std::forward<TFunc>(func)(
            self.locations,
            self.emission_directions,
            self.sources
        );
    }

    template <typename Self, typename Other, typename TFunc>
    auto apply_array_pairs(this Self&& self, Other&& other, TFunc&& func)
        -> decltype(auto) {
        return std::forward<TFunc>(func)(
            self.locations, other.locations,
            self.emission_directions, other.emission_directions,
            self.sources, other.sources
        );
    }

    auto get_view() -> View;
    auto get_view(int32 const offset, int32 const count) -> View;
    auto get_view() const -> ConstView;
    auto get_view(int32 const offset, int32 const count) const -> ConstView;
    auto get_const_view() const -> ConstView;
    auto get_const_view(int32 const offset, int32 const count) const -> ConstView;
    auto num() const noexcept -> int32;
    auto is_empty() const noexcept -> bool;
    void validate_array_sizes() const;
    auto slice(int32 const offset, int32 const count) -> View;
    auto left(int32 const count) -> View;
    auto right(int32 const count) -> View;
    auto slice(int32 const offset, int32 const count) const -> ConstView;
    auto left(int32 const count) const -> ConstView;
    auto right(int32 const count) const -> ConstView;

    FVectors3f locations;
    FVectors3f emission_directions;
    TArray<ml::simulation::LaserSource> sources;
};
} // namespace ml::test_lasers
// clang-format on
