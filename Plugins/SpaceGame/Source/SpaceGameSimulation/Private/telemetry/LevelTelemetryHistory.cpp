// This file is autogenerated. Do not edit by hand.
// Edit lispb/schema and regenerate it instead.

#include "SpaceGameSimulation/telemetry/LevelTelemetryHistory.h"

#include "SandboxCore/array_checks.h"
#include "SandboxCore/container_ops.h"
#include "SandboxCore/soa_permutation.h"

#include "CoreMinimal.h"
#include "Containers/AllowShrinking.h"
#include "Containers/ArrayView.h"

namespace ml::level_telemetry {
auto FHistoryRowsConstView::get_view() const -> ConstView {
    return get_view(0, num());
}

auto FHistoryRowsConstView::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<uint64>{completed_ticks}.Slice(offset, count),
        TConstArrayView<uint64>{validity_masks}.Slice(offset, count),
        TConstArrayView<int32>{active_entities}.Slice(offset, count),
        TConstArrayView<FTestEntityRegistry::EntityTypeCounts>{active_entities_by_type}.Slice(
            offset, count),
        TConstArrayView<FTestEntityRegistry::EntityCounts>{active_entities_by_team_and_type}.Slice(
            offset, count),
        TConstArrayView<int32>{spawned_entities}.Slice(offset, count),
        TConstArrayView<int32>{destroyed_entities}.Slice(offset, count),
        TConstArrayView<int32>{kills}.Slice(offset, count),
        TConstArrayView<int32>{registry_slot_count}.Slice(offset, count),
        TConstArrayView<int32>{active_lasers}.Slice(offset, count),
        TConstArrayView<int32>{lasers_fired}.Slice(offset, count),
        TConstArrayView<int32>{occupied_spatial_cell_count}.Slice(offset, count),
        TConstArrayView<uint64>{grid_rebuild_count}.Slice(offset, count),
        TConstArrayView<uint64>{range_query_count}.Slice(offset, count),
        TConstArrayView<uint64>{line_trace_count}.Slice(offset, count),
        TConstArrayView<uint64>{sweep_trace_count}.Slice(offset, count),
        TConstArrayView<double>{requested_time_scale}.Slice(offset, count),
    };
}

auto FHistoryRowsConstView::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto FHistoryRowsConstView::get_const_view(int32 const offset, int32 const count) const
    -> ConstView {
    return ConstView{
        TConstArrayView<uint64>{completed_ticks}.Slice(offset, count),
        TConstArrayView<uint64>{validity_masks}.Slice(offset, count),
        TConstArrayView<int32>{active_entities}.Slice(offset, count),
        TConstArrayView<FTestEntityRegistry::EntityTypeCounts>{active_entities_by_type}.Slice(
            offset, count),
        TConstArrayView<FTestEntityRegistry::EntityCounts>{active_entities_by_team_and_type}.Slice(
            offset, count),
        TConstArrayView<int32>{spawned_entities}.Slice(offset, count),
        TConstArrayView<int32>{destroyed_entities}.Slice(offset, count),
        TConstArrayView<int32>{kills}.Slice(offset, count),
        TConstArrayView<int32>{registry_slot_count}.Slice(offset, count),
        TConstArrayView<int32>{active_lasers}.Slice(offset, count),
        TConstArrayView<int32>{lasers_fired}.Slice(offset, count),
        TConstArrayView<int32>{occupied_spatial_cell_count}.Slice(offset, count),
        TConstArrayView<uint64>{grid_rebuild_count}.Slice(offset, count),
        TConstArrayView<uint64>{range_query_count}.Slice(offset, count),
        TConstArrayView<uint64>{line_trace_count}.Slice(offset, count),
        TConstArrayView<uint64>{sweep_trace_count}.Slice(offset, count),
        TConstArrayView<double>{requested_time_scale}.Slice(offset, count),
    };
}

auto FHistoryRowsConstView::num() const noexcept -> int32 {
    return ml::num(completed_ticks);
}

auto FHistoryRowsConstView::is_empty() const noexcept -> bool {
    return num() == 0;
}

void FHistoryRowsConstView::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(completed_ticks),
        ml::num(validity_masks),
        ml::num(active_entities),
        ml::num(active_entities_by_type),
        ml::num(active_entities_by_team_and_type),
        ml::num(spawned_entities),
        ml::num(destroyed_entities),
        ml::num(kills),
        ml::num(registry_slot_count),
        ml::num(active_lasers),
        ml::num(lasers_fired),
        ml::num(occupied_spatial_cell_count),
        ml::num(grid_rebuild_count),
        ml::num(range_query_count),
        ml::num(line_trace_count),
        ml::num(sweep_trace_count),
        ml::num(requested_time_scale),
    });
}

auto FHistoryRowsConstView::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto FHistoryRowsConstView::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto FHistoryRowsConstView::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}

auto FHistoryRowsView::get_view() -> View {
    return get_view(0, num());
}

auto FHistoryRowsView::get_view(int32 const offset, int32 const count) -> View {
    return View{
        TArrayView<uint64>{completed_ticks}.Slice(offset, count),
        TArrayView<uint64>{validity_masks}.Slice(offset, count),
        TArrayView<int32>{active_entities}.Slice(offset, count),
        TArrayView<FTestEntityRegistry::EntityTypeCounts>{active_entities_by_type}.Slice(offset,
                                                                                         count),
        TArrayView<FTestEntityRegistry::EntityCounts>{active_entities_by_team_and_type}.Slice(
            offset, count),
        TArrayView<int32>{spawned_entities}.Slice(offset, count),
        TArrayView<int32>{destroyed_entities}.Slice(offset, count),
        TArrayView<int32>{kills}.Slice(offset, count),
        TArrayView<int32>{registry_slot_count}.Slice(offset, count),
        TArrayView<int32>{active_lasers}.Slice(offset, count),
        TArrayView<int32>{lasers_fired}.Slice(offset, count),
        TArrayView<int32>{occupied_spatial_cell_count}.Slice(offset, count),
        TArrayView<uint64>{grid_rebuild_count}.Slice(offset, count),
        TArrayView<uint64>{range_query_count}.Slice(offset, count),
        TArrayView<uint64>{line_trace_count}.Slice(offset, count),
        TArrayView<uint64>{sweep_trace_count}.Slice(offset, count),
        TArrayView<double>{requested_time_scale}.Slice(offset, count),
    };
}

auto FHistoryRowsView::get_view() const -> ConstView {
    return get_view(0, num());
}

auto FHistoryRowsView::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<uint64>{completed_ticks}.Slice(offset, count),
        TConstArrayView<uint64>{validity_masks}.Slice(offset, count),
        TConstArrayView<int32>{active_entities}.Slice(offset, count),
        TConstArrayView<FTestEntityRegistry::EntityTypeCounts>{active_entities_by_type}.Slice(
            offset, count),
        TConstArrayView<FTestEntityRegistry::EntityCounts>{active_entities_by_team_and_type}.Slice(
            offset, count),
        TConstArrayView<int32>{spawned_entities}.Slice(offset, count),
        TConstArrayView<int32>{destroyed_entities}.Slice(offset, count),
        TConstArrayView<int32>{kills}.Slice(offset, count),
        TConstArrayView<int32>{registry_slot_count}.Slice(offset, count),
        TConstArrayView<int32>{active_lasers}.Slice(offset, count),
        TConstArrayView<int32>{lasers_fired}.Slice(offset, count),
        TConstArrayView<int32>{occupied_spatial_cell_count}.Slice(offset, count),
        TConstArrayView<uint64>{grid_rebuild_count}.Slice(offset, count),
        TConstArrayView<uint64>{range_query_count}.Slice(offset, count),
        TConstArrayView<uint64>{line_trace_count}.Slice(offset, count),
        TConstArrayView<uint64>{sweep_trace_count}.Slice(offset, count),
        TConstArrayView<double>{requested_time_scale}.Slice(offset, count),
    };
}

auto FHistoryRowsView::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto FHistoryRowsView::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<uint64>{completed_ticks}.Slice(offset, count),
        TConstArrayView<uint64>{validity_masks}.Slice(offset, count),
        TConstArrayView<int32>{active_entities}.Slice(offset, count),
        TConstArrayView<FTestEntityRegistry::EntityTypeCounts>{active_entities_by_type}.Slice(
            offset, count),
        TConstArrayView<FTestEntityRegistry::EntityCounts>{active_entities_by_team_and_type}.Slice(
            offset, count),
        TConstArrayView<int32>{spawned_entities}.Slice(offset, count),
        TConstArrayView<int32>{destroyed_entities}.Slice(offset, count),
        TConstArrayView<int32>{kills}.Slice(offset, count),
        TConstArrayView<int32>{registry_slot_count}.Slice(offset, count),
        TConstArrayView<int32>{active_lasers}.Slice(offset, count),
        TConstArrayView<int32>{lasers_fired}.Slice(offset, count),
        TConstArrayView<int32>{occupied_spatial_cell_count}.Slice(offset, count),
        TConstArrayView<uint64>{grid_rebuild_count}.Slice(offset, count),
        TConstArrayView<uint64>{range_query_count}.Slice(offset, count),
        TConstArrayView<uint64>{line_trace_count}.Slice(offset, count),
        TConstArrayView<uint64>{sweep_trace_count}.Slice(offset, count),
        TConstArrayView<double>{requested_time_scale}.Slice(offset, count),
    };
}

auto FHistoryRowsView::num() const noexcept -> int32 {
    return ml::num(completed_ticks);
}

auto FHistoryRowsView::is_empty() const noexcept -> bool {
    return num() == 0;
}

void FHistoryRowsView::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(completed_ticks),
        ml::num(validity_masks),
        ml::num(active_entities),
        ml::num(active_entities_by_type),
        ml::num(active_entities_by_team_and_type),
        ml::num(spawned_entities),
        ml::num(destroyed_entities),
        ml::num(kills),
        ml::num(registry_slot_count),
        ml::num(active_lasers),
        ml::num(lasers_fired),
        ml::num(occupied_spatial_cell_count),
        ml::num(grid_rebuild_count),
        ml::num(range_query_count),
        ml::num(line_trace_count),
        ml::num(sweep_trace_count),
        ml::num(requested_time_scale),
    });
}

auto FHistoryRowsView::slice(int32 const offset, int32 const count) -> View {
    return get_view(offset, count);
}

auto FHistoryRowsView::left(int32 const count) -> View {
    return slice(0, count);
}

auto FHistoryRowsView::right(int32 const count) -> View {
    return slice(num() - count, count);
}

auto FHistoryRowsView::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto FHistoryRowsView::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto FHistoryRowsView::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}

void FHistoryRows::reset() {
    ml::reset(completed_ticks);
    ml::reset(validity_masks);
    ml::reset(active_entities);
    ml::reset(active_entities_by_type);
    ml::reset(active_entities_by_team_and_type);
    ml::reset(spawned_entities);
    ml::reset(destroyed_entities);
    ml::reset(kills);
    ml::reset(registry_slot_count);
    ml::reset(active_lasers);
    ml::reset(lasers_fired);
    ml::reset(occupied_spatial_cell_count);
    ml::reset(grid_rebuild_count);
    ml::reset(range_query_count);
    ml::reset(line_trace_count);
    ml::reset(sweep_trace_count);
    ml::reset(requested_time_scale);
}

void FHistoryRows::reserve(int32 const count) {
    ml::reserve(completed_ticks, count);
    ml::reserve(validity_masks, count);
    ml::reserve(active_entities, count);
    ml::reserve(active_entities_by_type, count);
    ml::reserve(active_entities_by_team_and_type, count);
    ml::reserve(spawned_entities, count);
    ml::reserve(destroyed_entities, count);
    ml::reserve(kills, count);
    ml::reserve(registry_slot_count, count);
    ml::reserve(active_lasers, count);
    ml::reserve(lasers_fired, count);
    ml::reserve(occupied_spatial_cell_count, count);
    ml::reserve(grid_rebuild_count, count);
    ml::reserve(range_query_count, count);
    ml::reserve(line_trace_count, count);
    ml::reserve(sweep_trace_count, count);
    ml::reserve(requested_time_scale, count);
}

void FHistoryRows::add_uninitialised(int32 const count) {
    ml::add_uninitialised(completed_ticks, count);
    ml::add_uninitialised(validity_masks, count);
    ml::add_uninitialised(active_entities, count);
    ml::add_uninitialised(active_entities_by_type, count);
    ml::add_uninitialised(active_entities_by_team_and_type, count);
    ml::add_uninitialised(spawned_entities, count);
    ml::add_uninitialised(destroyed_entities, count);
    ml::add_uninitialised(kills, count);
    ml::add_uninitialised(registry_slot_count, count);
    ml::add_uninitialised(active_lasers, count);
    ml::add_uninitialised(lasers_fired, count);
    ml::add_uninitialised(occupied_spatial_cell_count, count);
    ml::add_uninitialised(grid_rebuild_count, count);
    ml::add_uninitialised(range_query_count, count);
    ml::add_uninitialised(line_trace_count, count);
    ml::add_uninitialised(sweep_trace_count, count);
    ml::add_uninitialised(requested_time_scale, count);
}

void FHistoryRows::add_defaulted(int32 const count) {
    ml::add_defaulted(completed_ticks, count);
    ml::add_defaulted(validity_masks, count);
    ml::add_defaulted(active_entities, count);
    ml::add_defaulted(active_entities_by_type, count);
    ml::add_defaulted(active_entities_by_team_and_type, count);
    ml::add_defaulted(spawned_entities, count);
    ml::add_defaulted(destroyed_entities, count);
    ml::add_defaulted(kills, count);
    ml::add_defaulted(registry_slot_count, count);
    ml::add_defaulted(active_lasers, count);
    ml::add_defaulted(lasers_fired, count);
    ml::add_defaulted(occupied_spatial_cell_count, count);
    ml::add_defaulted(grid_rebuild_count, count);
    ml::add_defaulted(range_query_count, count);
    ml::add_defaulted(line_trace_count, count);
    ml::add_defaulted(sweep_trace_count, count);
    ml::add_defaulted(requested_time_scale, count);
}

void FHistoryRows::set_num(int32 const count, EAllowShrinking const allow_shrinking) {
    ml::set_num(completed_ticks, count, allow_shrinking);
    ml::set_num(validity_masks, count, allow_shrinking);
    ml::set_num(active_entities, count, allow_shrinking);
    ml::set_num(active_entities_by_type, count, allow_shrinking);
    ml::set_num(active_entities_by_team_and_type, count, allow_shrinking);
    ml::set_num(spawned_entities, count, allow_shrinking);
    ml::set_num(destroyed_entities, count, allow_shrinking);
    ml::set_num(kills, count, allow_shrinking);
    ml::set_num(registry_slot_count, count, allow_shrinking);
    ml::set_num(active_lasers, count, allow_shrinking);
    ml::set_num(lasers_fired, count, allow_shrinking);
    ml::set_num(occupied_spatial_cell_count, count, allow_shrinking);
    ml::set_num(grid_rebuild_count, count, allow_shrinking);
    ml::set_num(range_query_count, count, allow_shrinking);
    ml::set_num(line_trace_count, count, allow_shrinking);
    ml::set_num(sweep_trace_count, count, allow_shrinking);
    ml::set_num(requested_time_scale, count, allow_shrinking);
}

void FHistoryRows::apply_permutation(TArrayView<int32> indices) {
    validate_array_sizes();
    check(indices.Num() == num());
    ml::apply_permutation(completed_ticks, indices);
    ml::apply_permutation(validity_masks, indices);
    ml::apply_permutation(active_entities, indices);
    ml::apply_permutation(active_entities_by_type, indices);
    ml::apply_permutation(active_entities_by_team_and_type, indices);
    ml::apply_permutation(spawned_entities, indices);
    ml::apply_permutation(destroyed_entities, indices);
    ml::apply_permutation(kills, indices);
    ml::apply_permutation(registry_slot_count, indices);
    ml::apply_permutation(active_lasers, indices);
    ml::apply_permutation(lasers_fired, indices);
    ml::apply_permutation(occupied_spatial_cell_count, indices);
    ml::apply_permutation(grid_rebuild_count, indices);
    ml::apply_permutation(range_query_count, indices);
    ml::apply_permutation(line_trace_count, indices);
    ml::apply_permutation(sweep_trace_count, indices);
    ml::apply_permutation(requested_time_scale, indices);
}

auto FHistoryRows::get_view() -> View {
    return get_view(0, num());
}

auto FHistoryRows::get_view(int32 const offset, int32 const count) -> View {
    return View{
        TArrayView<uint64>{completed_ticks}.Slice(offset, count),
        TArrayView<uint64>{validity_masks}.Slice(offset, count),
        TArrayView<int32>{active_entities}.Slice(offset, count),
        TArrayView<FTestEntityRegistry::EntityTypeCounts>{active_entities_by_type}.Slice(offset,
                                                                                         count),
        TArrayView<FTestEntityRegistry::EntityCounts>{active_entities_by_team_and_type}.Slice(
            offset, count),
        TArrayView<int32>{spawned_entities}.Slice(offset, count),
        TArrayView<int32>{destroyed_entities}.Slice(offset, count),
        TArrayView<int32>{kills}.Slice(offset, count),
        TArrayView<int32>{registry_slot_count}.Slice(offset, count),
        TArrayView<int32>{active_lasers}.Slice(offset, count),
        TArrayView<int32>{lasers_fired}.Slice(offset, count),
        TArrayView<int32>{occupied_spatial_cell_count}.Slice(offset, count),
        TArrayView<uint64>{grid_rebuild_count}.Slice(offset, count),
        TArrayView<uint64>{range_query_count}.Slice(offset, count),
        TArrayView<uint64>{line_trace_count}.Slice(offset, count),
        TArrayView<uint64>{sweep_trace_count}.Slice(offset, count),
        TArrayView<double>{requested_time_scale}.Slice(offset, count),
    };
}

auto FHistoryRows::get_view() const -> ConstView {
    return get_view(0, num());
}

auto FHistoryRows::get_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<uint64>{completed_ticks}.Slice(offset, count),
        TConstArrayView<uint64>{validity_masks}.Slice(offset, count),
        TConstArrayView<int32>{active_entities}.Slice(offset, count),
        TConstArrayView<FTestEntityRegistry::EntityTypeCounts>{active_entities_by_type}.Slice(
            offset, count),
        TConstArrayView<FTestEntityRegistry::EntityCounts>{active_entities_by_team_and_type}.Slice(
            offset, count),
        TConstArrayView<int32>{spawned_entities}.Slice(offset, count),
        TConstArrayView<int32>{destroyed_entities}.Slice(offset, count),
        TConstArrayView<int32>{kills}.Slice(offset, count),
        TConstArrayView<int32>{registry_slot_count}.Slice(offset, count),
        TConstArrayView<int32>{active_lasers}.Slice(offset, count),
        TConstArrayView<int32>{lasers_fired}.Slice(offset, count),
        TConstArrayView<int32>{occupied_spatial_cell_count}.Slice(offset, count),
        TConstArrayView<uint64>{grid_rebuild_count}.Slice(offset, count),
        TConstArrayView<uint64>{range_query_count}.Slice(offset, count),
        TConstArrayView<uint64>{line_trace_count}.Slice(offset, count),
        TConstArrayView<uint64>{sweep_trace_count}.Slice(offset, count),
        TConstArrayView<double>{requested_time_scale}.Slice(offset, count),
    };
}

auto FHistoryRows::get_const_view() const -> ConstView {
    return get_const_view(0, num());
}

auto FHistoryRows::get_const_view(int32 const offset, int32 const count) const -> ConstView {
    return ConstView{
        TConstArrayView<uint64>{completed_ticks}.Slice(offset, count),
        TConstArrayView<uint64>{validity_masks}.Slice(offset, count),
        TConstArrayView<int32>{active_entities}.Slice(offset, count),
        TConstArrayView<FTestEntityRegistry::EntityTypeCounts>{active_entities_by_type}.Slice(
            offset, count),
        TConstArrayView<FTestEntityRegistry::EntityCounts>{active_entities_by_team_and_type}.Slice(
            offset, count),
        TConstArrayView<int32>{spawned_entities}.Slice(offset, count),
        TConstArrayView<int32>{destroyed_entities}.Slice(offset, count),
        TConstArrayView<int32>{kills}.Slice(offset, count),
        TConstArrayView<int32>{registry_slot_count}.Slice(offset, count),
        TConstArrayView<int32>{active_lasers}.Slice(offset, count),
        TConstArrayView<int32>{lasers_fired}.Slice(offset, count),
        TConstArrayView<int32>{occupied_spatial_cell_count}.Slice(offset, count),
        TConstArrayView<uint64>{grid_rebuild_count}.Slice(offset, count),
        TConstArrayView<uint64>{range_query_count}.Slice(offset, count),
        TConstArrayView<uint64>{line_trace_count}.Slice(offset, count),
        TConstArrayView<uint64>{sweep_trace_count}.Slice(offset, count),
        TConstArrayView<double>{requested_time_scale}.Slice(offset, count),
    };
}

auto FHistoryRows::num() const noexcept -> int32 {
    return ml::num(completed_ticks);
}

auto FHistoryRows::is_empty() const noexcept -> bool {
    return num() == 0;
}

void FHistoryRows::validate_array_sizes() const {
    ml::fatal_if_nums_not_equal({
        ml::num(completed_ticks),
        ml::num(validity_masks),
        ml::num(active_entities),
        ml::num(active_entities_by_type),
        ml::num(active_entities_by_team_and_type),
        ml::num(spawned_entities),
        ml::num(destroyed_entities),
        ml::num(kills),
        ml::num(registry_slot_count),
        ml::num(active_lasers),
        ml::num(lasers_fired),
        ml::num(occupied_spatial_cell_count),
        ml::num(grid_rebuild_count),
        ml::num(range_query_count),
        ml::num(line_trace_count),
        ml::num(sweep_trace_count),
        ml::num(requested_time_scale),
    });
}

auto FHistoryRows::slice(int32 const offset, int32 const count) -> View {
    return get_view(offset, count);
}

auto FHistoryRows::left(int32 const count) -> View {
    return slice(0, count);
}

auto FHistoryRows::right(int32 const count) -> View {
    return slice(num() - count, count);
}

auto FHistoryRows::slice(int32 const offset, int32 const count) const -> ConstView {
    return get_view(offset, count);
}

auto FHistoryRows::left(int32 const count) const -> ConstView {
    return slice(0, count);
}

auto FHistoryRows::right(int32 const count) const -> ConstView {
    return slice(num() - count, count);
}
} // namespace ml::level_telemetry
